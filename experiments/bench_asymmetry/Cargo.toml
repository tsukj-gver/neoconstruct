[package]
name = "bench_asymmetry"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
# 复用 construct-rs rlib 以调用 BitsIntegerNode / StructNode / create_class 等公共 API
construct-rs = { path = "../../construct-rs" }
pyo3 = "0.22"

[profile.release]
lto = "fat"
codegen-units = 1
strip = true
opt-level = 3
