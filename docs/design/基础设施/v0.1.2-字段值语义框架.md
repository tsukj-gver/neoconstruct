---
id: DESIGN-v0.1.2-FieldValueSemantics
status: active
phase: v0.1.2
version: v2.2
depends_on: [ADR-003, ADR-004, ADR-005, ADR-006, ADR-007, ADR-014]
last_updated: 2026-09-04
revision:
  - v2.2 (2026-09-04, v0.1.2-1s): REV 约束审计（v0.1.2-3n）R1-R5 修订——R1
    Control ctx 改"缺省哑值 None/显式值透传"（选项 B，IEC104 round-trip 保持，
    I5 全绿、I6 缺省报错锁定）；R2 锚点降级声明 + N 族类型断言；R3 等价判定
    重述为自身源码事实链；R4 散点原版对照表述替换；R5 行为变更全清单补全
    （10 处变更，原 #9 改保持）
  - v2.1 (2026-09-04, REV-N1): 复审条件修订（一句话级）——I5 期望改为 IEC104
    真实链 parse 全链 PASS + build 腿差异锁定报错；§5.1d 补"差异锁定见 I5"指针
  - v2 (2026-09-04, v0.1.2-1r): REV 驳回后修订——P1 Seek/Peek/Checksum 分类缺口闭合
    （三者→Control，对账脚本全量清点）；P2 迁移映射补全 11 分支；P3 现状表更新至
    止血后；P4 全集声明系统化（AST 脚本 15 叶类+7 传播类）；P5 矩阵补 B2 数据负例
    与 P1 关联用例（76→96）；P7 B4-build 门禁判据修正；P6/P8 顺带（owned 表述、
    WO 伪码、Sequence 取值、边界补录、depends_on 补 ADR-005）
  - v1 (2026-09-04, v0.1.2-1): 初版（候选 2 框架 + spike 验证）
---

# v0.1.2 字段值语义框架（ValueKind 单源分类）

> 任务：v0.1.2-1 [字段值语义问题·框架方案探索与原型验证]（v2 修订 = v0.1.2-1r，
> REV 审查报告 `plans/v0.1.2/traces/v0.1.2-3-REV设计审查.md`；v2.2 修订 =
> v0.1.2-1s，REV 约束审计 `plans/v0.1.2/traces/v0.1.2-3n-REV约束审计.md`）。
> 问题：字段值语义在构造器分支间各自特判、无公共收敛，组合场景塌方（B1-B4，见
> `plans/v0.1.2/总纲.md`）。本文给出框架级公共解：多方案对比 + spike 原型证据 +
> 原版锚点实测表 + 组合测试矩阵。
> 原版行为证据分级（v2 起显式标注）：**[实测]** = `.venv-pc`（construct 2.10.70）
> 运行锚点（`anchor_construct*.py`/`anchor3.py`）；**[AST]** = 源码 AST 脚本提取
> （`audit_flagbuildnone.py`，非手工 grep）。

## 1. 问题模型：三个值解析点，五处散落特判

neoconstruct 的 dataclass 模型比原版多出一个**实例化时机**。字段值语义必须在三个点
解析，当前散落在五处（无单源）：

| 解析点 | 现状实现位置 | 缺陷形态 |
|--------|-------------|----------|
| **I 实例化**（`__init__` 默认值） | `_mixin.py _apply_dataclass_field_config` 的 `value_providing` isinstance 元组 | B1：Const/Default 实例化值 None；B3：表达式派生字段强制实参 |
| **P parse**（存/弃） | `FieldMode::{Rw,Ro,Wo}`（struct_node.rs） | 语义本身正确，但与 I/B 无关联 |
| **B build**（值来源） | ① `FieldMode` 分支 ② `Node::compute_ro_value`（mod.rs，按节点类型 match）③ 各节点 build 的 None 特判（Const/Default/Padding…） | B1e：RW 字段 getattr 到 None 时 **ctx 写入原始 None**（非有效值），流入表达式报 wrong type；B1f：`rfield(Const)` build 直接失败（compute_ro_value 无 Const 分支） |
| **ctx 注入**（B4） | `_FieldDescriptor._context_injections` 存了但**无任何消费** | 静默忽略（比报错更危险） |
| **表达式求值域**（B2） | `_expr_params` 协议 + 槽位递归收集 + expr_programs | 嵌套收集缺陷（DEV v0.1.2-2 止血中）；框架层缺口=ctx 写原始值而非有效值 |

## 2. 原版锚点实测表（construct 2.10.70，.venv-pc 实测）

> **v2.2 角色声明（R2）**：本表为**历史参考与交叉验证材料，非 neoconstruct
> 期望值的规格来源**。期望值由 neoconstruct 自主语义定义（自身行为基线 + 语义
> 自然性，见 §8）；本表及对账结论仅作分类完备性的置信度交叉参考（§5.1）。

原版的字段值语义框架是三个机制（`Struct._build`/`_parse` 源码 + 实测确认）：

1. **`flagbuildnone` 布尔**＝build 值来源唯一分类点：True 的构造器缺键时收到
   `obj.get(name, None)`；False 缺键直接 `KeyError`。
2. **parse 存储**＝`if sc.name: obj[name] = subobj`（凡有名必存，Padding 存 None）。
3. **ctx 回写**＝build 前全量 `context.update(obj)`，每个 subcon build 后
   `context[name] = buildret`（**有效值回写**——后续表达式读到的是节点实际使用的值）。
   v2 精确化：buildret 因构造器而异——值类=编码值；Const/Default=有效值；
   Peek=传入哑值（None）[A1-A3]；Seek=**新流位置 int**（非传入值）[S1]；
   Checksum=**重算 digest**（非传入值）[C1]；Padding/Pass=传入哑值。

| 构造器 | flagbuildnone | parse 结果 | build 缺省 | build 显式值 | build 值=None |
|--------|:---:|--------|--------|--------|--------|
| Const(5,Byte) | T | 5 | b'\x05'（用 value） | 等值→用；不等→ConstError | 用 value |
| Default(Byte,0) | T | inner 值 | b'\x00' | 用显式值 | 用默认 |
| Default(Byte,this.x+1) | T | inner 值 | 表达式求值 | 用显式值 | 表达式求值 |
| Rebuild(Byte,f) | T | inner 值 | 重算 f(ctx) | **忽略**，重算 | 重算 |
| Computed(expr) | T | 求值值 | no-op 不写不校验 | 忽略，no-op | no-op |
| Computed(42) | T | 42 | no-op | no-op | no-op |
| Padding(2) | T | **None（存入）** | 写 pattern×2 | **忽略**，写 pattern | 写 pattern |
| Tell | T | 流位置 | 不写字节 | 忽略 | — |
| Pass | T | None | 空 | 忽略 | — |
| Peek(Int8ub) [实测A1-A4] | T | peek 值（流回退，失败吞为 None） | **no-op**（b'\x01'） | **忽略**（同缺省） | no-op |
| Seek(at) [实测S1-S2] | T | **新流位置 int** | seek 移动（忽略值） | **忽略**，仍 seek | seek |
| Checksum(Bytes(2),f,bytesfunc) [实测C1-C3] | T | 校验→存 digest（不符→ChecksumError） | **重算写入** | **忽略**，仍重算 | 重算 |
| If(cond,Padding) 传播 [实测W1] | T(=and) | — | no-op 值传导→写 pattern（**缺键可省**） | 忽略 | — |
| If(cond,Byte) 传播 [实测W2] | F(=and) | — | **KeyError: 字段名**（不进 If） | 用显式值 | TypeError |
| Bytes(n)/Byte/Prefixed(普通) | F | 值 | **KeyError: 字段名** | 用显式值 | TypeError |
| 顶层/嵌套（双层 Prefixed）Bytes(this.x+1) | F | 值 | KeyError | 正常 | — |
| 数据负例：Bytes(this.n) 长度>剩余 [实测N1] | — | **StreamError**（"expected 5, found 3"） | — | — | — |
| 数据负例：Prefixed 长度声明不符 [实测N2/N3] | — | **StreamError** | — | — | — |

**原版 flagbuildnone 全集 [AST]**（`experiments/v0.1.2-spike/audit_flagbuildnone.py`
对 core.py 做 AST 提取，构造器级 `flagbuildnone = True` 共 **15** 个叶类）：
{Const:2840, Computed:2912, Index:2960, Rebuild:3000, Default:3051, Check:3094,
Error:3142, StopIf:4093, Peek:4481, Seek:4595, Tell:4639, Pass:4676, Terminated:4716,
RestreamData:5182, Checksum:5553}。其中 neoconstruct 未实现：Error、RestreamData
（预留分类见 §5.1b）；Padding 经 `Padded(length, Pass)` 包装获得透传 True（v1 声明
"实例级"不准确，v2 修正为透传语义）。
**传播规则 [AST]**（7 类）：Subconstruct/Renamed 透传（:795）；Struct/Sequence/
LazyStruct/Switch 取 all（:2222/:2379/:5991/:4031）；Select 取 any（:3850）；
IfThenElse 取 and（:3964）。Construct 基类默认 False（:360）。
v1 全集声明漏 Index/Terminated/Error（REV P4），v2 以脚本对账取代手工枚举——
**对账结论：neoconstruct 导出构造器 100 个 × ValueKind 分类 100% 覆盖且与原版
语义一致**（详见 `audit_flagbuildnone_result.md`：哑值可省叶类 13 已实现全部非
Instance / 值类与传播类全部 Instance / 未实现 2 个预留 Control）。
**原版无实例化时机**——I 点语义由自身推导：dataclass 实例化模型（自身）+
B1 已交付修复行为 + 字段值语义自然性（见 §5.3），非从原版分类推导。

## 3. 现状差异（当前 venv 实测，`experiments/v0.1.2-spike/current_neoconstruct.py`）

> v2 注：REV 复跑确认 B1/B1e/B1f/B3 已由 v0.1.2-2/-4 止血修复（2026-09-04），
> 下表已更新至止血后状态；框架仍需落地以消灭散落特判与错误时机。

| 场景 | 原版 | neoconstruct 现状 | 对应 |
|------|------|------------------|------|
| `C2().c`（field(Const(5,Int8ub)) 实例化） | —（无实例化） | **=5 ✓（止血已修**，`_mixin.py` value_providing 常量补值，v0.1.2-2） | B1 |
| `A(y=b'ab').build()`（Const x + Bytes(x+1)） | x 缺省由 flagbuildnone 补，y 有值 → b'\x01ab' | **=b'\x01ab' ✓（止血已修**，struct_node.rs ctx 写有效值，v0.1.2-4） | B1e |
| `rfield(Const(1,Byte))` + Bytes(x+1) build | — | **OK ✓（止血已修**，mod.rs:588 compute_ro_value 补 Const 分支，v0.1.2-4） | B1f |
| `W(x=1)`（wfield(Bytes(x+1)) 实例化） | —（build 时 KeyError） | **可实例化 ✓（止血已修**，错误后移到 build；v0.1.2-2 wo 哑值缓解） | B3 |
| `field(Peek/Seek/Checksum)` 实例化与 build 哑值 | 哑值可省（§2 表） | **现状 value_providing 不含三者**（dataclass 必填哑值——B3 同型；test_iec104:180 `field(Peek)` / test_modbus_rtu:513 `field(Checksum)` 为已交付用户面形态）→ 框架 Control 分类修复（§5.1） | **B5（本框架新编号）** |
| `field(Inner, context={...})` | parse/build 显式 context 传参 | **静默忽略**（未修，框架 §5.6 分期处理） | B4 |
| 嵌套 Prefixed×Bytes(x+1)（B2 场景） | OK | 用户 v0.1.1 实测静默错读；**止血已修**（B2a-c PASS，v0.1.2-2）；数据负例（长度不符→显式 StreamError）由矩阵 N 族锁定（§8） | B2 |
| Padding parse 存储 / build 缺省 / 显式忽略；Default 全矩阵；Rebuild 重算；Computed no-op；Tell | 上表 | **一致** ✓ | — |

## 4. 候选方案（≥2，含否决理由）

### 候选 1【协议化补丁集】（否决）
- **机制**：把 `value_providing` isinstance 元组泛化为描述符协议 `_init_default`
  属性；Rust 侧不动；B2 靠 DEV 止血。
- **覆盖**：B1 仅实例化面（**ctx None 流入未修**，B1e/B1f 仍在）；B3 需另加规则；
  B4 不覆盖。
- **§0**：合规（纯冷路径）。**性能**：零热路径变化。
- **否决理由**：build 侧语义仍散落三处（FieldMode/compute_ro_value/节点特判），
  rfield(Const) 类缺陷修一处漏一处——本质是止血泛化，不满足"公共收敛"要求。

### 候选 2【ValueKind 编译期单源分类 + resolve 统一】（**推荐**，spike 已验证）
- **机制**：见 §5。编译期把每个字段的 build 值来源分类为封闭枚举 `ValueKind`，
  build 循环统一走 `resolve → 有效值 → ctx 回写 → node.build`；实例化默认值从
  **同一分类**派生；`compute_ro_value` 全量迁移删除。
- **覆盖**：B1（实例化 + ctx 流入）、B1f、B2 机制层（ctx 有效值 + 矩阵锁定）、
  B3（Optional + 错误时机后移到 build——值使用点）、B4（分期，见 §5.6）。
- **§0**：合规（§6 对照表）。**性能**：spike 实测噪声级（§7）。

### 候选 3【build 签名返回值全量回写（construct 全对齐）】（否决）
- **机制**：`Construct::build` 改返回 `Result<Py<PyAny>>`，StructNode 用返回值做
  ctx 回写（逐节点复刻原版 buildret）。
- **否决理由**：30+ 节点签名全改（改动面 ×5）；每个节点强制产出返回值（多数字段
  弃用）带来 per-field 引用计数churn，555 测量点回退风险未经证；候选 2 的字段级
  resolve 已覆盖全部值语义构造器（§8 矩阵逐族锁定），语义增益为零。

## 5. 推荐方案详细设计（候选 2）

### 5.1 核心类型（Rust，`compile.rs`/`struct_node.rs`）

```rust
/// 字段 build 值来源分类（编译期封闭枚举，静态 match 分派，禁 trait）。
pub enum ValueKind {
    /// 值必须来自实例。build 时 None → BuildValueMissing（错误时机＝值使用点 build）。
    /// expr_derived：subcon 树内含表达式消费者（expr_programs[idx] 非空）→
    ///              init_default 降为 Optional（B3：错误时机后移）。
    Instance { expr_derived: bool },
    /// Const：编译期持有 Python 常量对象；None→补值（Bound clone）；ctx 写常量。
    Const(Py<PyAny>),
    /// Default：None→求值；非 None→用显式；ctx 写有效值。
    Default(Arc<ExprProgram>),
    /// Rebuild：忽略实例值，求值；ctx 写求值值；inner.build(求值值)。
    Rebuild(Arc<ExprProgram>),
    /// Computed：求值→ctx；build no-op（不写不校验）。
    Computed(Arc<ExprProgram>),
    /// Tell：ctx 写 stream.tell()；build no-op。
    Tell,
    /// Padding/Pass：ctx 写 None；节点自身写 pattern 字节/空。
    Void,
    /// 低频控制与流操作（StopIf/Check/Terminated/Element/**Peek/Seek/Checksum**，
    /// v2 新增后三者）：resolve 产**透传值**（显式实例值/parse 产出值透传，缺省
    /// 产哑值 None——实例可省；v2.2 R1 选项 B），build 值语义由节点自理
    /// （Peek no-op / Seek 移动流指针 / Checksum 重算 digest 写入 / Check 校验 /
    /// StopIf 哨兵 / Terminated no-op——实例值不参与字节编码）。
    Control,
    /// Index：resolve 产 ctx.index()。
    Index,
}
```

**分类规则**（编译期，按字段 subcon **根节点**判定——字段持有根节点，编译期
单点判定即可封闭分类、无需递归拆包；交叉参考：§2 锚点验证 Prefixed(普通) 归
Instance）：
ConstNode→Const、DefaultNode→Default、RebuildNode→Rebuild、ComputedNode→Computed、
TellNode→Tell、Padding/BitPadding/Pass→Void、
StopIf/Check/Terminated/Element/**Peek/Seek/Checksum**→Control（v2 新增后三者）、
IndexNode→Index、其余（含一切包装器根）→Instance{expr_derived}。

**哑值可省的语义依据（v2.2 重述，R3——结论不变）**：等价判定标准可**完全从
自身源码推导**——① `mod.rs compute_ro_value` 既有 11 分支即"build 值不来自
实例"的自身清单（Tell/Computed/Index/Rebuild/Default/Const/Padding/StopIf/
Check/Terminated/Element）；② Peek/Seek/Checksum 的 build 签名以 `_obj` 模式
忽略实例值（peek.rs:100-110 no-op / seek.rs:117-128 移针 / checksum.rs:297-360
重算，自身单测锁定）；③ Padding/Const/Default 节点对 None 容忍是自身既有行为。
框架等价判定 = "**节点 build 不依赖实例语义值 → resolve 缺省补哑值、实例侧
init_default=None 可省**"。原版 flagbuildnone 15 叶类对账（§2 [AST]）**降级为
交叉参考**：结果与自身推导一致（非 Instance ⇔ flagbuildnone=True 无一例外），
仅作完备性置信度佐证、非判定依据。三者归类 Control 而非新增枚举变体的理由：
① resolve 视角行为同构（缺省哑值 None，无 i64 ExprProgram 可承载的单一求值——
Checksum 的 digest 来自 bytes_source 双表达式+hashfunc 组合（checksum.rs 自身
实现形态），Seek 依赖流副作用，Peek 无 build 值语义）；② 枚举形态不变，DEV
无返工；③ §5.2 分工原则不被破坏（hash 计算/流移动留在节点内，resolve 只做
缺省补值/透传）。

**§5.1b 未实现构造器预留分类**（交叉参考：原版对账表单列，§2）：
Error→Control（parse/build 无条件 ExplicitError）、RestreamData→Control（build
no-op return obj）。实现时直接落 Control，无需改枚举。

**§5.1c 包装器传播不实现（v2 识别，v2.2 表述重述——行为不变）**：分类按字段
subcon **根节点**单点判定（§5.1 规则）——包装器根（If/IfThenElse/Switch 等）
一律 `Instance{expr_derived:true}`，build 缺省报 BuildValueMissing。这是分类
规则的直接结果，非例外豁免。决策：v0.1.2 不实现"包装器内全哑值分支可省"的
传播分析，理由：① 值类 inner（主流场景）下实例值必须提供，Instance 判定正确；
全哑值分支 inner（如 If(cond,Padding)）为罕见组合，缺省显式报错优于静默猜测；
② Switch 的 case 运行期选择与编译期分类存在根本张力，正确实现需引入"运行期
可选"第三态，属 v0.1.3+ 演进项；③ 缺省方向的错误形态是显式
BuildValueMissing（非静默），安全。矩阵 W 族锁定该行为（§8），防止未来语义
漂移无感知。（历史交叉参考：原版传播规则见 §2 [AST]。）

**§5.1d Control 的 ctx 回写（v2.2 修订，R1 选项 B）**：Control 字段 resolve 产
**透传值**——getattr 到显式实例值/parse 产出值则透传，缺省（None）产哑值 None
（与 Const/Default 的"缺省补值、显式值优先"模式统一）；节点字节行为不变
（Peek no-op / Seek 移针 / Checksum 重算——实例值不参与编码）。效果：
① parse 产出的有效值经实例透传回 ctx，后继表达式（Computed/Switch 分派）读到
有效值——peek→派生表达式→分派链（Peek 的典型正当用法，parse 腿全链支持即
自证）round-trip 保持全绿（I5，已交付行为不回归）；② 从零 build 缺省时哑值
None 流入，后继消费者显式报错（I6 锁定）——引用 build 期计算值（seek 新流
位置/重算 digest）且实例未携带的场景，v0.1.3 有真实需求再升级专用 kind（如
`SeekPos`/`ChecksumDigest`）；需要流位置应使用 Tell（ctx 写 stream.tell()）。

### 5.2 build 循环（StructNode/SequenceNode 同模式）

```rust
for (idx, field) in self.fields.iter().enumerate() {
    let value: Bound<'_, PyAny> = match &field.kind {
        ValueKind::Instance { .. } => {
            let v = obj.getattr(field.name.py_name())?;       // C API
            if v.is_none() { return Err(BuildValueMissing { field, path }) }
            v
        }
        ValueKind::Const(c) => {
            let v = obj.getattr(field.name.py_name())?;       // RW/RO 均取（校验留给节点）
            if v.is_none() { c.bind(py).clone() } else { v }  // 就地补值，无 owned 中转
        }
        ValueKind::Default(p) => { /* None→eval_expr_int(p,ctx)?.into_py→bind；否则 v */ }
        ValueKind::Rebuild(p)  => { /* 忽略 attr；eval → PyLong */ }
        ValueKind::Computed(p) => { /* eval → ctx；node.build 是 no-op */ }
        ValueKind::Tell        => stream.tell().into_py(py).into_bound(py),
        ValueKind::Void        => py.None().into_bound(py),
        ValueKind::Control     => obj.getattr(field.name.py_name())?,  // 透传显式/parse 产出值；缺省即哑值 None（R1 选项 B）
        ValueKind::Index       => ctx.index().into_py(py).into_bound(py),
    };
    if self.has_expressions { ctx.set_field_at(idx, field.name, &value, py)?; } // ★写有效值
    // WO 字段（v2 注）：编译期禁表达式引用（_check_wo_reference），故 WO 恒有
    // has_expressions=false 对其槽位——此行对 WO 字段不执行（"WO 不写 ctx"由
    // 编译期保证，非运行期分支；与正文一致，伪码特此对齐）。
    field.node.build(py, &value, stream, ctx, path)?;
}
```

> Sequence 差异（v2 注，REV P8）：SequenceNode 的 Instance 分支取值是 **list
> 位置访问**（`obj.get_item(idx)`，非 getattr）——Sequence 实例是 tuple/list 而
> 非 dataclass。resolve 循环同模式迁移时仅取值入口不同，分类/哑值/ctx 语义一致。

**分工原则**：resolve 只做"缺省补值"（value completion）；节点保留"值校验与编码"
（ConstNode 的相等校验保留——Switch/Array 等**无字段包装**的语境仍依赖节点自治）。
节点 build 的 None 分支保留但字段路径不再触发。

**取代的散落特判**（审计清单）：
| 现有散落点 | 框架下去向 |
|-----------|-----------|
| `mod.rs compute_ro_value` 全部 **11 分支**（v2 修正：止血已加 Const/Padding 两分支，v1 误写 10） | 迁移进 resolve（映射：Tell→Tell、Computed→Computed、Index→Index、StopIf/Check/Terminated/Element→Control、Rebuild→Rebuild、Default→Default、**Const→Const、Padding→Void**（v2 补全））；**函数删除**，B1f（Const 缺支）随之消灭 |
| `struct_node.rs` build 的 Rw/Wo/Ro 三分支 | 统一 ValueKind 循环；Rw/Wo 差异仅剩"是否写 ctx"（WO 编译期禁表达式引用，维持） |
| `sequence.rs` RO 旁路 | 同模式迁移 |
| 节点层 Const/Default 的 None 容忍 | 保留（非字段语境必需），字段路径由 resolve 前置补值 |
| `_mixin.py value_providing` 元组 | 删除，改消费编译产物 init_default（§5.3） |
| WO 引用编译期禁令（`_check_wo_reference`） | 保留（与 resolve 正交：WO 不写 ctx 依旧） |

### 5.3 实例化默认值派生（I 点，单一事实源＝Rust 分类）

`compile_schema` 返回值增加 `init_defaults: Vec<Option<Py<PyAny>>>`（编译期一次
FFI 内计算，冷路径；备选：CompiledSchema 暴露 `init_default(idx)` 方法）。规则：

| ValueKind | init_default |
|-----------|--------------|
| Const(v) | **v**（int/bytes/str/bool/float 不可变标量；可变对象→None，防共享可变默认值） |
| Default/Rebuild/Computed 且程序可常量折叠（ops==[Const(c)]） | **c**（→PyLong） |
| 其余 Default/Rebuild/Computed/Tell/Void/Control/Index | None |
| Instance{expr_derived:false} | Required（必填 positional，现状保留） |
| Instance{expr_derived:true} | **None+kw_only**（B3：build 时才报 BuildValueMissing） |

v2.2 注：Peek/Seek/Checksum 落 Control（§5.1）→ init_default=None（实例化可省，
B5 修复：`field(Peek)`/`field(Checksum)` 不再要求必填哑值）；build 侧 resolve
**透传**实例值（显式值/parse 产出值；缺省产哑值 None）传节点——节点编码忽略
实例值（Peek no-op / Seek 移针 / Checksum 重算，字节行为不变），ctx 写透传值
（§5.1d R1 选项 B：IEC104 链 round-trip 保持；缺省 None 流入的报错路径由
I6 锁定）。

Python 侧 `_apply_dataclass_field_config` 消费该列表：RO→init=False（不变）；
有显式 `default=` 参数→显式优先（不变）；init_default 有值→`kw_only=True,
default=值`；None→`kw_only=True, default=None`；Required→无参 `field()`。
**用户显式 default 永远优先于框架派生**（现状语义保留）。

### 5.4 parse 侧（P 点）

不变：FieldMode Rw/Ro 存、Wo 弃。`field(Padding)` parse 存 None（既有已发布
行为，[行为稳定]）；
`wfield` 弃是 neoconstruct 有意的 API 设计（文档既有差异，矩阵记录）。
FieldMode 职责收敛为"init 可见性 + parse 存储"两维，**不再参与 build 分派**。

### 5.5 错误类型

新增 `ConstructError::BuildValueMissing { field, path }` → Python
`FieldValueMissingError(ConstructError)`（新增异常类属**追加**非变更；PM 确认）。
错误信息含字段名与"构造实例时未提供该字段值"指引。

### 5.6 B4（context= 注入）分期决策

- **v0.1.2**：编译期拒绝——`_context_injections` 非 None → `CompilationError`
  （明确"将于 v0.1.3 提供注入"）。消除静默忽略（危险面）。
- **v0.1.3 机制预留**：注入映射编译为 `Vec<(child_field_idx, outer_expr)>`，
  StructRefNode 进入子树前求值外层表达式写入子 ctx 槽位（子类编译期提供
  注入名→槽位映射）；parse/build 对称。机制依据 ADR-003（嵌套跨层显式 context
  注入，自身架构决策）。
- **需 PM 决策**：v0.1.2 是否随本框架一并交付"编译期拒绝"（建议是，改动 3 行）。

### 5.7 B2 关系

B2 根因（嵌套收集）由 DEV v0.1.2-2 止血修复（当前 venv 已 PASS，§3）。框架贡献：
① ctx 写**有效值**使嵌套消费者读到正确前序值（B1e 同根）；② 表达式求值统一收敛
到 resolve 内单一路径；③ §8 矩阵 F 族锁定嵌套×表达式×消费者组合，防回归。

## 6. §0 原则对照表

| # | 原则 | 本设计 |
|---|------|--------|
| 1 | 一次 FFI | 不新增穿越：resolve/ctx 回写全在既有 `_build_raw` 入口内（C API 进程内调用）；init_defaults 走既有编译入口 |
| 2 | 无中间表示 | resolve 产出即 PyObject（Bound），无 Rust 中间枚举/DTO；就地替换，无 owned 中转 |
| 3 | 输入/输出侧无 trait 抽象 | ValueKind 为封闭 enum + match 静态分派（enum_dispatch 同族），无 Box<dyn> |
| 4 | pyo3 核心依赖 | 全部经 pyo3/C API 操作 |
| 5 | mashumaro 式 API | `field/rfield/wfield` 签名不变；101 构造器公开 API 不变（新增 FieldValueMissingError 为追加项） |
| 6 | enum_dispatch 静态分派 | 同形态（编译期枚举 + match） |
| 7 | Result + thiserror + path | BuildValueMissing 携带 field+path |
| 8 | Stream 纯 Rust 不跨 FFI | 未触碰 Stream |

## 7. 性能假设与证据（spike：`experiments/v0.1.2-spike/`）

- **瓶颈识别**：改动仅 StructNode/SequenceNode 字段循环（热路径），节点内部不动。
  Instance 路径增量＝一次 `is_none()` 指针比较；值提供型路径增量＝None 时一次
  Bound clone（refcount inc）＋一次 `into_bound`；Control 路径增量＝一次 getattr
  （与 Instance 同型取值，v2.2 R1）。**无新增 FFI/分配**。
- **可证伪预测**：字段循环形态替换差异 < 噪声阈（0.3x，L-09）。
- **spike 实测**（`bench_ab.py`，同会话交替 A/B，6 字段负载＝3×Instance+Const+
  Default+Rebuild，pyo3 0.22 与主 crate 同版本，lto+codegen-units=1 同主 crate）：
  - 会话 1：Δ 中位 **+2.52%**（+24ns/6 字段≈4ns/字段；3 轮中 1 轮 −0.70%）
  - 会话 2：Δ 中位 **+0.84%**（3 轮中 2 轮 ≤ −0.12%）
  - 结论：**噪声级，PASS**。注意：曾测得 +6.7%~+9.6% 的实现形态（resolve 返回
    owned `Py<PyAny>` 中转）——**设计明令禁止该形态**，必须就地 Bound 替换
    （v2 修正表述：owned 形态的对比**数据已测、代码未保留**在 spike 仓库——
    lib.rs 仅存就地形态 + 禁令注释；REV P6 复核确认不可重跑，数字仅作方向警示）。
- **主基线**（DEV 实施后对照用，`bench_struct.py` 3 轮，rs 侧 ns 中位）：
  B1-parse 384/384/354，B1-build 247/251/245；B2-parse 710/725/706；
  B4-build 3389/4357/3922；全 14 点 3 轮门禁 PASS（10.66x-34.77x）。
  **验收判据（v2 修订，REV P7）——按点自身波动分级**：
  - **判据 A（默认，适用于自身 3 轮极差/中位 ≤20% 的点**，如 B1/B2 全部）：
    实施后同法 3 轮，各点中位回退 <10%。
  - **判据 B（B4-build 等自身波动 >20% 的点**：实测 3389/4357/3922 极差/中位
    =28%，判据 A 在该点会随机误报）：改用**同会话 Controlled A/B**（L-09）——
    主分支与实施分支**同一会话内交替**跑 `bench_struct.py` B4-build，3 轮 ×
    交替 2 组，取配对 Δ 的中位，|Δ中位| <5% 判 PASS（该点绝对值 ~3.9µs，本
    框架字段循环增量为 ns 级，5% ≈ 195ns 远大于预期真实增量且远小于自身
    28% 波动——A/B 配对消除会话间漂移后该阈有效）；或等价替代：单会话连续
    7 轮取中位对 3 轮中位比较（中位数对 ±28% 离群鲁棒）。DEV 验收报告须
    标注每个门禁点所用判据（A/B）。

## 8. 组合测试矩阵（96 用例，v2：76 + I/J/K/N/W 五族 20 例；期望值由 neoconstruct
自主语义定义——自身行为基线 + 语义自然性，§2 锚点仅历史参考（R2））

> 维度：D1 值语义构造器（v2 +Peek/Seek/Checksum）× D2 模式(field/rfield/wfield) ×
> D3 时机(实例化/parse/build 缺省/显式等值/显式不等值/显式 None) ×
> D4 位置(顶层/Prefixed×1/Prefixed×2/Switch 内) × D5 表达式(字段引用/算术/常量) ×
> D6 消费者(Bytes/Array/Switch/If/Computed/Prefixed) ×
> **D7 数据负例（v2，B2 回归锁定：长度声明 vs 实际 payload 不符）**。
> 每用例断言**对象字段值**（非仅字节）——总纲 §4 教训。

| 族 | 用例 | 组合要点 | 期望（自主语义基线） |
|----|------|---------|----------------|
| A1-6 | Const(5,Byte)×field | 六时机 | 实例化=5；parse=5；缺省/None→b'\x05'；等值→b'\x05'；不等→ConstError |
| A7-12 | Const(b'Z',Bytes(1))×field | bytes 形态 | 同上（B1 验收 P1 场景） |
| B1-6 | Default(Byte,0)×field | 六时机 | 实例化=0；parse 转发；缺省/None→b'\x00'；显式→用之 |
| B7-12 | Default(Byte,x+1)×field | 表达式默认 | 实例化=None；build(x=4)→b'\x04\x05'；显式 9→b'\x04\t'；后继 Computed 读到**有效值** |
| C1-4 | Rebuild×rfield | RO | 实例化 r 不入 init；build(n=3)→b'\x03\x06'；parse→r=255；显式传 r→TypeError（rfield init=False 既有语义，[行为稳定]） |
| C5-8 | Rebuild×field | RW | 实例化=None；r=99 仍写 b'\x06'（忽略）；parse 正常 |
| D1-4 | Computed(x*2)×rfield | 表达式 | parse→c=10；build no-op；实例化=None |
| D5-8 | Computed(42)×field | 常量折叠 | **实例化=42**（§5.3）；parse=42；build no-op |
| E1-4 | Padding(2)×wfield | WO | parse 消费不存；缺省/显式均写 pattern；parse 后实例 p=None |
| E5-8 | Padding(2)×field | RW | **parse 存 None**（既有已发布行为，[行为稳定]）；显式值忽略；实例化=None |
| F1-4 | Bytes(x+1)×field 顶层 | B3/B1e | W(x=1) 可实例化；build 缺 y→**FieldValueMissing**（时机后移）；A(y=..) 的 Const x 常量补值后 build 成功 |
| F5-8 | Prefixed(Int8ub,Bytes(x+1)) | 嵌套×1 | parse(b'\x01\x02ab')→payload=b'ab'；build round-trip；Const x 组合 |
| F9-12 | Prefixed×2 / Switch(typ+1) 内 | 嵌套×2 | parse(b'\x01\x03\x02ab')；round-trip；case 内 Bytes(x+1) |
| F13-16 | Array(x+1)/If(x>0)/Computed(x+1)/PrefixedArray | 消费者面 | 各消费者读 ctx 有效值；round-trip |
| G1-8 | B1-B3 验收回归 | 用户场景 | G1 A().x==1；G2 A(y=b'ab').build()==b'\x01ab'；G3 parse；G4 C2().c==5；G5 P1(tag=1).c==b'Z'；G6 W(x=1) 实例化 OK+build 缺 y 报错；G7 W(x=1,y=b'ab').build()==b'\x01ab'；G8 rfield(Const)+Bytes(x+1) build OK |
| H1-4 | 边界 | B4/可变/优先级/bit | H1 context=→CompilationError；H2 Const(list)→实例化 None；H3 显式 default 优先；H4 BitStruct Padding 对称 |
| I1-6 | Peek(Int8ub)×field | **v2 P1 关联** | I1 实例化=None（Control 哑值可省，B5 修复）；I2 parse 存有效值（p=5）；I3 build 缺省 no-op（b'\x01'）；I4 build 显式值忽略（字节同 I3——节点编码不读实例值）；I5 **v2.2 R1 修订：round-trip 全绿（parse+build 均 PASS）**——IEC104 真实链 cf1_peek(Peek)→Computed(引用 cf1_peek)→Switch 分派（test_iec104:178-191 已交付形态）：parse 腿 peek 存有效值→Computed 求值→Switch 正确分派；build 腿 RW getattr 取到 parse 存的有效值→ctx 透传有效值（§5.1d 选项 B）→Computed 求值成功→分派一致（已交付行为保持，§11 门禁不回归）；I6 从零 build（实例未携带值）+后继 Computed 引用 peek → **报错**（缺省哑值 None 流入，Computed 消费 None——表达式 VM 对 None 消费者的既有类型错误；缺省报错行为锁定） |
| J1-4 | Seek×field | **v2 P1 关联** | J1 实例化=None（可省）；J2 parse 值=新流位置（s=3，实测S2）；J3 build 缺省 seek 移动（Bytes(2)+Seek(1,1)+Byte → b'ab\x00\t'，实测S1）；J4 round-trip（Seek(0) 绝对定位场景） |
| K1-5 | Checksum(Bytes(2),crc16)×field | **v2 P1 关联** | K1 实例化=None（可省——modbus_rtu M 场景，B5 修复）；K2 parse 校验+存 digest；K3 build 缺省重算（b'\x01\x02\x81\xe1'，实测C1）；K4 build 显式错误值忽略（仍写正确 digest，实测C2）；K5 parse 校验失败→ChecksumError（实测C3）；start/end 引用 Tell 字段（body_start/body_end 形态） |
| N1-3 | 数据负例 | **v2 B2 回归锁定** | N1 `field(Int8ub)+field(Bytes(this.n))` parse(b'\x05abc')→**StreamError 类型断言**＋消息含关键语义词（自身消息格式 "stream read less than specified amount, expected 5, found 3"，stream.rs:141——断言"expected 5, found 3"子串、非逐字全句）；N2 Prefixed(Int8ub,GreedyBytes) 长度声明不符→StreamError（同法类型断言）；N3 嵌套 Prefixed 内层需求不符→StreamError（同法）——三者锁定 B2 修复语义=显式报错而非静默错读（R2：期望为类型+语义词断言，不逐字锚定原版消息） |
| W1-2 | 包装器分类结果锁定 | **v2 §5.1c** | W1 field(If(this.flag>0,Padding(2))) 缺省 build→**BuildValueMissing**（包装器根=Instance 的分类规则结果，§5.1c；显式传值仍成功）；W2 field(If(this.flag>0,Byte)) 缺省 build→BuildValueMissing（同分类规则结果——值类 inner 本就必须提供实例值；错误形态统一为框架异常） |

红灯先行（v2.2 更新至止血后基线）：F 族（ctx 有效值机制）、I1/J1/K1（Control 哑值
可省——B5，框架落地前 dataclass 必填哑值）、I6（缺省哑值 None 流入的 build 期
报错——落地前为实例化期 TypeError，时机不符）、W1/W2（包装器分类结果锁定）在
框架落地前预期 FAIL/行为不符；I5 当前已绿（已交付 round-trip，R1 选项 B 下
**不得回归**）；G 族多数已被止血转绿（§3），保留作回归防线；N 族锁定止血已修
的 B2 显式报错语义（当前应已 PASS——若 FAIL 即回归）。

## 9. 与止血修复（v0.1.2-2/-4）的关系

| 止血 | 框架下去向 |
|------|-----------|
| B1 default 取常量值（`_apply_dataclass_field_config`） | **行为就是框架 §5.3 的 Python 消费端先行实现**——保留；框架落地时把值来源从 isinstance 元组切换到 init_defaults 列表（切换点一处） |
| B2 嵌套收集修复 | **保留**（收集机制 `_expr_params`+槽位协议不变）；框架叠加 ctx 有效值回写 + F 族矩阵锁定 |
| B3 wo 哑值缓解 | 收敛为 §5.3 的 `Instance{expr_derived}→Optional` 规则；止血若与该语义一致则保留，实现统一到分类 |
| （无对应止血）B5：Peek/Seek/Checksum 实例化必填哑值 | 框架 Control 分类修复（§5.1 v2）——止血的 `value_providing` 元组本就不含三者（未引入回归），框架以单源分类一次性收敛（I/J/K 族验收） |

### 9.1 行为变更全清单（相对止血后 main = v0.1.2 基线；v2.2 R5 补全）

| # | 变更 | 方向 | 依据 |
|---|------|------|------|
| 1 | field(Peek/Seek/Checksum) 实例化必填→可省（B5） | 放宽（兼容） | [自身语义] 节点 build 编码忽略实例值 |
| 2 | field(Tell)（RW）实例化必填→可省 | 放宽（兼容） | [自身语义] 同型——build 值来自 stream.tell() 而非实例（v2.2 补标注） |
| 3 | RW 表达式字段实例化必填→可省＋build 报（B3 扩展 RW 面；止血仅 WO） | 放宽＋时机变更 | [自身语义] 值由前序字段推导，实例化点无须提供（v2.2 补标注） |
| 4 | 缺值错误：实例化 TypeError→build FieldValueMissingError | 错误类型/时机 | [自身语义][§0#7] 错误携带 field+path |
| 5 | 显式 None（Bytes 等）：节点层类型错→BuildValueMissing | 错误类型 | [自身语义] None＝缺值统一语义 |
| 6 | B4 context=：静默忽略→CompilationError | **破坏性**（PM 已批） | [自身语义] fail fast 消除静默忽略 |
| 7 | Computed(常量)/Rebuild(常量) 实例化 None→常量 | 字段值变更 | [自身语义] 与 B1 同型"字段值＝构造器值语义"（v2.2 补标注） |
| 8 | Const(可变对象) 实例化共享值→None | 字段值变更 | [自身语义] 防 dataclass 共享可变默认值（v2.2 补标注） |
| 9 | Control 字段 ctx 值 | **保持**（v2.2 起） | [自身语义] 显式值/parse 产出值透传、缺省哑值 None（R1 选项 B）——与 Const/Default"缺省补、显式尊重"统一，IEC104 round-trip 已交付行为不回归 |
| 10 | W1 缺省 build：报 BuildValueMissing（显式传值仍成功） | 缺省面变更 | [自身语义] 根节点分类＋显式错误 |
| 11 | W2 同 W1（错误形态统一框架异常） | 缺省面变更 | [自身语义] 同上 |

变更共 **10 处**（#1-#8、#10、#11；#6 为唯一破坏性、PM 已批）；#9 保持、
无理由变更＝0。追加项（非变更）：FieldValueMissingError 异常类、Sequence list
取值为实现细节。本清单供 v0.1.2-5 发布说明与 §8 矩阵红灯基线核对。

## 10. 边界条件清单

1. Const 值为可变对象（list/dict）→ init_default 退化为 None（防 dataclass 共享可变默认值）；build 仍用该值。
2. 显式 `default=` 参数 > 框架派生（永远）。
3. `field(Computed(expr))` RW + 实例化 None → build no-op 不报缺值（Computed 无字节语义）。
4. WO + 表达式引用 → 编译期禁令保留（`_check_wo_reference`）。
5. Rebuild/Default 表达式求值失败（前序字段缺）→ 错误沿既有 Expr 通道，path 完整。
6. 双层 Prefixed 内表达式 → 读宿主字段有效值（F9-12 锁定）。
7. frozen dataclass：resolve 不 setattr（只 getattr），行为不变。
8. `has_expressions=false` 的 Struct：resolve 照常（仅跳过 ctx 写），Instance None 检查仍生效（错误时机一致）。
9. BitStruct 域内 Padding/Void 分类同字节域（编译期 bitwise 上下文无关分类）。
10. **非 i64 常量默认值（v2，REV P8）**：`Default(b'ab')/Default('x')` 等非 int
    常量无法经 ADR-006 i64 ExprProgram 常量折叠，init_default 退化为 None（实例
    化不带默认值），build 时走 Default 求值分支（DefaultNode 节点内处理 bytes/str
    常量，现状能力不变）。边界延续至 v0.1.3 表达式 VM 扩展，矩阵不设该形态用例。
11. **Bytes 显式 None（v2，REV P8）**：None 非任何字段的合法 build 值——框架
    Instance 分支 None → BuildValueMissing，缺值语义统一、异常类型统一为框架
    异常；H 族外单独记录（差异文档随 DEV 交付）。
12. **包装器全哑值分支（v2，§5.1c）**：field(If(cond,Padding)) 等包装器内全
    哑值分支场景，框架按根节点判定为 Instance——缺省报 BuildValueMissing
    （分类规则结果，非例外），W1 锁定，v0.1.3+ 候选演进。

## 11. 实施改动面（DEV 参考）

| 文件 | 改动 |
|------|------|
| `src/nodes/struct_node.rs` | StructField 增 `kind: ValueKind`；build 循环改 §5.2 |
| `src/compile.rs` | 分类函数（§5.1 规则）+ init_defaults 计算 + 返回值扩展 |
| `src/nodes/mod.rs` | **删除** `compute_ro_value`（分支迁入 resolve） |
| `src/nodes/sequence.rs` | RO 旁路迁移同模式 |
| `src/error.rs` + `python/_errors.py` | BuildValueMissing / FieldValueMissingError |
| `python/_mixin.py` | `_apply_dataclass_field_config` 消费 init_defaults；`value_providing` 删除；B4 编译期拒绝（若 PM 批准） |
| 测试 | §8 矩阵 96 用例落 `tests/`（v0.1.2-3 协同；v2：+I/J/K/N/W 五族）；删除 compute_ro_value 相关单测迁移 |

验收门禁（v2.2 更新）：cargo 全绿 + pytest 889+96 无失败（**test_iec104 等既有
round-trip 用例不回归**——R1 选项 B 保持 build 腿绿，I5）+ 性能按 §7 判据 A/B
分级（波动 ≤20% 点 3 轮中位回退 <10%；B4-build 等 >20% 点同会话 A/B
|Δ中位|<5%）+ §8 矩阵 96 用例转绿（W1/W2 按包装器根=Instance 分类结果期望
判定）。

## 12. v2 修订证据索引（REV 复审导航）

| 修订项 | 证据位置 |
|--------|----------|
| P1 三者哑值锚点 | `experiments/v0.1.2-spike/anchor3.py` A1-A4/S1-S2/C1-C3（15 条全记录） |
| P1/P4 全量对账 | `experiments/v0.1.2-spike/audit_flagbuildnone.py`（AST 脚本）→ `audit_flagbuildnone_result.md`（100 构造器全表，PASS） |
| P4 传播语义 | 同上脚本提取 7 传播类 + W1/W2 实测（anchor3.py） |
| P5 数据负例锚点 | anchor3.py N1-N3（StreamError 实测） |
| P7 判据数据 | §7 主基线 3 轮原始值（3389/4357/3922，极差/中位 28%） |

口径说明：对账表"100 构造器" = `__init__.py` 导出的用户面构造器名（AST 提取，
排除异常类/Descriptor 类型名/工厂函数/Mixin/HashAlgo 参数枚举/CompiledSchema
编译产物）；与 `docs/constructors-inventory.csv` 的 101 implemented 口径不同
（后者含 StructMixin 等非导出名、不含单例别名展开差异）——两者各自自洽，对账
断言基于前者（每个可作为 `field()` 参数的公开名都必须有分类）。

### v2.3 回填（ARCH 裁决，2026-09-04）

1. **§5.3 补**：延迟桩 init_defaults 回退统一为 Optional（Value 档不再终身退化必填；与"错误时机=build 值使用点"哲学一致；build 侧行为由 resolve 保证不变）
2. **§5.2 补**：Instance/Control 分支 getattr 错误判据收敛单源——AttributeError≡缺值/缺省（Instance→BuildValueMissing / Control→哑值 None），其余 Err 保持 Generic 包装；Const/Default 同判据（F1 泛化）。sequence.rs 的 get_item（IndexError 位置语义）不属本判据，维持现状
3. **§10.10 澄清**：DefaultNode 节点面 bytes/str 常量能力保留；字段面 `field(Default(Bytes(2), b"ab"))` 因 Descriptor 编译期非 i64 校验当前即拒绝，端到端形态待 v0.1.3 表达式 VM 扩展随 i64 限制一并解除