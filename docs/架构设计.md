# construct-rs 架构设计

> 本文档定义 construct-rs 的完整架构，覆盖 Python 接口、FFI 边界、Rust 执行模型、
> 模块分层、Schema 编译管线、Phase 1 范围六个维度。
>
> 本文档基于 `docs/FFI设计.md` 和 `docs/Mixin设计.md` 两份设计原则文档，是其原则在
> construct-rs 项目中的具体落地。读者应先阅读这两份文档以理解核心设计理念。
>
> 本文自包含——读者无需阅读任何参考项目源码即可理解全部设计。
> DEV 拿到本文档应能直接开始编码：所有接口签名、数据结构、模块划分均具体到可实现。

---

## 0. 文档定位与最高约束

### 0.1 适用对象

- **实现阶段（DEV）**：本文档是 Phase 1 编码的唯一架构依据。所有 Rust 类型签名、Python
  接口、模块层次以本文档为准。
- **审查阶段（VET/REV）**：本文档是"实现是否偏离架构"的检查基准。
- **后续阶段**：Phase 2+ 的扩展必须与本文档定义的分层和执行模型兼容。

### 0.2 最高约束（不可违反，违反即架构错误）

以下约束源自 `AGENTS.md §0` 与两份设计原则文档，本文档的所有设计均在其之下：

1. **一次 FFI**：编译、parse、build 各只有一次 Python↔Rust 边界穿越。Rust 内部通过
   CPython C API 直接操作 Python 对象（FFI 设计 §4）。
2. **无中间表示层**：parse 直接从 bytes 构造 PyObject，build 直接从 PyObject 读属性
   写字节，不在 Rust 侧引入独立的中值枚举再转换（FFI 设计 §5）。
3. **pyo3 是核心依赖**：单 crate，不存在独立的 Python 绑定层。
4. **静态分派**：执行树节点用封闭 `enum` + `match` 分派，禁止 `Box<dyn Trait>`（FFI 设计 §6）。
5. **mashumaro 式 API**：`@dataclass class X(StructMixin)`，编译在 `__init_subclass__`
   中完成，用户无感（Mixin 设计 §3）。
6. **Rust 编码红线**：禁止 `unwrap()`/`expect()`/`panic!` 在非测试代码；非法输入返回
   `Err`；所有 `pub` 项有文档注释；错误携带 path。

### 0.3 性能目标

- **硬目标**：≥ 4x vs Python construct 2.10.70（Phase 1 验收门槛）
- **理想目标**：10x
- 低于 4x 视为架构失败，须回退至设计阶段。

---

## 1. 术语表

| 术语 | 含义 |
|------|------|
| **FFI 穿越** | 一次 Python 解释器框架与 Rust 框架之间的完整切换调用，代价约 100–300ns |
| **C API 调用** | Rust 代码（已持有 GIL）调用 CPython 暴露的 C 函数，进程内调用，代价约 20–80ns，**不是** FFI 穿越 |
| **执行树** | parse/build 在 Rust 侧展开的、由构造器节点组成的不可变树形结构 |
| **构造器节点（Node）** | 执行树中的一个节点，对应一种二进制读写原语（如读取 int8、组装 Struct） |
| **编译产物（CompiledSchema）** | 类定义时一次性构建的、可在运行时反复使用的执行树，不可变 |
| **构造器（Construct）** | 执行树节点实现的统一 trait，定义 parse/build/sizeof 行为 |
| **类型描述符（Descriptor）** | 用户通过 `field()` 指定的二进制格式声明（如 `Int8ub`、`Bytes(4)`），Python 可见对象 |
| **Schema** | 用户类中字段信息的集合（字段名、顺序、描述符），编译的输入 |
| **字段（Field）** | StructMixin 子类中通过 `name: type = field(subcon)` 声明的一个二进制字段 |
| **Mixin 基类（StructMixin）** | 框架提供的、用户类需继承的基类，提供 parse/build 方法 |
| **类创建钩子** | Python 的 `__init_subclass__` 机制，在子类创建时自动调用 |
| **延迟桩（Stub）** | 前向引用未解析时安装在类上的占位 parse/build 方法，首次调用时触发真正的编译 |
| **前向引用** | 字段类型描述符引用了在同一类定义之后才定义的类 |
| **Context** | Rust 内部的上下文数据结构（持有 Python dict 引用），用于字段间引用与嵌套层传递 |
| **Stream** | Rust 内部的字节流游标抽象（parse 读、build 写），不跨 FFI |
| **Path** | 错误追踪路径（如 `header.flags[2]`），用于定位嵌套结构中的出错字段 |
| **用户钩子回调** | Rust 执行树内部不可避免地回调 Python 函数（如构造用户类实例），归类为 FFI 设计 §4.4 的例外 |

---

## A. Python 接口设计

### A.1 用户 API 总览

用户视角下的完整 API（与 `examples/example.py` 对齐）：

```python
from dataclasses import dataclass
from construct import StructMixin, field, Int8ub, GreedyBytes

@dataclass
class ModbusRTUMessage(StructMixin):
    address: int = field(Int8ub)
    function_code: int = field(Int8ub)
    data: bytes = field(GreedyBytes)

# build：从实例构造字节
msg = ModbusRTUMessage(address=1, function_code=3, data=b'\x00\x01')
built = msg.build()                       # → b'\x01\x03\x00\x01'

# parse：从字节构造实例
parsed = ModbusRTUMessage.parse(built)    # → ModbusRTUMessage(address=1, ...)
```

### A.2 StructMixin 基类

`StructMixin` 是纯 Python 实现的基类（不使用 pyclass），负责协调编译流程与 @dataclass。
Rust 仅承担编译/parse/build 三个计算入口。

```python
class StructMixin:
    """所有用户二进制结构类的基类。

    子类需配合 @dataclass 装饰器使用。类创建时自动触发 Schema 编译，
    编译产物存于类属性 _construct_compiled。
    """

    # 编译产物缓存槽（由 __init_subclass__ 设置），类属性
    _construct_compiled: "CompiledSchema | None" = None

    def __init_subclass__(cls, **kwargs):
        super().__init_subclass__(**kwargs)
        _compile_schema_for_class(cls)

    @classmethod
    def parse(cls, data: bytes) -> "StructMixin":
        """从字节解析为本类的实例。

        恰好一次 FFI 穿越：Rust 内部直接构造用户类实例并返回。
        不再有 Python 侧 cls(**dict)——Rust 内构造独立 PyDict + tp_new 创建空实例 +
        PyObject_GenericSetAttr 整体替换 __dict__，可选调用 __post_init__。
        （方案 B'，与 pydantic-core 对齐，详见 §B.7）
        """
        schema = cls._construct_compiled
        if schema is None:
            raise ConstructError(
                f"{cls.__name__} 尚未完成编译（可能存在未解析的前向引用），"
                "请确保所有被引用的类型已定义后再调用 parse"
            )
        return schema._parse_raw(data)          # ← 唯一一次 FFI 穿越，直接返回实例

    def build(self) -> bytes:
        """从本实例构造字节。

        恰好一次 FFI 穿越：Rust 内部遍历执行树，通过 C API 读取本实例属性，
        直接写入输出缓冲区。
        """
        schema = type(self)._construct_compiled
        if schema is None:
            raise ConstructError(
                f"{type(self).__name__} 尚未完成编译"
            )
        return schema._build_raw(self)          # ← 唯一一次 FFI 穿越
```

**关键设计决策：parse 直接在 Rust 内构造实例（方案 B'，设计修订 2026-06-22 R3）**

`_parse_raw` 在 Rust 内部直接构造用户类实例，采用方案 B'（与 pydantic-core 对齐）：
1. `PyDict::new(py)` 创建独立 dict（可选 Presized 预分配）
2. 逐字段 `dict.set_item(interned_key, value)`
3. `tp_new(cls, (), NULL)` 创建空实例（`create_class` 辅助函数）
4. `PyObject_GenericSetAttr(instance, "__dict__", dict)` 整体替换 `__dict__`
5. 可选 `__post_init__` 调用

Python 侧直接返回实例。原因：

1. **消除 O(N²) kwargs unpacking**：原方案 `cls(**dict)` 的 `**dict` unpacking
   是 O(N²)（实测 ~5 ns/N²），100 字段结构占 parse 总耗时 63%。改为 Rust 内构造
   实例（O(N)），彻底消除此瓶颈。
2. **符合 §0 "无中间表示层"原则**：parse 直接从 bytes 构造目标 PyObject（用户类实例）。
   Rust 内构造的 PyDict 是构造过程的内部临时对象，不跨 FFI 边界返回，不是 §0 禁止的
   "中间数据类型再转换"（详见 `docs/设计修订-parse路径优化.md` §0.3）。
3. **严格保持"一次 FFI"**：`_parse_raw` 进入 Rust 一次，返回一次。Rust 内部的
   `tp_new`、`PyDict_SetItem`、`PyObject_GenericSetAttr`、`__post_init__` 调用均为
   C API 操作（§FFI 设计 §2.4），不计为 FFI 穿越。
4. **`PyObject_GenericSetAttr` 绕过自定义 `__setattr__`**：方案 B' 用
   `object.__setattr__` 的 C 实现（`PyObject_GenericSetAttr`）整体替换 `__dict__`，
   绕过 frozen dataclass 生成的会 raise `FrozenInstanceError` 的 `__setattr__`。
   语义更明确、更安全（vs R2 方案 B 的逐字段 dict.set_item）。
5. **`__post_init__` 语义覆盖**：编译期检查类是否定义 `__post_init__`（`has_post_init`
   标志），有则在字段设置完毕后从 Rust 调用一次（用户钩子回调，归类为 FFI 设计
   §4.4 例外）。无则零开销。
6. **嵌套字段的实例化**：StructRefNode.parse 委托对方 root（StructNode）的 parse，
   后者内部已构造对方实例，**无需 Rust→Python 回调**（详见 §B.7 与 §C.3.5）。

> **设计修订背景**：详见 `docs/设计修订-parse路径优化.md`（R3）。原方案"parse 返回 dict，
> Python 侧 cls(**dict)"在 Phase 1 基准测试中导致 B4（100 字段）加速比仅 0.94x，
> 且违反 §0"无中间表示层"原则（dict 跨 FFI 返回是中间表示）。R3 采用方案 B'
> （与 pydantic-core 对齐）彻底修正。

### A.3 field() 函数

```python
def field(subcon):
    """声明一个二进制字段。

    参数:
        subcon: 类型描述符对象，决定该字段的二进制格式。
                必须是框架已知的描述符类型（Int8ub、Bytes(n)、GreedyBytes、
                或另一个 StructMixin 子类）。

    返回:
        一个 _FieldDescriptor 对象，同时满足:
        - 作为 @dataclass 的字段默认值（实现 dataclass 字段协议）
        - 携带 subcon 引用，供 __init_subclass__ 提取

    该函数在被调用时记录全局递增序号，保证字段顺序与声明顺序一致。
    """
```

`field()` 的返回值 `_FieldDescriptor` 必须同时满足两套协议：

| 协议 | 要求 | 用途 |
|------|------|------|
| `@dataclass` 字段协议 | 表现为合法的默认值或 dataclass Field 对象 | 让 `@dataclass` 能生成正确的 `__init__` |
| Mixin 编译协议 | 内部存储 `subcon` 引用，可被 `__init_subclass__` 提取 | 提供二进制格式信息给编译器 |

**字段顺序保证**：`field()` 利用 Python 3.7+ 类体命名空间（`__dict__`）的有序性，在
`__init_subclass__` 中按 `cls.__dict__` 的插入顺序提取字段。不依赖 `__annotations__`
的顺序（避免 PEP 526 与 `@dataclass` 重排的歧义）。

### A.4 类型描述符（Descriptor）

类型描述符是 Python 可见的对象，用户通过 `field()` 传入。三种形态：

#### A.4.1 预定义单例（无参数原子描述符）

```python
Int8ub    # 1 字节无符号大端整数（等价于 FormatField(">", "B")）
Int8sb    # 1 字节有符号大端整数
Int16ub   # 2 字节无符号大端整数
Int16sb, Int16ul, Int16sl
Int32ub, Int32sb, Int32ul, Int32sl
Int64ub, Int64sb, Int64ul, Int64sl
GreedyBytes   # 读取剩余全部字节
```

这些是 Rust pyclass 实例（在模块加载时创建一次，作为模块级常量）。每个实例内部存储
构建对应执行树节点所需的全部参数（如 `Int8ub` 内部存 `endianity='>', format='B'`）。

#### A.4.2 可实例化描述符（带参数）

```python
Bytes(length)            # 读取 length 字节（length 为正整数常量）
```

Phase 1 仅支持 `length` 为编译期常量整数。`length` 为上下文 lambda（如
`Bytes(this.length)`）的能力推迟到 Phase 2（需表达式系统支持）。

#### A.4.3 StructMixin 子类引用（嵌套结构）

```python
@dataclass
class Inner(StructMixin):
    x: int = field(Int8ub)

@dataclass
class Outer(StructMixin):
    inner: Inner = field(Inner)   # ← 直接传 StructMixin 子类作为描述符
```

`field(Inner)` 中 `Inner` 是一个 Python 类对象。编译器识别其为 `StructMixin` 子类后，
在执行树中创建一个递归引用节点（详见 §C.5 与 §E.5）。

### A.5 与标准 @dataclass 的集成

#### A.5.1 执行顺序（已由 REV 在 0.1 实测验证）

用户写 `@dataclass class X(StructMixin): ...` 时，Python 执行顺序：

1. 执行类体 → 收集 `address = field(Int8ub)` 到命名空间
2. `type.__new__` 创建类对象 `X`
3. **`StructMixin.__init_subclass__(X)` 触发**（此刻 `@dataclass` 尚未执行）
4. `@dataclass(X)` 装饰器执行 → 生成 `__init__`、`__dataclass_fields__`

**关键约束**：步骤 3 中编译器**不能**依赖 `__dataclass_fields__`（它还不存在），必须从
类体原始属性（`field()` 的返回值）提取字段信息。

#### A.5.2 字段信息来源协调

| 信息 | @dataclass 的用途 | StructMixin 的用途 |
|------|-------------------|-------------------|
| 字段名 | `__init__` 参数名 | 执行树节点标识；parse dict 的 key；build 时 getattr 的名字 |
| 类型注解（`address: int`） | `__init__` 类型提示（不影响运行时） | 不影响二进制格式（仅文档作用） |
| `field()` 返回值 | 作为字段默认值/描述符 | **从中提取 subcon，决定二进制格式** |

`field()` 返回值是两者的唯一桥梁，必须同时满足两套协议（见 §A.3）。

### A.6 空 StructMixin 子类的行为（落实 REV 建议 4）

无任何 `field()` 声明的子类是合法的边界情况：

```python
@dataclass
class Empty(StructMixin):
    pass
```

**定义行为**：编译成功，执行树为空 Struct 节点（零子节点）。

| 操作 | 行为 |
|------|------|
| `Empty.parse(b'')` | 返回 `Empty()`（无字段实例），不读任何字节 |
| `Empty.parse(b'abc')` | 返回 `Empty()`，**忽略**多余字节（与 Python construct 的 Struct 语义一致：Struct 不要求消费全部输入） |
| `Empty().build()` | 返回 `b''`（空字节） |
| `Empty().build()` 内部 | 不写任何字节，输出缓冲为空 |

此行为对协议头/尾的占位类型、纯标记类有意义。编译器不应对零字段报错。

---

## B. FFI 边界设计

本节定义 Python↔Rust 之间的三个 FFI 入口，以及 Schema 传递、编译产物结构、对象构造
与属性读取的精确机制。核心约束：每个入口恰好一次 FFI 穿越。

### B.1 三个 FFI 入口总览

| 入口 | 触发时机 | Python→Rust 输入 | Rust→Python 输出 | FFI 穿越次数 |
|------|---------|------------------|------------------|-------------|
| **编译** | `__init_subclass__` 中一次 | 类对象 + 描述符列表 | `CompiledSchema`（pyclass 实例） | 1 |
| **parse** | `cls.parse(data)` 中一次 | `CompiledSchema` + `bytes` | 用户类实例（方案 B'：PyDict 构造 + `tp_new` + `GenericSetAttr` 整体替换 `__dict__`） | 1 |
| **build** | `obj.build()` 中一次 | `CompiledSchema` + 用户实例 | `bytes` | 1 |

三个入口都在热路径之外或为单次穿越。编译在类定义时一次性发生；parse/build 每次调用
各一次穿越。Rust 内部遍历执行树的所有 C API 调用（创建 int、set_item、getattr 等）
**不计为 FFI 穿越**（FFI 设计 §2.4）。

### B.2 编译入口：compile_schema

```rust
/// Python 可见签名（概念性）：
///   compile_schema(cls: type, descriptors: list[Descriptor]) -> CompiledSchema
#[pyfunction]
fn compile_schema(
    py: Python<'_>,
    cls: &Bound<'_, PyType>,
    descriptors: &Bound<'_, PyList>,
) -> PyResult<CompiledSchema>;
```

**输入**：
- `cls`：用户类对象（`StructMixin` 子类）。用于：(a) 编译失败时在错误信息中报类名；
  (b) 传入 StructNode 作为 `cls` 字段（设计修订后：parse 时 `create_class(cls)` 读
  `tp_new` 槽位构造实例 + 编译期检查 `__post_init__`）；(c) 编译期检测 `__slots__`
  （命中则 fail fast）；(d) R3：记录 `field_count`（用于 `PyDict_NewPresized`）。
- `descriptors`：字段描述符列表，**按字段声明顺序排列**。每项是一个 pyclass 实例
  （Int8ub、Bytes 实例、GreedyBytes、或 StructMixin 子类）。

**Rust 内部流程**（一次 FFI 内完成，全为 C API 调用）：
1. **R2（P2）**：检测 `cls` 是否定义 `__slots__`，命中则返回
   `ConstructError::Compilation`（fail fast，无运行时回退，见 §B.7）。
2. **R3 变更**：~~缓存 `object.__new__`~~ → 不再缓存。`create_class` 辅助函数
   在 parse 时直接从 `cls.as_type_ptr()` 读 `tp_new` 槽位（~1-2ns），与 pydantic-core
   `create_class`（model.rs:348-365）一致。
3. 创建空的字段节点收集 `Vec<Node>`。
4. 遍历 `descriptors` 列表，对每个描述符：
   - 尝试 `extract::<FormatFieldDescriptor>` → 成功则构建 `Node::FormatField(...)`。
   - 尝试 `extract::<BytesDescriptor>` → 成功则构建 `Node::Bytes(...)`。
   - 尝试 `extract::<GreedyBytesDescriptor>` → 成功则构建 `Node::GreedyBytes`。
   - 尝试识别为 `StructMixin` 子类（检查 `issubclass`）→ 构建 `Node::StructRef(...)`。
   - 全部失败 → 返回 `ConstructError::UnknownDescriptor`，携带字段名与描述符 repr。
5. 检测 `has_post_init`（`cls.getattr("__post_init__")`）。
6. **R3**：记录 `field_count = fields.len()`（用于 parse 时 `PyDict_NewPresized`）。
7. 将字段节点列表组装为 `Node::Struct { fields, cls, has_post_init, field_count }` 作为根节点。
8. 返回 `CompiledSchema { root, cls }`。

**描述符类型识别策略**：依赖 pyo3 的 `extract`（基于 `PyTypeInfo`）。每个描述符 pyclass
在 Rust 侧有对应的 Rust 结构体，`extract` 成功即匹配。这是类型安全的，无字符串标识。

**前向引用处理**：当描述符是 `StructMixin` 子类但该子类**尚未完成编译**（`_construct_compiled`
为 `None`，即对方类还在延迟桩阶段），编译器不为该字段报错，而是构建
`Node::StructRef { cls: Py<PyType>, lazy: true }`——存类引用，运行时动态解析（详见 §C.5）。
这保证互相引用的结构体（NodeA↔NodeB）可行（落实 REV 建议 1，关键）。

### B.3 parse 入口：CompiledSchema._parse_raw

```rust
#[pymethods]
impl CompiledSchema {
    /// Python 可见签名：
    ///   _parse_raw(self, data: bytes) -> Any  （用户类实例）
    #[pyo3(signature = (data))]
    fn _parse_raw<'py>(
        &self,
        py: Python<'py>,
        data: &Bound<'py, PyBytes>,
    ) -> PyResult<Bound<'py, PyAny>>;
}
```

**输入**：`data`（bytes 对象），用户传入的二进制数据。

**输出**：用户类的实例（`Bound<PyAny>`）。Rust 内部通过方案 B' 构造实例：
独立 PyDict 构造 + `tp_new` 创建空实例 + `PyObject_GenericSetAttr` 整体替换 `__dict__`
+ 可选调用 `__post_init__`。

**Rust 内部流程**（一次 FFI 内完成）：
1. 从 `PyBytes` 提取 `&[u8]`，创建 `ParseStream { data, pos: 0 }`（纯 Rust）。
2. 创建根 `Context`。
3. 调用 `self.root.parse(stream, ctx, path)` —— 进入执行树遍历。
   - 每个节点通过 `match` 静态分派（FFI 设计 §6）。
   - 叶子节点（如 FormatField）读字节、通过 C API 创建 PyObject、返回。
   - **Struct 节点直接构造实例（方案 B'）**：`PyDict::new` → 逐字段
     `PyDict_SetItem`（interned key）→ `create_class(cls)`（`tp_new`）→
     `force_setattr(instance, "__dict__", dict)`（`PyObject_GenericSetAttr` 整体替换）
     → 可选 `__post_init__`。
   - 嵌套 StructRef 节点：委托对方 root.parse（对方 StructNode 内部构造实例）。
4. 返回根 Struct 节点产出的用户类实例。
5. 任何节点返回 `Err` → 通过 `?` 传播至入口，pyo3 自动转为 Python 异常（FFI 设计 §7）。

**为什么直接构造实例而非返回 dict（方案 B'，设计修订 2026-06-22 R3）**：

原方案返回 dict 由 Python 侧 `cls(**dict)` 构造实例，有两个问题：(1) `**dict` kwargs
unpacking 是 **O(N²)**（实测 ~5 ns/N²），100 字段时占 parse 总耗时 63%；(2) dict 跨
FFI 返回违反 §0"无中间表示层"原则。R3 采用方案 B'（与 pydantic-core 对齐）：Rust 内
构造独立 PyDict + `tp_new` + `PyObject_GenericSetAttr` 整体替换 `__dict__`（O(N)），
dict 不跨 FFI。详见 `docs/设计修订-parse路径优化.md` §0、§2.2。

### B.4 build 入口：CompiledSchema._build_raw

```rust
#[pymethods]
impl CompiledSchema {
    /// Python 可见签名：
    ///   _build_raw(self, obj: StructMixin) -> bytes
    fn _build_raw<'py>(
        &self,
        py: Python<'py>,
        obj: &Bound<'py, PyAny>,
    ) -> PyResult<Bound<'py, PyBytes>>;
}
```

**输入**：`obj`，用户类的实例（任意 StructMixin 子类实例）。Rust 通过 C API 读取其属性。

**输出**：`PyBytes`，构建的字节数据。

**Rust 内部流程**（一次 FFI 内完成）：
1. 创建 `BuildStream { buf: Vec<u8>::with_capacity(预估容量) }`（纯 Rust）。
2. 创建根 `Context`。
3. 调用 `self.root.build(obj, stream, ctx, path)` —— 进入执行树遍历。
   - Struct 节点：遍历字段，对每个字段 `getattr(obj, field_name)`（C API）取值，
     传递给子节点的 build。
   - 叶子节点（如 FormatField）：从 PyObject 提取数值（`extract::<i64>` 等），写入缓冲。
   - 嵌套 StructRef：`getattr(obj, field_name)` 取得子实例，递归 build。
4. 将 `Vec<u8>` 转为 `PyBytes` 返回。

**属性读取方式**：使用 `obj.getattr(name)`（对应 C API `PyObject_GetAttr`）。这兼容
普通 dataclass 实例（属性存在 `__dict__` 或为 slot）、`@dataclass(frozen=True)`、
以及任何实现了属性访问的对象。**不**假设对象内部是 dict 还是 slot。

**为什么不读 `__dict__` 直接访问**：`getattr` 是 Python 属性访问的标准语义，会触发
描述符协议、property、`__getattr__` 等。直接读 `__dict__` 会绕过这些，可能漏掉
`@property` 定义的字段。Phase 1 采用 `getattr` 保证语义正确。性能上 `getattr` 约
40–80ns（C API），可接受。

### B.5 Schema 从 Python 传到 Rust 的机制

```
┌─────────────────────────────────────────────────────────────────┐
│ Python 侧（__init_subclass__ 中）                                │
│                                                                 │
│  1. 遍历 cls.__dict__，按顺序收集 field() 返回值                 │
│  2. 从每个 field() 返回值提取 subcon（类型描述符）               │
│  3. 组装为 [(field_name, subcon), ...] 列表                     │
│  4. 调用 _native.compile_schema(cls, [subcon1, subcon2, ...])   │
│     （field_name 信息由描述符在列表中的位置隐式传递，             │
│      compile_schema 同时读取 cls 的 __annotations__ 获取字段名）  │
└─────────────────────────────────────────────────────────────────┘
                              │  1 次 FFI 穿越
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ Rust 侧（compile_schema 内）                                     │
│                                                                 │
│  1. 读取 cls.__annotations__，获取有序字段名列表                 │
│  2. 遍历 descriptors 列表，逐个 extract 为具体描述符类型          │
│  3. 根据描述符类型构建对应 Node                                   │
│  4. 组装为 Node::Struct { fields: Vec<(String, Node)>, cls }     │
│  5. 包装为 CompiledSchema 返回                                   │
└─────────────────────────────────────────────────────────────────┘
```

**字段名来源**：`cls.__annotations__`（类型注解字典，Python 3.7+ 保序）。`field()`
声明的字段必然有类型注解（`address: int = field(Int8ub)`），故 `__annotations__` 的
键集 ⊇ 字段集。编译器按 `__annotations__` 顺序取字段名，与 `descriptors` 列表位置对应。

**为什么不直接传字段名列表**：可以传。但利用 `__annotations__` 已有的有序信息更简洁，
且字段名与类型注解天然绑定（用户写 `address: int` 时，`address` 既是注解键又是字段名）。
实现上 Python 侧可显式传 `[(name, subcon), ...]` 元组列表，Rust 侧解包。两种等价，
DEV 实现时任选其一，本文档推荐传元组列表以减少 Rust 侧的 `__annotations__` 读取。

### B.6 编译产物 CompiledSchema 的结构与缓存

```rust
/// 编译产物：一棵不可变的执行树，关联到用户类。
#[pyclass(frozen, name = "CompiledSchema")]
pub struct CompiledSchema {
    /// 执行树根节点（通常是 Node::Struct）。
    /// StructNode 内部持有 cls、has_post_init、field_count（R3：用于 Presized）。
    /// R3 移除了 R2 的 object_new 字段（改用 create_class 辅助函数读 tp_new 槽位）。
    root: Node,
    /// 用户类的引用，用于:
    /// - 调试与错误信息中报告类名
    /// - 与根 StructNode.cls 指向同一对象（N1：非冗余存储，见设计修订 §3.2）
    cls: Py<PyType>,
}
```

**不可变性**：`#[pyclass(frozen)]` 使 Python 侧无法修改其属性。Rust 侧 `root` 为
`Node` 枚举（不可变）。运行时不允许增删节点或修改参数（FFI 设计 §3.4）。

**缓存机制**（落实 Mixin 设计 §5.2 方案 A）：
- `CompiledSchema` 实例存为类属性 `cls._construct_compiled`。
- Python 属性查找是 O(1)（dict 查找）。
- `parse`/`build` 通过 `cls._construct_compiled` 访问，零编译开销。
- 重新定义类（重新执行 `class X(StructMixin): ...`）触发新的 `__init_subclass__`，
  覆盖旧的 `_construct_compiled`——这是预期行为（新类定义 = 新执行树）。

**线程安全**：`CompiledSchema` 是 `frozen` pyclass，内部 `Node` 不可变，可被多线程
安全共享（在 GIL 约束下）。parse/build 调用无副作用，可并发。

### B.7 dataclass 实例构造与属性读取（设计修订 2026-06-22 R3，方案 B'）

**parse 侧（Rust 内直接构造实例，方案 B'）**：

Rust 内部遍历执行树时，Struct 节点直接构造用户类实例，采用方案 B'（与 pydantic-core
对齐）：
1. `PyDict::new(py)`（或 `PyDict_NewPresized(field_count)`）创建**独立**空 dict。
   R3 的 Presized 预分配消除大 N 时的 dict resize 开销。
2. 对每个字段：递归 parse 子节点 → 用编译期缓存的 interned key 执行
   `PyDict_SetItem(dict, key, value)`（set_item 到独立 dict，非 instance.__dict__）。
3. `create_class(cls)` 创建空实例——直接读 `cls.as_type_ptr()` 的 `tp_new` 槽位
   并调用（`new_func(raw_type, empty_tuple, NULL)`），等价 `cls.__new__(cls)`，
   绕过 Python 层方法查找。与 pydantic-core `create_class`（model.rs:348-365）一致。
4. `force_setattr(instance, "__dict__", dict)` 整体替换实例的 `__dict__` 指针——
   通过 `PyObject_GenericSetAttr`（`object.__setattr__` 的 C 实现）执行，**绕过**
   任何自定义 `tp_setattro`（含 frozen dataclass 的 `__setattr__`）。与 pydantic-core
   `set_model_attrs`（model.rs:367-379）+ `force_setattr`（model.rs:381-394）一致。
5. 可选：如果类定义了 `__post_init__`，调用 `instance.__post_init__()`（用户钩子回调）。

**R3 vs R2（方案 B' vs 方案 B）的差异**：
- R2 方案 B：`object.__new__(cls)` → 获取 `instance.__dict__` → 逐字段 set_item
  到 instance.__dict__（就地修改）。
- R3 方案 B'：独立 dict 构造 → `tp_new` → `GenericSetAttr` 整体替换。
- **优势**：(1) `GenericSetAttr` 绕过 frozen `__setattr__`（语义更安全）；
  (2) Presized dict 消除大 N resize（性能更优）；(3) 与 pydantic-core 生产验证对齐。

**为什么不用 `cls(**dict)`**（设计修订）：

原方案 parse 返回 dict，Python 侧 `cls(**dict)` 构造实例。但：(1) `**dict` kwargs
unpacking 是 **O(N²)**（~5 ns/N²），100 字段时占 parse 总耗时 63%；(2) dict 跨 FFI
返回违反 §0"无中间表示层"原则。R3 采用方案 B' 彻底修正。

**语义等价性保证**：

| `@dataclass.__init__` 做的事 | 方案 B' 是否覆盖 | 方式 |
|------------------------------|-----------------|------|
| 逐字段 setattr | ✅ 等价 | 独立 dict 构造 + `GenericSetAttr` 整体替换 `__dict__` |
| 调用 `__post_init__` | ✅ 条件覆盖 | `has_post_init` 标志触发回调 |
| 类型转换/校验 | ❌ 不覆盖 | `@dataclass` 默认不生成类型转换 |

对于标准 `@dataclass`（无自定义 `__init__`），方案 B' 的 `create_class` +
`force_setattr(__dict__)` 与 `cls(**dict)` 语义完全等价。

**`__post_init__` 处理**：

编译期检查 `cls` 是否定义了 `__post_init__`（检查 `cls.__dict__` 和 MRO）。
结果存为 `StructNode.has_post_init: bool`。parse 时：

```rust
// force_setattr 替换 __dict__ 后（R3 方案 B'）
if self.has_post_init {
    instance.call_method_bound(py, "__post_init__", (), None)?;
    // 这是 Rust→Python 回调，归类为 FFI 设计 §4.4 的用户钩子例外
    // 每次调用约 200-400ns，但只在有 __post_init__ 的类上发生
}
```

无 `__post_init__` 的类（大多数情况）：零开销。

**嵌套字段的实例化**：

StructRefNode.parse 委托对方 root（StructNode）的 parse，后者内部已构造对方类
实例。不再需要 Rust→Python 回调来调用 `cls(**dict)`。

```rust
// StructRef 节点的 parse（简化，设计修订后）
fn parse(&self, py, stream, ctx, path) -> Result<Py<PyAny>, ConstructError> {
    let schema = self.resolve_schema(py)?;
    let root = schema.root();
    // root.parse 内部已构造对方类实例（因为 StructNode 持有 cls 并直接 __new__）
    root.parse(py, stream, ctx, path)
    // 不再需要 cls.call((), Some(&sub_dict)) —— 回调消除
}
```

**`__slots__` 处理（R2 统一，R3 保留）**：使用 `__slots__` 的 dataclass 无 `__dict__`，
方案 B' 的整体替换不可用。**编译期**（`compile_schema` 中）检测 `__slots__`，命中则返回
`ConstructError::Compilation`——类定义时（`__init_subclass__`）即 fail fast 报错，
**不保留运行时回退路径**。理由：Phase 1 是垂直切片，`__slots__` dataclass 罕见，
fail fast 符合"编译错误导入时报出"原则，避免引入 slow_path 分支复杂度。
> **R3 补充**：pydantic-core 的 DataclassValidator 对 slots 类用逐字段 `force_setattr`
> （dataclass.rs:658-662），后续阶段如需支持 slots 可参考。
详见 `docs/设计修订-parse路径优化.md` §3.6。

**语义差异与已知限制（R3 更新 N2、保留 N3）**：
- 标准 `@dataclass`（含 `frozen=True`）不受影响——方案 B' 的 `GenericSetAttr`
  绕过 frozen `__setattr__`（会 raise `FrozenInstanceError`），整体替换 `__dict__`。
- 用户**额外**自定义 `__setattr__`（非 dataclass 生成）的类：`GenericSetAttr` 会绕过
  用户逻辑，此为已知限制，Phase 1 文档标注。
- `dataclasses.InitVar` 字段不应声明 `field()`（无二进制表示）；误用按普通字段处理。

**build 侧（读取属性）**：
- Rust 对每个字段 `obj.getattr(field_name)`（C API `PyObject_GetAttr`），取得值后传给
  子节点 build。
- 嵌套字段：`getattr(obj, "inner")` 返回 Inner 实例，递归 build。
- **无 Python 回调**——build 全程通过 C API 读取，严格一次 FFI。
- **Phase 1 优化**：不调用 `ctx.set_field`（Phase 1 无 this 表达式，context 不被读取）。

### B.8 ConstructError 与 PyResult 的边界（落实 REV 建议 2）

**Rust 内部错误类型**：

```rust
/// construct-rs 内部统一错误类型，携带结构化信息。
#[derive(Debug, thiserror::Error)]
pub enum ConstructError {
    #[error("stream error: {message} at {path}")]
    Stream { message: String, path: String },

    #[error("format field error: {message} at {path}")]
    FormatField { message: String, path: String },

    #[error("field length error: {message} at {path}")]
    FieldLength { message: String, path: String },

    #[error("compilation error: {message}")]
    Compilation { message: String },

    #[error("unresolved type reference: {message}")]
    UnresolvedReference { message: String },

    #[error("{message} at {path}")]
    Generic { message: String, path: String },
}
```

**与 PyResult 的关系**：

```
节点方法签名:  fn parse(...) -> Result<T, ConstructError>     ← Rust 内部用结构化错误
                              ↓ impl From<ConstructError> for PyErr
FFI 入口签名:  fn _parse_raw(...) -> PyResult<PyDict>          ← 入口处转为 PyErr
                              ↓ pyo3 自动抛出
Python 侧:     except ConstructError as e: ...                 ← 用户捕获 Python 异常
```

- **Rust 内部**：所有节点方法返回 `Result<T, ConstructError>`。错误通过 `?` 传播。
  `ConstructError` 携带 `path`（出错位置）和结构化 `kind`（错误类别）。
- **FFI 入口**：三个入口函数返回 `PyResult<T>`。通过 `impl From<ConstructError> for PyErr`
  自动转换：`ConstructError::Stream` → Python `StreamError`，依此类推。
- **Python 侧**：异常层次与 Python construct 对齐（`ConstructError` 基类 → 各子类），
  携带 `path` 属性，`str(e)` 格式为 `"Error in path {path}\n{message}"`（与 Python construct 一致）。

**成功路径零成本**（FFI 设计 §7.3）：`Result` 是普通值，`?` 在成功时不分配错误对象。
错误对象（含 path 字符串）只在失败路径构造。

---

## C. Rust 执行模型

本节定义执行树的内部结构、Construct trait、enum_dispatch 静态分派、Stream 与 Context
抽象。这些都是 Rust 内部数据结构，**不跨 FFI**（FFI 设计 §4）。

### C.1 Construct trait

执行树的每个节点实现统一的 `Construct` trait。签名严格遵循"直接操作 Python 对象"原则
（FFI 设计 §5）：parse 返回 `Py<PyAny>`，build 接收 `&Bound<PyAny>`。

```rust
use pyo3::prelude::*;
use pyo3::types::{PyAny, PyDict};

/// 所有构造器节点实现的统一接口。
/// parse 直接产出 Python 对象，build 直接读取 Python 对象——不经过任何 Rust 中间类型。
pub trait Construct {
    /// 从流中解析，直接构造并返回 Python 对象。
    ///
    /// - `stream`: 字节流游标（纯 Rust，可读写位置）
    /// - `ctx`: 当前上下文（持有 Python dict 引用，供字段间引用）
    /// - `path`: 错误追踪路径栈（如 "header.flags"）
    fn parse(
        &self,
        py: Python<'_>,
        stream: &mut ParseStream<'_>,
        ctx: &mut Context<'_>,
        path: &mut Path,
    ) -> Result<Py<PyAny>, ConstructError>;

    /// 从 Python 对象构建，将字节写入流。
    ///
    /// - `obj`: 待构建的 Python 对象（用户实例或标量值）
    /// - `stream`: 输出缓冲
    fn build(
        &self,
        py: Python<'_>,
        obj: &Bound<'_, PyAny>,
        stream: &mut BuildStream,
        ctx: &mut Context<'_>,
        path: &mut Path,
    ) -> Result<(), ConstructError>;

    /// 静态大小（字节）。无法预知时返回 Err（对应 Python construct 的 SizeofError）。
    fn sizeof(&self, ctx: &Context<'_>) -> Result<usize, ConstructError>;
}
```

**签名设计要点**：
- `parse` 返回 `Py<PyAny>`：直接持有 Python 对象引用，无 Rust 中间枚举。
- `build` 接收 `&Bound<PyAny>`：直接读取任意 Python 对象，无 Rust 中间枚举。
- `Python<'_>` token 作为首参数：pyo3 要求持有 GIL 才能调用 C API，显式传递避免隐式获取。
- `ParseStream`/`BuildStream`/`Context`/`Path` 均为 Rust 内部类型，按引用传递（零拷贝）。

### C.2 Node 枚举与 enum_dispatch 静态分派

执行树的节点类型用封闭 `enum` 列举。使用 `enum_dispatch` 宏自动生成 `match` 分派代码，
使每个具体节点类型只需实现 `Construct` trait，无需手写 match 臂。

```rust
/// 执行树节点：封闭枚举，编译期类型集合固定。
/// 新增节点类型需在此枚举添加变体（Phase 2+ 扩展时修改）。
#[enum_dispatch(Construct)]
pub enum Node {
    // --- 叶子节点（原子构造器）---
    FormatField(FormatFieldNode),       // Int8ub / Int16ub / ... （struct 模块格式）
    Bytes(BytesNode),                   // Bytes(n)
    GreedyBytes(GreedyBytesNode),       // GreedyBytes

    // --- 复合节点 ---
    Struct(StructNode),                 // StructMixin 子类（字段序列）
    StructRef(StructRefNode),           // 嵌套引用其他 StructMixin 子类
}

// enum_dispatch 自动生成:
// impl Construct for Node {
//     fn parse(&self, ...) { match self {
//         Node::FormatField(n) => n.parse(...),
//         Node::Bytes(n) => n.parse(...),
//         ...
//     }}
//     fn build(&self, ...) { ... }
//     fn sizeof(&self, ...) { ... }
// }
```

**为什么用 enum_dispatch 而非手写 match**：
- 避免重复代码（每个方法都要 match 所有变体）。
- 每个节点类型只写一次 `impl Construct for XxxNode`。
- 宏展开后等价于手写 match，无运行时开销。
- 新增节点只需：在 enum 加变体 + 实现 Construct trait。

**禁止 Box<dyn Construct>**（FFI 设计 §6）：动态分派导致间接分支预测失败 + 堆分配，
对 50+ 字段结构退化约 50%。Node 枚举内联在 `Vec` 中，零堆分配，跳转表分派。

### C.3 节点类型定义（Phase 1 子集）

#### C.3.1 FormatFieldNode（整数读写）

```rust
/// 使用 struct 模块格式读写整数/浮点。
/// 对应 Python construct 的 FormatField。
pub struct FormatFieldNode {
    /// struct 格式字符串，如 ">B"（Int8ub）、"<h"（Int16sl）
    fmtstr: std::sync::OnceLock<PythonFormat>,
    /// 字节长度（编译期已知）
    length: usize,
}

/// 预编译的 struct 格式（避免每次 parse 重新解析 fmtstr）。
/// Phase 1 用 Rust 原生整数类型直接读写，不依赖 Python struct 模块。
pub enum PythonFormat {
    UnsignedInt8,    // >B, <B, =B
    SignedInt8,      // >b, <b, =b
    UnsignedInt16,   // >H, <H
    SignedInt16,     // >h, <h
    UnsignedInt32,   // >I, <I
    SignedInt32,     // >i, <i
    UnsignedInt64,   // >Q, <Q
    SignedInt64,     // >q, <q
    // Phase 2: Float32, Float64, Bool
}
```

**parse 行为**（对齐 Python construct `FormatField._parse`）：
1. `stream.read(self.length)` 读取 N 字节。
2. 根据 `fmtstr` 的字节序与符号性，用 Rust 原生方法解析为整数（如 `u8::from_be_bytes`）。
3. 通过 C API 创建 Python int 对象（`PyLong_FromLongLong` 等），返回 `Py<PyAny>`。
4. 失败（字节不足）→ `ConstructError::Stream`。

**build 行为**：
1. `obj.extract::<i64>()`（或对应类型）从 Python 对象提取数值。
2. 按字节序写入 stream（`to_be_bytes` / `to_le_bytes`）。
3. 类型不匹配 → `ConstructError::FormatField`。

#### C.3.2 BytesNode（固定长度字节）

```rust
/// 读取/写入固定长度的原始字节。
/// 对应 Python construct 的 Bytes(length)。
pub struct BytesNode {
    length: usize,   // Phase 1: 仅支持编译期常量
}
```

**parse**：`stream.read(length)` → 创建 `PyBytes`（C API）→ 返回。
**build**：`obj.extract::<&[u8]>()` 提取 bytes → 校验长度 == `length` → 写入 stream。
**sizeof**：返回 `length`。

#### C.3.3 GreedyBytesNode（剩余字节）

```rust
/// 读取流中剩余所有字节。
/// 对应 Python construct 的 GreedyBytes。
pub struct GreedyBytesNode;
```

**parse**：`stream.read_remaining()` → 创建 `PyBytes` → 返回。
**build**：提取 bytes → 写入（不校验长度）。
**sizeof**：返回 Err（大小未知，对应 SizeofError）。

#### C.3.4 StructNode（字段序列，根节点）—— 设计修订 2026-06-22 R3（方案 B'）

```rust
/// 编译期缓存的字段名（interned PyString），避免每次 SetItem 重新创建 str。
pub struct FieldName {
    /// 缓存的 interned Python str（编译期创建一次）
    py_name: Py<PyString>,
    /// Rust 侧用的字段名（path、错误信息）
    rust_name: String,
}

/// StructMixin 子类的根节点：按顺序解析/构建一组命名字段。
/// 对应 Python construct 的 Struct。
pub struct StructNode {
    /// 有序字段列表：(字段名, 子节点)
    fields: Vec<(FieldName, Node)>,
    /// 用户类的 Python 引用（parse 时 create_class(cls) 读 tp_new 构造实例）
    cls: Py<PyType>,
    /// 编译期检测：类是否定义了 __post_init__（有则 parse 后调用）
    has_post_init: bool,
    /// R3 新增：字段数，用于 PyDict_NewPresized 预分配（消除大 N dict resize）。
    field_count: usize,
    // R3 移除：object_new: Py<PyAny>
    //   改用 create_class 辅助函数直接读 cls.as_type_ptr() 的 tp_new 槽位。
    //   与 pydantic-core create_class（model.rs:348-365）一致。
}
```

**parse 行为**（设计修订 R3：方案 B'，直接构造用户类实例）：
1. `PyDict::new(py)`（或 `PyDict_NewPresized(self.field_count)`）创建**独立**空 dict。
   Presized 预分配消除大 N 时的 dict resize（N=100 省 ~800-2000ns）。
2. 对每个字段 `(field_name, node)`：
   - `path.push(field_name.rust_name)`（进入字段路径）
   - `let value = node.parse(stream, ctx, path)?;`（递归）
   - `dict.set_item(field_name.py_name, value)?;`（C API，set_item 到独立 dict，
     使用缓存的 interned key）
   - Phase 1：不调用 `ctx.set_field`（无 this 表达式，context 不被读取）
   - `path.pop()`（离开字段路径）
3. `create_class(self.cls.bind(py))?` 创建空实例——直接读 `tp_new` 槽位调用
   （`new_func(raw_type, empty_tuple, NULL)`），等价 `cls.__new__(cls)`。
4. `force_setattr(py, &instance, "__dict__", &dict)?` 整体替换实例的 `__dict__`
   指针——`PyObject_GenericSetAttr` 绕过自定义 `__setattr__`（含 frozen dataclass）。
5. 如果 `has_post_init`：调用 `instance.__post_init__()`（用户钩子回调）。
6. 返回用户类实例（`Py<PyAny>`）。

**为什么直接构造实例而非返回 dict（方案 B'，设计修订 R3）**：

原方案创建 PyDict 返回，Python 侧 `cls(**dict)` 构造实例。但：(1) `**dict` kwargs
unpacking 是 O(N²)，100 字段时占 parse 总耗时 63%；(2) dict 跨 FFI 返回违反 §0"无中间
表示层"原则。R3 方案 B' 在 Rust 内构造独立 dict + `tp_new` + `GenericSetAttr` 整体替换
（O(N)），dict 不跨 FFI。详见 `docs/设计修订-parse路径优化.md` §0、§2.2。

**为什么缓存字段名为 interned PyString**：

原方案每次 `dict.set_item(name.as_str(), value)` 都创建新的 PyUnicodeObject 并
计算 hash。改为编译期创建一次 `Py<PyString>`（interned），每次 SetItem 复用，
消除每字段 ~20-25ns 的 str 创建 + hash 开销。

**build 行为**：
1. 对每个字段 `(field_name, node)`：
   - `let value = obj.getattr(field_name.rust_name)?;`（C API `PyObject_GetAttr`）
   - `path.push(field_name.rust_name)`
   - Phase 1：不调用 `ctx.set_field`
   - `node.build(py, &value, stream, ctx, path)?;`（递归）
   - `path.pop()`
2. 不返回值（字节已写入 stream）。

**sizeof**：累加所有字段的 sizeof（任一字段 SizeofError 则整体 SizeofError）。

#### C.3.5 StructRefNode（嵌套引用，落实 REV 建议 1 关键）

```rust
/// 引用另一个 StructMixin 子类的执行树，用于嵌套结构。
/// 存储类引用（Py<PyType>），运行时动态获取对方的 CompiledSchema——
/// 这保证互相引用（NodeA↔NodeB）可行。
pub struct StructRefNode {
    /// 对方类的 Python 引用
    cls: Py<PyType>,
    /// 缓存对方 CompiledSchema（首次 parse/build 时填充）
    /// 用 OnceCell 避免重复查找，但允许前向引用首次解析后更新
    schema_cache: std::sync::OnceLock<Py<CompiledSchema>>,
}
```

**resolve_schema 方法**（运行时动态解析，关键设计）：

```rust
impl StructRefNode {
    fn resolve_schema<'py>(
        &self,
        py: Python<'py>,
    ) -> Result<PyRef<'py, CompiledSchema>, ConstructError> {
        // 优先从缓存读取
        if let Some(schema) = self.schema_cache.get() {
            return Ok(schema.borrow(py));
        }
        // 缓存未命中：通过 C API 读取 cls._construct_compiled
        let cls_bound = self.cls.bind(py);
        let compiled = cls_bound
            .getattr("_construct_compiled")
            .map_err(|_| ConstructError::UnresolvedReference {
                message: format!(
                    "类 {} 尚未完成编译（可能存在未解析的前向引用）",
                    cls_bound.name()?
                ),
            })?;
        // compiled 为 None → 对方仍是延迟桩
        if compiled.is_none() {
            return Err(ConstructError::UnresolvedReference {
                message: format!("类 {} 仍是延迟桩，前向引用未解析", cls_bound.name()?),
            });
        }
        let schema: PyRef<CompiledSchema> = compiled.extract()?;
        // 填充缓存（OnceLock::set 失败说明并发已被填充，无碍）
        let _ = self.schema_cache.set(schema.into());
        Ok(schema)
    }
}
```

**为什么存类引用而非编译产物引用**（落实 REV 建议 1）：
- **互相引用场景**（NodeA.peer: NodeB，NodeB.peer: NodeA）：
  - NodeA 编译时，NodeB 尚未定义或未编译。
  - 若存编译产物引用，NodeA 编译时无法获取 NodeB 的产物。
  - 存类引用（`Py<PyType>`），NodeA 编译成功（只持有 NodeB 的类对象）。
  - 首次 `NodeA.parse(data)` 时，NodeB 已完成编译，`resolve_schema` 通过
    `cls._construct_compiled` 获取 NodeB 产物。
- **缓存机制**：`OnceLock` 首次解析后缓存，后续调用零查找开销。
- **延迟桩兼容**：若对方仍是延迟桩（`_construct_compiled` 为 None），返回
  `UnresolvedReference` 错误，Python 侧捕获后可触发对方的延迟编译并重试。

**parse 行为**（设计修订 2026-06-22 R3）：

StructRefNode.parse 委托对方 root（StructNode）的 parse。由于 StructNode 现在内部
用方案 B' 直接构造对方类实例（§C.3.4），StructRefNode 不再需要自己调用 `cls(**dict)`：

```rust
fn parse(&self, py, stream, ctx, path) -> Result<Py<PyAny>, ConstructError> {
    let schema = self.resolve_schema(py)?;
    let root = schema.root();
    // root.parse 内部已通过 create_class(cls) + force_setattr 构造对方实例（方案 B'）
    root.parse(py, stream, ctx, path)
    // 不再需要 cls.call((), Some(&sub_dict)) —— Rust→Python 回调消除
}
```

**收益**：嵌套结构不再有 Rust→Python 回调开销（原方案每层 ~200-400ns）。

**build 行为**：与 parse 对称——委托对方 root.build，obj 已是对方类实例。

### C.4 Stream 抽象（落实 REV 建议 3）

Stream 是 Rust 内部的字节流游标，**不跨 FFI**。parse 和 build 各有一个 Stream 类型。

```rust
/// 解析流：包装输入字节切片，维护读取游标。
pub struct ParseStream<'a> {
    data: &'a [u8],
    pos: usize,
}

impl<'a> ParseStream<'a> {
    pub fn new(data: &'a [u8]) -> Self {
        Self { data, pos: 0 }
    }

    /// 读取 n 字节。不足时返回 StreamError（对齐 Python construct stream_read）。
    pub fn read(&mut self, n: usize, path: &Path) -> Result<&'a [u8], ConstructError> {
        if self.pos + n > self.data.len() {
            return Err(ConstructError::Stream {
                message: format!(
                    "stream read less than specified amount, expected {}, found {}",
                    n,
                    self.data.len() - self.pos
                ),
                path: path.to_string(),
            });
        }
        let result = &self.data[self.pos..self.pos + n];
        self.pos += n;
        Ok(result)
    }

    /// 读取剩余所有字节（GreedyBytes 用）。
    pub fn read_remaining(&mut self) -> &'a [u8] {
        let result = &self.data[self.pos..];
        self.pos = self.data.len();
        result
    }

    pub fn tell(&self) -> usize { self.pos }
    pub fn is_at_end(&self) -> bool { self.pos >= self.data.len() }
}

/// 构建流：输出缓冲区。
pub struct BuildStream {
    buf: Vec<u8>,
}

impl BuildStream {
    pub fn new() -> Self { Self { buf: Vec::new() } }

    pub fn write(&mut self, data: &[u8]) {
        self.buf.extend_from_slice(data);
    }

    pub fn into_bytes(self) -> Vec<u8> { self.buf }
}
```

**设计要点**：
- `ParseStream` 借用 `&[u8]`，零拷贝，读取返回切片引用（无需分配）。
- `BuildStream` 持有 `Vec<u8>`，预分配容量减少 reallocate。
- 所有方法纯 Rust，无 C API，无 FFI。
- read 失败时的错误信息与 Python construct `stream_read` 对齐（含 path、expected/found）。
- Phase 1 不支持 `seek`/`tell` 的复杂场景（如 Pointer、OnDemand），推迟到 Phase 3+。

### C.5 Context 抽象（落实 REV 建议 3）

Context 用于字段间引用（如 `Bytes(this.length)` 中 `this.length` 引用前序字段）和
嵌套层传递（Python construct 的 `_` 指向外层 context）。Phase 1 简化版仅存储已解析
字段，不支持 this 表达式（this 表达式推迟到 Phase 2）。

```rust
/// 解析/构建上下文。Rust 内部持有 Python dict 引用。
/// Phase 1: 仅存储字段值，不支持 this 引用（无表达式系统）。
/// Phase 2: 扩展支持 this.xxx 字段引用（通过 ctx.get_field 读取）。
pub struct Context<'py> {
    /// 当前层字段字典（PyDict 引用）
    /// parse 时存入已解析字段；build 时存入从对象读取的字段值
    fields: Bound<'py, PyDict>,
    /// 外层上下文（嵌套 Struct 时指向父 Context），对应 Python construct 的 _
    /// Phase 1 可为 None（顶层）
    parent: Option<&'py Context<'py>>,
}

impl<'py> Context<'py> {
    /// 创建顶层 context（parse/build 入口处）。
    pub fn new_root(py: Python<'py>) -> PyResult<Self> {
        Ok(Self {
            fields: PyDict::new(py),
            parent: None,
        })
    }

    /// 创建嵌套 context（Struct 节点进入时）。
    pub fn new_child(parent: &'py Context<'py>, py: Python<'py>) -> PyResult<Self> {
        Ok(Self {
            fields: PyDict::new(py),
            parent: Some(parent),
        })
    }

    /// 存入字段（parse/build 每个字段完成后调用）。
    pub fn set_field(&self, name: &str, value: &Bound<'_, PyAny>) -> PyResult<()> {
        self.fields.set_item(name, value)
    }

    /// 读取字段（Phase 2 的 this.xxx 引用使用）。
    /// Phase 1 不调用此方法（无 this 表达式）。
    pub fn get_field(&self, name: &str) -> PyResult<Option<Bound<'_, PyAny>>> {
        self.fields.get_item(name)
    }
}
```

**Phase 1 行为**（设计修订 2026-06-22）：
- Struct 节点 parse/build 时 **不调用 `set_field`**（Phase 1 无 this 表达式，
  context 不被读取，`set_field` 是纯开销，每字段 ~40-70ns）。
- 不调用 `get_field`（无 this 引用）。
- `Bytes(n)` 的 `n` 在编译期为常量，不从 context 读取。
- Context 结构保留（trait 签名不变），Phase 2 加入 this 表达式时恢复 `set_field`。

**Phase 2 扩展点**：
- `get_field` 实现 this.xxx 引用。
- 表达式系统（this/obj_）在 Phase 2 设计，届时 Context 增加 `eval_expr` 方法。
- 完整的 `_root`、`_io`、`_index`、`_subcons` 等内部键（对齐 Python construct 的 Container）。

**为什么 Context 持有 PyDict 而非 Rust HashMap**：
- 字段值本身就是 Python 对象（`Py<PyAny>`），存入 PyDict 是 C API 调用（`PyDict_SetItem`），
  无需 Rust→Python 转换。
- this 引用读取时直接返回 Python 对象，无中间类型（FFI 设计 §5）。
- 与 Python construct 的 Container 语义一致。

### C.6 Path 错误追踪

```rust
/// 错误路径追踪栈。Struct 节点进入字段时 push，离开时 pop。
/// 错误发生时 to_string() 生成如 "header.flags[2]" 的路径。
pub struct Path {
    segments: Vec<PathSegment>,
}

pub enum PathSegment {
    Field(String),      // 结构体字段，如 "address"
    Index(usize),       // 数组元素，如 "[2]"（Phase 2 Array 支持）
}

impl Path {
    pub fn new() -> Self { Self { segments: vec![PathSegment::Field("(root)".into())] } }
    pub fn push_field(&mut self, name: &str) { self.segments.push(PathSegment::Field(name.into())); }
    pub fn push_index(&mut self, i: usize) { self.segments.push(PathSegment::Index(i)); }
    pub fn pop(&mut self) { self.segments.pop(); }
}

impl std::fmt::Display for Path {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        // 生成 "root.field1.field2[0]" 格式
        // 与 Python construct 的 path 格式对齐
        ...
    }
}
```

**性能保证**：`push`/`pop` 是 `Vec` 操作（~5ns），成功路径开销极小。`to_string` 只在
错误路径调用（FFI 设计 §7.3 成功路径零成本）。

---

## D. 分层和模块隔离

### D.1 Crate 结构：单 Crate

construct-rs 是**单 crate**，pyo3 是核心依赖（非 optional feature）。不存在独立的 Python
绑定层（AGENTS.md §7）。Crate 同时产出 Rust 库（供测试）和 Python 扩展模块（cpython
`.so`/`.pyd`）。

```
construct-rs/
├── Cargo.toml              # pyo3 核心依赖，crate-type = ["cdylib", "rlib"]
├── pyproject.toml          # maturin 构建配置
└── src/
    ├── lib.rs              # crate 入口 + pyo3 module 注册
    ├── error.rs            # ConstructError + PyErr 转换
    ├── path.rs             # Path 错误追踪
    ├── stream.rs           # ParseStream / BuildStream
    ├── context.rs          # Context
    ├── compile.rs          # compile_schema 入口 + Schema 收集
    ├── parse.rs            # CompiledSchema._parse_raw 入口
    ├── build.rs            # CompiledSchema._build_raw 入口
    ├── schema.rs           # CompiledSchema pyclass 定义
    ├── nodes/
    │   ├── mod.rs          # Node 枚举 + enum_dispatch + Construct trait
    │   ├── format_field.rs # FormatFieldNode
    │   ├── bytes.rs        # BytesNode
    │   ├── greedy_bytes.rs # GreedyBytesNode
    │   ├── struct_node.rs  # StructNode
    │   └── struct_ref.rs   # StructRefNode
    └── descriptors/
        ├── mod.rs          # 类型描述符 pyclass（Int8ub 等）
        └── format_field.rs # FormatFieldDescriptor 等
```

**为什么单 crate**（AGENTS.md §7）：
- 消除 crate 间边界开销。
- pyo3 在主 crate 中，Rust 内部代码可直接调用 Python C API。
- 简化构建（一个 Cargo.toml，一个 maturin 配置）。

### D.2 Rust 模块层次与可见性

| 模块 | 职责 | 可见性 |
|------|------|--------|
| `lib` | pyo3 module 注册，导出公共 API | crate root |
| `error` | ConstructError 定义 + From<ConstructError> for PyErr | `pub` |
| `path` | Path 错误追踪 | `pub(crate)` |
| `stream` | ParseStream / BuildStream | `pub(crate)` |
| `context` | Context | `pub(crate)` |
| `compile` | compile_schema `#[pyfunction]` | `pub`（pyo3 注册） |
| `parse` | _parse_raw 入口逻辑 | `pub(crate)` |
| `build` | _build_raw 入口逻辑 | `pub(crate)` |
| `schema` | CompiledSchema `#[pyclass]` | `pub`（pyo3 注册） |
| `nodes/*` | 节点类型 + Construct trait | `pub(crate)` |
| `descriptors/*` | 描述符 pyclass | `pub`（pyo3 注册） |

**可见性原则**：
- `#[pyfunction]`/`#[pyclass]` 必须 `pub`（pyo3 注册需要）。
- 内部数据结构（Stream/Context/Path/Node）用 `pub(crate)`，不暴露给 crate 外。
- 节点类型不暴露为 Python 可见（用户通过描述符间接创建节点）。

### D.3 Python 包结构

```
construct/                      ← Python 包（用户 import construct）
├── __init__.py                 # 公共 API 导出
├── _mixin.py                   # StructMixin（纯 Python）+ field()（纯 Python）
├── _descriptors.py             # 描述符基类（纯 Python，委托给 Rust pyclass）
├── _errors.py                  # Python 异常类定义（ConstructError 等）
└── _native.pyi                 # Rust 扩展的类型存根（maturin 生成）
```

**Rust 扩展**由 maturin 编译为 `_construct_rust` 模块（`.so`/`.pyd`），Python 侧通过
`from ._construct_rust import *` 导入。

**分工原则**：
- **纯 Python 实现**：StructMixin、field()、Python 异常类、@dataclass 协调、延迟桩逻辑。
  这些是 Python 元编程逻辑，用 Python 实现更灵活、更易维护，且不在热路径。
- **Rust 实现**：编译入口（compile_schema）、parse 入口、build 入口、CompiledSchema、
  类型描述符 pyclass（Int8ub 等单例）、Node 执行树。这些是计算密集或需类型安全的部分。

### D.4 依赖方向

```
用户代码
   ↓ import
construct/__init__.py  ──────────→  construct/_mixin.py (StructMixin, field)
   │                                       │
   │                                       ↓ 调用
   │                              construct/_native (Rust 扩展)
   │                                       │
   ↓                                       ↓
examples/example.py               construct-rs/src/ (Rust crate)
                                          │
                                   ┌──────┴──────┐
                                   ↓             ↓
                              pyo3 (核心)   thiserror, enum_dispatch
```

**关键约束**：
- Rust 代码不依赖 Python 包的任何文件（单向依赖）。
- Python 包依赖 Rust 扩展（通过 import）。
- 用户代码只 import `construct`（顶层包），不直接 import Rust 扩展。

### D.5 Phase 1 范围的模块划分

**Phase 1 实现**（本文档定义的全部）：
- `lib`, `error`, `path`, `stream`, `context`, `compile`, `parse`, `build`, `schema`
- `nodes/{format_field, bytes, greedy_bytes, struct_node, struct_ref}`
- `descriptors/{format_field 单例, bytes, greedy_bytes}`
- Python: `_mixin`, `_descriptors`, `_errors`, `__init__`

**Phase 2+ 扩展点**（本文档定义框架，实现推迟）：
- `nodes/{array, sequence, adapter, enum_node, if_node, switch}` —— 复合构造器
- `nodes/{float_field, string_node}` —— 浮点与字符串
- `expr.rs` —— this/obj_ 表达式系统
- `nodes/{pointer, seek, ondemand}` —— 流定位（Phase 3+）
- `nodes/{lazy}` —— 惰性解析（Phase 3+）

---

## E. Schema 编译管线

本节定义从用户类定义到执行树构建的完整流程，覆盖 `__init_subclass__` 全过程、字段信息
收集、描述符传递、执行树构建、缓存与前向引用延迟桩。

### E.1 `__init_subclass__` 完整流程

```
┌─────────────────────────────────────────────────────────────────────┐
│ 用户代码执行: @dataclass class MyMsg(StructMixin): ...               │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼  Python 解释器创建类对象
┌─────────────────────────────────────────────────────────────────────┐
│ StructMixin.__init_subclass__(cls=MyMsg) 被调用                      │
│                                                                      │
│  1. 调用 _collect_field_descriptors(cls)                             │
│     → 遍历 cls.__dict__，提取所有 field() 返回值                      │
│     → 从每个 field() 返回值提取 subcon                                │
│     → 返回 [(field_name, subcon), ...] 有序列表                       │
│                                                                      │
│  2. 判断是否为空类（无字段）                                           │
│     → 是: 创建空 CompiledSchema（空 Struct 节点），存入 cls 属性       │
│           cls._construct_compiled = schema                            │
│           return                                                      │
│                                                                      │
│  3. 尝试调用 _native.compile_schema(cls, descriptors)                │
│     ← 1 次 FFI 穿越（进入 Rust）                                      │
│     → Rust 构建执行树，返回 CompiledSchema                            │
│     ← 1 次 FFI 穿越（返回）                                           │
│                                                                      │
│  4a. 编译成功:                                                        │
│      cls._construct_compiled = schema                                 │
│      （parse/build 方法继承自 StructMixin，可直接使用）                │
│                                                                      │
│  4b. 编译抛出 UnresolvedReference（前向引用未解析）:                   │
│      → 安装延迟桩（详见 §E.6）                                        │
│      cls.parse = classmethod(_lazy_parse_stub)                        │
│      cls.build = _lazy_build_stub  (实例方法)                         │
│                                                                      │
│  4c. 编译抛出其他错误（描述符无效等）:                                 │
│      → 立即重新抛出（fail fast，编译错误在导入时报出）                 │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼  __init_subclass__ 返回
┌─────────────────────────────────────────────────────────────────────┐
│ @dataclass(MyMsg) 装饰器执行                                          │
│   → 生成 __init__, __repr__, __eq__, __dataclass_fields__            │
│ （StructMixin 不依赖这些，已用原始类体属性完成编译）                    │
└─────────────────────────────────────────────────────────────────────┘
```

### E.2 Python 端字段信息收集

```python
def _collect_field_descriptors(cls):
    """从类体收集字段信息，返回 [(field_name, subcon), ...] 有序列表。

    利用 Python 3.7+ __annotations__ 的有序性保证字段顺序。
    不依赖 __dataclass_fields__（此刻尚未生成）。
    """
    import_annotations = getattr(cls, '__annotations__', {})

    fields = []
    for field_name, _annotation in import_annotations.items():
        # field() 的返回值存为类属性的默认值
        default_value = cls.__dict__.get(field_name)
        if isinstance(default_value, _FieldDescriptor):
            # 提取 subcon（类型描述符）
            subcon = default_value.subcon
            fields.append((field_name, subcon))
        # 非 field() 声明的属性（普通方法、类变量等）跳过

    return fields
```

**字段顺序保证**（Mixin 设计 §4.3）：
- `__annotations__` 在 Python 3.7+ 保序（PEP 526）。
- 用户写 `address: int = field(Int8ub)` 时，`address` 同时进入 `__annotations__`
  和 `cls.__dict__`，顺序一致。
- `@dataclass` 的字段重排（如有默认值字段在无默认值之后）发生在 `__init_subclass__`
  之后，不影响编译收集的字段顺序。

**类型注解的作用**：仅用于 IDE 提示，不影响二进制格式。用户写 `address: int` 或
`address: Any` 都不影响 `field(Int8ub)` 的二进制语义。

### E.3 字段信息传递给 Rust 编译器

```python
# StructMixin.__init_subclass__ 中
descriptors = [subcon for (_name, subcon) in fields]
field_names = [name for (name, _subcon) in fields]

# 调用 Rust 编译入口（1 次 FFI）
cls._construct_compiled = _native.compile_schema(
    cls=cls,
    field_names=field_names,        # list[str]
    descriptors=descriptors,        # list[Descriptor pyclass]
)
```

**传递方式**：两个并行列表（字段名 + 描述符）。Rust 侧按下标配对，构建
`Vec<(String, Node)>`。这种传递方式比传元组列表更符合 pyo3 的类型提取约定
（`Vec<String>` + `Vec<Py<PyAny>>`）。

### E.4 编译器构建执行树

Rust 侧 `compile_schema` 流程：

```rust
#[pyfunction]
fn compile_schema(
    py: Python<'_>,
    cls: &Bound<'_, PyType>,
    field_names: Vec<String>,
    descriptors: Vec<Py<PyAny>>,
) -> PyResult<CompiledSchema> {
    // 1. 校验：field_names 与 descriptors 长度一致
    if field_names.len() != descriptors.len() {
        return Err(ConstructError::Compilation {
            message: format!("field_names ({}) 与 descriptors ({}) 长度不一致",
                field_names.len(), descriptors.len()),
        }.into());
    }

    // 2. 逐字段构建节点
    let mut fields: Vec<(String, Node)> = Vec::with_capacity(field_names.len());
    for (name, desc) in field_names.iter().zip(descriptors.iter()) {
        let desc_bound = desc.bind(py);
        let node = build_node_from_descriptor(py, desc_bound)
            .map_err(|e| e.with_field_context(name))?;
        fields.push((name.clone(), node));
    }

    // 3. 组装根 Struct 节点
    let root = Node::Struct(StructNode { fields });

    // 4. 包装为 CompiledSchema
    Ok(CompiledSchema {
        root,
        cls: cls.into_py(py),
    })
}

fn build_node_from_descriptor(
    py: Python<'_>,
    desc: &Bound<'_, PyAny>,
) -> Result<Node, ConstructError> {
    // 按优先级尝试 extract 为具体描述符类型
    // 顺序：先具体 pyclass，最后 StructMixin 子类引用

    if let Ok(fmt) = desc.extract::<FormatFieldDescriptor>() {
        return Ok(Node::FormatField(FormatFieldNode::from_descriptor(&fmt)));
    }
    if let Ok(b) = desc.extract::<BytesDescriptor>() {
        return Ok(Node::Bytes(BytesNode { length: b.length }));
    }
    if desc.extract::<GreedyBytesDescriptor>().is_ok() {
        return Ok(Node::GreedyBytes(GreedyBytesNode));
    }
    // StructMixin 子类引用：检查 isinstance(desc, type) 且 issubclass
    if let Ok(cls) = desc.extract::<Py<PyType>>() {
        let cls_bound = cls.bind(py);
        if is_structmixin_subclass(cls_bound)? {
            // 检查对方是否已编译
            let compiled = cls_bound.getattr("_construct_compiled")?;
            if compiled.is_none() {
                // 前向引用：对方仍是延迟桩
                return Ok(Node::StructRef(StructRefNode {
                    cls,
                    schema_cache: OnceLock::new(),
                }));
            }
            // 对方已编译：仍用 StructRef（运行时 resolve_schema 会命中缓存）
            return Ok(Node::StructRef(StructRefNode {
                cls,
                schema_cache: OnceLock::new(),
            }));
        }
    }

    Err(ConstructError::Compilation {
        message: format!("未知的字段描述符类型: {}", desc.repr()?),
    })
}
```

**描述符识别策略**：
- 用 pyo3 `extract::<T>` 按优先级匹配。
- 每个描述符 pyclass 在 Rust 侧有对应结构体（如 `FormatFieldDescriptor`）。
- 最后检查是否为 StructMixin 子类（`issubclass`）。
- 匹配失败 → 编译错误（fail fast，导入时报出）。

### E.5 编译产物缓存与递归引用解析

编译产物存为 `cls._construct_compiled`（Mixin 设计 §5.2 方案 A）。

**递归引用的解析时机**（落实 REV 建议 1，核心）：

| 场景 | 编译时 | 首次 parse 时 |
|------|--------|---------------|
| 简单引用（Inner 已编译） | StructRef 存 Inner 类引用 | `resolve_schema` 读取 `Inner._construct_compiled`，缓存 |
| 前向引用（Inner 未编译，延迟桩） | StructRef 存 Inner 类引用（编译成功） | `resolve_schema` 读取 `_construct_compiled`，若为 None 报错；否则缓存 |
| 互相引用（NodeA↔NodeB） | 两方都存对方类引用，都编译成功（延迟桩在另一类上） | parse NodeA 时，NodeB 已完成延迟编译，`resolve_schema` 成功 |

**关键洞察**：StructRef 存**类引用**（`Py<PyType>`）而非编译产物引用。这切断编译时
的循环依赖——互相引用的两方都能在 `__init_subclass__` 中编译成功（因为不立即获取对方
产物，只存类引用）。运行时通过 `_construct_compiled` 类属性动态获取。

### E.6 延迟桩机制（落实 Mixin 设计 §6）

当 `compile_schema` 抛出 `UnresolvedReference`（某字段的 StructRef 指向的类连延迟桩都
没有，即类完全未定义），编译器无法构建该字段节点。此时安装延迟桩：

```python
def _install_lazy_stubs(cls):
    """安装延迟桩：首次 parse/build 时重试编译。"""
    cls._construct_compiled = None  # 标记未编译

    @classmethod
    def _lazy_parse(cls_, data):
        _retry_compile(cls_)              # 首次调用重试编译
        return _StructMixin_parse(cls_, data)  # 调用真正的 parse

    def _lazy_build(self):
        _retry_compile(type(self))        # 首次调用重试编译
        return _StructMixin_build(self)   # 调用真正的 build

    cls_.parse = _lazy_parse
    cls_.build = _lazy_build
```

```python
import threading
_lazy_compile_lock = threading.Lock()

def _retry_compile(cls):
    """首次调用时重试编译。线程安全。"""
    if cls._construct_compiled is not None:
        return  # 已编译（可能是其他线程已完成）
    with _lazy_compile_lock:
        if cls._construct_compiled is not None:
            return  # 双重检查
        descriptors = _collect_field_descriptors(cls)
        field_names = [n for (n, _) in descriptors]
        subcons = [s for (_, s) in descriptors]
        # 重试编译：此时前向引用的类可能已完成编译
        schema = _native.compile_schema(cls, field_names, subcons)
        cls._construct_compiled = schema
        # 替换桩为真正方法
        cls.parse = _StructMixin_parse
        cls.build = _StructMixin_build
```

**线程安全**（Mixin 设计 §6.4）：`threading.Lock` + 双重检查确保编译只发生一次。
首次调用后，桩被替换为真正方法，后续调用零开销。

**延迟桩的适用边界**：
- 仅当 `compile_schema` 抛出 `UnresolvedReference` 时安装。
- 其他编译错误（无效描述符等）立即抛出，不延迟。
- 一旦编译成功，桩被永久替换，不再触发重试。

**注意**：StructRef 指向"已定义但未编译"的类（对方有延迟桩）时，`compile_schema` **不**
抛 `UnresolvedReference`——它成功构建 StructRef 节点（存类引用）。只有在描述符既不是
已知 pyclass 也不是 StructMixin 子类时，才抛编译错误。

---

## F. Phase 1 范围定义

Phase 1 是垂直切片，目标是验证架构可行性与性能 >4x。范围严格最小化，非必要功能推迟。

### F.1 Phase 1 实现的格式类型（最小集合）

| 格式类型 | 描述符 | Rust 节点 | 说明 |
|---------|--------|-----------|------|
| 1/2/4/8 字节无符号整数（大/小端） | `Int8ub, Int8ul, Int16ub, Int16ul, Int32ub, Int32ul, Int64ub, Int64ul` | `FormatFieldNode` | 8 个预定义单例 |
| 1/2/4/8 字节有符号整数（大/小端） | `Int8sb, Int8sl, Int16sb, Int16sl, Int32sb, Int32sl, Int64sb, Int64sl` | `FormatFieldNode` | 8 个预定义单例 |
| 固定长度原始字节 | `Bytes(n)`（n 为编译期常量正整数） | `BytesNode` | 可实例化描述符 |
| 剩余全部字节 | `GreedyBytes` | `GreedyBytesNode` | 预定义单例 |
| StructMixin 子类（扁平） | `field(Int8ub)` 等组合 | `StructNode` | 字段序列 |
| StructMixin 子类（嵌套） | `field(OtherStruct)` | `StructRefNode` | 递归引用 |

**共 18 个预定义单例 + 1 个可实例化描述符 + StructMixin 组合能力**。足以实现
`examples/example.py` 的 ModbusRTUMessage 及其嵌套变体。

### F.2 Phase 1 的出口标准

#### F.2.1 功能出口标准

1. **examples/example.py 可运行**：
   - `ModbusRTUMessage(address=1, function_code=3, data=b'\x00\x01').build()` 产出
     `b'\x01\x03\x00\x01'`。
   - `ModbusRTUMessage.parse(b'\x01\x03\x00\x01')` 产出等价实例。
   - parse/build 往返一致（`parse(build(msg)) == msg`）。

2. **嵌套结构可用**：
   - 定义 `Outer { inner: Inner }`，parse/build 正确递归。
   - 互相引用（NodeA↔NodeB）可定义并可 parse/build（验证延迟桩与 StructRef）。

3. **空 StructMixin 子类可用**：
   - `Empty.parse(b'')` 返回 `Empty()`。
   - `Empty().build()` 返回 `b''`。

4. **错误携带 path**：
   - 字节不足时错误信息含 `"Error in path xxx"`。
   - 嵌套结构错误路径正确（如 `"outer.inner.address"`）。

5. **与 @dataclass 完整兼容**：
   - `@dataclass` 生成的 `__init__`、`__repr__`、`__eq__` 正常工作。
   - `frozen=True` dataclass 可 build。
   - `__post_init__` 在 parse 构造实例时被触发。

#### F.2.2 性能出口标准

- **≥ 4x vs Python construct 2.10.70**（等价 Struct 定义）。
- 理想 10x。
- 验证方法见 §G.4。
- **性能门禁**：< 0.5x 时暂停后续阶段（AGENTS.md 性能门禁 §2）。

#### F.2.3 工程出口标准

- `cargo build` + `cargo clippy`（零 warning）+ `cargo fmt --check` + `cargo test` 全 PASS。
- Python 侧测试：pytest 覆盖 examples/example.py 与边界情况。
- 单元测试覆盖率：节点类型 100%，编译管线 ≥ 80%。

### F.3 Phase 1 明确推迟的功能

| 功能 | 推迟到 | 原因 |
|------|--------|------|
| **浮点数（Float32/64）** | Phase 2 | 需处理 IEEE 754、NaN、字节序；非示例所需 |
| **字符串（CString, PascalString, GreedyString）** | Phase 2 | 需编码处理（utf-8/ascii/latin1）；非示例所需 |
| **表达式系统（this/obj_）** | Phase 2 | 需完整的 Path/BinExpr 系统；Phase 1 字段引用用编译期常量替代 |
| **变长字段（Bytes(this.length)）** | Phase 2 | 依赖表达式系统 |
| **Adapter / Enum / Validator** | Phase 2 | 适配器模式与值映射 |
| **Array / Sequence / Repeat / GreedyRange** | Phase 2 | 重复结构 |
| **条件分支（If / Switch / Union / Select）** | Phase 2 | 运行时分支 |
| **Bitwise / 位操作（Nibble, Bit, BitsInteger）** | Phase 2 | 位流包装 |
| **Const / Padding / Check / Terminated** | Phase 2 | 协议辅助构造器 |
| **Pointer / Seek / OnDemand / Tell** | Phase 3+ | 流定位，需扩展 Stream |
| **Lazy / LazyStruct** | Phase 3+ | 惰性解析 |
| **用户回调（lambda this: ...）** | Phase 2 | 引入额外 FFI 穿越，需专门设计 |
| **BytesInteger（任意宽度整数，如 24-bit）** | Phase 2 | 非 struct 模块格式 |
| **VarInt / ZigZag** | Phase 2 | 变长编码 |
| **嵌套 dict 到嵌套实例的自动转换** | 见 §B.7 | 已通过 StructRef 回调实现，Phase 1 覆盖 |

### F.4 Phase 1 的语义对齐保证

Phase 1 实现的格式类型必须与 Python construct 2.10.70 的语义**完全一致**：

| 检查项 | Python construct 行为 | Phase 1 对齐 |
|--------|----------------------|--------------|
| Int8ub.parse 读取 1 字节大端无符号 | `struct.unpack(">B", data)[0]` | Rust `u8::from_be_bytes`，结果一致 |
| Bytes(n).parse 读取 n 字节 | `stream.read(n)`，返回 bytes | Rust 切片，转 PyBytes |
| Bytes(n).build 校验长度 | `len(data) != length` → StreamError | Rust 校验，同错误 |
| GreedyBytes.parse 读取剩余 | `stream.read()` | Rust `read_remaining` |
| Struct 不要求消费全部输入 | 多余字节忽略 | Phase 1 同样忽略（ParseStream 不校验 EOF） |
| Struct.build 缺字段 | `obj[name]` → KeyError | Rust `getattr` 失败 → ConstructError |
| 错误携带 path | `"Error in path {}\n".format(path) + message` | Rust Path::to_string，同格式 |

DEV 实现时须对照 `construct/construct/core.py` 的 `FormatField._parse/_build`、
`Bytes._parse/_build`、`GreedyBytes._parse/_build`、`Struct._parse/_build` 的逐行语义。

---

## G. 性能假设（性能门禁 §1 落实，设计修订 2026-06-22 R3）

本节列出 Phase 1 路径上的所有性能瓶颈来源、可证伪预测、验证方法。这是性能门禁要求的
"性能假设"段落。

> **设计修订说明**（2026-06-22）：Phase 1 基准测试（1.8）揭示了原设计未预见的两项瓶颈——
> Python 侧 `**dict` kwargs unpacking 的 O(N²) 开销，以及 Rust 侧 dict/SetItem + context
> 的偏高成本。以下瓶颈分析已根据实测数据更新，并标注了设计修订带来的变化。
> 完整分析见 `docs/设计修订-parse路径优化.md`。

> **实测修正（2026-06-22）**：P0 性能优化（fat LTO + Path/Context/map_err 移除）实施后，
> 加速比实测达 **8-12x**（B1=8.09x, B2=9.66x, B3=11.54x, B4=12.06x），远超 R3 预测的
> 5-9x。根因是 R3 性能模型低估了 pyo3 抽象层在无 LTO 时的隐性开销（~140ns/field，
> 详见 §G.6 性能模型修正）。以下各节保留 R3 预测原文作为历史记录，并以"实测修正"
> 标注补充实测结论。完整 P0 优化分析见 `docs/analysis/perf-p0-analysis.md`。

### G.1 瓶颈识别与可证伪预测

#### 瓶颈 1：FFI 穿越次数

- **来源**：parse/build 入口的 Python→Rust 穿越。
- **设计目标**：parse 严格 1 次（进入 + 返回），build 严格 1 次。
- **可证伪预测**：若 parse 的 FFI 穿越次数从 1 增加到 N（N = 字段数），性能退化为 O(N)
  FFI 开销。10 字段 × 200ns/穿越 = 2000ns 纯 FFI 开销，占总开销 > 50%。
- **量级预期**：parse FFI 开销恒定 ~100ns（实测：1.8 基准 B1 _parse_raw = 523ns，
  其中 FFI ~100ns，3 字段处理 ~420ns），与字段数无关。

#### 瓶颈 2：节点分派开销

- **来源**：执行树遍历时每个节点的 match 分派。
- **设计目标**：静态分派（enum + match），编译器优化为跳转表。
- **可证伪预测**：若改用 `Box<dyn Construct>`（动态分派），每节点额外 6ns 间接分支预测
  失败 + 堆分配。50 字段 = 300ns + 堆分配开销，占纯计算 ~50%。
- **量级预期**：静态分派每节点 ~2-5ns（跳转表 + 内联）。10 字段 = ~30-50ns。

#### 瓶颈 3：C API 调用（PyObject 构造与属性操作）

- **来源**：parse 时创建 int 对象（`PyLong_FromLongLong`）、`__dict__` SetItem；
  build 时 getattr、extract 数值。
- **不可避免**：这是"直接操作 Python 对象"的必要成本（FFI 设计 §5）。
- **可证伪预测**：若改用 Rust 中间类型再转换，每字段双重遍历 + 额外内存分配，
  实测退化为 0.3-1.8x（FFI 设计 §5.3 历史教训）。
- **量级预期（设计修订后）**：每字段 C API 开销 ~55-75ns（创建 int ~25ns +
  `__dict__` SetItem ~25ns + path/overhead ~10ns）。10 字段 = ~650ns。
  - **优化点**：使用编译期缓存的 interned PyString 作为 key（消除每字段 str 创建+
    hash ~20ns）。Phase 1 不调用 ctx.set_field（消除 ~40ns/field）。

#### 瓶颈 4：Python 侧 kwargs unpacking（**设计修订消除**）+ §0 中间表示违反（**R3 修正**）

- **来源**（原方案）：Python 侧 `cls(**dict)` 的 `**dict` unpacking + dict 跨 FFI 返回。
- **实测发现**（1.8 基准）：`**dict` unpacking 是 **O(N²)**，~5 ns/N²。
  100 字段时占 Full parse 的 63%（55782 ns / 88098 ns）。
- **§0 违反**（R3 发现）：dict 跨 FFI 返回 Python 侧消费，是 §0"无中间表示层"禁止的
  "中间数据类型跨边界"。
- **设计修订（R3 方案 B'）**：改为 Rust 内 `PyDict::new` + 逐字段 set_item +
  `tp_new` + `PyObject_GenericSetAttr` 整体替换 `__dict__`（O(N)）。dict 不跨 FFI。
- **量级预期（R3）**：**此瓶颈完全消除**。实例构造成本 = `tp_new` ~30ns（一次性）+
  `GenericSetAttr` ~20ns（一次性）+ 已计入瓶颈 3 的 set_item 开销。

#### 瓶颈 5：用户类实例构造 / `__post_init__` 回调（R3 方案 B'）

- **来源**：`create_class(cls)`（`tp_new`）创建实例 + `force_setattr` 替换 `__dict__`
  + 可选 `__post_init__` 调用。
- **归类**：`tp_new` 和 `GenericSetAttr` 是 C API（非 FFI 穿越）；`__post_init__` 是
  Rust→Python 回调（FFI 设计 §4.4 用户钩子例外），但仅在类定义了 `__post_init__` 时发生。
- **量级预期（R3）**：`tp_new` 直接调用 ~30ns（从 `cls.as_type_ptr()` 读槽位，
  无需缓存对象）+ `GenericSetAttr` ~20ns（一次指针替换）；`__post_init__` ~200-400ns
  （仅 has_post_init=True 时）。大多数类无 `__post_init__`，零回调开销。
  - **R3 vs R2**：R2 用 `object.__new__` call1 ~50ns + getattr `__dict__` ~30ns；
    R3 用 `tp_new` ~30ns + `GenericSetAttr` ~20ns。R3 固定开销省 ~30ns。

#### 瓶颈 6：Context 操作（**Phase 1 消除**）

- **来源**（原方案）：每字段 `ctx.set_field(name, value)`（PyDict_SetItem）。
- **设计修订**：Phase 1 不调用 `ctx.set_field`（无 this 表达式，context 不被读取）。
- **量级预期**：**此瓶颈在 Phase 1 完全消除**。Phase 2 加入 this 表达式时恢复。

#### 瓶颈 7：内存分配

- **来源**：执行树节点（编译期，非热路径）；`create_class`（`tp_new`）创建实例；
  `PyDict::new` 创建独立 dict；build 输出 Vec<u8>。
- **设计目标**：执行树节点内联在 Vec（零堆分配）；实例和 dict 由 Python 内存管理；
  BuildStream 预分配容量。
- **量级预期（R3）**：热路径仅 `tp_new` ~30ns + `PyDict::new` ~20ns（一次性）+
  Vec 预分配（一次性）。可忽略。**R3 新增**：Presized dict 消除大 N resize
  （N=100 省 ~800-2000ns）。

#### 瓶颈 8：pyo3 抽象层的隐性开销（**P0 优化后实测发现，2026-06-22**）

> **此瓶颈在 R3 设计阶段未被识别**，是 R3 性能预测（5-9x）远低于实测（8-12x）的根因。
> P0 优化实施后通过线性回归偏差分析发现。完整分析见 `docs/analysis/perf-p0-analysis.md` §9。

- **来源**：pyo3 的抽象层（`Bound<T>`、`IntoPy` trait、GIL token、参数转换）在**未启用 LTO**
  时，每个 C API 操作点都是真实的跨 crate 函数调用而非内联展开。
- **R3 设计阶段的盲区**：瓶颈 3 只计算了"用户可见的 3 个 C API 调用"（set_item + bind +
  into_py），忽略了 pyo3 抽象层内部的隐藏调用（`into_py` 参数转换、`Bound<T>` 的 drop
  引用计数、GIL token 传递、Result 重建等）。实际每字段 **10-15 次** 跨 crate 函数调用。
- **量级**：无 LTO 时约 **~140ns/field**（R3 设计阶段估计的"5-15ns/field"严重低估，
  低估约 10-28 倍）。这是 R3 预测与实测偏差的主要来源。
- **可证伪预测（修正后）**：若移除 `Cargo.toml` 的 `[profile.release] lto = "fat"`，
  每字段开销从 ~33ns 回升至 ~190ns，加速比从 ~12x 跌回 ~1.8x。
- **设计约束（修正后）**：`lto = "fat"` + `codegen-units = 1` 是 pyo3 项目的**必要条件**，
  非锦上添花。任何 pyo3 项目的性能预测都必须以 LTO 已开启为前提。pydantic-core、orjson、
  ruff 全部在 Cargo.toml 中强制此配置。

> **教训**：LTO 不是"消除几个 call 指令"的微优化，而是把 pyo3 的多层抽象折叠成单条
> 内联代码的**结构性优化**。详见 §G.6 性能模型修正。

### G.2 性能加速比预测（设计修订 2026-06-22 R3，方案 B'）

以 10 字段扁平 Struct 为基准（Phase 1 典型用例），方案 B' 修订后：

| 组件 | construct-rs 预期（R3 方案 B'） | Python construct 预期 | 加速来源 |
|------|-------------------------------|----------------------|---------|
| FFI 穿越（parse） | ~100ns | 0（纯 Python） | — |
| 节点分派 | ~40ns | ~3000ns | enum_dispatch |
| C API 调用（int创建 + dict SetItem） | ~650ns | ~3000ns | C API vs Python 字节码 |
| Context 操作 | 0ns（Phase 1 不调用） | ~2000ns | 已消除 |
| 实例构造（tp_new + GenericSetAttr，R3） | ~50ns | ~2000ns | 方案 B' 直接构造，无 kwargs；tp_new 直接调槽位 |
| **总计** | **~840ns** | **~10000ns** | **~11.9x** |

**预测加速比（R3 方案 B'）：约 5-9x**（满足 ≥4x 硬目标）。

**各用例预测**（基于 1.8 基准实测数据和每字段 ~65-90ns 的优化后模型）：

| 用例 | N | 当前 Full 加速比 | R3 预测 | Python 基线 | 依据 |
|------|---|----------------|--------|------------|------|
| B1 | 3 | 3.49x | **~7.3x** | 3095 ns | 消除 kwargs(115ns) + ctx(120ns)；固定开销 ~230ns |
| B2 | 10 | 2.80x | **~6.8x** | 6017 ns | 消除 kwargs(400ns) + ctx(400ns)；Presized 省 1 次 resize |
| B3 | 50 | 3.03x | **~10.3x** | 43743 ns | 消除 kwargs(12558ns) + ctx(2000ns)；Presized 省 3 次 resize |
| B4 | 100 | 0.94x | **~9.0x** | 82706 ns | 消除 kwargs(55782ns) + ctx(4000ns)；Presized 省 4 次 resize |

> R3 修正：固定开销由 R2 的 ~250ns 修正为 ~230ns（`tp_new` + `GenericSetAttr` 比
> `object.__new__` call1 + getattr `__dict__` 更快）。大 N 加速比因 Presized 消除
> dict resize 进一步上调。详见 `docs/设计修订-parse路径优化.md` §4。
> 预测区间反映 cache 效应和实现细节的不确定性。保守估计下所有用例均 ≥4x。

#### 实测确认（2026-06-22，P0 优化后）

> **R3 预测已被实测大幅超越。** P0 优化（fat LTO + Path/Context/map_err 移除）实施后，
> 实测加速比如下，远超 R3 预测的 5-9x。根因是 R3 性能模型低估了瓶颈 8（pyo3 抽象层
> 隐性开销 ~140ns/field），详见 §G.6。

| 用例 | N | R3 预测加速比 | **P0 实测加速比** | R3 预测 Full (ns) | **P0 实测 Full (ns)** | 偏差方向 |
|------|---|-------------|-----------------|-------------------|---------------------|---------|
| B1 | 3 | ~7.3x | **8.09x** ✓ | ~425 ns | **386 ns** | 实测优于预测 |
| B2 | 10 | ~6.8x | **9.66x** ✓ | ~880 ns | **628 ns** | 实测优于预测 |
| B3 | 50 | ~10.3x | **11.54x** ✓ | ~4230 ns | **2020 ns** | 实测优于预测 |
| B4 | 100 | ~9.0x | **12.06x** ✓ | ~9230 ns | **3656 ns** | 实测优于预测 |

> **结论**：所有用例（B1-B4）实测加速比均 **≥ 8x**，满足并大幅超越项目 ≥4x 硬目标
> （10x 理想目标也已达成）。性能目标**已验证通过**，无需进一步架构调整。
>
> **偏差根因**：R3 每字段预测 ~65-90ns，实测每字段仅 ~33ns（线性回归：固定 ~229ns +
> per-field ~33ns）。偏差来自瓶颈 8（LTO 消除 pyo3 抽象层开销 ~140ns/field）未被
> R3 模型计入。完整偏差分析见 `docs/analysis/perf-p0-analysis.md` §8-§9。

### G.3 性能退化场景（须在基准测试中暴露，设计修订更新）

| 场景 | 退化原因 | 预期退化（修订后） | 验证方式 |
|------|---------|------------------|---------|
| 深度嵌套（10 层） | ~~每层 StructRef 回调~~（已消除） | 加速比提升至 4-6x | 嵌套基准测试 |
| 小结构（1-2 字段） | FFI 固定开销占比高 | 加速比 ~3.5-4.5x | 小结构基准测试 |
| 有 `__post_init__` 的类 | 每次额外 Rust→Python 回调 | ~200-400ns 额外 | post_init 基准测试 |
| `__slots__` dataclass | 方案 B' 整体替换不可用（无 `__dict__`） | 编译期报 CompilationError（无运行时回退，R2 统一，R3 保留） | slots 编译期报错测试 |

**设计修订改善**：原方案中深度嵌套的 StructRef 回调（每层 ~200-400ns）已消除
（StructNode 内部直接构造实例），嵌套性能预期显著提升。

这些退化是设计权衡的已知代价（FFI 设计 §8.3），基准测试必须包含对应场景以暴露。

### G.4 验证方法（基准测试设计，落实性能门禁 §1）

#### G.4.1 基准用例矩阵

| 用例 | 结构 | 字段数 | 嵌套层 | 数据大小 | 目的 |
|------|------|--------|--------|---------|------|
| B1 | ModbusRTUMessage（example.py） | 3 | 0 | 6 字节 | 真实用例，小结构 |
| B2 | 扁平 Struct | 10 | 0 | ~20 字节 | 中等结构，典型 |
| B3 | 扁平 Struct | 50 | 0 | ~100 字节 | 大结构，验证线性 |
| B4 | 扁平 Struct | 100 | 0 | ~200 字节 | 大结构上限 |
| B5 | 嵌套 Struct | 5×3 | 3 | ~30 字节 | 嵌套验证 |
| B6 | 深嵌套 | 3×10 | 10 | ~30 字节 | 嵌套退化暴露 |
| B7 | 空 Struct | 0 | 0 | 0 字节 | 边界情况 |

#### G.4.2 对照组

每个用例的对照组：
- **绝对基线**：Python construct 2.10.70 的等价 `Struct("a"/Int8ub, ...)` 定义。
  用 `Struct.parse(data)` / `Struct.build(dict)` 测量。
- **不可用相对基线**：禁止用 construct-rs 的旧实现作为基线（AGENTS.md 性能门禁 §2）。

#### G.4.3 测量方法

```python
import timeit

# construct-rs
t_rs = timeit.timeit(lambda: ModbusRTUMessage.parse(data), number=100000)

# Python construct 基线
t_py = timeit.timeit(lambda: py_struct.parse(data), number=100000)

speedup = t_py / t_rs
assert speedup >= 4.0, f"性能未达标: {speedup}x < 4x"
```

- **number**：足够大（≥ 100000）以消除计时误差。
- **首次调用排除**：timeit 自动预热（取最小值）。
- **重复测量**：5 次取中位数。
- **parse 与 build 分别测量**。

#### G.4.4 环境记录

每次基准测试记录：
- Python 版本（3.10+）
- 平台（OS、CPU 架构）
- CPU 型号与主频
- construct-rs commit hash
- 数据表格（用例 × 操作 × 加速比）

#### G.4.5 通过标准

- **B1-B5 加速比 ≥ 4x**（硬目标）。
- **B6（深嵌套）加速比 ≥ 2x**（已知退化，可接受）。
- **B7（空 Struct）不回归**（功能性，性能无意义）。
- 任一用例 < 0.5x → 暂停后续阶段（性能门禁 §2）。

> **实测验证结果（2026-06-22，P0 优化后）：已通过** ✓
>
> | 用例 | 通过标准 | 实测加速比 | 状态 |
> |------|---------|-----------|------|
> | B1 | ≥ 4x | 8.09x | ✓ 通过 |
> | B2 | ≥ 4x | 9.66x | ✓ 通过 |
> | B3 | ≥ 4x | 11.54x | ✓ 通过 |
> | B4 | ≥ 4x | 12.06x | ✓ 通过 |
>
> B1-B4 全部达到 8-12x，远超 4x 硬目标（10x 理想目标也已达成）。性能门禁 §3 验收
> 证据已满足（对比数据表，非"编译通过"）。完整数据见
> `docs/analysis/perf-p0-analysis.md` §8.1。

### G.5 性能门禁触发点

| 门禁 | 触发时机 | 动作 |
|------|---------|------|
| §1 设计阶段性能假设 | 本文档（已完成） | ARCH 已写入瓶颈识别 + 可证伪预测 + 验证方法 |
| §2 首个实现后烟雾测试 | Phase 1 首个可运行版本 | PM 执行 B1 基准测试，< 0.5x 暂停 |
| §3 验收阶段证据 | Phase 1 验收 | PM 检查 B1-B7 数据表，"≥4x" 须对比数据 |

### G.6 性能模型修正（P0 优化后实测推导，2026-06-22）

> 本节记录 P0 优化实施后，基于实测数据对 R3 性能模型的修正。此修正作为后续阶段
> （Phase 2+）性能预估的方法论基线。完整分析见 `docs/analysis/perf-p0-analysis.md`
> §10-§11。

#### G.6.1 旧模型缺陷（R3 设计阶段）

R3 性能模型（§G.1 瓶颈 3）的 per-field 估算：
- PyLong 构造 ~25ns + `__dict__` SetItem ~25ns + path/overhead ~10ns = **~55-75ns/field**

**缺陷**：只计算了"有效工作"（用户可见的 C API 调用），完全忽略了 pyo3 抽象层的
间接开销（瓶颈 8）。实际每字段的 pyo3 抽象层隐性开销约 ~140ns（无 LTO 时），使
R3 的 per-field 估算偏低约 3-6 倍。

#### G.6.2 修正后的性能模型（P0 实测推导）

基于 P0 优化后实测数据（per-field ~33ns，固定 ~229ns）拆解：

| 组成 | ns/field | 说明 |
|------|---------|------|
| PyLong 构造 | ~15-20 | `ffi::PyLong_FromUnsignedLong`（CPython 不可压缩） |
| PyDict SetItem | ~10-15 | `ffi::PyDict_SetItem`（CPython 不可压缩） |
| 字节读取 + 转换 | ~2-3 | `from_be_bytes`（已被 LTO 内联到几乎零成本） |
| enum_dispatch 分派 | ~1-2 | match（已被 LTO 优化） |
| **每字段有效工作合计** | **~28-40** | 接近理论下界 |
| 固定开销 | ~229 ns/call | `tp_new` + `PyDict::new` + `force_setattr` + FFI（CPython 不可压缩） |

> 修正模型的关键前提：**LTO 已开启**（`lto = "fat"` + `codegen-units = 1`）。
> 在此前提下，pyo3 抽象层的 ~140ns/field 开销被内联消除，per-field 降至 ~33ns。

#### G.6.3 P0 优化贡献来源分解

| 优化项 | per-field 收益 | 固定收益 | 说明 |
|--------|--------------|---------|------|
| P0-1 fat LTO + codegen-units=1 | ~140 ns | ~0 | 消除 pyo3 抽象层跨 crate 调用开销（**最大贡献**） |
| P0-2 移除 Context::new_root | 0 | ~500 ns | Phase 1 不读 context，移除空 PyDict 创建 |
| P0-3 移除成功路径 Path 维护 | ~30 ns | ~50-100 ns | 延迟到错误路径构建（RAII + 引用已有 PyString） |
| P0-4 set_item 改用 `?` + `From<PyErr>` | ~3 ns | 0 | 移除 map_err 闭包构造 |
| **合计** | **~173 ns/field** | **~550-600 ns** | per-field 从 ~223 降至 ~33；固定从 ~1093 降至 ~229 |

> P0-1（LTO）贡献最大（~140ns/field），占 per-field 总收益的 ~81%。
> P0-2/P0-3/P0-4 的预估与实际吻合，说明 ARCH 对"用户可见的显式操作开销"估算准确，
> 问题出在"pyo3 抽象层隐藏开销"的盲区。

#### G.6.4 关键教训（方法论，后续阶段必须遵守）

1. **pyo3 抽象层是"隐性税"**：`Bound<T>`、`IntoPy` trait、GIL token 在无 LTO 时是
   一层隐性税——每个操作经过多层函数调用，累积起来使 Rust 重写几乎失去意义（1.8x）。

2. **fat LTO 是必要条件而非锦上添花**：`lto = "fat"` + `codegen-units = 1` 不是
   微优化，而是让 pyo3 抽象层"消失"的必要条件。pydantic-core、orjson、ruff 全部
   强制此配置，这不是巧合。

3. **ARCH 性能模型的修正原则**：
   - 逐个列出每字段的**所有**函数调用（不只是用户可见的 3 个）
   - 对每个调用评估无 LTO 时的 call/ret 开销
   - **任何 pyo3 项目的性能预测都必须以 `lto = "fat"` + `codegen-units = 1` 为前提**
   - 在 Cargo.toml 有 LTO 配置的前提下，才能假设 pyo3 抽象层开销被消除

4. **后续阶段（Phase 2+）性能预估基线**：必须采用本节修正后的方法论（计入 pyo3
   抽象层开销，并以 LTO 已开启为前提）。预测偏差方向应为"实测优于预测"（保守低估），
   而非"实测差于预测"（乐观高估）。

---

## 附录 A：与 Python construct 的 API 差异说明

construct-rs 的用户 API 与 Python construct 2.10.70 有意不同（mashumaro 式 vs 命令式）：

| 维度 | Python construct | construct-rs | 差异原因 |
|------|-----------------|--------------|---------|
| 结构定义 | `Struct("a"/Int8ub, "b"/Bytes(4))` 命令式 | `@dataclass class X(StructMixin): a=field(Int8ub)` 声明式 | 用户无感、类型提示、IDE 友好 |
| parse 调用 | `Struct.parse(data)` 返回 Container | `X.parse(data)` 返回 X 实例 | 与 @dataclass 集成 |
| build 调用 | `Struct.build(dict)` 传 dict | `instance.build()` 传实例 | 实例方法更自然 |
| 结果类型 | Container（dict 子类） | 用户类实例 | 类型安全、属性访问 |
| 字段引用 | `this.length`（运行时表达式） | Phase 1 无，Phase 2 支持 | 渐进实现 |

Python construct 的 `Struct("a"/Int8ub)` 命令式 API **不在** construct-rs 的 Phase 1
范围（推迟到 Phase 2+ 评估）。Phase 1 仅支持 StructMixin 声明式 API。

## 附录 B：设计决策溯源

| 决策 | 备选方案 | 选择理由 |
|------|---------|---------|
| StructMixin 用纯 Python | Rust pyclass | 元编程逻辑（__init_subclass__、延迟桩）Python 更灵活 |
| 类型描述符用 pyclass | 纯 Python 对象 | 类型安全（extract）、Rust 直接读取参数 |
| Node 用 enum_dispatch | Box<dyn Construct> | 静态分派，消除堆分配与间接跳转（FFI 设计 §6） |
| ~~parse 返回 dict~~ → **Rust 内直接构造实例（方案 B'）** | ~~原方案：Python 侧 cls(**dict)~~ | **设计修订 2026-06-22 R3**：`**dict` unpacking 是 O(N²)，实测 B4 加速比仅 0.94x；且 dict 跨 FFI 返回违反 §0"无中间表示层"。R3 方案 B'：`PyDict::new` + `tp_new` + `PyObject_GenericSetAttr` 整体替换 `__dict__`（与 pydantic-core 对齐）。详见 `docs/设计修订-parse路径优化.md` |
| StructRef 存类引用 | 存编译产物引用 | 解决互相引用的循环依赖（REV 建议 1） |
| Context 持有 PyDict | Rust HashMap | 字段值本就是 PyObject，直接操作无中间类型 |
| ~~Phase 1 调用 ctx.set_field~~ → **不调用** | ~~原方案：每字段存入 context~~ | **设计修订 2026-06-22**：Phase 1 无 this 表达式，context 不被读取，`set_field` 是纯开销（~40-70ns/field）。Phase 2 恢复 |
| ~~字段名为 String~~ → **缓存为 interned PyString** | ~~原方案：每次 set_item 用 name.as_str()~~ | **设计修订 2026-06-22**：消除每字段 str 创建+hash 开销（~20-25ns/field） |
| ~~object_new 编译期缓存~~（R2）→ **create_class 辅助函数**（R3 2026-06-22） | ~~R2：缓存 object.__new__ 为 StructNode.object_new~~ | **R3**：移除 object_new 字段，改用 `create_class(cls)` 直接读 `tp_new` 槽位（~1-2ns），与 pydantic-core `create_class` 一致。更简洁、无缓存一致性开销 |
| **`__slots__` 编译期报错**（R2 2026-06-22） | slow_path 运行时回退 | **R2 P2**：统一 R1 矛盾表述为 fail fast。slots dataclass 罕见，避免引入运行时分支复杂度；后续阶段可加 slow_path 标志 |
| 错误用 ConstructError + From | 直接用 PyErr | 结构化 path/kind，成功路径零成本 |

---

*本文档定义的架构是 Phase 1 编码的唯一依据。任何与本文档冲突的实现方案，必须经 PM
协调、ARCH 修改本文档后方可实施。本文档的修订须在 `plans/phase0-architecture/过程记录.md`
中记录。*

> **实测修正（2026-06-22）**：本文档 §G 性能假设章节已合并 P0 性能优化实测结论。
> P0 优化（fat LTO + Path/Context/map_err 移除）实施后，加速比实测达 8-12x
> （B1=8.09x, B2=9.66x, B3=11.54x, B4=12.06x），远超 R3 预测的 5-9x。
> 受影响章节：§G（引言实测修正说明、新增瓶颈 8、§G.2 实测确认表、§G.4.5 通过标准
> 已验证标注、新增 §G.6 性能模型修正）。保留 R3 原始预测作为历史记录，以"实测修正"
> 标注补充实测结论。完整分析见 `docs/analysis/perf-p0-analysis.md`。
>
> **R3 修订（2026-06-22）**：本文档已同步 parse 路径优化 R3 修正——方案 B 升级为
> 方案 B'（与 pydantic-core 对齐）：`PyDict::new` + `tp_new`（`create_class`）+
> `PyObject_GenericSetAttr`（`force_setattr`）整体替换 `__dict__`。移除 R2 的
> `object_new` 字段（改用 `create_class` 辅助函数读 `tp_new` 槽位），新增
> `field_count`（用于 `PyDict_NewPresized`）。新增 §0 原则对照表和 build 路径评估。
> 受影响章节：§A.2 / §B.1 / §B.2 / §B.3 / §B.6 / §B.7 / §C.3.4 / §G.1 / §G.2 / §G.3 /
> 附录B。详见 `docs/设计修订-parse路径优化.md`。
>
> **R2 修订（2026-06-22）**：~~本文档已同步 parse 路径优化 R2 修正——object_new 编译期
> 缓存（P1）、`__slots__` 编译期报错统一（P2）、纳入 N1-N5 非阻塞建议。~~
> （R2 的 P1 object_new 缓存已被 R3 移除；P2 __slots__ 编译期报错和 N1-N5 由 R3 保留。）
