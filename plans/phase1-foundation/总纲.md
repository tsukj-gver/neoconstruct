# Phase 1: 垂直切片验证

> **注意**：本文件在 Phase 0 架构设计完成后由 PM 根据设计文档填写。

## 阶段目标

实现最小端到端路径，证明架构正确性和性能目标可达。

## 前置依赖

- Phase 0 架构设计验收通过

## 预期出口标准

1. `@dataclass class Msg(StructMixin): a: int = field(Int32ub)` 可 parse/build
2. `cargo build` + `cargo clippy` + `cargo fmt --check` 通过
3. `cargo test` 全部 PASS
4. **性能烟雾测试**：parse/build ≥4x vs Python construct 2.10.70

## 子任务清单

（Phase 0 完成后由 PM 填写）
