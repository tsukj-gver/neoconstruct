---
id: trace-v0.1.3-pre2-compliance-fix
task: v0.1.3-pre2 [发布合规修复]
role: DEV
date: 2026-09-08
---

# v0.1.3-pre2 发布合规修复记录

依据：`plans/v0.1.2/traces/v0.1.3-pre-发布合规审查.md` 违规清单（A1-A13 / B1-B9 / C1 = 23 条）。
原则：仅动注释/docstring/测试内自有变量名，零逻辑变更；中文保持中文；范围外观察 3 项（O1 pyclass module 名 / O2 Union as usize / O3 存量对照注释）未动。

## 修复对照表

### A 类：用例族/裁决编号清除

| # | 位置 | 原文 → 新文 |
|---|------|------------|
| A1-A6 | test_conditional_roundtrip_semantics.py :67,79,91,107,119,130 / :219,229,239 / :268,282,294 / :321,335,347,360 / :386,397,409,422 / :455,487,512,526,536（共 26 个用例 docstring） | `"""A1 If(x>0, Byte) 真分支：…"""` → `"""If(x>0, Byte) 真分支：…"""`——统一删除 docstring 起首的「XN 」编号前缀（A1-A6/C1-C3/D1-D3/E1-E4/F1-F4/G1-G5），语义描述原样保留 |
| A1 特例 | 同文件 :229 | `（与 C1 同字节）` → `（与 parse 产出 None 同字节）`——删除编号后内部编号引用改为语义引用 |
| A7 | 同文件 :381 | `条件根 init_default → Optional（R4）；` → `条件根 init_default → Optional；` |
| A8 | 同文件 :55,147,207,256,309,374,442（7 处分节注释） | `# 族 A：回环属性（parse(x).build() == x）` → `# 回环属性（parse(x).build() == x）`——统一删「族 X：」前缀，语义名保留（回环属性 / 不一致防护 / None 三来源等价 / ctx 可见性 / 分支内值提供器 / init 面 / 边界） |
| A8 连带 | 同文件 :14-21 模块 docstring；:60,152,212,261,314,379,447 class docstring | 「七族用例（每用例 docstring 标注所属族与维度组合）：- A 回环属性：…」→「用例按语义分组覆盖以下维度：- 回环属性：…」；class docstring「A 族：…」→「回环属性：…」等同模式。连带理由：删用例编号后族编号体系描述失效，同步清理保持文件内部一致（仍为注释级，零逻辑变更） |
| A9 | src/nodes/struct_node.rs:1936 | `（实例化可省，R4）` → `（实例化可省）` |
| A10 | 同文件 :1967 | `同语义（R3）：cond 假` → `同语义：cond 假` |
| A11 | 同文件 :1989 | `非 BuildValueMissing——R1 分支自治` → `非 BuildValueMissing——分支自治` |
| A12 | 同文件 :1077-1078 | `（AttributeError ≡ 缺值，设计 v2.3 回填 §5.2——错误判据单源收敛，报 BuildValueMissing 含字段+path）` → `（AttributeError ≡ 缺值——错误判据单源收敛，报 BuildValueMissing 含字段+path）` |
| A12 | 同文件 :1704-1705 | `（错误判据单源收敛，设计 v2.3 §5.2 补：AttributeError ≡ 缺值 → BuildValueMissing）` → `（错误判据单源收敛：AttributeError ≡ 缺值 → BuildValueMissing）` |
| A13 | src/nodes/process_rotate_left.rs:319 | `PR-5 对称：ProcessRotateLeft(4, 0, ...) build → RotationError（非 panic）` → `ProcessRotateLeft(4, 0, ...) build → RotationError（非 panic，与 parse 侧同形态）`——编号删除，对称语义改写保留 |

### B 类：过程叙述/过程词汇清除

| # | 位置 | 原文 → 新文 |
|---|------|------------|
| B1 | tests/parity/test_array_parity.py:536-541 | 「**历史处理记录**：R1-R3 曾因…现已修复：R1/R3 改为…R2 重定义为…容错 fixture 保留——…」6 行整段 → `# 容错语义：case 子进程基础设施故障以 skip 呈现，而非让整个 module 崩溃。` 1 行（仅保留现状理由） |
| B2 | tests/unit/test_compile_rejections.py:3 | `覆盖两类用户误用（此前均为静默错误输出/静默错误解析）：` → `覆盖两类用户误用：` |
| B3 | 同文件 :6-8 | `按用户裁决（继承无空间信息、字节排布有位置语义）在 ``__init_subclass__`` 编译期显式拒绝` → `字节排布有位置语义，继承无法表达基类字段占位，故在 ``__init_subclass__`` 编译期显式拒绝` |
| B4 | 同文件 :189-192 | docstring 内 `此前该形态 parse 零字节消费、静默返回默认值。` 句删除，恢复单行 docstring |
| B5 | tests/errors/test_untrusted_count_hardening.py:3-5 | `曾按 count 预分配结果 Vec——不可信输入…触发 34GB 级分配失败` → `按 count 预分配结果 Vec 的朴素实现面对不可信输入…会触发 34GB 级分配失败`（无时间指涉，描述未加固机理） |
| B6 | 同文件 :31-38, :50-53 | 注释 `注入 CRS python 目录` → `注入 neoconstruct python 目录`；模板变量 `CRS_PYTHON_DIR`（3 行）→ `NEOCONSTRUCT_PYTHON_DIR`；format 占位符 `{crs_python_dir!r}` → `{neoconstruct_python_dir!r}`；`_run_isolated` 形参 `crs_python_dir` → `neoconstruct_python_dir`（.format 调用同步）。测试函数签名的 fixture 形参 `crs_python_dir` 为 conftest 存量接口（审查观察 7），保留；调用为位置传参不受影响 |
| B7 | tests/errors/test_repeat_generic_adapter_errors.py:248 | `可达性说明（[文档] _errors.py docstring + 实测探针）：` → `可达性说明（依据 _errors.py docstring 与实测行为）：` |
| B8 | tests/_helpers/parity.py 模板内注释 + assert_loopback docstring（2 处） | 「此前 harness 只 build 工厂构造的实例，从不 build 解析产物——本断言补上该方向」→ 现状陈述「harness 对 build 验证两个方向——工厂构造实例（built 维度）与解析产物（本断言）」；模板注释同步改为两方向各验证什么 |
| B9 | src/error.rs:82 | `用户面 sizeof API 尚未提供（follow-up）；` → `用户面 sizeof API 尚未提供；`（与 _errors.py 侧「后续版本引入时」陈述风格对齐） |

### C 类：construct 独立性

| # | 位置 | 原文 → 新文 |
|---|------|------------|
| C1 | src/nodes/greedy_bytes.rs:39-40 | `不校验长度（对应 Python construct \`GreedyBytes._build\` 的 \`stream_write(stream, data, len(data), path)\`，长度始终匹配）` → `不校验长度（长度始终匹配）` |

## 明确未动（范围外/豁免）

- conftest.py 存量 fixture 名 `crs_python_dir` / `rs_python` / `_CRS_ROOT` / 环境变量 `CRS_PYTHON`（v0.1.1 存量基础设施，观察 7 豁免）
- `src/descriptors/mod.rs` pyclass `module = "construct"`（O1）、`src/compile.rs` `idx as usize`（O2）、存量「对齐 Python construct」注释含 greedy_bytes.rs 其余存量 Python 参考行与 PR-5:304/FE-1 存量行（O3）——PM 另行立项
- bench 元数据（审查第五节 1 倾向豁免）

## 自检结果

- 复扫：23 条违规按审查清单逐条 grep 模式核对，全部清零
- pytest 全量：**1296 passed / 0 failed** / 8 skipped（既有 parity 容错 skip，与基线一致）
- cargo fmt --check ✅；cargo clippy 零 warning ✅；cargo test **1429 passed / 0 failed**（+ bench 1 ignored / doc-tests 3 ignored，既有）
- 变更文件 10 个：8 个仅注释/docstring，1 个含测试内模板字符串变量改名（B6，模板与 format 同步、位置传参不破坏调用），1 个含 helper 形参改名（B8 所在文件仅 docstring；B6 文件形参改动经 pytest 全量验证）

## 复审节（v0.1.3-pre3，VET）

方法：工作区未提交 diff（10 文件，+80/-87）逐 hunk 精读，对照原审查 23 条清单与修复对照表；新增行四类模式扫描（编号/过程词/construct 引用/自指）；B6 调用点逐一核验；全量 pytest 复跑。

**23 处逐条核对**：A1-A13 / B1-B9 / C1 全部落地，与修复对照表逐条一致。
- A1-A7：26 个 docstring 编号前缀（A1-G5）全删；:229 内部编号引用改语义引用；:381 `（R4）` 已删。
- A8 + 连带：7 处分节「族 X：」删；模块 docstring「七族用例…」与 7 处 class docstring「X 族：」同步清理——连带理由成立（删用例编号后族体系描述失效），仍为注释级零逻辑变更，合规。
- A9-A13（struct_node R1/R3/R4/设计 v2.3 §5.2 ×2、rotate PR-5 对称）全落地。
- B1-B9 全落地；B6 见下；B5 改写后无时间指涉；B8 两处均为现状陈述。
- C1：greedy_bytes construct 引用清除，语义无损。

**新引入扫描**：新增行零新违规。唯一模式命中均判定合规——`0xC0000409 族`（Windows 错误码族，技术术语）；construct 命中均为 diff 文件头/neoconstruct 自身/新变量名；`（本断言）` 为 B8 修复后现状自指，非过程叙述。

**B6 专项**：`_run_isolated` 形参改名与 `.format` 关键字同步正确；文件内 6 处调用均为位置传参（第二位置传 conftest 存量 fixture `crs_python_dir`），绑定新形参不受影响；全 tests/ 目录 `CRS*` 命中余量均为存量豁免基础设施（conftest / parity helper / system helper，原审查误报 7 + 本记录「明确未动」节）。

**全量测试**：pytest **1296 passed / 0 failed / 8 skipped**（复跑确认，与自检一致；warning 为 .pytest_cache 权限环境噪音）。

**范围外观察（不阻塞，供 PM 参考）**：
1. test_conditional_roundtrip_semantics.py 类名 `TestFamilyA..G*` 仍含族字母——功能性测试类名（带语义后缀），原审查 A8 仅列注释行未列类名，与第五节 3 形态类名容忍倾向一致，后续可顺手语义化。
2. 存量残留 common.rs:7「此前」/ process_rotate_left.rs:304「PR-5:」——原审查误报 8 已裁定范围外（非本次 diff 新增，本次未触碰），O3 类待 PM 立项。

**Verdict：RELEASE-GO**——23 处全部修复、连带清理合规、零新引入、B6 改名同步完整、全量测试绿。
