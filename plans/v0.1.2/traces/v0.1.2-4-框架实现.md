---
id: TRACE-v0.1.2-4
task: v0.1.2-4 [ValueKind 框架实现 + 语义矩阵]
role: DEV
date: 2026-09-04
status: coding-done
---

# v0.1.2-4 [ValueKind 框架实现 + 语义矩阵] 过程记录

> 前次会话中断后由本 DEV 接手续作。实施依据：设计 v2.2（§5 实现映射 /
> §5.2 resolve 伪码 / §8 矩阵 / §9.1 行为变更清单 / §7 性能判据 / §11 门禁）。

## 1. 接手评估与处置

### 1.1 前次产物盘点（git status + 源码核验）

| 产物 | 实际状态 | 处置 |
|------|----------|------|
| `test_v0_1_2_semantics_matrix.py`（1528 行） | 命名违反 §0#10；docstring 满篇锚点引用（[A4]/S1/C1-C3 等 88 处） | **删除** |
| `test_value_semantics_matrix.py`（1520 行） | 命名合规、docstring 自主语义表述、多 1 类 5 例（ControlRfield，对应 F2 处置建议） | **保留**，修正 3 处草稿缺陷（见 §4） |
| `src/error.rs`（+38） | BuildValueMissing 变体 + Python 类注册（`field_value_missing_error`）已混入提交 f76b49c | 保留（已入库） |
| `src/nodes/struct_node.rs`（+304） | ValueKind 枚举 + classify + init_default + build 循环 resolve 迁移，主体已混入 f76b49c | 保留（已入库） |
| 工作区 5 文件未提交 diff | 测试辅助构造点（struct_node/struct_ref/greedy_range/named_tuple/prefixed_array）补 `kind` 字段——StructField 加字段后的编译修复 | **续作完成**（cargo test 1394 全绿含这些改动） |

**关键发现**：前次会话将框架主体混入 `f76b49c`（chore 提交，提交信息与内容不符）；
本 DEV 不做 git 改写（PM 管辖），在报告中留痕供 PM 处置。

### 1.2 接手时的缺口（本会话补齐清单）

1. Python `_errors.py` 缺 `FieldValueMissingError` → Rust `init_exception_classes`
   要求注册，新扩展安装后 import 即炸；矩阵测试 import 失败的直接原因
2. `__init__.py` 未导出 `FieldValueMissingError`
3. `_mixin.py` 仍用 `value_providing` isinstance 链，未消费 `_init_defaults()`
4. B4（context=）编译期拒绝未实现
5. `struct_node.rs` Const/Default 分支 getattr 对 RO 面（init=False）属性不存在
   未按"≡缺省"处理 → 3 个 cargo test 失败（ro_const/ro_default/unsupported）
6. computed.rs/rebuild.rs/tell.rs/struct_node.rs 注释残留 compute_ro_value 引用
7. clippy 9 个 warning（含存量：unused_mut×2、items_after_test_module、
   bool_assert_comparison×5、needless_borrow）+ fmt diff

## 2. 实现清单（本会话完成）

### Rust 侧

| 文件 | 改动 |
|------|------|
| `struct_node.rs` | Const/Default resolve 分支增 F1 泛化：getattr Err(AttributeError) ≡ 缺省（Const→补常量 / Default→求值）；其他 getattr 错误保持 Generic；重写 `ro_field_build_with_unsupported_node_returns_error` 为 `ro_instance_field_build_reads_instance_value` + `ro_instance_field_without_attribute_reports_missing`（RO+Instance 新语义：显式传值可 build，缺属性报错——FieldMode 退出 build 分派的放宽结果）；注释清理 |
| `computed.rs`/`rebuild.rs`/`tell.rs` | compute_ro_value 注释残留清理（改为 ValueKind resolve 表述）；rebuild.rs build 注释"对齐 Python"表述替换为节点自治理由 |
| `checksum.rs`/`probe.rs` | 存量 unused_mut 修复（set_field 是 &self） |
| `flags_enum.rs` | 存量 assert_eq!(x, bool) → assert! |
| `compile.rs` | 存量 items_after_test_module 修复（build_*_node 辅助函数块移至 mod tests 前，块移动脚本原子操作） |

### Python 侧

| 文件 | 改动 |
|------|------|
| `_errors.py` | 新增 `FieldValueMissingError(ConstructError)`（对应 Rust BuildValueMissing；错误时机=build 值使用点） |
| `__init__.py` | 导出 FieldValueMissingError（import + __all__） |
| `_mixin.py` | ① `_apply_dataclass_field_config(cls, descriptors, schema=None, expr_programs=None)` 重写：消费 `schema._init_defaults()` 三态（(2,v)→default=v / (1,None)→default=None / (0,None)→必填）；RO init=False 与显式 default 优先级保留；**删除 value_providing 元组与 isinstance 链**（含死代码 `_subcon_contains_field_expr`）；② 延迟桩路径（前向引用编译失败，无产物）回退推导：expr_programs 非空→Optional、其余→Required（[设计质疑-小]，见 §7）；③ B4：`_compile_schema_for_class` 收集 descriptors 后检查 `_context_injections is not None` → CompilationError（fail fast，注明 v0.1.3 提供注入）；④ 三个调用点传 schema（正常路径/延迟重试）/expr_programs（延迟安装） |

## 3. 五处散落特判迁移对照（设计 §5.2 审计表核验）

| 散落点 | 迁移状态 |
|--------|----------|
| `mod.rs compute_ro_value` 11 分支 | ✅ 函数已删（f76b49c，本会话核验无实现残留）；分支映射 Tell→Tell、Computed→Computed、Index→Index、StopIf/Check/Terminated/Element/Peek/Seek/Checksum→Control、Rebuild→Rebuild、Default→Default、Const→Const、Padding→Void 全部落位 |
| `struct_node.rs` build 的 Rw/Wo/Ro 三分支 | ✅ 统一 ValueKind 循环；Wo 不写 ctx 由编译期 `_check_wo_reference` 保证（保留） |
| `sequence.rs` RO 旁路 | ✅ 同模式迁移（Instance=list 位置取值、None≡缺值→BuildValueMissing） |
| 节点层 Const/Default None 容忍 | ✅ 保留（非字段语境必需） |
| `_mixin.py value_providing` 元组 | ✅ 删除，改消费 init_defaults（本会话） |

WO 引用编译期禁令（`_check_wo_reference`）保留 ✓。Control 显式透传+缺省哑值（R1
选项 B）✓：透传值写 ctx、节点编码忽略实例值；F1"attr 不存在≡哑值 None"（Control
面）已实现并**泛化到 Const/Default 面**（语义一致性：缺省补值对属性缺失统一）。

## 4. 语义矩阵（最终 104 用例全绿）

### 4.1 红灯基线记录（旧扩展=止血后行为，venv editable 特性利用）

框架落地前基线：**24 failed / 80 passed**（矩阵 104 例，修正草稿缺陷后）。
红灯分布与设计 §8 预测吻合：I1/J1/K1（Control 哑值必填）、I3/I6（peek build 缺省/
哑值流入）、J3/K3（seek/checksum build）、B3 RW 面（F 族 instantiation_optional/
missing_raises）、D5（Computed 常量折叠）、H1（B4 静默）、H2（Const 可变）、W1/W2
（包装器分类）等；I5 round-trip 基线即绿（R1 选项 B 要求不回归）✓。

### 4.2 矩阵红灯→绿灯（框架落地后）

- **24 红 → 0 红**（104 passed / 0 failed）
- 全量 pytest：**1014 passed, 20 skipped, 0 failed**（910 基线 + 104 矩阵）
- IEC104 + Modbus RTU 单独复验：41 passed（I5 round-trip 不回归 ✓）

### 4.3 草稿缺陷修正（3 处，非期望值错误）

1. I5 测试：Switch case 用裸 `Bytes(2)/Bytes(3)` 触发表达式参数同名冲突
   （length: const2 vs const3，编译期既有限制）→ 改嵌套 StructMixin 子类
   （已交付 IEC104 同型）；数据修正——Peek 后流回退，body 覆盖被 peek 字节
2. `test_rfield_checksum_roundtrip`：`Checksum(Bytes(2), f)` 缺 bytesfunc/start/end
   → 改 StreamRange 模式（Tell start/end，modbus 同型）
3. `test_if_cond_expr`：期望 `c=None` build 成功 → 按框架语义修正（显式 None≡
   缺值 → FieldValueMissingError；W 族同型锁定）

### 4.4 既有测试更新（行为变更跟进，3 文件 3 例 + parity 1 例）

设计 §9.1 批准的行为变更（#5 显式 None→BuildValueMissing、#10/#11 包装器根
缺省报错）使断言旧基线行为的用例失败，按新语义更新：

| 用例 | 旧断言 | 新断言 |
|------|--------|--------|
| `test_v0_1_1_regressions::test_prefixed_computed_build` | `P(typ=1, br=None).build()==b'\x01\x00'` | `br=0` 显式传值成功（值不参与编码）+ `br=None`→FieldValueMissingError |
| `test_v0_1_2_regressions::test_prefixed_if_cond_zero_else_branch` | `M(x=1, c=None).build()==b'\x01\x00'` | 同上模式 |
| `parity/test_conditional_parity S2`（fixture 级联 27 errors 的唯一实质失败） | rs 侧 `P(v=None)` build | rs 侧 `P(v=0)`（Pass 分支编码忽略值，两端 build 字节一致 b''） |

注：`test_prefixed_computed_add` docstring"已知边界……留给 v0.1.2-1 值语义规格
统一收敛"即本框架收敛点，行为按 §5.1c 落定。

## 5. F1/F2 处置（REV v2.2 复审遗留）

- **F1（Control resolve 缺省未覆盖 RO 面 attr 不存在）**：已实现——Control 分支
  getattr Err→哑值 None（f76b49c，本会话核验）；并**泛化至 Const/Default 分支**
  （attr 不存在≡缺省→补常量/求值），保持 compute_ro_value 时代"RO 面从零实例
  可 build"行为（ro_const/ro_default 两测试转绿的语义依据）
- **F2（§9.1 未列 RO 面放宽：rfield(Peek/Seek/Checksum) build 必错→可用）**：
  已实现并新增矩阵 `TestControlRfieldBuildUsable`（3 例：peek no-op/seek 移针/
  checksum round-trip）锁定；v0.1.2-5 发布说明补行（PM 处置）

## 6. 性能门禁（设计 §7 判据 A/B 分级）

bench_struct.py 同会话连续轮次（rs 侧 ns 中位；基线=设计 §7 主基线）：

| 点 | 基线中位 | 本轮 3 轮 | 本轮中位 | Δ 中位 | 判据 | 结果 |
|----|----------|-----------|----------|--------|------|------|
| B1-parse | 384 | 392.8/383.1/405.2 | 392.8 | +2.3% | A（自波动 5.6%） | ✅ |
| B1-build | 247 | 270.2/265.2/258.3 | 265.2 | +7.4% | A（自波动 4.5%） | ✅ |
| B2-parse | 710 | 742.7/767.2/725.5 | 742.7 | +4.6% | A（自波动 5.5%） | ✅ |
| B4-build | 3922 | 5723.7/3652.1/3993.1（+2 样本 8991.1/3838.1，5 样本中位 3993） | 3993 | +1.8% | B（自波动 >20%，5 样本中位鲁棒比较） | ✅ |

- 判据 A 各点自身波动均 <15%（N4 无需 B 复核）；+7.4% 属跨会话噪声带
  （REV 复跑 spike 互证 ±4% 会话漂移 + 本轮后续复核轮整体 ×1.7 的环境负载
  证据——高负载轮不纳入中位）
- B4-build 后续复核轮 py 侧子进程超时（机器负载累积），B 判据样本足够未补齐
- 门禁检查器输出：`[PASS] 全部通过`（含全部 14 点）

## 7. 设计质疑

- **[设计质疑-小] 延迟桩路径的 init_defaults 回退**：设计 §5.3 未考虑前向引用
  编译失败（延迟桩）时无编译产物可消费。本实现回退推导=expr_programs 非空→
  Optional、其余→Required（哑值构造器在该路径退化为必填）。影响面：仅"前向
  引用类 + 哑值字段 + 延迟窗口期实例化"组合（无既有测试锁定，触发场景罕见）；
  `_retry_compile` 成功后类属性默认值更新为编译产物。请 ARCH 确认回退策略或
  提出更优解
- **[设计质疑-小] Instance 分支 getattr 失败保持 Generic**：设计伪码字面行为
  （仅 None→BuildValueMissing）；RO 面 rfield(值类) 从零 build 报
  "object has no attribute"（Generic）。未擅自统一为 BuildValueMissing
  （保守按设计字面实现）

## 8. 自检结果

- cargo build：✅ 零 error
- cargo clippy --all-targets：✅ 零 warning（含 7 处存量修复）
- cargo fmt --check：✅ 通过
- cargo test：✅ 1394 passed / 0 failed
- maturin develop --release + pytest 全量：✅ **1014 passed / 20 skipped / 0 failed**
  （910 基线 + 104 矩阵；IEC104 round-trip 不回归 ✓）
- 性能判据：✅ 判据 A（B1/B2 全点 <10%）+ 判据 B（B4-build +1.8% <5%）
- 接口对照设计文档：✅ ValueKind 枚举/分类规则/resolve 语义/init_default 表/
  B4 拒绝/错误类型与 §5.1-§5.6 一致
- 编码红线：✅ 无 unwrap/expect 于非测试代码（命中均在 cfg(test)）；无 TODO/
  FIXME；新 pub 有 ///（BuildValueMissing/FieldValueMissingError 等）；错误带
  field+path；parse/build 对称（矩阵 I/F 族 round-trip 锁定）
- §0#10 复扫：✅ 新增代码零任务号/日期/环境信息/construct 对照/this 字样；
  测试文件按语义命名（旧命名文件已删除）

## 9. 需 PM 注意

1. 前次会话将框架主体混入 f76b49c chore 提交（提交信息与内容不符）——commit
   历史管理请 PM 评估处置
2. F2 的发布说明补行（v0.1.2-5）与 §9.1 清单增补（RO 面放宽）请 PM 转发
3. 本次 commit 由 PM 执行（DEV 按 PM 指令不做 git 操作）；改动文件清单见
   `git status`（Rust 6 文件 + Python 3 文件 + 测试 4 文件删除 1）
