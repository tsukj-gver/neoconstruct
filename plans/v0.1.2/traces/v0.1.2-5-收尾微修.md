---
id: TRACE-v0.1.2.5
phase: v0.1.2
task: "v0.1.2-5 [框架收尾微修]"
status: coding
owners: [DEV]
started: 2026-09-04
completed: 2026-09-04
manifest_refs: []
---

## 操作日志

- [2026-09-04] [DEV] 读设计 v2.3 回填节（3 项裁决）+ VET 审查 §5/§6 → 定位 5 项改动 + 伴随测试面。
- [2026-09-04] [DEV] 完成全部 5 项修改 + 伴随测试更新（4 处），自检全绿。

## 修改明细（对照设计 v2.3 回填）

### 1. 延迟桩回退统一 Optional（裁决 1 → §5.3 补）
- `_mixin.py` `_fallback_default`：删除 expr_programs/idx 消费，恒返回 `(1, None)`；
  回退调用改为 `[_fallback_default() for _ in range(len(descriptors))]`。
- `_apply_dataclass_field_config` 删除不再消费的 `expr_programs` 形参（内部函数，
  调用点 3 处同步：延迟桩路径/正常路径/重试路径——后两者本就不传）。
- docstring 回退路径段与延迟桩安装处注释同步为"统一 Optional，缺值错误后移
  build 值使用点"。

### 2. Instance getattr 错误收敛（裁决 2 → §5.2 补）
- `struct_node.rs` Instance 分支：AttributeError ≡ 缺值 → 与 None 同路径报
  `BuildValueMissing`（复用原 None 构造，含 push_path_segment）；其余 Err 保持
  Generic 包装（消息形态与 Const/Default 分支统一为 "reading attribute ...
  failed for build"）。
- `error.rs` BuildValueMissing 文档注释同步补充 AttributeError 触发路径。

### 3. Control 分支同判据（裁决 2 → §5.2 补）
- `struct_node.rs` Control 分支：`Err(_)` 收敛为 AttributeError → 哑值 None；
  其余 Err → Generic 包装（与 Const/Default/Instance 单源同判据）。
- sequence.rs get_item（IndexError 位置语义）按裁决明确不动。

### 4. VET minor 1（_errors.py 一致性）
- 模块 docstring Python↔Rust 映射表补 `FieldValueMissingError` ↔
  `ConstructError::BuildValueMissing` 行；`__all__` 同步（29 项，
  位置随类定义序，GenericConstructError 之后）。

### 5. VET minor 2（compute_ro_value 残留清理）
- `_mixin.py` TellDescriptor / ComputedDescriptor docstring、`_descriptors.py`
  Sequence docstring 共 3 处：`compute_ro_value` 表述 → "字段值 resolve"。

## 伴随测试更新（4 处，均注明裁决出处）

| 文件 | 测试 | 变更 |
|------|------|------|
| struct_node.rs | `build_missing_field_returns_generic_error_with_field_name` | 改名 `..._missing_value_error_...`，期望 Generic→BuildValueMissing（断言 field=="b"、path=="root.b"），裁决 2 |
| struct_node.rs | `ro_instance_field_without_attribute_reports_missing` | 期望 Generic→BuildValueMissing（断言 field=="v"、path 含 "v"），裁决 2 |
| test_field_types.py | `test_rw_without_default_becomes_required` / `test_wo_without_default_becomes_required` | 改名 `..._fallback_optional`，断言 kw_only=True + default=None（走 schema=None 回退路径），裁决 1 |
| test_field_types.py | `test_mixed_modes` 字段 a | 断言 MISSING→None + kw_only=True，裁决 1 |

失败清单核对：cargo test 首轮仅 `build_missing_field_returns_generic_error_with_field_name`
1 例失败（即裁决 2 预期变化），更新后全绿，无其他遗漏。

## 自检结果

- cargo fmt --check：✅
- cargo build：✅ 零 warning
- cargo clippy --all-targets：✅ 零 warning
- cargo test：✅ 1394 passed / 0 failed
- maturin develop --release：✅（cp313）
- pytest 全量：✅ 1014 passed / 20 skipped / 0 failed（39s，与 VET 基线一致；
  矩阵+W 族 147 例单独复跑通过——W 族断言不变、I5 不回归）
- 语义抽验（.venv）：裸对象（非 dataclass）缺属性 → FieldValueMissingError
  （裁决 2 Python 面生效）；显式传值 build 正常（b"\x01\x02"）；`__all__`
  含 FieldValueMissingError（import 验证）。
- 复扫：改动新增行零任务号/批次/日期/core.py 引用/原版对照/this 字样 ✅
  （文件头 +++ 路径含 "neoconstruct" 为项目名本身，非对照）。
- 性能门禁：本任务为错误面/注释/回退微修，不触碰热路径（build 主循环仅错误
  分支判据变化，成功路径零增语句）——按总纲微修类任务不要求跑 benchmark。

## 备注

1. 生产代码注释不引用设计版本号（§0#10 保守口径），仅伴随测试按 PM 要求注明
   裁决出处（"设计 v2.3 回填 §5.2/§5.3"）。
2. 残留待 PM 排期（非本任务清单）：`bench/bench_adapters_struct_streams.py:218`
   注释仍引用已删除的 compute_ro_value（VET 问题 2 未列出的同类存量）；
   `test_v0_1_2_regressions.py` 中 compute_ro_value 为"v0.1.2 前报错"历史叙述，
   建议保留。
3. 工作区 `docs/design/基础设施/v0.1.2-字段值语义框架.md` 的 +6 行为 ARCH
   v2.3 回填改动（未提交），非本任务产物，未触碰。
