---
id: TRACE-v0.1.2.6
phase: v0.1.2
task: "v0.1.2-6 [0.1.1 后变更合规梳理]"
status: coding
owners: [DEV]
started: 2026-09-04
completed: 2026-09-04
manifest_refs: []
---

## 操作日志

- [2026-09-04] [DEV] 接手中断分派：核实工作区状态——前次已完成改名（旧文件已删、
  新文件未跟踪、test_field_value 已部分清理、test_value_provider 清理到一半）。
- [2026-09-04] [DEV] 界定终扫范围：git diff v0.1.1 变更的 neoconstruct/ 下
  .py/.rs + 点名遗留 2 处；存量（非变更引入）逐类归类不清（见 §4）。
- [2026-09-04] [DEV] 完成剩余全部清理 + 引用同步，自检全绿。

## 1. 改名映射（前次完成，本次核实+收尾）

| 旧文件 | 新文件 | 语义 |
|--------|--------|------|
| tests/integration/test_v0_1_1_regressions.py | tests/integration/test_field_value_regressions.py | 用户报障场景回归集（嵌套表达式 key + 值提供型 default + 行为/负例锁定） |
| tests/integration/test_v0_1_2_regressions.py | tests/integration/test_value_provider_regressions.py | 值提供型止血回归集（Const/Default 实例化值、rfield、算术表达式嵌套、wfield） |

命名沿用前次分派的选择（语义命名合规、与新矩阵文件引用一致，不再反复改名）。

## 2. 本次清理明细

### 2.1 test_field_value_regressions.py（30 处）
- BUG-1/BUG-2 标记全量中性化：模块 docstring、3 个分节注释、2 个类名
  （TestBug1PrefixedSwitch→TestPrefixedSwitchNestedKey、
  TestBug2ValueProviderDefaults→TestValueProviderImplicitDefaults）、27 个用例
  docstring 前缀。旧类名无源码外部引用（仅 plans/ 历史档案，不改）。

### 2.2 test_value_provider_regressions.py（31 处）
- B1/B1f/B2/B3 标记 21 处：类名 3 个（→TestRfieldConstExpression/
  TestRfieldPadding/TestArithmeticExprNestedInWrapper/TestWfieldExprLength，
  实为 4 个含 B3）+ 分节注释 2 + 用例 docstring 前缀。
- construct 对照 6 处："原版 D3/D5/P1/P2/E3/E4 基线"、"原版 construct
  2.10.70 基线（anchor E3/E4）" → 行为语义锁定值表述。
- 任务号/版本叙事 4 处："v0.1.2 前报 …"×2、"v0.1.1-3 修复前同症状"、
  "留给 v0.1.2-1 值语义规格" → 删除或改"属当前已知边界"。
- 环境引用 4 处："experiments/v0.1.2-probe/matrix_b2.py" → 指向
  test_value_semantics_matrix.py；"spike B2c/B3c/B3d"×3 → 删。
- 过时机制名 3 处：compute_ro_value → 值 resolve / 删。

### 2.3 变更行引入的违规（其余文件）
- _mixin.py（2 处）：注释与 CompilationError 文案"将于 v0.1.3 提供机制" →
  "暂不支持，需通过外层字段引用传值"（matrix 测试只 raises 不断言文案，安全）。
- _descriptors.py（1 处）：Sequence docstring"已知 parity 差异"段 →
  "RO 字段 build 输入约定"，删"Python 原版 list 需含占位 None"对照句
  （该段为本次变更行 compute_ro_value→ValueKind resolve 所在块）。
- test_field_types.py（3 处）：变更行注释"设计 v2.3 回填 §5.3"×3 → 行为描述。
- struct_node.rs（1 处）：文件头"Python 参考：construct/construct/core.py …"删（点名遗留）。
- bench_adapters_struct_streams.py（1 处）：TM1 注释 compute_ro_value（已废弃
  机制名）→ 值 resolve 表述（点名遗留）。

### 2.4 引用同步
- test_conditional_parity.py:15 旧文件名引用 → test_field_value_regressions.py。
- git grep 确认 neoconstruct/、testing/、.github/ 内旧文件名引用 = 0
  （plans/ 历史档案保留旧名，属历史事实）。

## 3. 自检

| 项 | 结果 |
|----|------|
| pytest collect-only | 1056 collected，0 error |
| test_field_value + test_value_provider + test_field_types | 123 passed |
| test_value_semantics_matrix（_mixin 文案改动联动） | 104 passed |
| test_conditional_parity（引用同步联动） | 27 passed |
| cargo check / clippy / fmt --check | 零 error / 零 warning / 通过 |
| cargo test | 1394 passed, 0 failed |
| 复扫整改文件三类模式 | 残留 = 0（存量归类见 §4） |

## 4. 复扫归类（合法保留 / 范围外存量，未改动）

**合法保留**：
- bench/ 内 construct 引用与测量点编号（B1/E3 等）——§0#9 性能基线豁免。
- tests/parity/test_adapter_parity.py 的 `this`——pc.this 原版 fixture 豁免。
- _mixin.py:889、compile.rs:2213/2220 的 `this`——英文代词。
- _errors.py "对应 Rust 的 ConstructError::X"——本项目 Rust↔Python 内部映射，
  非 construct 库引用。

**范围外存量**（非 0.1.1..HEAD 变更引入，建议另立全量清理任务）：
- construct 对照 docstring（"对应 Python construct 的 X"）：_descriptors.py
  ~100 处（该文件仅 1 行变更）、error.rs ~50 处、compile.rs 7 处、nodes 各文件
  若干（struct_node.rs L321/347/854 等）、checksum.rs 等——均为 §0#9 生效前存量。
- 版本叙述"v0.1.1 起"：compile.rs 11 处、_mixin.py 4 处、_descriptors.py 5 处、
  rebuild.rs 1 处（范围内文件存量行）；_conditional.py 5 处、
  test_expr_compile.py 2 处（范围外文件）。
- tests/parity 全套与 system tests 的 pc 对照断言：总纲 §6 已列为独立评估项。

## 5. 备注

- 并行任务（v0.1.2-7a）产物（docs/测试设计 + plans trace + 新测试文件）未触碰；
  .pytest_cache 权限警告为并行 pytest 占用，与本任务无关。
- git 状态：改名对（旧 D + 新 ??）+ 本次 8 文件修改，未 commit（按分派不做 git）。
