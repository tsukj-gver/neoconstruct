---
id: trace-v0.1.3-pre-release-compliance
task: v0.1.3-pre [发布前全量合规精读审查]
role: VET
date: 2026-09-08
verdict: RELEASE-HOLD
---

# v0.1.3-pre 发布合规审查报告

**审查范围**：`git diff v0.1.1 --name-only` 全部 138 个变更文件；源码侧（neoconstruct/ 68 文件 + opencode.json）逐文件精读变更部分，管理侧（plans/harness/docs/experiments）确认为非 §0#10 适用对象（豁免归类）。
**注意**：`.github/workflows` 本次无变更文件（任务预期与实际不符，已核实）。
**方法**：逐文件 `git diff` 精读新增行 + 必要上下文回读；关键词初筛仅作定位兜底，每处命中均人工判定。

## 一、逐文件结论表（源码侧）

| 文件 | 变更性质 | 结论 |
|------|---------|------|
| Cargo.toml | version 0.1.1→0.1.3 | ✅ |
| src/lib.rs | version() 改 env!(CARGO_PKG_VERSION) | ✅ 消除硬编码 |
| src/schema.rs | init_defaults 字段+_init_defaults | ✅ |
| src/error.rs | Sizeof/BuildValueMissing/ExprOverflow+classify_user_pyerr | ⚠️ 1 处 follow-up |
| src/expr.rs | wrapping→checked 溢出语义 | ✅ 逻辑正确 |
| src/compile.rs | ValueKind 接线+build_*抽函数+测试重排 | ✅ |
| src/nodes/struct_node.rs | ValueKind 枚举+build 循环重写 | ❌ 5 处编号/设计版本引用 |
| src/nodes/common.rs | capped_result_capacity+extract_bytes_like | ✅ 常量命名典范 |
| src/nodes/format_field.rs | describe() 可读错误消息 | ✅ |
| src/nodes/sequence.rs | ValueKind resolve+Conditional 位置对称 | ✅ |
| src/nodes/bytes.rs / greedy_bytes.rs | bytes-like 接受链 | ⚠️ greedy 1 处 construct 残留 |
| src/nodes/array.rs / prefixed_array.rs | capped 接线+防 abort 测试 | ✅ |
| src/nodes/index.rs | parse 非数组上下文→IndexFieldError | ✅ 与 _errors.py 同步 |
| src/nodes/flags_enum.rs | 移除 _flagsenum 内部键 | ✅ |
| src/nodes/probe.rs | path_label 注入 | ✅ |
| src/nodes/process_rotate_left.rs | group<1 防除零 panic | ❌ 1 处 PR-5 |
| src/nodes/{adapter_callback,aligned,checksum,computed,rebuild,tell,struct_ref,mod,named_tuple,greedy_range}.rs | 分类接线/错误分类学/文档同步 | ✅ |
| python/__init__.py | 版本同源+新异常导出 | ✅ |
| python/_descriptors.py | docstring 语义化 | ✅ |
| python/_errors.py | 精确映射 docstring 重写 | ✅（移除多处原版对照） |
| python/_macros.py | Timestamp fail-fast | ✅ |
| python/_mixin.py | init_defaults 消费+编译期防御 | ✅（1 处技术耦合见误报节） |
| tests/_helpers/parity.py | assert_loopback | ❌ 2 处"此前"叙述 |
| tests/integration/test_conditional_roundtrip_semantics.py | 全新 545 行 | ❌ A1-G5 编号体系 26 处 |
| tests/integration/test_value_semantics_matrix.py | 全新 1550 行 | ✅ 语义组织典范（2 类名待甄别） |
| tests/integration/ 其余 12 文件 | 全新语义测试 | ✅ 零命中 |
| tests/parity/test_array_parity.py | R1-R3 case 重定义 | ❌ "历史处理记录"段 |
| tests/parity/test_conditional_parity.py | loopback+改名引用同步 | ✅ |
| tests/unit/test_compile_rejections.py | 全新 216 行 | ❌ 3 处过程叙述 |
| tests/unit/ 其余 4 文件 | 新增 | ✅ |
| tests/errors/ 8 文件 | 全新 | ❌ hardening 2 处 / repeat_generic 1 处 |
| bench/bench_adapters_struct_streams.py | 注释同步 | ✅ |
| bench/results/bench_struct.{json,md} | 测量数据更新 | ⚠️ 元数据待甄别 |
| opencode.json | temp 目录权限 | ✅（配置必要） |

## 二、违规清单（待修）

### A. 用例族/裁决编号（§0#10，任务明令排查项）

| # | 文件:行 | 原文（摘要） | 判定 | 修法 |
|---|--------|-------------|------|------|
| A1 | tests/integration/test_conditional_roundtrip_semantics.py:67,79,91,107,119,130 | docstring 前缀 A1-A6 | 违规 | 删编号前缀（描述已语义化，删后无损） |
| A2 | 同上:219,229,239 | C1-C3 | 违规 | 同上 |
| A3 | 同上:268,282,294 | D1-D3 | 违规 | 同上 |
| A4 | 同上:321,335,347,360 | E1-E4 | 违规 | 同上 |
| A5 | 同上:386,397,409,422 | F1-F4（任务明令 F1/F2） | 违规 | 同上 |
| A6 | 同上:455,487,512,526,536 | G1-G5（任务明令 G1-G4） | 违规 | 同上 |
| A7 | 同上:381 | "Optional（R4）" 裁决编号 | 违规 | 删"（R4）" |
| A8 | 同上:55,147,207,256,309,374,442 | "族 A"~"族 G"分节 | 违规(轻) | 分节改语义名（"回环属性"等，删"族 X："） |
| A9 | src/nodes/struct_node.rs:1936 | "（实例化可省，R4）" | 违规 | 删"，R4" |
| A10 | src/nodes/struct_node.rs:1967 | "同语义（R3）" | 违规 | 删"（R3）" |
| A11 | src/nodes/struct_node.rs:1989 | "R1 分支自治" | 违规 | 删"R1 "，保"分支自治" |
| A12 | src/nodes/struct_node.rs:1077,1705 | "设计 v2.3 回填 §5.2"/"设计 v2.3 §5.2 补" | 违规 | 删设计文档版本/章节引用，保语义描述 |
| A13 | src/nodes/process_rotate_left.rs:319 | "PR-5 对称：…" | 违规（新增行） | 删"PR-5 对称："前缀 |

### B. 过程叙述/过程词汇（§0#10）

| # | 文件:行 | 原文（摘要） | 判定 | 修法 |
|---|--------|-------------|------|------|
| B1 | tests/parity/test_array_parity.py:536-543 | "**历史处理记录**：R1-R3 曾因…现已修复：R1/R3 改为…R2 重定义为…" | 违规（整段开发史叙述） | 删历史段，仅保留容错 fixture 现状理由 |
| B2 | tests/unit/test_compile_rejections.py:3 | "（此前均为静默错误输出/静默错误解析）" | 违规 | 删该括注 |
| B3 | 同上:6-7 | "按用户裁决（继承无空间信息…）" | 违规 | 改客观语义："字节排布有位置语义，继承无法表达基类字段占位" |
| B4 | 同上:191 | "此前该形态 parse 零字节消费、静默返回默认值。" | 违规 | 删该句 |
| B5 | tests/errors/test_untrusted_count_hardening.py:3-5 | "曾按 count 预分配…触发 34GB 级分配失败" | 违规(轻) | "曾按"改无时间指涉表述（描述未加固机理） |
| B6 | 同上:31-38 | "注入 CRS python 目录"+CRS_PYTHON_DIR/crs_python_dir 变量 | 违规 | CRS=construct-rs 旧名。改 NEOCONSTRUCT_PYTHON_DIR（本文件自有命名；helper 存量参数名见观察节） |
| B7 | tests/errors/test_repeat_generic_adapter_errors.py:248 | "可达性说明（[文档] _errors.py docstring + 实测探针）" | 违规（任务明令"探针"过程词） | 改"（依据 _errors.py docstring 与实测行为）" |
| B8 | tests/_helpers/parity.py 模板注释+assert_loopback docstring | "此前 parity harness 只 build 工厂构造的实例，从不 build 解析产物——本断言补上该方向" ×2 | 违规(轻) | 改现状描述（harness 两个方向各验证什么） |
| B9 | src/error.rs Sizeof 变体 docstring | "用户面 sizeof API 尚未提供（follow-up）" | 违规(轻) | 删"（follow-up）"或改"（该 API 提供时即接线此类）"，与 _errors.py 侧表述对齐 |

### C. §0#9 construct 独立性

| # | 文件:行 | 原文（摘要） | 判定 | 修法 |
|---|--------|-------------|------|------|
| C1 | src/nodes/greedy_bytes.rs:41-42 | docstring 重排行携带"（对应 Python construct `GreedyBytes._build` 的 stream_write(stream, data, len(data), path)，长度始终匹配）" | 违规(轻，存量语义在重排+行内保留) | 重排时顺手清除：改"不校验长度（长度始终匹配）" |

## 三、误报归类（命中但判定合规）

1. **B1-B4（bench_struct.md/json）**：性能场景 ID（perf-scenarios.csv 单一事实源的测量点标识，Phase 1 起既有命名体系），benchmark 领域豁免。
2. **R1/R2/R3（test_array_parity.py:514-516）**：parity 参数化 case ID（B/I/P/R 前缀=构造器语义+序号，v0.1.1 存量体系，功能性标识非注释叙述）。注：与 A 组新增 docstring 编号性质不同（存量基础设施 vs 新写过程标注）。
3. **class B2/B4（test_roundtrip.py:181-221）**：B2=Bytes(2)、B4=Bytes(4)，后缀即长度，语义命名。
4. **"探针"×7（display_semantics 等）**：Probe 构造器（用户面产品功能）的中文语义，非开发探针。
5. **[基线]/[自然]/[文档] 标签**：期望值来源方法论标注（vetter-extension 认可"语义行为基线"），非任务过程信息。
6. **"v0.1.1 起"×5**（_descriptors.py/_mixin.py/rebuild.rs/test_expr_compile.py[存量]）：用户可感知变更标注，PM 既定裁决合规保留。
7. **venv_pair / crs_python_dir（_helpers/parity.py 存量接口参数）**：v0.1.1 已有测试基础设施 API，新文件按接口调用（B6 仅针对新文件自有命名与注释）。
8. **FE-1（flags_enum.rs:262）/ PR-5:304 / PR-7:340**：存量行（非本次 diff 新增），范围外。
9. **_mixin.py `module == "construct"`**：检测存量 pyclass `__module__` 值的技术必要耦合（见观察 O1），非原版对照表述。
10. **opencode.json `~/AppData/Local/Temp/opencode/**`**：opencode 官方约定的临时目录权限白名单（通配模式非机器具体路径），不打包进发布产物。
11. **error.rs "对应 Python 的 SizeofError"等**：指本包 Python 侧异常类映射（错误分类学接线），非原版 construct。
12. **expr.rs "Python `//` 语义/`a << b` 抛 ValueError"**：CPython 语言标准语义参照，非 construct 库。

## 四、新代码硬编码专节（§3 红线）

| 位置 | 数值/文案 | 判定 |
|------|----------|------|
| common.rs `DYNAMIC_ELEMENT_INITIAL_CAPACITY = 16` | **合规典范**：命名常量+完整文档（保守下界/摊还增长论证）+4 个测试锁定 |
| format_field.rs describe() 表（22 项） | 合规：用户面错误消息文案映射（自描述字符串，非魔法数字），pub 有 ///，测试锁定 |
| schema.rs `_init_defaults` (0,1,2) | 合规：InitDefault 枚举在 Rust/Python 两侧均有文档枚举说明，消费端注释对应 |
| __init__.py 回退 "0.0.0" | 合规：占位版本号+注释论证（扩展未构建场景） |
| lib.rs `env!("CARGO_PKG_VERSION")` | 合规：消除双处硬编码（正向改进） |
| expr.rs 移位边界 `i64::BITS` | 合规：类型常量非魔法数字，文档说明 0..63 语义 |

**结论：无裸魔法数字违规。** 重点抽查的 capped 分配（array/prefixed_array 接线）、Conditional 臂（struct_node/sequence 的 None 透传/位置对称）、bytes_like 提取（bytes 零拷贝/bytearray/memoryview 拷贝语义）逻辑均正确且 parse/build 对称。

## 五、边缘待 PM 甄别（不阻塞，建议表态）

1. **bench_struct.{json,md} 测量元数据**：`Timestamp: 2026-09-04`+`Python: 3.13.15`+`Platform: Windows-11`——bench 框架自动生成的测量环境记录（既有格式更新，性能数据必需元数据）。倾向豁免；若按 §0#10 字面零容忍，可改 .gitignore 生成产物。
2. **_errors.py SizeofError "后续版本引入时"/error.rs "尚未提供"**：API 缺失的路线图说明（无具体版本号）。倾向合规（B9 仅修 "follow-up" 措辞）。
3. **test_value_semantics_matrix.py 局部类名 C2/P1**：矩阵内用例序号形态类名（比 GS2/GN2 等首字母+序号惯例略机械）。倾向可接受（局部作用域，不影响用户）。

## 六、范围外观察（存量，建议后续任务）

- **O1**：`src/descriptors/mod.rs:37,94,143,181` pyclass `module = "construct"`（4 处，v0.1.1 存量）——repr 显示 `construct.XxxDescriptor`，与 §0#9 独立定位不符，且迫使 `_mixin.py` 新增检测耦合 `"construct"` 字符串。建议后续统一改 `module = "neoconstruct"`（连动 _mixin.py 检测与 repr 相关测试）。
- **O2**：`src/compile.rs` build_union_node `parsefrom` 负数 `idx as usize`（v0.1.1 已有，未变更）——建议后续补编译期负数拒绝。
- **O3**：test_errors.py:180-184 等存量"对齐 Python construct"注释（本次未触碰区域）——存量清理由 PM 决定是否立项。

## 七、逻辑/错误处理/资源安全审查结论

- **checked 溢出语义**（expr.rs）：Add/Sub/Mul/Neg/FloorDiv(i64::MIN/-1)/Shl(移位量+值域)/Shr 实现正确；`-1<<63==i64::MIN` 合法边界有测试锁定；Mod i64::MIN%-1=0 合法不报错 ✅
- **ValueKind 体系**（struct_node/sequence/compile/schema/_mixin 五层接线一致；AttributeError≡缺值单源收敛；expr_derived init 可省+build 报错的"错误时机=值使用点"自洽）✅
- **防 abort 加固**（capped_result_capacity：min(count, remaining/size) 与动态退化 min(count,16)；子进程隔离测试矩阵）✅
- **非测试代码 unwrap/expect/panic/TODO/unsafe 零命中** ✅
- **新 pub 项均有 /// 文档** ✅；pyclass/pymethod 同

## 八、Verdict

**RELEASE-HOLD**

待修清单：A1-A13（13 项，集中在 2 个文件）+ B1-B9（9 项，8 个文件）+ C1（1 项）= **23 处，全部为注释/docstring/变量命名级修改，零逻辑变更**。预计 1 个 DEV 子任务可完成（含测试运行验证）。

修复优先级：A5/A6（任务明令的 F/G 编号）> B1（历史叙述段）> B6（旧名 CRS）> 其余编号与措辞 > C1。

修复合并后需 VET 复审（仅复审修改点+确认无新引入）。
