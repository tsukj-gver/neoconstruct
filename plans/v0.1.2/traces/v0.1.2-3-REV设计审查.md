---
id: TRACE-v0.1.2-3-REV
task: v0.1.2-3 [字段值语义框架设计审查]
role: REV
date: 2026-09-04
target: docs/design/基础设施/v0.1.2-字段值语义框架.md（实际 309 行，非分派所述 258 行）
verdict: FAIL（驳回至 DESIGNING，核心机制认可，补充后重审成本低）
---

# v0.1.2-3 REV 设计审查报告

## 0. 审查执行记录（独立验证）

| 验证项 | 方法 | 结果 |
|--------|------|------|
| 锚点抽查 3 条 | 复跑 `anchor_construct.py`（.venv-pc） | C7 Const 显式 None→用 value=b'\x05'；P1 Padding parse 存 None；R3 Rebuild 显式值被忽略重算——3/3 与设计 §2 表吻合 |
| flagbuildnone 全集 | 只读 grep 原版 core.py（15 处 `self.flagbuildnone = True`） | 见问题 P2/P4 |
| 五处特判位置 | grep 源码 | 5/5 实锚：`_mixin.py:1046` value_providing；`struct_node.rs:453-506` FieldMode 分支（B1e 位置=Line 470 ctx 写原始值）；`mod.rs:535` compute_ro_value；`const_node.rs:118`/`default_node.rs:100` 节点 None 特判；`_mixin.py:183,198` _context_injections 存而无消费（grep 全库仅 tests 引用，"无消费"属实） |
| spike 复跑 | `bench_ab.py`（.venv） | 机制验证 4/4（v0 ctx['c']=None 缺陷形态 / v1 ctx['c']=5 有效值回写 / v1 None→ValueError / 字节一致）；A/B 3 轮 Δ=-3.48%/-0.50%/-2.51%，总中位 **-1.82%** |
| 现状对照 | 复跑 `current_neoconstruct.py` | B1/B1e/B1f/B3 已被止血修复转绿（见 P3）；B4 静默忽略仍成立 |

## 1. 分项结论表

| 检查项 | 结论 | 依据摘要 |
|--------|------|----------|
| 延续性 | ⚠️ 有问题 | 五处特判全部有取代路径 ✓；但 compute_ro_value 实际 **11** 分支（止血已加 Const:588/Padding:592），设计写"10 分支"且 §5.2 迁移映射表**缺 Const→Const、Padding→Void 两行**（§5.1 分类规则本身覆盖）；切换点一处（Python 消费端）成立 ✓；§3 现状表 4 行过时（P3） |
| §0 合规 | ✅ 通过 | 对照表 8 条齐备（extension 硬要求）；ValueKind 为编译期封闭 enum+静态 match，无 Box<dyn>，不跨 FFI；resolve 产出即 Bound PyObject 无中间表示；init_defaults 走既有编译入口（冷路径一次）；FieldValueMissingError 属追加（§6 表 5 行明示），B4 拒绝用既有 CompilationError；owned-Py 形态被实测否决且 §7 明令禁止。小点：§5.2 伪码 `if has_expressions` 未体现"WO 不写 ctx"（正文有说明，DEV 需对齐） |
| 性能声明 | ✅ 通过（附 2 notes） | 方法论合规 L-09（同会话交替 2 会话×3 轮×9 批×20000 次）；复跑 -1.82% 与设计 +2.52%/+0.84% 方向相反但幅度同在 ±4% 内——互证"噪声级无损"成立且更强。N1：owned 形态 +6.7~9.6% 的对比代码不在当前 spike 仓库（README 声称保留，lib.rs 仅就地形态+注释），否决结论方向可信但不可复核；N2：主基线 B4-build 3 轮 3389/4357/3922（自身 ±28%），"<10% 回退"门禁在该点会随机误报，DEV 验收应对该点用同会话 A/B（L-09）或单独定阈 |
| 整体性 | ❌ 问题 | Rust enum 穷尽 match 无 else 兜底 ✓（真正的结构改善）；"其余→Instance"兜底方向安全 ✓；节点层 None 特判保留有正当理由（非字段语境）✓。**但 Seek/Peek/Checksum 分类缺口（P1）** |
| 可行性 | ✅ 通过 | 改动面 6 文件与实锚一致；`expr_programs[idx]` 结构（`_mixin.py:795-816`）支持 expr_derived 判定；`ops==[Const(c)]` 折叠判定可行（`default_node.rs:61`"编译期常量也包装为 ExprProgram"证实 Default 单一程序形态自洽）；pyo3 0.22 spike 同版本已验证。小点：Sequence 的 Instance 分支取值是 list 位置而非 getattr，§5.2"同模式"未说明该差异 |
| 测试遗漏 | ⚠️ 有问题 | 76 用例期望锚定原版 ✓（抽查吻合，F5/F9 输入输出与 E3/E7 实测一致）、红灯先行 ✓。**缺 B2 数据负例（P5）**；Bytes 显式 None（原版 TypeError vs 框架 BuildValueMissing 合并）无锁定用例；Seek/Peek/Checksum 零用例（P1 关联） |
| 完备性 | ❌ 问题 | B4 拒绝位置明确（_mixin.py 编译期 3 行，PM 已批）✓；v0.1.3 预留只述机制不越权 ✓；边界 9 条合理 ✓。**P1 分类缺口 + P5 矩阵缺口** |

## 2. 问题列表

### 必修（驳回依据）

**P1【设计缺陷·中】Seek/Peek/Checksum 分类缺口——"未来 B5"**
- 位置：§5.1 分类规则（Line 126-130）
- 事实链：①原版三者 flagbuildnone=True（源码实证 core.py:4481/4595/5553，设计 §2 Line 54 全集声明自己也列了）；②设计分类规则未提及 → 无声落 `Instance{expr_derived:false}` → §5.3 init_required 必填哑值；③`field(Peek(Int8ub))`（test_iec104:180,469）、`field(Checksum(...))`（test_modbus_rtu:513）是**已交付用户面真实形态**；④哑值必填正是 B3 同型缺陷在别的构造器上延续。现状 `value_providing` 同样不含三者（框架不引入回归），但"单源分类消灭特判"的完整性声明不成立——分类表内部不自洽（§2 列出全集，§5.1 却无声跳过三者）。
- 建议：ARCH 补显式分类决策。方向参考：Peek/Seek→Control 类（resolve 产 None、节点 build 自理：Peek no-op / Seek 移动流指针）或文档化"有意差异+哑值约定"；Checksum 需结合其 start/end 表达式求值语义单独决策（注意 §5.6 v0.1.3 注入与 Checksum 的 start/end 引用关系）。同时矩阵补 3 者用例锁定。

**P5【测试遗漏·中】B2 数据负例未入矩阵**
- 位置：§8（76 用例全表）
- 事实：总纲 §5 PM 决策记录明确"数据-schema 不匹配的显式 StreamError……数据负例将入矩阵锁定"（B2 定性修正的交付承诺），§8 F 族全部为正例（parse 正常数据/round-trip），无"长度字节与内层需求不匹配 → 显式报错"负例。缺此用例则 B2 修复语义（显式报错而非静默）无回归防线。
- 建议：F 族追加 2-4 例数据负例（如 `Prefixed(Int8ub, Bytes(x+1))` 长度字节声明与 payload 实际不符 → StreamError，期望锚定 .venv-pc 实测）。

### 随修订处理（不单独构成驳回，但 P3/P4 应在重提交版修正）

**P3【文档时效·低】§3 现状表 4 行过时**
- B1（C2().c→None，实测现=5）、B1e（FAIL，实测现 b'\x01ab' OK）、B1f（FAIL"无 Const 分支"，实测现 OK——止血已在 mod.rs:588 补 Const 分支）、B3（"__init__ 即 missing"，实测 W(x=1) 可实例化、错误已后移到 build）均已被 v0.1.2-2 止血修复。仅 B2 行标注了"当前 venv 已由并行止血修复"。会误导 DEV 以为仍是红灯。建议各行补止血状态标注。

**P4【文档精度·低】§2 全集声明非系统枚举**
- 声称"flagbuildnone=True 的类全集＝{Check,Checksum,Computed,Const,Default,Peek,Rebuild,RestreamData,Seek,StopIf}＋{Padding,Tell,Pass}"。源码 grep 实际 15 处（含 anchor 脚本未实测的）：声明**漏 Index、Terminated、Error**。框架/现状均覆盖 Index（ValueKind::Index）/Terminated（Control），Error 构造器未实现。且该全集无 spike 脚本证据（anchor2 仅实测 10 个），与文档开头"所有原版行为均来自 .venv-pc 实测"的声明不符。建议改为源码枚举结果并区分"脚本实测/源码 grep"。

**P2【文档完备·低】compute_ro_value 迁移映射表不完整**
- §5.2 审计表写"全部 10 分支"，实际 11 分支；映射列举缺 `Const→Const`、`Padding→Void`。分类规则（§5.1）已覆盖故无架构影响，但迁移清单是 DEV 实施依据，应补全防漏迁。

**P6【证据链·低】owned-Py 对比代码缺失**
- README/设计 §7 称"spike 仓库保留了两种形态的对比作为 DEV 实施警示"，lib.rs 实际只有就地 Bound 形态+注释，owned 形态 +6.7~9.6% 数字不可复核。建议补 owned 形态函数（或修正 README/设计表述为"数据已测、代码未保留"）。

**P7【验收判据·低】B4-build 点门禁失配**
- §7 主基线 B4-build 3 轮 3389/4357/3922ns（自身波动 ±28%），"各点中位回退 <10%"在该点会随机误报。建议对该点（及其他 >10% 自身波动的点）改用同会话 Controlled A/B（L-09 判据）或注明豁免。

**P8【小点汇总】**
- §5.2 伪码未体现 WO 不写 ctx（正文有述，伪码与文字不一致）；
- anchor_construct.py L1 Tell 用例自身 bug（`Tell()` 单例被调用 → 永久 ERR；anchor2 已修正，链路完整）；
- Sequence Instance 取值（list 位置 vs getattr）未在 §5.2/§11 说明；
- 非 i64 默认值（Default(bytes/str)）延续 ADR-006 i64 VM 限制，矩阵/文档未记录该边界；
- Bytes 显式 None：原版 TypeError、框架合并为 BuildValueMissing，行为差异未在矩阵/差异文档声明；
- depends_on 缺 ADR-005（default 自动 kw_only，§5.3 直接相关）。

## 3. verdict

**FAIL —— 驳回至 DESIGNING**（revise & resubmit，非方向性推翻）

- **认可项**：候选 2 框架方向正确——机制验证 4/4 成立，性能噪声级无损（复跑互证），锚点 31+ 扎实可复现，五处特判取代路径完整，§0 对照齐备合规。ARCH **无需重做方案探索/spike**。
- **驳回依据**：P1（分类覆盖缺口，用户面已暴露形态，直接违反"单源分类消灭特判"的设计目标）+ P5（PM 已批交付范围内的用例缺失）均为完备性缺口，且 P1 的补决策可能改变 ValueKind 枚举形态与矩阵内容——必须在 DEV 开工前闭合，否则 DEV 面临枚举/矩阵返工。
- **重提交要求**：修订设计文档处理 P1/P2/P3/P4（+P7 判据修正），矩阵补 P5 负例与 P1 关联用例后重新提交 REV 快速复审（重点仅查修订增量）。

**驳回目标状态**：DESIGNING（AGENTS.md §1）

## 复审（v2）——v0.1.2-3r 快速复审（仅查修订增量）

**检视文档**：`docs/design/基础设施/v0.1.2-字段值语义框架.md` v2（2026-09-04）
**复审日期**：2026-09-04 · **verdict：PASS（附 1 项 DEV 开工前置条件，见 N1）**

### r0. 复审执行记录（独立验证）

| 验证项 | 方法 | 结果 |
|--------|------|------|
| AST 对账复跑 | `audit_flagbuildnone.py` 双 venv（.venv-pc + .venv） | 均 exports=100/leaves=15/propagators=7/**PASS**；结果文件 git 追踪且复跑后无 diff（确定性输出，无漂移） |
| 源码行号抽验 | 读 core.py 原文 | Seek:4595 flagbuildnone / 4602-4605 `_build` 不用 obj 只 evaluate+stream_seek 返回新位置 ✓；Checksum:5553 / 5567-5570 不用 obj、hashfunc(bytesfunc(ctx)) 重算写入 ✓；Peek:4481 / 4494 `_build(obj) return obj` no-op ✓ |
| 传播类抽查 2/7 | 读 core.py 原文 | Subconstruct:795 透传 `=subcon.flagbuildnone` ✓；IfThenElse:3964 `then and else` ✓ |
| 锚点全量复跑 | `anchor3.py`（.venv-pc）15 条 | 15/15 与设计 §2/§8 期望吻合：S1=b'ab\x00\t'、S2 s=3、C1/C2=b'\x01\x02\x81\xe1'、C3 ChecksumError、A1/A2 b'\x01'、A3 TypeError、A4 p=5、W1 b'\x01\x00\x00'、W2 KeyError: 'b'、N1 "expected 5, found 3" 逐字匹配 |
| P2 迁移映射 | 读 mod.rs:535-604 原文 | 实际 11 分支（Tell/Computed/Index/StopIf/Element/Rebuild/Check/Terminated/Default/Const:588/Padding:592），错误消息名单交叉印证；与 §5.2 映射逐项一致（含 v2 补的 Const→Const、Padding→Void） |
| IEC104 形态查证 | 读 test_iec104.py:150-191/455-530 | `cf1_peek(Peek) → Computed((cf1_peek&1)*…) → Switch(frame_type)` 链成立（见 N1） |

### r1. P1-P8 增量核验表

| 项 | 结论 | 依据摘要 |
|----|------|----------|
| P1 | ✅ 闭合（附 N1/N3） | 等价判定（"build 忽略实例值→哑值可省"）经源码抽验成立：Seek/Checksum `_build` 完全不读 obj；非 Instance ⟺ flagbuildnone=True 由 AST 对账 100% 验证（13 已实现 + Element 扩展 + 2 未实现预留 Control §5.1b）。Control 不新增枚举变体三理由自洽（Checksum digest=双函数组合 [5567-5575] 无法用单一 i64 ExprProgram 承载，实读确认）。传播 7 类抽查 2/7 准确。I/J/K 族红灯先行 + B5 编号 + §9 关系表齐备 |
| P2 | ✅ 闭合 | 11 分支与 mod.rs 逐项一致（r0 表）；"函数删除"目标与 §11 改动面一致 |
| P3 | ✅ 闭合 | §3 现状表 8 行与上轮 REV 实测吻合（B1=5/B1e b'\x01ab'/B1f mod.rs:588 实读确认/B3 可实例化/B2 止血+N 族锁定/B4 未修/B5 新行）；止血版本号（v0.1.2-2/-4）标注明确 |
| P4 | ✅ 闭合 | 全集改为 AST 脚本对账（15 叶类行号 + 7 传播 + 基类 :360 False），[实测]/[AST] 证据分级标注；Padding "透传语义"修正 v1 不准确表述；100 vs inventory 101 口径说明自洽 |
| P5 | ✅ 闭合 | N1-N3 StreamError 实测锚定（错误消息逐字）；I/J/K/W 20 例期望全部锚定 anchor3；红灯先行区分"B5 FAIL 预期 / N 族应 PASS（FAIL 即回归）"；D7 维度入表 |
| P6 | ✅ 闭合 | §7 改口"数据已测、代码未保留"，与 lib.rs 实况一致（上轮已实锚） |
| P7 | ✅ 闭合（附 N2/N4） | A/B 分级 + 同会话配对 Δ 中位判据方向正确：配对差消除公共模式漂移（spike 实测配对 Δ 均 <±4% 佐证），5%≈195ns 远大于 ns 级预期增量、远小于自身波动；等价替代（7 轮中位）鲁棒；要求 DEV 标注各点判据 |
| P8 | ✅ 闭合 | WO 伪码对齐（编译期保证说明）、Sequence list 位置取值注、非 i64 边界 §10.10、Bytes 显式 None §10.11、包装器差异 §10.12、ADR-005 入 depends_on——逐条落实 |
| §12 | ✅ 完整 | 5 项证据位置全部实际存在且内容与索引描述匹配 |

### r2. 新发现问题

**N1【矩阵用例矛盾·中低】I5 期望与 §5.1d 决策冲突（DEV 开工前置条件）**
- 位置：§8 I5 行 vs §5.1d/§10
- 事实链：①IEC104 真实形态（test_iec104:180-191）= `cf1_peek(Peek) → Computed(引用 cf1_peek) → Switch(引用 frame_type)`——正是 §5.1d 声明的差异影响面（"后继表达式引用 peek 显式值"）；②框架 Control build 时忽略实例值→ctx 写 None→Computed 求值 None 必失败，I5"round-trip"期望无法成立（原版显式值透传 ctx 故成立，框架 §5.1d 已决策接受差异）；③§5.1d 明言"矩阵不设该形态用例"，I5 恰设了该形态且期望未锚定；④I5 与 I6（同形态、期望报错）互斥。parse 方向不受影响（P 点存有效值，IEC104 parse-only 用例安全）。
- 处置：**不构成驳回**（架构/枚举/其余 95 例健全；§5.1d 决策本身正确且有 K5 正面示范——start/end 引用 Tell 正是推荐替代路径；性质同上轮 P2"DEV 实施依据单点缺陷→随修订处理"先例）。**要求：PM 转发 ARCH 对 I5 出一句话澄清后 DEV 方可开工**——方向：I5 改为"IEC104 形态 parse 全链 + build 腿按 §5.1d 锁定显式报错"，或 Switch 条件改引用独立字段使 round-trip 成立。该澄清不触发全案重审。

**N2【数字口径·低】** §7"极差/中位=28%"实为 **24.7%**（968/3922；28.6% 是极差/最小值口径）。>20% 分级判定不受影响，ARCH/DEV 统一口径即可（v1 REV 报告"±28%"同源偏差，非 v2 新引入）。

**N3【措辞精度·低】** §5.1 等价判定括号把 Peek 列入"完全忽略"——Peek `_build` return obj（透传传入值）：字节面 no-op 成立、ctx 面回写传入值（显式时非 None），实为"透传"型。§2 机制 3 与 §5.1d 已精确记录且框架行为（缺省 None）与原版等价，仅括号内措辞不精确。同类：总括句"不依赖 obj 的语义值"对 Const/Default 显式值路径不严格（显式值参与校验/使用），已由"哑信号"二分覆盖。文档微调，不阻塞。

**N4【判据增强建议·低】** 判据 A 对自身波动 15-20% 的边界点仍有误报可能（10% 门禁 vs 最高 20% 噪声）。建议 DEV 执行时：判据 A 单点失败且该点波动 >15% → 以判据 B 复核再定论。实施指南级补充，不改设计。

### r3. verdict

**PASS —— 进入 DEV 实现（附前置条件 N1：I5 期望澄清，PM 转发 ARCH 一句话修订，无需全案重审）**

- v2 修订质量：P1-P8 全部闭合且证据链升级（手工 grep → AST 脚本对账；抽验锚点 → 15 条全量复跑吻合）。上轮驳回依据（分类缺口/矩阵缺口）已消除，无新增架构级缺陷。
- N2-N4 随 ARCH 澄清 N1 时顺带处理或 DEV 实施时注意，均不阻塞开工。
