# Phase 13 总纲：FFI 执行层重写（桩）

> **阶段目标**：重写 FFI 执行层，实现 direct-to-Python + 惰性 Context + PyCallback 统一封装。
>
> **状态**：待 Phase 12 验收后细化。

## 预估子任务方向

- 13.x CompiledSchema::parse_bytes direct-to-Python（PyDict）
- 13.x Input trait：build 方向按需读 PyObject
- 13.x PyContextView 惰性映射（Mapping 协议）
- 13.x PyCallback 节点统一封装
- 13.x construct-py FFI 层重写（conversions/expr_bridge/py_adapter/api）

## REV 重要问题（本阶段必须解决）

- #3 PyContextView FFI 开销量化
- #7 Lazy 不兼容 direct-to-PyDict
- #9 错误路径 PyDict 处理语义

## 出口标准

（待 Phase 12 完成后细化）
