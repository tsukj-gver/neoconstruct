# Phase 14 总纲：dataclass-first API

> **阶段目标**：实现 ConstructMixin + cs_field + direct-to-dataclass API，让用户用 @dataclass + cs_field 定义二进制格式。
>
> **设计文档**：`docs/refactor/模块设计-dataclass-API.md` v2
>
> **状态**：DESIGN_REVIEW 有条件通过（14.1 可进入 CODING，14.2 需修正 N-1）

## REV 重要问题（本阶段解决）

- #6 PyO3 wrapper parse/build 方法 → ConstructMixin 提供 parse/build 方法 + 编译缓存

## 子任务清单

- [x] **14.1 ConstructMixin + cs_field MVP（扁平 dataclass）**
  - Rust 暴露层：CompiledSchemaHolder pyclass + parse_bytes_py/build_from_py/sizeof pymethods
  - Rust 暴露层：compile_schema pyfunction
  - Python 模块：ConstructMixin（parse/build/parse_stream/build_stream + 编译缓存）
  - Python 模块：cs_field（dataclasses.field 包装）
  - Python 模块：_collect_field_subcons（字段扫描 + Struct(**kwargs) 构建）
  - 出口：扁平 dataclass parse/build roundtrip 测试通过

- [x] **14.2 嵌套 dataclass 支持**（需先修正 N-1）
  - _resolve_nested_subcon（ConstructMixin 子类识别）
  - _build_struct_decl（递归构建 Struct 声明）
  - _dict_to_instance（PyDict → 嵌套 dataclass 实例）
  - 类型提示缓存
  - 出口：嵌套 dataclass parse/build roundtrip 测试通过

- [x] **14.3 前向引用 + Optional + 边界情况**
  - cs_field(None) 占位 + 字符串注解
  - typing.get_type_hints 解析（含 module globals）
  - Optional[T] 支持
  - 循环引用检测
  - 出口：前向引用场景测试通过

- [x] **14.4 @construct_dataclass 语法糖 + 声明式互操作**
  - @construct_dataclass 装饰器（type() 动态创建子类）
  - 与 Struct/Sequence 等声明式 API 互操作
  - 性能基线测试
  - 出口：装饰器 API 测试通过

- [ ] **14.5 PyDataclassSink 直构优化（可选，视性能基准）**
  - 方案 B：PyDataclassSink 直接 setattr
  - 消除 PyDict 中转开销
  - 出口：直构比 dict 中转快 ≥30%

## 出口标准

1. `cargo build` + `cargo clippy`（construct-rs + construct-py）零 warning
2. `maturin develop` + Python 测试通过
3. Phase 13 全部测试保持 PASS（回归）
4. 扁平 dataclass parse/build 端到端测试通过
5. 嵌套 dataclass parse/build 端到端测试通过
6. 前向引用 + Optional 场景测试通过
