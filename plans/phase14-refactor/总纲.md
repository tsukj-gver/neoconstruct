# Phase 14 总纲：dataclass-first API（桩）

> **阶段目标**：实现 ConstructMixin + cs_field + direct-to-dataclass API。
>
> **状态**：待 Phase 13 验收后细化（MVP 纯 Python 部分可提前启动）。

## 预估子任务方向

- 14.x ConstructMixin + cs_field（纯 Python MVP）
- 14.x direct-to-dataclass（Rust 直构 dataclass 实例）
- 14.x 嵌套类型自动解析 / Optional / 前向引用
- 14.x 与声明式 API 互操作

## REV 重要问题（本阶段必须解决）

- #6 direct-to-dataclass 机制（Rust 实例化任意 dataclass）

## 出口标准

（待 Phase 13 完成后细化）
