# 项目总进度

> 本文件是 PM 恢复工作时的入口。PM 每次处理子任务状态变更时同步更新此文件。

## 最后更新：2026-06-16（Phase 13 验收通过）

## 状态图例

| 图标 | 含义 |
|------|------|
| ⚪ | 未开始 |
| 🔵 | 进行中 |
| ✅ | 已完成（阶段验收通过） |
| 🔴 | 阻塞 |
| 🔄 | 架构重构中（推倒重做） |

## 阶段概览

| 阶段 | 状态 | 备注 |
|------|------|------|
| Phase 1-9 | ✅ | 内核基础+构造器全部完成（旧架构） |
| Phase 10 Python FFI | ✅ | PyO3 封装完成（旧架构），214/257 测试通过，性能 0.199x |
| **架构重构（5 个 Phase）** | | |
| Phase 11 内核分发迁移 | ✅ | enum_dispatch 替代 Box\<dyn\>，71+2+7 variant，1308/1312 测试通过 |
| Phase 12 编译执行树 | ✅ | CompiledNode + SchemaCompiler + build-once，72 构造器全实现，1476 测试通过 |
| Phase 13 FFI 执行层重写 | ✅ | direct-to-Python + PyContextView + PyCallback，1515+208 测试通过 |
| Phase 14 dataclass-first API | 🔵 | **当前活跃** — ConstructMixin + cs_field + direct-to-dataclass |
| Phase 15 性能验证与最终验收 | ⚪ | 全量基准 + S-PERF/S-FUNC/S-ARCH/S-QUAL |

## Phase 拆解依据

基于 `docs/refactor/顶层架构设计.md` §6 实施路径，REV 检视通过（0 阻塞 / 9 重要 / 6 建议）。

依赖链：
```
Phase 11 (分发迁移，低风险铺底)
    ↓
Phase 12 (编译执行树，核心)
    ↓
Phase 13 (FFI 执行层，性能主战场)
    ↓ (可与 14 部分并行)
Phase 14 (dataclass-first API)
    ↓
Phase 15 (性能验证，最终验收)
```

## 当前焦点

- **当前阶段**：🔵 Phase 14 dataclass API
- **当前状态**：Phase 13 验收通过（FFI 执行层全部实现），进入 Phase 14 ARCH 设计
- **下一步**：ARCH 输出 Phase 14 模块设计（ConstructMixin + cs_field + direct-to-dataclass）
- **阻塞项**：无
- **待处理的 REV 重要问题**（9 项，分配到各 Phase 的模块设计中解决）：
  - #1 D1/D2 耦合（CombinedConstruct vs CompiledNode 关系）→ Phase 12
  - #2 嵌套 sink 机制 → Phase 12
  - #3 PyContextView FFI 开销量化 → Phase 13
  - #4 SchemaCompiler 内省机制 → Phase 12（最高优先）
  - #5 sink 实际机制（复合构造器双输出）→ Phase 12
  - #6 direct-to-dataclass 机制 → Phase 14
  - #7 Lazy 不兼容 direct-to-PyDict → Phase 13
  - #8 映射表遗漏 ~20 构造器 → Phase 11 补全清单
  - #9 错误路径 PyDict 处理语义 → Phase 13

## 战略转向记录（2026-06-16）

### 决策

1. **直接架构重构**，跳过短期 API 改造和局部性能优化。
2. **自顶向下设计**：先大框架，再模块，最后接口。
3. **流程变更**：REF→VET 合并；REV→设计检视；新增 DESIGN_REVIEW 阶段。
4. **exmex 否决**：类型壁垒缩减覆盖范围，不引入。表达式系统保留 Rust 原生树 + enum_dispatch。

### 目标架构

**build-once 编译模型 + direct-to-Python 执行 + enum_dispatch + 惰性 Context**

详见 `docs/refactor/顶层架构设计.md`（924 行，D1-D7 决策 + §8 验收标准）。

## 活跃的设计质疑

（无）
