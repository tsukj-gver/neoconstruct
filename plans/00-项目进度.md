# 项目总进度

> 本文件是 PM 恢复工作时的入口。每次状态变更后更新。

## 最后更新：2026-06-23（Phase 2 验收通过）

## 项目定位

用 Rust 重写 Python construct 库的内核，使其更快。交付物是 Python 包。

**核心原则**：
- pyo3 是核心依赖，不是可选附加
- 编译、parse、build 各只有一次 FFI 边界穿越
- Rust 内部直接操作 CPython C API，无中间表示层（parse 直接产 PyObject，build 直接读 PyObject）
- API 风格：mashumaro 式 dataclass mixin（非 pydantic BaseModel）
- 性能目标：≥4x vs Python construct 2.10.70（10x 为理想目标）

## 状态图例

| 图标 | 含义 |
|------|------|
| ⚪ | 未开始 |
| 🔵 | 进行中 |
| ✅ | 已完成（阶段验收通过） |
| 🔴 | 阻塞 |

## 阶段概览

| 阶段 | 状态 | 备注 |
|------|------|------|
| Phase 0 架构设计 | ✅ | tag `phase-0-complete` |
| Phase 1 垂直切片 | ✅ | tag `phase-1-complete`，性能 8-12x |
| Phase 2 表达式系统框架 | ✅ | tag `phase-2-complete`，表达式 build 7-10x，parse 5.6-7x |
| Phase 3 表达式系统完整 | ⚪ | Switch/IfThenElse/Const/context=/Array/Checksum/Prefixed/Pointer |

## 当前焦点

- **当前阶段**：Phase 2 已验收通过，等待用户指定 Phase 3 方向
- **下一步**：Phase 3 子任务规划（需用户确认优先级）

### Phase 2 子任务完成状态

| 子任务 | 名称 | 状态 | 测试数 |
|--------|------|------|--------|
| 2.1 | ExprOp VM Core（Rust） | ✅ | 78 Rust |
| 2.2 | Python Field 函数 + 表达式类型 | ✅ | 108 Python |
| 2.3 | 编译管线扩展 | ✅ | 60 Python + 8 Rust |
| 2.4 | StructNode 改造 + Context 恢复 | ✅ | 13 Rust |
| 2.5 | Bytes 表达式长度 | ✅ | 34 Rust + 18 Python |
| 2.6 | Tell + Computed 节点 | ✅ | 45 Rust + 23 Python |
| 2.7 | 集成测试 + 性能验证 | ✅ | 28 Python + 8 编译错误 |
| **合计** | | | **401 Rust + 337 Python** |

## 关键性能数据

### Phase 1 基准（无表达式）

| 用例 | 字段数 | parse 加速比 | build 加速比 |
|------|--------|-------------|-------------|
| B1 | 3 | 7.63x | 11.71x |
| B2 | 10 | 8.83x | 13.44x |
| B3 | 50 | 13.66x | 17.40x |
| B4 | 100 | 13.44x | 13.17x |

### Phase 2 表达式基准

| 场景 | 描述 | parse 加速比 | build 加速比 |
|------|------|-------------|-------------|
| E1 | Bytes(count) | 5.62x | 9.11x |
| E2 | Bytes(count+flag) | 6.20x | 10.07x |
| E3 | Tell+Computed(6字段) | 6.20x | 7.03x |

**性能分析**：parse 未达 8x 目标，根因是 P0 模型低估固定开销（实测 ~370ns vs 预测 229ns）。
Build 方向达标（7-10x）。优化方向已明确（VM 栈改栈分配、interned key dict 查找）。
详见 `plans/phase2-expression/总纲.md` S-PERF 分析。

## 核心设计决策（Phase 2，8 项）

1. **废弃 `this`，用编译期字段引用**：`Bytes(count)` 而非 `Bytes(this.count)`
2. **表达式编译为 Rust 栈式 VM**：零 FFI 求值
3. **不提供 `len_()`**：用 `Tell()` + `Computed()` 替代
4. **嵌套跨层引用用显式 `context=`**：不用魔法 `this._._`
5. **三种 field 函数 RW/RO/WO**：数学完备分类，用户一眼可知字段角色
6. **default 自动 kw_only**：`field(subcon, default=value)` 解决字段顺序冲突
7. **`Default` 构造器不再需要**：field 层管用户语义，构造器层管字节语义
8. **字节顺序 vs init 顺序解耦**：声明顺序=字节布局，init 顺序=按性质重排

详见 `docs/模块设计-表达式系统.md`

## 已知技术债务

1. **parse 方向表达式性能 < 8x**：VM 栈堆分配 + to_str()+get_item(str) 开销，Phase 3 早期优化
2. **Bytes build 不校验长度**：设计 §4.7 规定不校验，但 Python construct 实际校验（SF-1 待定）
3. **sizeof Expr 返回 Generic**：无 SizeofError 变体，跟随 greedy_bytes 先例

## 参考资源

| 资源 | 位置 | 用途 |
|------|------|------|
| Python construct 原版 | `construct/` | 语义参考（只读） |
| pydantic-core | `refs/pydantic/pydantic-core/` | FFI 架构参考 |
| mashumaro | `refs/mashumaro/` | dataclass mixin 模式参考 |
| 表达式系统分析 | `docs/analysis/construct-expr-system.md` | Python construct this/obj_/len_ 机制完整分析 |
| 表达式系统设计 | `docs/模块设计-表达式系统.md` | Phase 2-3 设计文档（3227 行） |
| P0 性能分析 | `docs/analysis/perf-p0-analysis.md` | pyo3 开销归档基线 |

## 活跃的设计质疑

（无）
