# 项目总进度

> 本文件是 PM 恢复工作时的入口。PM 每次处理子任务状态变更时同步更新此文件。

## 最后更新：2026-06-20（Phase 16.5 build 优化部分完成，规划 Phase 17）

## 状态图例

| 图标 | 含义 |
|------|------|
| ⚪ | 未开始 |
| 🔵 | 进行中 |
| ✅ | 已完成（阶段验收通过） |
| 🔴 | 阻塞 |
| 🔄 | 架构重构中（推倒重做） |

## 阶段概览

| 阶段 | 状态 | 备注 |
|------|------|------|
| Phase 1-9 | ✅ | 内核基础+构造器全部完成（旧架构） |
| Phase 10 Python FFI | ✅ | PyO3 封装完成（旧架构），214/257 测试通过，性能 0.199x |
| **架构重构（5 个 Phase）** | | |
| Phase 11 内核分发迁移 | ✅ | enum_dispatch 替代 Box\<dyn\>，71+2+7 variant，1308/1312 测试通过 |
| Phase 12 编译执行树 | ✅ | CompiledNode + SchemaCompiler + build-once，72 构造器全实现，1476 测试通过 |
| Phase 13 FFI 执行层重写 | ✅ | direct-to-Python + PyContextView + PyCallback，1515+208 测试通过 |
| Phase 14 dataclass-first API | ✅ | ConstructMixin + cs_field + 嵌套/前向引用/装饰器，61 测试通过 |
| Phase 15 性能验证与最终验收 | 🔴 | S-ARCH/S-QUAL 通过，**S-PERF 未达标**（0.26x parse / 0.08x build vs Python 原版），tag 已撤回 |
| **Phase 16 Python-first 产出层** | | |
| Phase 16.1-16.4 PySink/PyInput/exec_py | ✅ | pyo3 optional feature + 71 变体 exec_parse_py/exec_build_py + 入口切换 |
| Phase 16.5 build 方向优化 | 🔵 | RC2+RC3+RC5 实施，simple build 0.74x→0.86x，array build 修复+0.34x；build≥1.0x 推迟 Phase 17 |

## Phase 拆解依据

基于 `docs/refactor/顶层架构设计.md` §6 实施路径，REV 检视通过（0 阻塞 / 9 重要 / 6 建议）。

依赖链：
```
Phase 11 (分发迁移，低风险铺底)
    ↓
Phase 12 (编译执行树，核心)
    ↓
Phase 13 (FFI 执行层，性能主战场)
    ↓ (可与 14 部分并行)
Phase 14 (dataclass-first API)
    ↓
Phase 15 (性能验证，最终验收)
```

## 当前焦点

- **当前阶段**：🔵 Phase 16.5 部分完成，规划 Phase 17
- **当前状态**：build 方向优化（RC2+RC3+RC5）已实施并通过 VET 审查
- **实际性能**（3 次平均，5000 迭代）：
  - parse: simple 1.79x ✅ / nested 1.45x ✅ / array 0.54x / geomean 1.12x ✅
  - build: simple 0.86x / nested 0.62x / array 0.34x / geomean 0.56x
- **已达成的改善**：
  - parse 方向：geomean ≥1.0x ✅（Phase 16.3 目标达成）
  - simple build：0.74x → 0.86x（+16%）
  - array build：0.17x（old path）→ 0.34x（new path，1.9x 提升）+ 正确性修复
- **未达标项**：build geomean 0.56x（目标 ≥1.0x）
- **主要瓶颈**：固定入口开销 1.18µs（占 simple build 总时间 35%），需 RC4 优化
- **下一步**：Phase 17 — 入口开销优化（RC4）+ schema-guided 类型已知 extract + Array 非 Dynamic 包装
- **阻塞项**：S-PERF build ≥1.0x 未达标

## 战略转向记录（2026-06-16）

### 决策

1. **直接架构重构**，跳过短期 API 改造和局部性能优化。
2. **自顶向下设计**：先大框架，再模块，最后接口。
3. **流程变更**：REF→VET 合并；REV→设计检视；新增 DESIGN_REVIEW 阶段。
4. **exmex 否决**：类型壁垒缩减覆盖范围，不引入。表达式系统保留 Rust 原生树 + enum_dispatch。

### 目标架构

**build-once 编译模型 + direct-to-Python 执行 + enum_dispatch + 惰性 Context**

详见 `docs/refactor/顶层架构设计.md`（924 行，D1-D7 决策 + §8 验收标准）。

## §0 项目定位修正（2026-06-19）

Phase 1-15 将项目错误地架构为"碰巧有 Python 绑定的 Rust 库"。Phase 16+ 以 Python-first 视角修正：
- pyo3 是核心依赖（construct-rs optional feature）
- Value 枚举不出现在 Python 关键路径
- Rust 代码直接产出 PyObject

详见 `AGENTS.md` §0 + `docs/总设计文档.md` §1。

## Phase 16 进展记录（2026-06-19 ~ 2026-06-20）

### Phase 16.1-16.4（✅ 完成）
- pyo3 optional feature + PySink/PyInput + 71 变体 exec_parse_py/exec_build_py
- parse 入口已切换，build 入口已切换
- 烟雾测试：simple parse 1.94x ✅

### Phase 16.5 build 优化（🔵 部分完成）
- ARCH profiling 定位 3 根因（RC1 shallow list / RC2 per-field extract / RC3 标量双重提取）
- REV 检视发现 P-MUST-1（nested build 预测算术错误）/ P-MUST-2（flagbuildnone bug）/ P-MUST-3（内存泄漏）
- ARCH 修正 + DEV 实施 RC2+RC3+RC5 + 16.5.4
- RC1 引入 array build 回归 → 紧急回退为浅提取
- 最终性能：simple build 0.86x / nested 0.62x / array 0.34x（均 <1.0x）
- **结论**：build ≥1.0x 推迟 Phase 17

### Phase 17 方向（待规划）
1. **RC4 入口开销优化**：固定 1.18µs 占 simple build 35%，需缩减 ctx["_"] 浅提取 + dispatch 开销
2. **Array 非 Dynamic 包装**：Python 端 Array 通过 extract_subcon → Dynamic → CompiledDynamic，绕过了 CompiledArray 的 batch fast-path
3. **Schema-guided 类型已知 extract**：nested build 需消除 per-field extract_ctx_value_py

## 活跃的设计质疑

（无）
