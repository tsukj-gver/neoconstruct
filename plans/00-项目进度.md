# 项目总进度

> 本文件是 PM 恢复工作时的入口。每次状态变更后更新。

## 最后更新：2026-06-21（Phase 0 架构设计验收通过，准备启动 Phase 1 垂直切片）

## 项目定位

用 Rust 重写 Python construct 库的内核，使其更快。交付物是 Python 包。

**核心原则**：
- pyo3 是核心依赖，不是可选附加
- 编译、parse、build 各只有一次 FFI 边界穿越
- Rust 内部直接操作 CPython C API，无中间表示层（parse 直接产 PyObject，build 直接读 PyObject）
- API 风格：mashumaro 式 dataclass mixin（非 pydantic BaseModel）
- 性能目标：≥4x vs Python construct 2.10.70（10x 为理想目标）

## 状态图例

| 图标 | 含义 |
|------|------|
| ⚪ | 未开始 |
| 🔵 | 进行中 |
| ✅ | 已完成（阶段验收通过） |
| 🔴 | 阻塞 |

## 阶段概览

| 阶段 | 状态 | 备注 |
|------|------|------|
| Phase 0 架构设计 | ✅ | 0.1 设计原则文档 + 0.2 架构设计文档均通过 REV 检视 |
| Phase 1 垂直切片 | ⚪ | 最小端到端路径，性能 >4x 烟雾测试 |

> 后续阶段由用户在 Phase 1 验收通过后指定。

## 当前焦点

- **当前阶段**：Phase 0 已完成 → 准备启动 Phase 1
- **当前状态**：架构设计全部通过（`docs/FFI设计.md` + `docs/Mixin设计.md` + `docs/架构设计.md`）
- **下一步**：根据架构设计 §F 制定 Phase 1 子任务清单，分派 DEV 实现垂直切片

## 参考资源

| 资源 | 位置 | 用途 |
|------|------|------|
| Python construct 原版 | `construct/` | 语义参考（只读） |
| pydantic-core | `refs/pydantic/pydantic-core/` | FFI 架构参考 |
| mashumaro | `refs/mashumaro/` | dataclass mixin 模式参考 |
| API 定义 | `examples/example.py` + `examples/construct.py` | 用户指定的 API 样式 |
| pydantic-core 分析 | `docs/analysis/架构对比分析-pydantic-core.md` | 架构对比（8 个缺陷分析） |
| mashumaro 分析 | `docs/analysis/Container必要性与dataclass集成分析.md` §3 | mashumaro 代码生成机制 |

## 活跃的设计质疑

（无）
