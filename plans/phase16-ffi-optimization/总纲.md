# Phase 16 总纲：Python-first 产出层重写

> **阶段目标**：让 Rust 编译执行树直接产出 PyObject，消除 Value 中间类型，使 S-PERF-1 达到 ≥1.0x vs Python 原版。
>
> **动机**：Phase 1-15 将项目架构为"碰巧有 Python 绑定的 Rust 库"，Value/OutputSink/conversions.rs 间接层导致 0.26x 性能。§0 项目定位修正后，pyo3 是核心依赖，Rust 代码应直接产出 PyObject。
>
> **状态**：16.1 设计已通过（ACCEPTED），进入 CODING

## 性能现状（2026-06-19 实测）

| 格式 | parse vs Python 原版 | build vs Python 原版 |
|------|---------------------|---------------------|
| simple (3字段) | 0.46x | 0.13x |
| nested (2级) | 0.36x | 0.11x |
| array (100元素) | 0.12x | 0.04x |

## S-PERF 标准

| 编号 | 标准 | 基线 | 目标值 |
|------|------|------|--------|
| S-PERF-1 | Python 调用场景几何平均加速比 | **Python 原版 construct 2.10.70** | **≥1.0x** |

## 修正后性能预测（ARCH + REV 检视后）

| 预测 | 修正后 | 目标 | 达标 |
|------|--------|------|------|
| simple parse | ~7.1x | ≥1.0x | ✅ |
| simple build | ~5.4x | ≥1.0x | ✅ |
| nested parse | ~4.4x | ≥1.0x | ✅ |
| nested build | ~3.1x | ≥1.0x | ✅ |
| array parse | ~3.1x | ≥1.0x | ✅ |
| array build | ~5.4x | ≥4.0x | ✅ |
| **geomean(6)** | **~4.6x** | **≥1.0x** | ✅ |

## 子任务清单

- [x] **16.1 Python-first 产出层设计**（ARCH → REV → ACCEPTED）
  - 设计文档：`docs/refactor/模块设计-Python-first产出层.md`（~1585 行）
  - REV 检视：有条件通过 → ARCH 修正 3 项 → 通过

- [ ] **16.2 pyo3 引入 + PySink/PyInput 基础设施**（DEV → VET）
  - construct-rs/Cargo.toml: 加 pyo3 optional + python feature
  - construct-rs/src/compiled/py_sink.rs（新）: PySink trait + PyDictSink2 + value_to_py_scalar + Container/ListContainer 包装
  - construct-rs/src/compiled/py_input.rs（新）: PyInput trait + PyDictInput + PyScalarInput + extract_batch_int_raw
  - construct-rs/src/compiled/mod.rs: 加模块声明
  - 验收: cargo build --features python 编译通过 + cargo test（无 python）1515 测试 PASS + PySink/PyInput 单元测试 ~20 个 PASS

- [ ] **16.3 Leaf + Composite 的 exec_parse_py 实现 + parse 入口切换**（DEV → VET）
  - construct-rs/src/compiled/py_exec.rs（新）: PyCompiledExec trait + 全变体 impl + dispatch
  - construct-py/src/compiled_ext.rs: parse_bytes_raw 改用 exec_parse_py_dispatch + PyDictSink2
  - **⚠️ 烟雾测试检查点**: test_perf_comparison.py -n 1000，simple parse ≥1.0x（<1.0x 红灯暂停）

- [ ] **16.4 exec_build_py 实现 + schema-guided extract**（DEV → VET）
  - construct-rs/src/compiled/py_exec.rs: 补全 exec_build_py impl
  - construct-rs/src/compiled/py_input.rs: 补全 extract_batch_int_raw（裸 CPython API）
  - construct-py/src/compiled_ext.rs: build_from_raw 改用 exec_build_py_dispatch + PyInput + shallow_py_to_value_for_ctx

- [ ] **16.5 conversions.rs 缩减 + 清理 + 全量验收**（DEV → VET → PM）
  - conversions.rs 从 788 行缩减至 ~250 行
  - py_sink.rs / py_input.rs 删除或 deprecated
  - S-PERF-1 全量验收: test_perf_comparison.py -n 5000, geomean(6) ≥1.0x

## 出口标准

1. S-PERF-1 ≥1.0x（vs Python 原版，几何平均，≥3 种格式复杂度）
2. cargo build + clippy + fmt + test 全通过（含 --features python）
3. cargo test（无 python）1515 测试全 PASS
4. construct-py 61 dataclass 测试全 PASS
5. **性能对比数据表**（不接受"编译通过"）
6. Value 不出现在 Python parse/build 关键路径上（代码审查确认）

## 已废弃

- ~~方案 A（修复 conversions.rs + 批量转换）~~：在"construct-rs 不碰 pyo3"伪约束下设计，已被 §0 推翻。
  旧设计文档保留为 `docs/refactor/模块设计-FFI批量化优化.md`（瓶颈分析参考）。
