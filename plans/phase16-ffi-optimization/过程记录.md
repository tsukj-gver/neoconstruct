# Phase 16 过程记录：Python-first 产出层重写

## 子任务 16.1: Python-first 产出层设计

| 项目 | 内容 |
|------|------|
| 状态 | 已通过（ACCEPTED） |
| 负责人 | ARCH |
| 开始时间 | 2026-06-19 |
| 完成时间 | 2026-06-19 |

### 操作日志
- [2026-06-19] [PM] 创建 Phase 16 总纲（FFI 批量化优化方向）
- [2026-06-19] [ARCH] 产出方案 A 设计（`模块设计-FFI批量化优化.md`），在 construct-rs 不引入 pyo3 的约束下优化转换层
- [2026-06-19] [PM] 用户质疑项目定位：本项目从头到尾是 Python 项目，Rust 是内核实现手段不是独立产品
- [2026-06-19] [PM] 确认：§0 项目定位修正——pyo3 是核心依赖，Value 不出现在关键路径
- [2026-06-19] [PM] AGENTS.md / 总设计文档 / 顶层架构设计 三份核心文件写入 §0 项目定位
- [2026-06-19] [PM] Phase 16 总纲重写为"Python-first 产出层重写"方向，旧方案 A 废弃
- [2026-06-19] [PM] 分派 ARCH 以 Python-first 视角重新设计产出层
- [2026-06-19] [ARCH] 阅读全部必读文件：compiled/mod.rs（CompiledNode 71v + CompiledExec）、sink.rs（OutputSink + ValueSink）、input.rs（Input + ValueInput）、Cargo.toml×2、py_sink.rs（PyDictSink）、py_input.rs（PyInput）、conversions.rs（788行）、compiled_ext.rs（CompiledSchemaHolder）、旧方案 A 瓶颈分析、Phase 16 总纲、performance-gate skill
- [2026-06-19] [ARCH] 完成 Python-first 产出层设计，产出 `docs/refactor/模块设计-Python-first产出层.md`（~1200 行，9 节 + 2 附录）
- [2026-06-19] [REV] 完成设计检视，结论：有条件通过。核心架构正确（PySink 双产物、PyCompiledExec 独立 trait、per-field 类型已知 extract），但识别 3 项必修问题（P-CRIT-1/C-CRIT-1/C-CRIT-2）+ 3 项 DEV 注意事项（P-IMP-1/C-IMP-1/C-IMP-2）
- [2026-06-19] [ARCH] 完成 3 项必修设计澄清修正（详见下方"ARCH 设计澄清修正"段）。修正后维持 ≥1.0x 性能预测（geomean ~4.6x，保守下界 ~2.5x）。待 PM 确认进入 CODING。

### ARCH 设计说明

**核心技术决策**：
1. **pyo3 作为 construct-rs 的 optional feature**（方案 A，非合并 crate）：PySink/exec_parse_py 全在 construct-rs 内，可访问编译树内部字段，与 CompiledExtension 无缝集成。纯 Rust 测试（1515 个）通过 `cargo test`（无 python feature）零影响保留。
2. **新增 PyCompiledExec trait + 宏生成 dispatch**（非修改 CompiledExec、非泛型化 OutputSink）：Value 路径 trait 零修改；Py 路径独立 trait + 71-arm 宏 match。
3. **PySink 双产物设计**（Value for ctx + PyObject for output）：leaf 一次解码同时产出标量 Value（供 context 表达式）和 PyObject（供输出），彻底消除 B1 round-trip。
4. **build 方向 per-field 类型已知 extract + schema-guided batch extract**：消除 B2（双重全量转换）和 B3（类型检查链）。100 元素 int array build 从 638 FFI 降至 ~5 FFI。

**性能假设摘要**（满足 performance-gate Checkpoint 1）：
- 识别 8 个瓶颈来源（B1-B8，含旧设计的 B1-B4 + 新增 B5-B8）
- 可证伪预测：simple parse ~10x / simple build ~7.7x / array parse ~3-5x / array build ~11x (batch)；geomean(6 场景) ≥2.0x（保守）
- 反例声明：6 条，每条有明确应对方案
- 验证方法：test_perf_comparison.py 分阶段验证 + 烟雾测试检查点（16.3 后 simple parse ≥1.0x）

**对现有代码的影响**：
- construct-rs：新增 3 文件（~800-1200 行），Cargo.toml + mod.rs 小改，现有代码零修改
- construct-py：compiled_ext.rs 重写入口，conversions.rs 从 788 行缩减至 ~250 行，py_sink.rs/py_input.rs 删除/deprecated
- 公共 Python API 不变；1515 纯 Rust 测试不变；61 dataclass 测试行为等价

**子任务拆分**：16.2（基础设施）→ 16.3（parse_py + 烟雾测试）→ 16.4（build_py + batch extract）→ 16.5（清理 + 验收）

### ARCH 设计澄清修正（回应 REV 3 项必修问题）

**修正日期**：2026-06-19
**修正文档**：`docs/refactor/模块设计-Python-first产出层.md`

#### P-CRIT-1 修正：extract_batch_int_py "单次 C 调用" 事实错误

**修正内容**：
1. §4.5 重写：承认 pyo3 `extract::<Vec<i64>>` 是 per-element（~202 C 调用/100 元素），非单次 C 调用
2. 新增 §4.5.1：**裸 CPython API**（`PyList_GET_SIZE` + `PyList_GET_ITEM` 宏 + `PyLong_AsLongLong`）作为首选实现方案，绕过 pyo3 `PySequence_GetItem` 方法解析开销
3. §4.5.2 修正 FFI 对比表：batch extract ~101 C 调用（非 ~2），耗时 ~3.5-4µs（非 ~500ns）
4. §6.3 P4 修正：array build 从 ~11.5x 修正为 ~5.4x（仍达 ≥4.0x 目标）
5. §6.4 反例声明修正：裸 API 已设为首选而非 fallback
6. §6.6 可选精化修正：裸 API 标注为 batch extract 首选
7. §4.4 B2 消除表修正：~2 FFI → ~101 FFI（降幅 99.7% → 84%，仍消除 B2/B3）

**batch extract 真实优势**（非 ~100x FFI 减少）：~2-2.5x 加速来自消除 per-element Box<dyn PyInput> + vtable（B7）+ 宏替代方法解析。

#### C-CRIT-1 修正：Container/ListContainer 包装机制未指定

**修正内容**：
1. 新增 §3.3.1「Container/ListContainer 包装机制」：选定**从创建起使用 Container/ListContainer 作为底层存储**
2. 关键洞察：Container 是 dict 子类、ListContainer 是 list 子类，PyDict_SetItem/PyList_Append 可直接作用，无需额外复制
3. PySinkMode 字段类型修正：`Py<PyDict>`/`Py<PyList>` → `Py<PyAny>`（支持子类）
4. 新增 `ContainerClasses` 结构 + `obtain()` 方法：root sink 获取类引用一次，sub-sink Clone 共享；可用 static OnceLock 全局缓存使 import 摊薄至 ~0
5. 新增 `sub_struct()`/`sub_array()` 方法：创建 Container/ListContainer 子类实例
6. 性能影响：每个复合节点 +120ns（call0 ~150ns 替代 PyDict::new ~30ns）；import 经 OnceLock 缓存后 ~0ns

**P1-P3 预测修正**：simple parse ~10x → ~7.1x；nested parse ~6x → ~4.4x；array parse ~3.2-4.6x → ~3.1x。全部仍 ≥1.0x。

#### C-CRIT-2 修正：build 方向 context._ 设置未指定

**修正内容**：
1. 新增 §4.6「build 方向 context._ 处理（浅转换方案）」
2. 选定方案 c：**入口处浅转换（非递归，仅 root 标量字段）**
3. 实现 `shallow_py_to_value_for_ctx()`：对 dict 提取顶层标量字段，嵌套 dict/list 存为空占位符（不递归）
4. 成本 O(root_scalar_fields) ~80-150ns（vs 全量转换 ~30µs for array）
5. build_from_raw 入口整合：`ctx.insert("_", shallow_py_to_value_for_ctx(py, obj))`
6. 已知限制：`this._.nested.deep_field` 深层引用返回空占位（罕见模式）；基准格式不用 this._，无影响；若有 dataclass 测试失败，Phase 17 可用 Context 支持 PyObject（选项 e）解决

**P1/P2 build 预测修正**：simple build ~7.7x → ~5.4x；nested build ~3.5x → ~3.1x。全部仍 ≥1.0x。

#### 修正后性能预测汇总

| 预测 | 原设计 | 修正后 | 目标 | 达标 |
|------|--------|--------|------|------|
| P1 simple parse | ~10x | ~7.1x | ≥1.0x | ✅ |
| P1 simple build | ~7.7x | ~5.4x | ≥1.0x | ✅ |
| P2 nested parse | ~6x | ~4.4x | ≥1.0x | ✅ |
| P2 nested build | ~3.5x | ~3.1x | ≥1.0x | ✅ |
| P3 array parse | ~3.2-4.6x | ~3.1x | ≥1.0x | ✅ |
| P4 array build | ~11.5x | ~5.4x | ≥4.0x | ✅ |
| P5 geomean(6) | ~5.8x | **~4.6x** | ≥1.0x | ✅ |

> 保守下界（所有开销被低估 2x）：geomean ≥2.5x。S-PERF-1 达标置信度高。

### REV 设计检视报告

**检视文档**：`docs/refactor/模块设计-Python-first产出层.md`（1210 行）
**检视日期**：2026-06-19
**检视范围**：延续性 / 性能（增强清单）/ 整体性 / 可行性 / 完备性

---

#### 一、延续性：✅ 通过

已验证（对照源码）：

1. **CompiledExec trait 零修改** — `construct-rs/src/compiled/mod.rs:75-120` 的 trait 定义完全不动。新 `PyCompiledExec` trait 是平行新增，通过 `#[cfg(feature = "python")]` 门控。
2. **CompiledNode enum (71 变体) 零修改** — `mod.rs:981-1142` 的 enum 定义不动。新 dispatch 函数消费同一个 enum。
3. **OutputSink / ValueSink / Input / ValueInput 零修改** — `sink.rs` / `input.rs` 全文审查确认。ValueSink 保留用于纯 Rust 测试。
4. **1515 纯 Rust 测试不受影响** — 编译矩阵正确：无 `python` feature 时 `py_sink.rs` / `py_input.rs` / `py_exec.rs` 不参与编译（`#![cfg(feature = "python")]` + `#[cfg(feature = "python")] pub mod`）。
5. **Python 用户 API 签名不变** — `parse_bytes_py(data, **kw)` / `build_from_py(obj, **kw)` 签名不变。仅 `*_raw` 内部实现切换。
6. **CompiledSchema / SchemaCompiler / Stream / Context / ConstructError** — 均共享不变。

**结论**：设计从 Phase 11-15 平滑演进，无破坏性变更。

---

#### 二、性能：⚠️ 有条件通过（3 项必须修正）

##### 2.1 瓶颈覆盖评估

B1-B8 识别覆盖了当前路径的主要瓶颈来源，对照源码逐项验证：
- **B1**（finish_field round-trip）：✅ 证实。`py_sink.rs:294-306` 的 Object 分支确有 `py_to_value` 逆向转换。
- **B2**（build 双重 as_value）：✅ 证实。`compiled_ext.rs:190` 一次 + `mod.rs:1764` 一次。
- **B3**（7× 类型链）：✅ 证实。`conversions.rs:110-213` 确有 None→Bool→BigInt→Float→Bytes→ByteArray→String→List→Dict→dataclass 的长链。
- **B4-B8**：✅ 合理识别。

**未列入但存在的新瓶颈来源**：
- **U1（已计入但量化不准）**：leaf `inner.parse()` 对 `Value::Bytes` / `Value::String` 的堆分配（~20-50ns，非 ~5ns）。不影响达标但影响估算精度。

##### 2.2 🔴 关键问题 P-CRIT-1：extract_batch_int_py "单次 C 调用" 声明事实错误

**文档位置**：§4.5 + §6.3 P4

**设计声明**：
> "items extract_batch_int_py | **1** extract(Vec\<i64\>) | 1 | ~500ns"
> "pyo3 对 Vec\<i64\> 的批量 extract 是单次 C 调用遍历 CPython list 内部数组，~5ns/元素"

**实际行为**（pyo3 0.22.6 源码 `types/sequence.rs:494-529` 已验证）：
```rust
fn extract_sequence<'py, T>(obj: &Bound<'py, PyAny>) -> PyResult<Vec<T>> {
    let seq = ...; // PySequence_Check
    let mut v = Vec::with_capacity(seq.len().unwrap_or(0));
    for item in seq.iter()? {       // per-element: PySequence_GetItem × N
        v.push(item?.extract::<T>()?);  // per-element: PyLong_AsLongLong × N
    }
    Ok(v)
}
```

**实际 FFI 计数（100 元素）**：~202 次 C 调用（1 PySequence_Check + 1 PySequence_Length + 100 PySequence_GetItem + 100 PyLong_AsLongLong），而非 1 次。实际耗时 ~3-5µs 而非 ~500ns。

**对 P4 预测的影响**：
| | 设计声明 | 修正估算 |
|---|---------|---------|
| batch extract FFI | 1 | ~202 |
| batch extract 耗时 | ~500ns | ~3-5µs |
| array build 总计 | ~2.6µs | ~5-6µs |
| vs Python ~30µs | ~11.5x | ~5-6x |

**判定**：P4 ≥4.0x 目标仍可能达标（5-6x > 4x），但余量大幅缩窄。设计声称的 batch extract 相对 per-index 的 "~100x FFI 优势" 实际为 "~0-1.2x"（两者都是 per-element）。batch 的真实优势仅在于避免 per-element trait object dispatch 和更好的缓存局部性。

**要求**：ARCH 必须修正 §4.5 和 §6.3 P4 的 batch extract 分析。选项：
(a) 更正为 per-element 估算，承认 batch 优势主要来自避免 `dyn PyInput` dispatch；或
(b) 将 batch extract 改为裸 CPython API 手动循环（`PyList_GET_ITEM` 宏 + `PyLong_AsLongLong`），明确这是绕过 pyo3 抽象的方案；或
(c) 将 array build 优化降级为 Phase 17 候选（如 numpy frombuffer）。

##### 2.3 🟡 重要问题 P-IMP-1：B7 (Box\<dyn PySink\>) 对大数组的影响被低估

**文档位置**：§6.2 B7 + §6.6

**设计声明**："B7 影响 ~20ns/字段，非主导"

**问题**：100 元素 array 的 B7 = 100 × 20ns = 2µs。P3 array parse batch 路径预测 ~6µs，B7 占 33%。若 B7 实际为 30-40ns（vtable + 堆分配 + 释放），则占 50-67%。

**判定**：非致命（P3 仍 >1.0x），但设计应诚实标注"对大数组 B7 占比可达 30-67%，Phase 17 栈式 sink 可获额外 1.3-2x 提升"。

##### 2.4 可证伪预测评估

| 预测 | 评估 | 备注 |
|------|------|------|
| P1 simple parse ~10x | ✅ 合理 | 7 FFI × ~40ns = ~300ns vs 3.0µs。量化推导充分 |
| P2 nested ~3.5-6x | ✅ 合理 | 按字段数线性推导，合理 |
| P3 array parse ~3-5x | ✅ 方向正确 | append 路径 FFI 计数合理；batch 路径的"1 FFI"对 parse 方向不适用（parse 无 batch extract） |
| P4 array build ~11.5x | ❌ **量化错误** | batch extract 不是 1 FFI（见 P-CRIT-1） |
| P5 geomean ≥2.0x | ⚠️ 依赖 P4 | 若 P4 实际为 ~5x（非 11.5x），geomean(10, 7.7, 6, 3.5, 3.2, 5) ≈ 5.3x，仍 ≥1.0x |

##### 2.5 反例声明评估

6 条反例声明方向正确。但 §6.4 中 "array build < 1.0x → 改用裸 CPython API" 的应对方案应提升为 **P4 的首选实现方案**，而非 fallback——因为 pyo3 Vec\<i64\> extract 本身就是 per-element 的。

##### 2.6 烟雾测试检查点

✅ 16.3 后 simple parse ≥1.0x 检查点位置正确。simple parse 不依赖 batch extract，可独立验证 PySink 设计有效性。

##### 2.7 常见陷阱逐项检查

| 陷阱 | 检查结果 |
|------|---------|
| leaf inner.parse() 瞬态 Value ~5ns？ | ⚠️ 对 Int/Float 合理（~5ns）；对 Bytes/String 低估（~20-50ns 堆分配）。不影响达标 |
| value_to_py_scalar 单 match 无类型链？ | ✅ 正确。§3.4 的实现确实是单 match arm per type |
| extract_batch_int_py 一次 C 调用？ | ❌ **错误**。pyo3 Vec\<i64\> 是 per-element（见 P-CRIT-1） |
| PySink Box 开销被低估？ | ⚠️ 对大数组（100 元素）占 33%（见 P-IMP-1） |
| ctx Value 树构建开销？ | ✅ 已计入。Struct 用 IndexMap insert(~30ns)，Array 用 Vec push(~5ns) |

---

#### 三、整体性：✅ 通过

1. **pyo3 版本统一**：construct-rs `pyo3 = { version = "0.22", optional = true, default-features = false }` + construct-py `pyo3 = "0.22"`。Cargo 合并 features 时统一版本。✅
2. **dispatch 完整性保证**：71-arm match on CompiledNode enum — Rust exhaustiveness checker 强制覆盖所有变体。新增变体漏实现 → 编译失败。✅
3. **CompiledExtension/External Py 路径**：§3.7 复用 `ext_parse()` → ProducedOutput → 适配 PySink。合理。✅
4. **feature 交互**：`Compressed` 变体 `#[cfg(feature = "compression")]` — dispatch 宏需同步门控。DEV 实现注意项。✅

---

#### 四、可行性：✅ 通过

1. **`#[cfg(feature = "python")]` 门控**：标准 Rust feature gating，无已知 pyo3 0.22 兼容性问题。✅
2. **PyCompiledExec trait + trait object**：`&mut dyn PySink` / `Box<dyn PySink>` 是标准 trait object 用法。无生命周期问题（Py\<PyDict\> 是 `'static`，`.unbind()` 后独立于 GIL token）。✅
3. **71-arm 宏 match 编译时间**：71 arm 不构成编译时间问题（Rust 支持数千 arm）。arm body 相同，LLVM 可优化。✅
4. **py: Python\<\'_\> threading**：从入口 `parse_bytes_raw` 获取 GIL token 后传递。标准 pyo3 模式，零 with_gil 开销。✅

---

#### 五、完备性：⚠️ 有条件通过（3 项必须补充）

##### 5.1 🔴 关键问题 C-CRIT-1：Container/ListContainer 包装机制未指定（BC-5）

**文档位置**：§3.3 PyDictSink2 + BC-5

**问题**：Python 原版 construct 的 parse 结果必须包装在 `Container`（dict 子类，支持 `result.field` 属性访问 + `search()` 方法）和 `ListContainer`（list 子类）中。当前 `conversions.rs:58-95` 的 `value_to_py` 对 List/Container 分支显式 import 并构造这些子类。

新设计的 `PyDictSink2::new_root_struct` 创建的是 **plain PyDict**，`into_root_py` 返回 **raw PyDict/PyList**，无 Container/ListContainer 包装。这将导致：
- `result.field_name` 属性访问失效（61 个 dataclass 测试可能失败）
- `result.search()` / `result.search_all()` 不可用
- Container 特有的 repr 格式丢失

设计在 P3 估算中提到了"ListContainer 包装 | call1 (root 时一次) | 2 | 200ns"，说明 ARCH 意识到了这个需求，但**未在接口设计中给出实现机制**。

**要求**：ARCH 必须在设计文档中补充 Container/ListContainer 包装的具体方案（建议：root sink 完成后一次性包装；或在 `new_root_struct` 时创建 Container 实例而非 plain PyDict）。

##### 5.2 🔴 关键问题 C-CRIT-2：build 方向 context._ 设置未指定（BC-3）

**文档位置**：§4 CompiledStruct::exec_build_py + BC-3

**问题**：当前 `compiled_ext.rs:190` 的 `build_from_raw` 执行：
```rust
if let Ok(embed) = input.as_value() {
    ctx.insert("_", embed);  // 供 IfThenElse(this._ == ...) 等条件构造使用
}
```

新设计的 `PyInput` trait **没有 `as_value()` 方法**（它有 `extract_scalar_py` / `sub_field_py` / `extract_batch_int_py`）。因此新 `build_from_raw` 如何设置 `ctx["_"]` 未定义。

选项：
(a) 在入口处对 root input 做一次缩减版 `py_to_value`（仅用于 `ctx["_"]`）——但这部分恢复了 B2；
(b) 跳过 `ctx["_"]` 设置——破坏 IfThenElse(this._ == ...) 等条件构造的正确性；
(c) 在 PyInput trait 增加 `as_value_for_ctx()` 方法——但这需要对 root dict 做全量转换。

**要求**：ARCH 必须明确指定新 `build_from_raw` 中 `ctx["_"]` 的处理方案。建议方案 (a)：仅在 root 入口做一次 `py_to_value` 用于 `ctx["_"]`，不传入 exec_build_py 链路（即 B2 的第一次 as_value 保留但只用于 ctx 初始化，不传入 Struct 的 per-field 读取）。

##### 5.3 🟡 重要问题 C-IMP-1：PyInput sub_field_py 需 getattr 回退（BC-7）

**文档位置**：§4.2 PyInput trait

**问题**：当前旧 `PyInput::get_field_bound`（py_input.rs:62-75）先试 `__getitem__`（dict），失败后试 `getattr`（dataclass/attribute）。新设计的 `PyInput::sub_field_py` 未指定是否保留此回退。

dataclass 实例的 build 输入依赖 getattr 访问。若 `sub_field_py` 仅用 `PyDict_GetItem`，dataclass build 会 FieldMissing。

**要求**：DEV 实现时 `sub_field_py` 必须保留 `get_item → getattr` 回退链（与旧 PyInput 一致）。

##### 5.4 🟡 重要问题 C-IMP-2：Adapter/Validator Py 路径模式未细化（BC-11）

**文档位置**：§3.7

**问题**：Adapter 的 decode 闭包对 Value → Value 变换。Py 路径中，wrapper 需要：(1) 通过 sub-PySink 获取 inner 的 (Value, PyObject)，(2) 对 Value 应用 decode，(3) 对 decode 后的 Value 重新 `value_to_py_scalar` 生成新 PyObject，(4) deposit 新 pair。步骤 (3) 的开销未在任何估算中计入。

**要求**：DEV 实现时注意 Adapter/Validator wrapper 的 exec_parse_py 需要"sub-sink → 取 Value → transform → 重新转 PyObject → deposit"模式。对非标量 decode 结果（如 Container），需走 `value_to_py_composite_fallback`。

##### 5.5 边界条件清单评估

| BC | 评估 | 备注 |
|----|------|------|
| BC-1 空 Struct | ✅ | PyDictSink2 root 空 dict |
| BC-2 嵌套 ctx 引用 | ✅ | ctx Value 正确插入 |
| BC-3 context._ 设置 | ❌ **见 C-CRIT-2** | 方案未指定 |
| BC-4 build None 输入 | ✅ | extract_scalar_py → Value::None |
| BC-5 Container 包装 | ❌ **见 C-CRIT-1** | 机制未指定 |
| BC-6 kw 注入 | ✅ | 入口逻辑保持 |
| BC-7 dataclass build | ⚠️ **见 C-IMP-1** | getattr 回退需保留 |
| BC-8 错误路径 PyObject 泄漏 | ✅ | sink drop → pyo3 decref |
| BC-9 CompiledExternal | ✅ | §3.7 覆盖 |
| BC-10 BigInt | ✅ | value_to_py_scalar BigInt 分支 |
| BC-11 Adapter/Validator | ⚠️ **见 C-IMP-2** | 模式需细化 |
| BC-12 batch extract 类型不匹配 | ✅ | 返回 None 回退 |
| BC-13 GreedyRange | ✅ | 循环 finish_item_py |

##### 5.6 Construct 类型覆盖评估

对照 `mod.rs` 的 71 个 CompiledNode 变体：
- **Leaf（16 种）**：§3.5 的 `impl_leaf_py_exec!` 宏覆盖全部 16 种。✅
- **Wrapper（Hex/HexDump/Renamed/Subconstruct/Const/Adapter/SymmetricAdapter/ExprAdapter/Validator/ExprValidator/Padded/Aligned/FixedSized/NamedTuple/TimestampAdapter/Default/Rebuild/Index/Computed/Enum/FlagsEnum/Mapping）**：需通过通用 wrapper 模式实现。设计 §3.7 提及但未逐一展开。⚠️ DEV 需注意每个 wrapper 的 Py 路径差异。
- **Composite（Struct/Sequence/Union/Select/FocusedSeq）**：§3.5 展示了 Struct 示例，其余类比。✅
- **Repetition（Array/ArrayExpr/GreedyRange/RepeatUntil）**：§4.5 展示了 Array 示例。✅
- **Lazy（Lazy/LazyStruct/LazyArray/Rebuffered）**：未显式提及。但编译树中 Lazy 是 eager delegation（mod.rs:608 注释），Py 路径应直接转发 inner。✅
- **Stream ops（Bitwise/Bytewise/Pointer/PointerExpr/Peek/RawCopy/Prefixed/Transformed/Restreamed/Compressed/Checksum/ByteSwapped/BitsSwapped）**：未显式提及。需"创建 sub-stream → 递归 exec_parse_py"模式。⚠️ DEV 需注意 sub-stream 场景下 `py: Python<'_>` 的传递。
- **Control flow（IfThenElse/Switch/Check/StopIf）**：未显式提及。需"求值条件 → 选择分支 → 递归 exec_parse_py"模式。✅
- **Escape-hatch（Dynamic/External）**：§3.7 显式覆盖。✅

**无遗漏的 Construct 类型**（所有 71 变体均有对应的实现路径，虽然部分需 DEV 根据通用模式推导）。

---

#### 六、检视结论

**总体结论：有条件通过（CONDITIONAL PASS）**

设计的核心架构（PySink 双产物、PyCompiledExec 独立 trait、per-field 类型已知 extract）是**正确的**，从架构层面能消除 B1-B6 的主要瓶颈。但存在 **3 项必须修正的问题**，需 ARCH 在 DEV 开始前以**设计澄清（design amendment）**形式补充（不需重新走完整 DESIGN_REVIEW，因架构不变）：

| 编号 | 级别 | 问题 | 要求 |
|------|------|------|------|
| P-CRIT-1 | 🔴 必修 | batch extract "单次 C 调用" 事实错误 | 修正 §4.5 + §6.3 P4 的 FFI 计数和耗时估算；明确 batch extract 实际是 per-element (~202 C 调用) |
| C-CRIT-1 | 🔴 必修 | Container/ListContainer 包装机制未指定 | 补充 root sink 完成后的包装方案 |
| C-CRIT-2 | 🔴 必修 | build 方向 context._ 设置未指定 | 补充新 build_from_raw 中 ctx["_"] 的处理方案 |

**2 项重要建议**（DEV 实现时必须注意，不需 ARCH 修改设计）：

| 编号 | 级别 | 问题 | DEV 注意事项 |
|------|------|------|-------------|
| P-IMP-1 | 🟡 注意 | B7 对大数组占 33% | 跟踪大数组性能；若不达标则优先实现栈式 sink |
| C-IMP-1 | 🟡 注意 | PyInput sub_field_py 需 getattr 回退 | 实现 sub_field_py 时保留 get_item → getattr 回退链 |
| C-IMP-2 | 🟡 注意 | Adapter/Validator Py 路径 | 实现 wrapper exec_parse_py 时注意"sub-sink → transform → 重新 deposit"模式 |

**进入 CODING 条件**：ARCH 提交上述 3 项必修设计澄清后，16.2 可开始 CODING。16.3 的烟雾测试检查点（simple parse ≥1.0x）是关键验证门——simple parse 不依赖 batch extract，可独立验证 PySink 核心设计。

---

### PM 烟雾测试记录（Checkpoint 2）

**测试时间**：2026-06-19
**测试命令**：`python tests/test_perf_comparison.py --iterations 5000`
**Python 环境**：3.14.2, construct 2.10.70

#### 结果

| 格式 | parse (Python) | parse (Rust) | parse 比率 | 预测 | build 比率 |
|------|---------------|-------------|-----------|------|-----------|
| simple (3字段) | 3.06µs | 1.58µs | **1.94x** | ~7.1x | 0.75x (16.4范围) |
| nested (2级) | 6.08µs | 3.98µs | **1.53x** | ~4.4x | 0.56x (16.4范围) |
| array (100元素) | 28.2µs | 51.5µs | 0.55x | ~3.1x | 0.16x (16.4范围) |

- **Geomean parse**: 1.172x ✅ ≥1.0x
- **Geomean build**: 0.401x ❌ (预期，16.4 范围)

#### 检查点判定

**simple parse ≥1.0x: ✅ PASS (1.94x)**

核心 PySink 双产物 + 零 round-trip 设计验证通过。parse 方向从 0.26x 提升到 1.94x（simple）/ 1.53x（nested）。

#### 低于预测的分析

实际值低于 ARCH 预测的几个可能原因（16.5 分析）：
1. Container/ListContainer 包装开销（每复合节点 ~120ns）可能被低估
2. B7 Box<dyn PySink> 开销（每字段 ~20-30ns）累积
3. Python 3.14 的 ABI3 兼容层可能有额外开销
4. array_heavy 0.55x 特别低——100 元素的 per-element PyLong + PyList_Append 开销可能主导，需在 16.5 详细分析

**结论**：烟雾测试 PASS，可继续 16.4（build 方向实现）。

### 备注
旧设计（方案 A）的瓶颈分析（B1-B4, FFI 调用计数）仍然有效，作为 ARCH 新设计的输入参考。
关键区别：旧设计试图优化 Value→PyObject 转换；新设计要消除这个转换本身。
新设计在 build 方向的预测（~7.7x-11.5x）远超旧设计（0.23x-0.33x 不达标），根本原因是将 PyInput/exec_build_py 引入 construct-rs 使类型已知 extract 成为可能。

**REV 检视补充备注**：
- pyo3 0.22.6 `Vec<T>` FromPyObject 源码（`types/sequence.rs:494-529`）已实证确认是 per-element 提取，非单次 C 调用。这是 P-CRIT-1 的依据。
- B1/B2/B3 瓶颈已对照源码逐行验证（py_sink.rs:294-306 / compiled_ext.rs:190 / mod.rs:1764 / conversions.rs:110-213）。
- 1515 纯 Rust 测试不受影响的判断基于编译矩阵分析（`#[cfg(feature = "python")]` 门控）。

---

## 子任务 16.2: Python-first 产出层基础设施（PySink / PyInput）

| 项目 | 内容 |
|------|------|
| 状态 | 待审查（CODE_REVIEW） |
| 负责人 | DEV |
| 开始时间 | 2026-06-19 |
| 完成时间 | 2026-06-19 |

### 操作日志
- [2026-06-19] [DEV] 阅读设计文档 §3.2-3.4（PySink/PyDictSink2/value_to_py_scalar）、§3.3.1（Container/ListContainer 包装）、§4.2-4.5.1（PyInput/PyDictInput/extract_batch_int_raw）；阅读 Python 原版 containers.py、binary.py；阅读 construct-py 旧实现 py_sink.rs/py_input.rs/conversions.rs 作为参考
- [2026-06-19] [DEV] 修改 `Cargo.toml`：添加 `pyo3 = { version = "0.22", optional = true, default-features = false }` + `python = ["dep:pyo3", "pyo3/num-bigint", "dep:num-bigint"]` feature；未启用 auto-initialize（并行测试死锁）
- [2026-06-19] [DEV] 修改 `compiled/mod.rs`：添加 `#[cfg(feature = "python")] pub mod py_sink; pub mod py_input;`
- [2026-06-19] [DEV] 创建 `compiled/py_sink.rs`（~970 行）：ContainerClasses（OnceLock 全局缓存 + 手动 Clone）、PySinkMode 枚举、PySink trait（含 into_pair 方法）、PyDictSink2 实现、value_to_py_scalar、value_to_py_composite_fallback、ensure_test_python、20 个单元测试
- [2026-06-19] [DEV] 创建 `compiled/py_input.rs`（~700 行）：PyInput trait（Debug bound）、PyDictInput（get_item→getattr 回退，C-IMP-1）、PyScalarInput、extract_batch_int_raw（裸 CPython API）、extract_scalar_short_circuit、20 个单元测试
- [2026-06-19] [DEV] 修复编译错误：ContainerClasses 手动 Clone（default-features=false 下 Py<PyAny> 无 Clone derive）、PyList_GET_SIZE/GET_ITEM→PyList_Size/GetItem、PyInput trait 加 Debug bound（unwrap_err 需要）、移除 #![cfg] 内层重复属性（clippy duplicated_attributes）、3.14→2.5（clippy approx_constant）、unwrap_or_else→unwrap_or（clippy unnecessary_lazy_evaluations）
- [2026-06-19] [DEV] 完成全部自检（见下方），所有测试通过

### DEV 自检结果

**编译检查**：
- `cargo build`（无 python feature）：✅ 零 error
- `cargo build --features python`：✅ 零 error
- `cargo clippy --all-targets`（无 python feature）：✅ 零 warning
- `cargo clippy --features python --all-targets`：✅ 零 warning
- `cargo fmt --check`：✅ 格式正确

**测试**：
- `cargo test --lib`（无 python feature）：✅ 1338 passed, 0 failed
- `cargo test --tests`（集成测试，无 python feature）：✅ 1338 + 30 + 6 + 7 + 12 = 1393 passed, 0 failed
- `cargo test --features python --lib compiled::py_ -- --test-threads=1`：✅ 40 passed (20 py_sink + 20 py_input), 0 failed
- 注：`--test-threads=1` 为环境要求（Python 3.14 + pyo3 0.22 GIL 初始化在并行测试下死锁），非代码问题

**接口对照**：
- PySink trait / PyDictSink2 / value_to_py_scalar：✅ 与设计文档 §3.2-3.4 签名一致
- PyInput trait / PyDictInput / extract_batch_int_raw：✅ 与设计文档 §4.2-4.5.1 签名一致
- ContainerClasses（§3.3.1 包装机制）：✅ obtain/fetch/new_container/new_listcontainer 全部实现
- 与已实现模块（sink.rs OutputSink / input.rs Input）接口风格一致：✅

**代码质量**：
- 无 unwrap()/expect() 在非测试代码中：✅
- 无 panic（非法输入返回 Err）：✅
- 无 TODO/FIXME：✅
- 无硬编码魔法数字：✅（提取类型顺序用注释标注，数值字面量均为测试固定值）
- 所有 pub 项有 /// 文档注释：✅
- 错误携带 path 字段：✅（py_err_to_construct / FieldMissing / TypeMismatch / Index 均含 path）

**parse/build 对称性**：✅ 本子任务为基础设施层（Sink = parse 方向产出，Input = build 方向读取），parse 产出与 build 读取构成对称的 FFI 直接路径。build→parse 还原验证将在 16.3/16.4 exec 层完成。

### 偏离设计文档的地方

1. **[设计质疑] sub_py_field / sub_py_item 增加 `py: Python<'_>` 参数**
   - 设计文档 §3.2 的 PySink trait 方法签名省略了 `py` 参数
   - 偏离原因：创建 Container/ListContainer 子类实例需要 GIL token（`pyo3::types::PyType::call` 等均需 `Python<'_>`），无法在持有 `Py<PyAny>` 的方法内隐式获取（避免每字段 `Python::with_gil` 开销）
   - 影响：调用方（exec_parse_py 层，16.3 实现）需传递已持有的 `py` token，无额外开销

2. **[设计质疑] PySink trait 新增 `into_pair()` 方法**
   - 设计文档的 7 方法 trait（deposit_leaf + sub_py_field/sub_py_item + finish_field_py/finish_item_py/finish_named_item_py + into_root_py）缺少 `into_pair()`
   - 偏离原因：`finish_*` 方法内部需要同时获取子节点的 Value（供 context）和 PyObject（供输出），需通过 trait 方法暴露 `(Value, PyObject)` 对。`into_root_py` 已通过默认实现委托给 `into_pair`
   - 影响：trait 增加一个方法，不破坏设计意图

3. **finish_field_py / finish_item_py 自动 Empty→Struct/Array 转换**
   - 设计文档未显式说明 Empty 状态下调用 finish_* 的行为
   - 实现：finish_* 在 sink 为 Empty 时隐式创建 Container/ListContainer（ensure_struct/ensure_array 私有方法）
   - 影响：调用方无需显式 begin_struct/begin_array，简化 exec 层逻辑

### 需要 PM 注意的事项

1. **Python 3.14 + pyo3 0.22 并行测试死锁**
   - 根因：系统 Python 为 3.14.2，pyo3 0.22 官方支持上限 3.13。需设置 `PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1` 环境变量方可编译
   - 现象：并行 `cargo test` 下 GIL 初始化竞争死锁；使用 `ensure_test_python()`（Once + prepare_freethreaded_python）+ `--test-threads=1` 可稳定通过
   - 建议：CI / 验收环境固定 `cargo test --features python -- --test-threads=1`，或在 `.cargo/config.toml` 中配置测试线程数

2. **construct_rust.lib.containers 在纯 cargo test 环境不可导入**
   - `ContainerClasses::obtain()` 在无法 import Container/ListContainer 时返回 None，sink 退化为 plain PyDict/PyList
   - 影响：测试中 into_root_py 返回普通 dict/list（无属性访问/search 方法）。真实 Python 环境下（通过 construct-py 包导入）行为完整
   - 16.3 端到端验证时需确保 construct-py 包可导入

### VET 代码审查报告

**审查日期**：2026-06-19
**审查范围**：`construct-rs/Cargo.toml`、`construct-rs/src/compiled/mod.rs`（py 模块声明）、`construct-rs/src/compiled/py_sink.rs`（960 行）、`construct-rs/src/compiled/py_input.rs`（708 行）
**对照设计**：`docs/refactor/模块设计-Python-first产出层.md` §3.2-3.4 + §3.3.1 + §4.2-4.5.1
**对照 Python 原版**：`construct/construct/lib/containers.py` (Container/ListContainer 子类定义)
**对照旧实现**：`construct-py/src/conversions.rs` (py_to_value / value_to_py 标量类型链)

#### 流程审计：✅ 通过

- ARCH 设计文档（`docs/refactor/模块设计-Python-first产出层.md`）：存在
- REV 设计检视报告：存在（含 3 项必修已修正 P-CRIT-1 / C-CRIT-1 / C-CRIT-2）
- 工作流管道顺序：正确（PENDING → DESIGNING → DESIGN_REVIEW → CODING → CODE_REVIEW）
- DEV 操作日志 + 自检结果：完整

#### 维度 1：逻辑正确性 — ✅ 通过

- **零 round-trip 设计**：`finish_field_py` / `finish_item_py` / `finish_named_item_py` 通过 `sub.into_pair(py)?` 消费子 sink 获得 `(Value, PyObject)` 对，Value 来自子 sink 的 parallel tree（ctx_fields/ctx_items），不走 py_to_value 回转。`pydictsink2_nested_struct_zero_round_trip` 测试验证 Container 返回是 parallel tree 而非 round-trip。✅
- **PySinkMode 状态机**：Empty → Leaf（deposit_leaf）/ Empty → Struct（finish_field_py via ensure_struct）/ Empty → Array（finish_item_py / finish_named_item_py via ensure_array）。`matches!(self.mode, PySinkMode::Empty)` 仅在 Empty 时转换，其他非法模式由调用方 match 的 `_ => Err(...)` 兜底。文档表 "Mode transitions" 列出的所有路径均有实现+测试覆盖。✅
- **value_to_py_scalar 覆盖**：对照 `value.rs` Value 枚举 10 变体，None/Bool/Int/UInt/BigInt/Float/Bytes/String 8 个标量均有 match arm；Container/List 走 `value_to_py_composite_fallback`（防御性，避免 panic，比设计文档原版"debug panic"更安全，符合 Rust 红线）。✅
- **Container/ListContainer 包装**：`ContainerClasses::obtain()` (OnceLock 全局缓存) + `new_container`/`new_listcontainer`，可用时 `cls.bind(py).call0()` 创建子类实例，不可用时降级 plain PyDict/PyList。对照 Python 原版 `containers.py:84` `class Container(dict)` + `__init__` 中 `self.__dict__ = self`，确认 Container() 空实例即可启用属性访问。✅
- **extract_batch_int_raw unsafe FFI**：GIL 由 `py: Python<'_>` 参数生命周期保证；`obj: &Bound<'_, PyAny>` 借用保证 list 不被 GC；`PyList_GetItem` 返回的 borrowed ref 仅在循环内立即用于 `PyLong_AsLongLong`（值拷贝），不跨迭代持有。SAFETY 注释充分。✅
- **PyDictInput getattr 回退（C-IMP-1）**：`get_field_bound` 先 `get_item`（dict）后 `getattr`（dataclass/attr），`pydictinput_sub_field_via_getattr_fallback` 测试通过 `type('O', (), {'x': 42})()` 验证。✅

#### 维度 2：行为一致性 — ⚠️ 有条件通过（1 项建议改进）

- **PySink trait 接口**：实现签名与设计文档 §3.2 基本一致，DEV 标注的 3 项偏离均合理（详见下方"设计偏离评估"）。
- **value_to_py_scalar vs 旧 value_to_py**：标量分支等价，性能更优（单 match arm，无类型检查链）。BigInt 分支正确通过 `num_bigint::BigInt::from(*i)` 中转（pyo3 0.22 不直接支持 i128 IntoPy），与旧 conversions.rs:49-52 完全一致。✅
- **extract_batch_int_raw vs 设计 §4.5.1**：设计用 `PyList_GET_SIZE`/`PyList_GET_ITEM` 宏，DEV 改用 `PyList_Size`/`PyList_GetItem` 函数形式（pyo3::ffi 不暴露宏）。功能等价，性能损失 ~2-5ns/调用（可忽略）。注释说明原因。✅
- **🟡 建议改进 1：extract_scalar_short_circuit 不支持 BigInt**
  - 旧 conversions.rs:122-124 的 py_to_value 通过 `extract::<BigInt>` 统一处理任意大 Python int，然后 narrow 到 Int/UInt/BigInt(i128)。
  - 新 `extract_scalar_short_circuit`（py_input.rs:197-225）仅探测 i64/u64，对超过 u64::MAX 的 Python int 返回 None → TypeMismatch。
  - **影响**：build 方向用户传入 >u64::MAX 的大整数时会失败。但场景罕见（多数格式用固定大小整数），且设计文档 §4.3 暗示 leaf exec_build_py 用类型已知 extract（16.4 实现），可能绕过此函数。
  - **建议**：在 16.4/16.5 补 `extract::<BigInt>` 分支或文档标注限制。**不阻塞 16.2 通过**（16.2 是基础设施层，BigInt build 端到端在 16.4 才集成）。

#### 维度 3：错误处理 — ✅ 通过

- 所有可能失败的操作（set_item / call_method1 / get_item / getattr / call0）均返回 Result 并 map_err 转 ConstructError。✅
- `py_err_to_construct` 将 PyErr 转 `ConstructError::Generic { path: String::new(), ... }`，path 由上层 `with_path_prefix` 填充（与 sink.rs/error.rs 现有模式一致）。✅
- FieldMissing / TypeMismatch / Index 错误变体使用正确，携带 path/field/expected/actual 字段。✅
- 无 panic 路径（match 完整，无 unwrap/expect 在非测试代码）。✅
- `ContainerClasses::obtain` 返回 None 时 `new_container`/`new_listcontainer` 降级为 plain PyDict/PyList，无 panic。✅
- `extract_batch_int_raw` 对非 list 输入、null item、PyLong_AsLongLong 溢出三种情况均返回 `Ok(None)` 让调用方退化，错误处理完备。✅

#### 维度 4：资源安全 — ✅ 通过

- **Py<PyAny> 引用计数**：`ContainerClasses::clone` 通过 `Python::with_gil(|py| c.clone_ref(py))` 正确 incref；`PyDictInput::from_bound` 通过 `obj.clone().unbind()` incref 并 detach。drop 时 pyo3 自动 decref。✅
- **Box<dyn PySink>**：通过 `sub.into_pair(py)?` 消费（self: Box<Self>），Box 自动 drop，无泄漏。✅
- **GIL 生命周期**：所有 py 方法接收 `py: Python<'_>`，从入口 threading；`ContainerClasses::clone` 内部 `with_gil` 在 GIL 已持有时为 no-op（pyo3 保证）。✅
- **unsafe 块**：`extract_batch_int_raw` 是唯一 unsafe，有详细 SAFETY 注释说明 GIL/borrow/mutation 三项保证。✅

#### 维度 5：API 一致性 — ✅ 通过

- 命名风格 snake_case，与 sink.rs / input.rs 一致。✅
- 所有 pub 项有 `///` 文档注释。✅
- 与设计文档 §3.2/§4.2 trait 签名一致（DEV 偏离已申报，详见下方评估）。✅
- 模块声明 `#[cfg(feature = "python")] pub mod py_sink / py_input;` 门控正确，纯 Rust 测试 1338 通过、Python feature 测试 40 通过。✅

#### 维度 6：边界条件 — ✅ 通过

- 空 Struct：`pydictsink2_into_root_py_empty_struct` 测试验证返回空 PyDict（len=0）。✅
- None 值 deposit_leaf：`value_to_py_scalar_none` + `pydictinput_extract_scalar_none` 验证。✅
- extract_batch_int_raw 非 list：`pydictinput_extract_batch_int_py_non_list_returns_none` 验证返回 None。✅
- extract_batch_int_raw 非整元素：`pydictinput_extract_batch_int_py_non_int_elements_returns_none` 验证混合 list 返回 None。✅
- PyDictInput getattr 回退：`pydictinput_sub_field_via_getattr_fallback` 验证。✅
- ContainerClasses OnceLock 缓存：`container_classes_obtain_is_cached` 通过指针相等验证。✅
- deposit_leaf on non-Empty：`pydictsink2_deposit_leaf_on_non_empty_errors` 验证报错。✅
- into_pair on Empty：`pydictsink2_into_pair_on_empty_errors` 验证报错。✅
- sub_index 越界：`pydictinput_sub_index_out_of_bounds` 验证 Index 错误。✅

#### 性能验证段 — ✅ 通过

- **value_to_py_scalar 单 match arm**：✅ py_sink.rs:586-606 是单 match，每变体一次 C API 调用，无 is_instance_of 链。
- **extract_batch_int_raw 裸 CPython API**：✅ py_input.rs:141-181 用 `pyo3::ffi` 直接调用 `PyList_Check`/`PyList_Size`/`PyList_GetItem`/`PyLong_AsLongLong`/`PyErr_Occurred`/`PyErr_Clear`，不走 pyo3 `extract::<Vec<i64>>` 抽象。
- **PySink finish_* 不调 with_gil**：✅ py 参数 threading（py_sink.rs 所有 trait 方法签名均含 `py: Python<'_>`）。
  - 注：`ContainerClasses::clone` 内部调 `with_gil`（在 sub_py_field/sub_py_item 的 `self.classes.clone()` 路径），但 GIL 已持有时为 no-op。**性能微优化机会**：改为 `clone_ref(py)` 直接用传入的 py 参数。详见建议改进 2。
- **PyDictSink2 不构建 Value 输出树**：✅ ctx_fields/ctx_items 是 parallel ctx（供 sink 自身 into_pair 返回 Value::Container/Value::List），输出为 PyObject 树（dict/list）。Value 树仅供 context 表达式求值，非输出。

#### Rust 编码红线检查 — ✅ 全部通过

- 禁止 unwrap/expect 在非测试代码：✅（grep 确认仅测试模块使用）
- 禁止 panic：✅
- 禁止 TODO/FIXME：✅
- 所有 pub 项有 /// 文档：✅
- 错误携带 path 字段：✅（多数为空字符串，由上层 with_path_prefix 填充，与现有模块一致）

#### 编译/测试验证（VET 复跑）

- `cargo build --features python`（设 PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1）：✅ 零 error
- `cargo clippy --features python --all-targets`：✅ 零 warning
- `cargo fmt --check`：✅ 通过
- `cargo test --lib`（无 python feature）：✅ 1338 passed
- `cargo test --features python --lib compiled::py_ -- --test-threads=1`：✅ 40 passed (20 py_sink + 20 py_input)

#### 设计偏离评估

| 编号 | 偏离 | VET 意见 | 理由 |
|------|------|---------|------|
| 1 | sub_py_field/sub_py_item 增加 `py: Python<'_>` 参数 | **接受** | 创建 Container/ListContainer 子类实例需 GIL token。当前实现用 `_py`（实际未使用，因 sub-sink 创建为 Empty，Container 在 ensure_struct/ensure_array 延迟创建），但作为面向未来的 trait 设计无害。建议未来若 classes.clone 改用 clone_ref(py) 即可激活此参数。 |
| 2 | PySink trait 新增 `into_pair() -> (Value, PyObject)` 方法 | **接受** | 设计文档 §3.3 示例 `sub.into_value_obj_pair(py)?` 已暗示需要此方法（仅未在 trait 定义中显式列出）。`into_root_py` 通过默认实现委托给 into_pair，不破坏设计意图。零 round-trip 机制的核心。 |
| 3 | finish_field_py / finish_item_py 在 Empty 状态隐式创建 Struct/Array（ensure_struct/ensure_array） | **接受** | 简化 exec 层（调用方无需显式 begin_struct）。文档表 "Mode transitions" 已列出 Empty → Struct/Array 转换。`pydictsink2_nested_struct_zero_round_trip` 测试验证此行为。替代方案（显式 begin_struct）会增加 exec 层复杂度，当前方案更优。 |

#### 建议改进项（不阻塞 16.2 通过，转 16.4/16.5 处理）

1. **🟡 extract_scalar_short_circuit 补 BigInt 分支**（行为一致性）
   - 位置：py_input.rs:197-225
   - 现状：仅探测 i64/u64，>u64::MAX 的 Python int 返回 None → TypeMismatch
   - 对照：旧 conversions.rs:122-124 用 extract::<BigInt> 统一处理
   - 建议：在 u64 分支后补 `if let Ok(b) = obj.extract::<num_bigint::BigInt>() { ... narrow to BigInt(i128) ... }`，或在 16.4 leaf exec_build_py 中对 BigInt leaf 直接用类型化方法绕过此函数
   - 阻塞性：否（build 方向 BigInt 罕见，16.2 未端到端集成）

2. **🟢 ContainerClasses::clone 改用 clone_ref(py)**（性能微优化）
   - 位置：py_sink.rs:87-97
   - 现状：`Python::with_gil(|py| ... clone_ref(py) ...)`，虽然 GIL 已持有时为 no-op，但仍有一次线程局部 GIL 状态检查
   - 建议：增加 `pub fn clone_ref(&self, py: Python<'_>) -> Self` 方法，sub_py_field/sub_py_item 用传入的 py 参数调用
   - 阻塞性：否（影响 ~5-10ns/字段）

3. **🟢 finish_item_py / finish_named_item_py 用 PyList_Append 替代 call_method1("append")**（性能微优化）
   - 位置：py_sink.rs:527-529, 549-551
   - 现状：通过 Python 方法解析调用 list.append，~25ns/调用
   - 建议：用 `pyo3::ffi::PyList_Append` 直接 C API（对 ListContainer 子类等价），~10ns/调用
   - 阻塞性：否（影响 ~15ns/元素，对 100 元素 array ~1.5µs）

#### VET 结论

**通过（PASS）** — 可进入子任务 16.3（Leaf + Composite exec_parse_py 实现 + 烟雾测试）。

**依据**：
- 流程审计完整（设计/检视/开发/审查四阶段齐备）
- 6 个维度全部通过或条件通过（仅 1 项 BigInt 建议改进，不阻塞）
- 3 项设计偏离评估全部接受（合理且有充分理由）
- 编译/clippy/fmt/40 测试 + 1338 纯 Rust 测试全部通过
- 性能验证段 4 项硬要求全部满足
- Rust 编码红线 5 项全部通过

**遗留事项**（16.4/16.5 处理时参考）：
- 建议改进 1（BigInt）应在 16.4 exec_build_py 端到端集成时验证或补充
- 建议改进 2/3（性能微优化）可在 16.5 性能验收阶段视基准数据决定是否实施
- DEV 标注的 Python 3.14 + pyo3 0.22 测试线程限制需在 CI 配置中固化（`--test-threads=1` 或 `.cargo/config.toml`）

---

## 子任务 16.3: Leaf + Composite exec_parse_py 实现

| 项目 | 内容 |
|------|------|
| 状态 | 待审查（CODE_REVIEW） |
| 负责人 | DEV |
| 开始时间 | 2026-06-19 |
| 完成时间 | 2026-06-19 |

### 操作日志
- [2026-06-19] [DEV] 阅读设计文档 §3.5/§3.6/§3.7、py_sink.rs、py_input.rs、compiled/mod.rs（71 变体模式）、compiled_ext.rs
- [2026-06-19] [DEV] 创建 `construct-rs/src/compiled/py_exec.rs`，实现全部 71 个 CompiledNode 变体的 `PyCompiledExec` trait
- [2026-06-19] [DEV] 实现 `exec_parse_py_dispatch` + `exec_build_py_dispatch` 71-arm match 函数（编译时穷举检查）
- [2026-06-19] [DEV] 为 `CompiledNode` 添加 blanket `impl PyCompiledExec`（委托到 dispatch，使 `Box<CompiledNode>` auto-deref 可用）
- [2026-06-19] [DEV] 编写 5 个单元测试：simple struct / nested struct / array / sequence / build stub error
- [2026-06-19] [DEV] 修改 `construct-py/src/compiled_ext.rs::parse_bytes_raw` 切换到新 Py 路径（`PyDictSink2` + `exec_parse_py_dispatch`）
- [2026-06-19] [DEV] 修改 `construct-py/Cargo.toml` 添加 `features = ["python"]` 到 construct 依赖
- [2026-06-19] [DEV] 执行自检：cargo build/clippy/fmt/test 全部通过
- [2026-06-19] [VET] CODE_REVIEW 驳回（REJECT → CODING）：B1 阻塞问题——CompiledLazyArray::exec_parse_py 错误使用 `deposit_leaf`（line 1446），导致 count==1 静默降级为标量、count>=2 第二次迭代报错
- [2026-06-19] [DEV] 修复 B1：将 CompiledLazyArray 循环体中的 `sub.into_pair` + `sink.deposit_leaf` 改为 `sink.finish_item_py(py, sub)?`，与 CompiledArray / CompiledGreedyRange / CompiledArrayExpr 的正确模式一致（py_exec.rs:1444-1457）
- [2026-06-19] [DEV] 全量排查其他 repetition 变体：确认 CompiledArray(1069)/CompiledArrayExpr(1103)/CompiledGreedyRange(1145)/CompiledRepeatUntil(1187) 均正确使用 `finish_item_py`；CompiledSelect(1247)/CompiledPeek(1838)/CompiledSwitch(2006) 的 `deposit_leaf` 使用场景正确（非循环内单次调用或 leaf 语义）。唯一 bug 为 CompiledLazyArray，已修复
- [2026-06-19] [DEV] 新增 3 个 LazyArray 边界单元测试（py_exec.rs tests 模块）：`lazy_array_parse_count_0_returns_empty_list`（根 Array sink，空 list）/ `lazy_array_parse_count_1_returns_single_element_list`（Struct 字段，验证返回单元素 list 而非标量）/ `lazy_array_parse_count_3_returns_three_element_list`（Struct 字段，验证多元素正常）
- [2026-06-19] [DEV] 回归有效性验证：临时还原 bug 后，count_1 测试因 `'int' object cannot be converted to 'Sequence'` 失败（标量降级）、count_3 测试因 `deposit_leaf on non-Empty sink` 失败（第二次迭代报错），精确复现 VET 描述的两个症状；恢复修复后 3 个测试全部通过
- [2026-06-19] [DEV] 修复轮自检通过：cargo build --features python 零 error / cargo clippy --features python 零 warning / cargo fmt --check 通过 / cargo test --features python --lib compiled::py_exec（单线程）8 passed / cargo test --lib（纯 Rust）1338 passed

### DEV 自检结果

**编译检查**：
- `cargo build` (construct-rs 无 python feature): ✅ 零 error
- `cargo build --features python` (construct-rs): ✅ 零 error、零 warning
- `cargo build` (construct-py): ✅ 零 error

**Clippy**：
- `cargo clippy --features python` (construct-rs): ✅ 零 warning
- `cargo clippy` (construct-py): ✅ 零 warning

**格式**：
- `cargo fmt --check` (construct-rs): ✅
- `cargo fmt --check` (construct-py): ✅

**测试**：
- `cargo test --lib` (construct-rs 无 python feature): ✅ 1338 passed, 0 failed
- `cargo test --features python --lib compiled::py_exec` (construct-rs): ✅ 5 passed, 0 failed
- `cargo test --features python --lib compiled::py_sink` (construct-rs): ✅ 20 passed, 0 failed
- `cargo test compiled_ext` (construct-py): ✅ 13 passed, 0 failed
- construct-py 全量测试：215 passed, 2 failed（2 个 failures 在 `constructs_adapter::tests::mapping_*`，属预存问题，与 16.3 无关）

**接口对照**：
- ✅ `PyCompiledExec` trait 签名与设计文档 §3.5 一致
- ✅ `exec_parse_py_dispatch` / `exec_build_py_dispatch` 与设计文档 §3.7 一致
- ✅ `parse_bytes_raw` 切换到 PyDictSink2 + dispatch，与设计文档 §3.6 一致

**代码质量**：
- ✅ 无 unwrap()/expect() 在非测试代码中
- ✅ 无 panic — 非法输入返回 Err
- ✅ 无 TODO/FIXME
- ✅ 无硬编码魔法数字（ALIGNED_MIN_MODULUS 等使用常量）
- ✅ 所有 pub 项有 /// 文档注释
- ✅ 错误携带 path 字段（通过 `.with_path_prefix()` / `.with_path_prefix(PARSE_PATH)`）

**parse/build 对称性**：
- ✅ exec_parse_py 全部 71 变体已实现
- ⚠️ exec_build_py 全部返回 stub error（Phase 16.4 实现）

### 设计偏离

1. **`CompiledExternal::ProducedOutput::Object` 路径限制**：construct-rs 无法从 `Box<dyn Any>` 中 downcast `Py<PyAny>`（opaque box）。返回 descriptive error，标注为 16.3 限制。`ProducedOutput::Value` 路径完全支持。
   - **原因**：`ProducedOutput::Object(Box<dyn Any>)` 在 construct-rs 内无法知道 Box 内的类型，跨 crate downcast 不可行
   - **影响**：仅影响使用 `ext_parse` 返回 `ProducedOutput::Object` 的自定义 CompiledExtension（极少见）
   - **后续**：如需要，Phase 16.5 可通过在 CompiledExtension trait 上添加 `ext_parse_py` 方法解决

### 实现规模
- `py_exec.rs`: ~2500 行（含测试），覆盖全部 71 个 CompiledNode 变体
- `compiled_ext.rs`: 修改 ~20 行（parse_bytes_raw 切换到新路径）
- `Cargo.toml`: 修改 1 行（添加 python feature）

### 备注
- 测试需 `--test-threads=1`（Python 解释器初始化的线程安全限制，与 16.2 相同）
- construct-py 的 2 个预存 mapping 测试失败与 16.3 无关（`constructs_adapter.rs` 未被修改）

### VET 代码审查报告

**审查日期**：2026-06-19
**审查范围**：
- `construct-rs/src/compiled/py_exec.rs`（2475 行，含测试）— 主要审查对象
- `construct-rs/src/compiled/mod.rs`（py_exec 模块声明 + 2 行）
- `construct-py/src/compiled_ext.rs`（parse_bytes_raw 切换，~20 行 diff）
- `construct-py/Cargo.toml`（python feature 添加，1 行）

**对照设计**：`docs/refactor/模块设计-Python-first产出层.md` §3.1-§3.7
**对照 Python 原版**：`construct/construct/core.py`（Switch/Peek/Prefixed/RepeatUntil 等行为）
**对照 Value 路径**：`construct-rs/src/compiled/mod.rs` 全部 71 个 `impl CompiledExec`

#### 流程审计：✅ 通过

- ARCH 设计文档（`docs/refactor/模块设计-Python-first产出层.md`）：存在（1586 行，9 节）
- REV 设计检视报告：存在（含 3 项必修 P-CRIT-1/C-CRIT-1/C-CRIT-2 + 3 项 DEV 注意 P-IMP-1/C-IMP-1/C-IMP-2，全部已修正）
- 工作流管道顺序：正确（16.1 ACCEPTED → 16.2 ACCEPTED → 16.3 CODING → CODE_REVIEW）
- DEV 操作日志 + 自检结果：完整

#### 维度 1：逻辑正确性 — ❌ 不通过（1 项阻塞）

**🔴 阻塞问题 B1：CompiledLazyArray::exec_parse_py 使用 `sink.deposit_leaf` 而非 `sink.finish_item_py`，导致运行时错误和错误输出**

- **位置**：`py_exec.rs:1432-1452`
- **代码**：
  ```rust
  for i in 0..self.count {
      let mut sub = sink.sub_py_item(py)?;
      match self.subcon.exec_parse_py(py, stream, ctx, sub.as_mut()) {
          Ok(()) => {
              let (value, obj) = sub.into_pair(py)?;
              sink.deposit_leaf(py, value, obj)?;  // ← 错误：应为 finish_item_py
          }
          ...
      }
  }
  ```
- **行为分析**（对照 `py_sink.rs:461-472` 的 deposit_leaf 仅在 Empty 模式成功）：
  - count == 0：sink 保持 Empty，父节点 finish_field_py 调 into_pair 时报错
  - count == 1：sink 从 Empty 转为 Leaf（持有单个标量），父节点收到标量而非 list — **静默错误输出**
  - count >= 2：第二次迭代 deposit_leaf 在 Leaf 模式上调用 → 返回 Err("deposit_leaf on non-Empty sink") — **运行时错误**
- **对照 Value 路径**（`mod.rs:3362-3379`）：Value 路径正确使用 `sink.push_item(value)?`（数组语义）。Py 路径应对应使用 `sink.finish_item_py(py, sub)?`。
- **对照 Python 原版**：Python LazyArray 解析产出 list。当前 Py 路径产出标量或报错，行为不一致。
- **测试覆盖**：py_exec.rs 的 5 个测试均未覆盖 LazyArray。该 bug 未被任何测试发现。
- **修复建议**：
  ```rust
  for i in 0..self.count {
      ctx.insert(REPETITION_INDEX_KEY, Value::UInt(i as u64));
      let mut sub = sink.sub_py_item(py)?;
      match self.subcon.exec_parse_py(py, stream, ctx, sub.as_mut()) {
          Ok(()) => sink.finish_item_py(py, sub)?,  // 正确：finish_item_py
          Err(e) => return Err(e.with_path_prefix(&format!("[{i}]"))),
      }
  }
  ```
  并补充 LazyArray(count>=2) 的单元测试。

**其他正确性验证（全部通过）**：
- ✅ Leaf macro（17 变体）：`inner.parse → value_to_py_scalar → deposit_leaf`，与设计 §3.5 一致
- ✅ CompiledStruct/Sequence/Array/ArrayExpr/GreedyRange：sub_py_field/sub_py_item → exec_parse_py → finish_field_py/finish_item_py 模式正确
- ✅ CompiledAdapter/ExprAdapter/SymmetricAdapter：C-IMP-2 模式（sub-sink → decode → deposit_pair_as_leaf）正确
- ✅ CompiledValidator/ExprValidator：check 失败时返回 Err ✓
- ✅ CompiledConst：parse 后比较 raw vs self.value，不匹配返回 Const error ✓
- ✅ CompiledEnum/FlagsEnum/Mapping：decmap/flags/decmapping 查找正确，无匹配时回退 / 报错
- ✅ CompiledComputed/Index/Check/StopIf：context-only 节点，无 stream 读取，与 Value 路径一致
- ✅ CompiledPadded/Aligned/FixedSized：padding/modulus 计算正确，ALIGNED_MIN_MODULUS=2 常量
- ✅ CompiledBitwise/Bytewise/ByteSwapped/BitsSwapped/Transformed/Restreamed/Compressed：sub-stream 创建 + inner 解析 + deposit 正确
- ✅ CompiledPointer/PointerExpr：seek + fallback restore + 内部 exec_parse_py 正确
- ✅ CompiledPeek/Prefixed/RawCopy/Checksum：与 Value 路径逐行对照一致
- ✅ CompiledIfThenElse/Switch：条件求值 + 分支选择 + 无默认时 deposit None（与 Value 路径 Switch 一致，对照 Python `default=Pass` 等价）
- ✅ CompiledSelect：fallback seek + 多候选回退 + StopField 传播正确
- ✅ CompiledUnion：subcontext + parsefrom 转发位置正确
- ✅ CompiledFocusedSeq：focused_value 提取 + deposit_pair_as_leaf 正确
- ✅ CompiledDynamic/LazyBound：逃生舱路径（inner.parse → deposit_pair_as_leaf），非热路径
- ✅ CompiledNamedTuple/TimestampAdapter：本地函数副本（named_tuple_decode_value / timestamp_to_secs_nanos）与 mod.rs 私有函数逐行一致

#### 维度 2：行为一致性（Python 对照） — ❌ 不通过（因 B1）

- LazyArray 行为不一致（见 B1）。
- 其余变体行为与 Python 原版及 Value 路径一致（已在维度 1 逐项验证）。

#### 维度 3：零 round-trip 验证 — ✅ 通过

- ✅ CompiledStruct::finish_field_py（`py_sink.rs:495-520`）：`sub.into_pair(py)` 返回的 Value 来自 parallel ctx tree，**不走 py_to_value**。`dict.set_item(name, obj)` 单次 FFI。对照 `pydictsink2_nested_struct_zero_round_trip` 测试（16.2）。
- ✅ CompiledArray::finish_item_py：sub 直接传给 finish_item_py，内部 `list.call_method1("append", (obj,))` 单次 FFI。无 round-trip。
- ✅ Leaf 实现：`inner.parse()` 返回瞬态标量 Value（~5ns），`value_to_py_scalar` 单 match arm 单 C API 调用，`deposit_leaf` 同时存 Value（供 ctx）和 PyObject（供输出）。**零 round-trip**。

#### 维度 4：Wrapper 模式正确性（C-IMP-2） — ✅ 通过

- ✅ CompiledAdapter：`exec_parse_inner_py` → 取 (raw, _obj) → `decode(raw)` → `deposit_pair_as_leaf(decoded)`。_obj 被丢弃，decoded 重新 value_to_py_scalar。符合 C-IMP-2 模式。
- ✅ CompiledValidator：check 失败 `(self.check)(&raw, ctx)?` 返回 Err ✓。成功时**复用** obj（`deposit_leaf(py, raw, obj)`），比 Adapter 更高效（无需重新转换）✓。
- ✅ CompiledComputed：`(self.func)(ctx)?` 求值（func 是 ComputeFunc 闭包，与 mod.rs:2835 Value 路径一致；设计文档"CompiledExpr"为术语简化，实际是 ComputeFunc）。求值后 deposit_pair_as_leaf ✓。

#### 维度 5：Stream ops 正确性 — ✅ 通过

- ✅ CompiledBitwise：`stream.read_remaining() → bytes2bits → sub_stream → exec_parse_inner_py → deposit`。sub-stream 创建正确。
- ✅ CompiledPointer：`fallback → seek_to_offset(offset) → inner.exec_parse_py → seek(fallback)`。与 Value 路径（mod.rs:3521-3533）逐行一致。
- ✅ CompiledPrefixed：`length_field 解析 → include_length 修正 → read_bytes(length) → sub_stream → inner 解析 → deposit`。与 Value 路径（mod.rs:3697-3714）一致。

#### 维度 6：入口切换正确性 — ✅ 通过

- ✅ `compiled_ext.rs::parse_bytes_raw`（line 148-161）正确使用 `exec_parse_py_dispatch` + `PyDictSink2::new_root_struct`。
- ✅ Container/ListContainer 包装在 root 级别正确（new_root_struct 通过 ContainerClasses::new_container 创建 Container 子类）。
- ✅ kw 参数注入保留：`ctx.insert("_parsing", true) / "_building"=false / "_sizing"=false` + `for (key, value) in kw { ctx.insert(...) }`。
- ✅ 错误路径：`Err(e) => Err(rust_err_to_py(py, e.with_path_prefix(PARSE_PATH)))`，partial sink 被 drop，pyo3 decref。
- ✅ `build_from_raw`（line 178-）**未被修改**，仍使用旧 exec_build 路径（16.4 将切换）。`crate::py_input::PyInput` 和 `crate::py_sink::PyDictSink` 旧 import 仍保留（build 路径仍需）。仅移除了 `PyDictSink` 的 parse 用途 import。
- ✅ Cargo.toml：`features = ["byteorder", "bigint", "compression", "python"]`，正确启用 python feature。

#### 维度 7：CompiledExternal 限制评估 — 🟡 接受为已知限制

- **DEV 偏离**：`ProducedOutput::Object(_)` 路径返回 descriptive error，不实现。
- **设计 §3.7 原文**："若 Object 则直接 deposit"。
- **VET 评估**：
  - Value 路径（mod.rs:4142-4158）对 Object 路径也只是 `sink.set_scalar(Value::None)`（占位），并未真正支持。
  - 在 `python` feature 下，construct-rs 已有 pyo3，理论上可 `downcast_ref::<Py<PyAny>>()`，但需假设 box 内类型为 `Py<PyAny>`（不确定）。
  - Py 路径 error out 比 Value 路径 silent None **更安全**（fail-fast）。
  - 仅影响使用 `ext_parse` 返回 `ProducedOutput::Object` 的自定义 CompiledExtension（极少见）。
- **结论**：接受为 16.3 已知限制，不阻塞。建议 16.5 可通过 CompiledExtension trait 增加 `ext_parse_py` 方法彻底解决。
- **Value 路径完全可用** ✓。

#### 维度 8：71 变体覆盖完整性 — ✅ 通过

- ✅ `exec_parse_py_dispatch`（line 2164-2246）：71 个非 cfg 变体 + 1 个 `#[cfg(feature = "compression")] CompiledNode::Compressed` = 72 arm。Rust 编译器强制穷举检查。
- ✅ 对照 `mod.rs:988-1148` CompiledNode enum 定义：72 个 variant 全部覆盖（含 Compressed cfg-gated）。
- ✅ **所有 72 个变体的 exec_parse_py 都有实际实现**（不是 stub）。Stub error 仅出现在 exec_build_py（Phase 16.4 实现）。
- ✅ 实现统计：50 个 `impl PyCompiledExec for` 块（4 个 macro 应用 27 类型 + 45 个 standalone + 1 个 CompiledNode blanket）= 73 个 impl（72 variant 类型 + CompiledNode 自身）。

#### 维度 9：Rust 编码红线 — ✅ 通过

- ✅ 禁止 unwrap/expect/panic 在非测试代码：grep 确认非测试段 0 次 unwrap / 0 次 expect / 0 次 panic
- ✅ 禁止 unsafe：py_exec.rs 非测试段 0 次（unsafe 仅在 py_input.rs 的 extract_batch_int_raw，16.2 已审查）
- ✅ 禁止 TODO/FIXME：0 次
- ✅ 所有 pub 项有 `///` 文档注释（PyCompiledExec trait + 两个 dispatch 函数 + BUILD_PY_STUB_MESSAGE 常量）
- ✅ 错误携带 path 字段：大部分错误通过 `with_path_prefix` 由上层（Struct/Array/dispatch）填充；leaf 错误由父 composite 添加 name/[index] 前缀；root 错误在 compiled_ext.rs 添加 PARSE_PATH 前缀。Const/Adapter/Validator 等闭包错误沿用 Value 路径模式（不在此层加前缀，由父层处理）。

#### 维度 10：边界条件 — ⚠️ LazyArray 边界（见 B1）

- ❌ LazyArray count=0/1/>=2 边界全部错误（见 B1）
- ✅ 其他变体边界条件与 Value 路径一致（已在维度 1 验证）

#### 编译/Clippy/Fmt/Test 复跑（VET）

- `cargo build --features python`：✅ 零 error（PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1，Python 3.14 环境）
- `cargo clippy --features python --all-targets`：✅ 零 warning
- `cargo fmt --check`（construct-rs + construct-py）：✅ 通过
- `cargo test --lib`（无 python feature）：✅ 1338 passed
- `cargo test --features python --lib compiled::py_exec -- --test-threads=1`：✅ 5 passed
- 注：construct-py 全量测试因 Python 3.14 + pyo3 0.22 GIL 初始化超时（5min 限制），未在本次审查中复跑。DEV 报告 215 passed / 2 failed（mapping 预存问题），与 16.3 无关。

#### 性能验证段（设计假设对照）

- ✅ PySink finish_field_py 单次 set_item（无 py_to_value）：已在维度 3 验证
- ✅ Leaf 单次 value_to_py_scalar（无类型链）：已在维度 3 验证
- ✅ Wrapper 模式（Adapter 等）走 deposit_pair_as_leaf，单次 value_to_py_scalar + 1 set_item
- ⚠️ CompiledRepeatUntil 每元素额外 1 次 value_to_py_scalar（见下方建议改进 2），非设计假设但影响大数组性能
- ⚠️ 烟雾测试检查点（simple parse ≥1.0x）**未在本次审查执行**——需 DEV 修复 B1 后由 PM 执行 `test_perf_comparison.py -n 1000`

#### VET 结论

**驳回至 CODING（REJECT）**

**依据**：
- 1 项阻塞问题 B1（CompiledLazyArray 错误使用 deposit_leaf）导致运行时错误和静默错误输出
- 流程审计通过；其余 9 个维度（零 round-trip / Wrapper 模式 / Stream ops / 入口切换 / 71 变体覆盖 / Rust 红线 / 边界）全部通过或有条件通过
- 修复 B1 后，需补充 LazyArray 单元测试（count=0/1/>=2 三种边界）
- 修复 B1 后，由 PM 执行烟雾测试检查点（simple parse ≥1.0x）

**DEV 需修复的问题**：

| 编号 | 级别 | 问题 | 修复建议 |
|------|------|------|---------|
| B1 | 🔴 阻塞 | CompiledLazyArray::exec_parse_py 使用 deposit_leaf（line 1446） | 改为 `sink.finish_item_py(py, sub)?` 并移除 `sub.into_pair` 行；补充 3 个 LazyArray 边界测试 |

**遗留事项（16.4/16.5 处理，不阻塞 16.3 重审）**：

| 编号 | 级别 | 问题 | 建议处理阶段 |
|------|------|------|-------------|
| I1 | 🟡 重要 | CompiledRepeatUntil（line 1207-1209）重新 value_to_py_scalar 已有 _obj，浪费 1 次转换/元素 | 16.4/16.5 性能验收时优化：直接用 `_obj`，移除 value_to_py_scalar 行 |
| I2 | 🟡 重要 | CompiledExternal ProducedOutput::Object 路径未实现（设计 §3.7 要求"直接 deposit"） | 16.5 通过 CompiledExtension::ext_parse_py 解决 |
| I3 | 🟡 次要 | named_tuple_decode_value / timestamp_to_secs_nanos 本地副本（与 mod.rs 私有函数重复） | 16.5 可将原函数改为 pub(crate) 消除重复 |
| I4 | 🟢 微优 | CompiledRebuffered/Adapter 丢弃 _obj 后重新转换（对包装 composite 的 wrapper 有额外开销） | 16.5 性能验收时视基准数据决定 |

### VET B1 修复重审报告（针对性复审）

**重审日期**：2026-06-19
**重审范围**（仅 B1 相关）：
- `construct-rs/src/compiled/py_exec.rs:1432-1472`（CompiledLazyArray::exec_parse_py 修复）
- `construct-rs/src/compiled/py_exec.rs:2437-2510`（3 个 LazyArray 边界测试）
- 其他 repetition 变体（CompiledArray / ArrayExpr / GreedyRange / RepeatUntil）的排查确认
**复审性质**：驳回后针对性重审（仅 B1 相关部分 + 是否引入新问题）

#### 重审要点 1：B1 修复正确性 — ✅ 通过

- **修复代码**（`py_exec.rs:1440-1459`）：循环体现在为
  ```rust
  for i in 0..self.count {
      ctx.insert(REPETITION_INDEX_KEY, Value::UInt(i as u64));
      let mut sub = sink.sub_py_item(py)?;
      match self.subcon.exec_parse_py(py, stream, ctx, sub.as_mut()) {
          Ok(()) => sink.finish_item_py(py, sub)?,   // ← 已改用 finish_item_py
          Err(e) => return Err(e.with_path_prefix(&format!("[{i}]"))),
      }
  }
  ```
- **原 `sub.into_pair` + `sink.deposit_leaf` 两行已移除**：✅（不再有 round-trip，不再有 Leaf 模式误用）
- **注释充分**（1450-1454 行）：明确说明为什么不能使用 `deposit_leaf`，并描述 count>=2 报错 / count==1 标量降级两种失败症状，对未来维护者是有效的回归防护。
- **错误路径保持一致**：`Err(e) => return Err(e.with_path_prefix(&format!("[{i}]")))`，与原实现相同，path 拼接正确。

#### 重审要点 2：与 CompiledArray / ArrayExpr / GreedyRange 模式一致性 — ✅ 通过

逐行对照四个 repetition 变体的循环体：

| 变体 | 行号 | 父 sink 调用 | discard 守卫 | 评价 |
|------|------|------------|-------------|------|
| CompiledArray | 1083 | `sink.finish_item_py(py, sub)?` | `if !self.discard` | 基准模式 |
| CompiledArrayExpr | 1127 | `sink.finish_item_py(py, sub)?` | 无 | 无 discard 字段 |
| CompiledGreedyRange | 1161 | `sink.finish_item_py(py, sub)?` | `if !self.discard` | 基准模式 |
| **CompiledLazyArray（修复后）** | **1455** | **`sink.finish_item_py(py, sub)?`** | **无** | ✅ 与 ArrayExpr 完全一致 |

- **discard 守卫省略的正当性**：`mod.rs:630-635` 确认 `CompiledLazyArray` 结构体仅含 `count: usize` + `subcon: Box<CompiledNode>`，**无 `discard` 字段**。因此省略 discard 守卫与 ArrayExpr 一致，语义正确，非遗漏。
- **Value 路径对照**（`mod.rs:3369-3377`）：Value 路径用 `sink.push_item(value)?`（数组语义，无 discard）。Py 路径 `finish_item_py` 与之语义对应。✅

#### 重审要点 3：3 个边界测试有效性 — ✅ 通过（精确覆盖 bug 场景）

| 测试 | 行号 | 场景 | 断言 | 是否真正复现/防护 bug |
|------|------|------|------|---------------------|
| `lazy_array_parse_count_0_returns_empty_list` | 2467 | count=0，root Array sink | `list.is_empty()` | ✅ 防护空数组路径（loop 不进入，sink 保持 Array 模式） |
| `lazy_array_parse_count_1_returns_single_element_list` | 2483 | count=1，Struct 包裹字段 "items"，data=[0x07] | `extract::<Vec<i64>>` == `vec![7]` | ✅ **关键回归测试**：pre-fix 标量降级会使 `extract::<Vec<i64>>` 在 int 上失败（`'int' object cannot be converted to 'Sequence'`） |
| `lazy_array_parse_count_3_returns_three_element_list` | 2498 | count=3，Struct 包裹，data=[1,2,3] | `extract::<Vec<i64>>` == `vec![1,2,3]` | ✅ **关键回归测试**：pre-fix 第二次迭代 `deposit_leaf on non-Empty sink` 报错 |

- **count=1 测试设计精妙**：通过 `Struct.field("items", ...)` 包裹 + `map["items"].extract::<Vec<i64>>()`，让"标量降级"以类型不匹配的形式暴露（int 无法 extract 成 Vec），而非依赖弱断言。这是对静默错误输出的有效捕获。
- **count=3 测试**：选择 3（>2）而非恰好 2，确保不仅第二次、且后续迭代也通过，覆盖更完整。
- **DEV 回归验证可信**：DEV 报告临时还原 bug 后 count_1 测试因标量降级失败、count_3 测试因 Leaf 模式拒绝失败，精确复现 VET 描述的两个症状，证明测试对 bug 敏感（非假绿）。
- **辅助函数 `parse_via_py_root_array`（2450-2464）**：为 count=0 场景提供 root Array sink（因 count=0 无字段包裹时 Struct 路径不适用），设计合理。

#### 重审要点 4：其他 repetition 变体排查可信度 — ✅ 通过

VET 独立复核全部 `deposit_leaf` 调用点（grep 共 19 处，扣除注释 9 处，实际代码 10 处）：

| 行号 | 位置 | 上下文 | 是否 loop-on-parent-sink 误用 |
|------|------|--------|------------------------------|
| 162 | `deposit_pair_as_leaf` helper | 单次辅助函数 | ❌ 否（helper，调用方单次） |
| 204 | leaf 宏 | leaf 节点单次 deposit | ❌ 否 |
| 389 / 431 | Adapter/Validator wrapper | 单次 transform 后 deposit | ❌ 否 |
| 613 / 642 | Computed/Index 等 | 单次 ctx 求值后 deposit | ❌ 否 |
| 1211 | RepeatUntil | **对 `item_sink`（每次迭代新建的 sub-sink）** | ❌ 否（sub-sink 每次迭代 Empty，合法） |
| 1247 | Select | **非循环**，deposit 后立即 `return Ok(())`（1248） | ❌ 否（单次选择） |
| 1847 | Peek | 单次窥探后 deposit | ❌ 否 |
| 2015 | Switch | 单次分支选择后 deposit | ❌ 否 |

**结论**：全文件无其他"在父 sink 上循环调用 deposit_leaf"的误用。DEV 排查报告中"唯一 bug 为 CompiledLazyArray，已修复"的结论**属实可信**。

> 注：RepeatUntil（1210-1212）的模式是"每次迭代新建 `item_sink = sink.sub_py_item(py)` → 对 item_sink deposit_leaf → 对父 sink finish_item_py"，这是合法的（item_sink 总是 Empty）。该变体的性能优化点（I1：_obj 丢弃后重新 value_to_py_scalar）已在上一轮审查中记录为遗留事项，不影响正确性。

#### 重审要点 5：是否引入新问题 — ✅ 无

- **未修改其他代码**：本次 fix 仅改动 LazyArray 循环体 2 行 + 新增 3 个测试函数 + 1 个辅助函数，无其他文件改动。
- **接口未变**：`finish_item_py` 是 PySink trait 既有方法（16.2 已审查通过），无新依赖。
- **编译/clippy/fmt/test 全绿**（VET 复跑）：
  - `cargo build --features python`：✅ 零 error
  - `cargo clippy --features python --all-targets`：✅ 零 warning
  - `cargo fmt --check`：✅ 通过
  - `cargo test --features python --lib compiled::py_exec -- --test-threads=1`：✅ **8 passed**（含新增 3 个边界测试 + 原 5 个）
  - `cargo test --lib`（无 python feature）：✅ 1338 passed（无回归）

#### VET 重审结论

**通过（PASS）** — B1 阻塞问题已正确修复，边界测试有效，无新引入问题。

**结论摘要**：
1. **B1 修复正确**：`finish_item_py` 替代 `deposit_leaf`，移除 `sub.into_pair`，与 CompiledArray/ArrayExpr/GreedyRange 模式一致；discard 守卫省略有据（LazyArray 无 discard 字段）。✅
2. **边界测试有效**：count=0/1/3 三测试精确覆盖原 bug 的两个症状（标量降级 + 第二次迭代报错），且 count=1 测试通过类型提取使静默错误变为可检测。✅
3. **排查可信**：全文件 deposit_leaf 调用点 VET 独立复核，确认无其他 loop-on-parent-sink 误用。✅
4. **无新问题**：仅改动 LazyArray + 测试，编译/clippy/fmt/1346 测试全绿。✅

**PM 可执行下一步**：烟雾测试检查点（`test_perf_comparison.py`，simple parse ≥1.0x vs Python 原版）。

**遗留事项**（沿用上一轮，16.4/16.5 处理，不阻塞本次通过）：I1（RepeatUntil _obj 复用）/ I2（External Object 路径）/ I3（本地函数副本）/ I4（wrapper _obj 丢弃）。
