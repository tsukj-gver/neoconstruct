# Phase 16 过程记录：Python-first 产出层重写

## 子任务 16.1: Python-first 产出层设计

| 项目 | 内容 |
|------|------|
| 状态 | 已通过（ACCEPTED） |
| 负责人 | ARCH |
| 开始时间 | 2026-06-19 |
| 完成时间 | 2026-06-19 |

### 操作日志
- [2026-06-19] [PM] 创建 Phase 16 总纲（FFI 批量化优化方向）
- [2026-06-19] [ARCH] 产出方案 A 设计（`模块设计-FFI批量化优化.md`），在 construct-rs 不引入 pyo3 的约束下优化转换层
- [2026-06-19] [PM] 用户质疑项目定位：本项目从头到尾是 Python 项目，Rust 是内核实现手段不是独立产品
- [2026-06-19] [PM] 确认：§0 项目定位修正——pyo3 是核心依赖，Value 不出现在关键路径
- [2026-06-19] [PM] AGENTS.md / 总设计文档 / 顶层架构设计 三份核心文件写入 §0 项目定位
- [2026-06-19] [PM] Phase 16 总纲重写为"Python-first 产出层重写"方向，旧方案 A 废弃
- [2026-06-19] [PM] 分派 ARCH 以 Python-first 视角重新设计产出层
- [2026-06-19] [ARCH] 阅读全部必读文件：compiled/mod.rs（CompiledNode 71v + CompiledExec）、sink.rs（OutputSink + ValueSink）、input.rs（Input + ValueInput）、Cargo.toml×2、py_sink.rs（PyDictSink）、py_input.rs（PyInput）、conversions.rs（788行）、compiled_ext.rs（CompiledSchemaHolder）、旧方案 A 瓶颈分析、Phase 16 总纲、performance-gate skill
- [2026-06-19] [ARCH] 完成 Python-first 产出层设计，产出 `docs/refactor/模块设计-Python-first产出层.md`（~1200 行，9 节 + 2 附录）
- [2026-06-19] [REV] 完成设计检视，结论：有条件通过。核心架构正确（PySink 双产物、PyCompiledExec 独立 trait、per-field 类型已知 extract），但识别 3 项必修问题（P-CRIT-1/C-CRIT-1/C-CRIT-2）+ 3 项 DEV 注意事项（P-IMP-1/C-IMP-1/C-IMP-2）
- [2026-06-19] [ARCH] 完成 3 项必修设计澄清修正（详见下方"ARCH 设计澄清修正"段）。修正后维持 ≥1.0x 性能预测（geomean ~4.6x，保守下界 ~2.5x）。待 PM 确认进入 CODING。

### ARCH 设计说明

**核心技术决策**：
1. **pyo3 作为 construct-rs 的 optional feature**（方案 A，非合并 crate）：PySink/exec_parse_py 全在 construct-rs 内，可访问编译树内部字段，与 CompiledExtension 无缝集成。纯 Rust 测试（1515 个）通过 `cargo test`（无 python feature）零影响保留。
2. **新增 PyCompiledExec trait + 宏生成 dispatch**（非修改 CompiledExec、非泛型化 OutputSink）：Value 路径 trait 零修改；Py 路径独立 trait + 71-arm 宏 match。
3. **PySink 双产物设计**（Value for ctx + PyObject for output）：leaf 一次解码同时产出标量 Value（供 context 表达式）和 PyObject（供输出），彻底消除 B1 round-trip。
4. **build 方向 per-field 类型已知 extract + schema-guided batch extract**：消除 B2（双重全量转换）和 B3（类型检查链）。100 元素 int array build 从 638 FFI 降至 ~5 FFI。

**性能假设摘要**（满足 performance-gate Checkpoint 1）：
- 识别 8 个瓶颈来源（B1-B8，含旧设计的 B1-B4 + 新增 B5-B8）
- 可证伪预测：simple parse ~10x / simple build ~7.7x / array parse ~3-5x / array build ~11x (batch)；geomean(6 场景) ≥2.0x（保守）
- 反例声明：6 条，每条有明确应对方案
- 验证方法：test_perf_comparison.py 分阶段验证 + 烟雾测试检查点（16.3 后 simple parse ≥1.0x）

**对现有代码的影响**：
- construct-rs：新增 3 文件（~800-1200 行），Cargo.toml + mod.rs 小改，现有代码零修改
- construct-py：compiled_ext.rs 重写入口，conversions.rs 从 788 行缩减至 ~250 行，py_sink.rs/py_input.rs 删除/deprecated
- 公共 Python API 不变；1515 纯 Rust 测试不变；61 dataclass 测试行为等价

**子任务拆分**：16.2（基础设施）→ 16.3（parse_py + 烟雾测试）→ 16.4（build_py + batch extract）→ 16.5（清理 + 验收）

### ARCH 设计澄清修正（回应 REV 3 项必修问题）

**修正日期**：2026-06-19
**修正文档**：`docs/refactor/模块设计-Python-first产出层.md`

#### P-CRIT-1 修正：extract_batch_int_py "单次 C 调用" 事实错误

**修正内容**：
1. §4.5 重写：承认 pyo3 `extract::<Vec<i64>>` 是 per-element（~202 C 调用/100 元素），非单次 C 调用
2. 新增 §4.5.1：**裸 CPython API**（`PyList_GET_SIZE` + `PyList_GET_ITEM` 宏 + `PyLong_AsLongLong`）作为首选实现方案，绕过 pyo3 `PySequence_GetItem` 方法解析开销
3. §4.5.2 修正 FFI 对比表：batch extract ~101 C 调用（非 ~2），耗时 ~3.5-4µs（非 ~500ns）
4. §6.3 P4 修正：array build 从 ~11.5x 修正为 ~5.4x（仍达 ≥4.0x 目标）
5. §6.4 反例声明修正：裸 API 已设为首选而非 fallback
6. §6.6 可选精化修正：裸 API 标注为 batch extract 首选
7. §4.4 B2 消除表修正：~2 FFI → ~101 FFI（降幅 99.7% → 84%，仍消除 B2/B3）

**batch extract 真实优势**（非 ~100x FFI 减少）：~2-2.5x 加速来自消除 per-element Box<dyn PyInput> + vtable（B7）+ 宏替代方法解析。

#### C-CRIT-1 修正：Container/ListContainer 包装机制未指定

**修正内容**：
1. 新增 §3.3.1「Container/ListContainer 包装机制」：选定**从创建起使用 Container/ListContainer 作为底层存储**
2. 关键洞察：Container 是 dict 子类、ListContainer 是 list 子类，PyDict_SetItem/PyList_Append 可直接作用，无需额外复制
3. PySinkMode 字段类型修正：`Py<PyDict>`/`Py<PyList>` → `Py<PyAny>`（支持子类）
4. 新增 `ContainerClasses` 结构 + `obtain()` 方法：root sink 获取类引用一次，sub-sink Clone 共享；可用 static OnceLock 全局缓存使 import 摊薄至 ~0
5. 新增 `sub_struct()`/`sub_array()` 方法：创建 Container/ListContainer 子类实例
6. 性能影响：每个复合节点 +120ns（call0 ~150ns 替代 PyDict::new ~30ns）；import 经 OnceLock 缓存后 ~0ns

**P1-P3 预测修正**：simple parse ~10x → ~7.1x；nested parse ~6x → ~4.4x；array parse ~3.2-4.6x → ~3.1x。全部仍 ≥1.0x。

#### C-CRIT-2 修正：build 方向 context._ 设置未指定

**修正内容**：
1. 新增 §4.6「build 方向 context._ 处理（浅转换方案）」
2. 选定方案 c：**入口处浅转换（非递归，仅 root 标量字段）**
3. 实现 `shallow_py_to_value_for_ctx()`：对 dict 提取顶层标量字段，嵌套 dict/list 存为空占位符（不递归）
4. 成本 O(root_scalar_fields) ~80-150ns（vs 全量转换 ~30µs for array）
5. build_from_raw 入口整合：`ctx.insert("_", shallow_py_to_value_for_ctx(py, obj))`
6. 已知限制：`this._.nested.deep_field` 深层引用返回空占位（罕见模式）；基准格式不用 this._，无影响；若有 dataclass 测试失败，Phase 17 可用 Context 支持 PyObject（选项 e）解决

**P1/P2 build 预测修正**：simple build ~7.7x → ~5.4x；nested build ~3.5x → ~3.1x。全部仍 ≥1.0x。

#### 修正后性能预测汇总

| 预测 | 原设计 | 修正后 | 目标 | 达标 |
|------|--------|--------|------|------|
| P1 simple parse | ~10x | ~7.1x | ≥1.0x | ✅ |
| P1 simple build | ~7.7x | ~5.4x | ≥1.0x | ✅ |
| P2 nested parse | ~6x | ~4.4x | ≥1.0x | ✅ |
| P2 nested build | ~3.5x | ~3.1x | ≥1.0x | ✅ |
| P3 array parse | ~3.2-4.6x | ~3.1x | ≥1.0x | ✅ |
| P4 array build | ~11.5x | ~5.4x | ≥4.0x | ✅ |
| P5 geomean(6) | ~5.8x | **~4.6x** | ≥1.0x | ✅ |

> 保守下界（所有开销被低估 2x）：geomean ≥2.5x。S-PERF-1 达标置信度高。

### REV 设计检视报告

**检视文档**：`docs/refactor/模块设计-Python-first产出层.md`（1210 行）
**检视日期**：2026-06-19
**检视范围**：延续性 / 性能（增强清单）/ 整体性 / 可行性 / 完备性

---

#### 一、延续性：✅ 通过

已验证（对照源码）：

1. **CompiledExec trait 零修改** — `construct-rs/src/compiled/mod.rs:75-120` 的 trait 定义完全不动。新 `PyCompiledExec` trait 是平行新增，通过 `#[cfg(feature = "python")]` 门控。
2. **CompiledNode enum (71 变体) 零修改** — `mod.rs:981-1142` 的 enum 定义不动。新 dispatch 函数消费同一个 enum。
3. **OutputSink / ValueSink / Input / ValueInput 零修改** — `sink.rs` / `input.rs` 全文审查确认。ValueSink 保留用于纯 Rust 测试。
4. **1515 纯 Rust 测试不受影响** — 编译矩阵正确：无 `python` feature 时 `py_sink.rs` / `py_input.rs` / `py_exec.rs` 不参与编译（`#![cfg(feature = "python")]` + `#[cfg(feature = "python")] pub mod`）。
5. **Python 用户 API 签名不变** — `parse_bytes_py(data, **kw)` / `build_from_py(obj, **kw)` 签名不变。仅 `*_raw` 内部实现切换。
6. **CompiledSchema / SchemaCompiler / Stream / Context / ConstructError** — 均共享不变。

**结论**：设计从 Phase 11-15 平滑演进，无破坏性变更。

---

#### 二、性能：⚠️ 有条件通过（3 项必须修正）

##### 2.1 瓶颈覆盖评估

B1-B8 识别覆盖了当前路径的主要瓶颈来源，对照源码逐项验证：
- **B1**（finish_field round-trip）：✅ 证实。`py_sink.rs:294-306` 的 Object 分支确有 `py_to_value` 逆向转换。
- **B2**（build 双重 as_value）：✅ 证实。`compiled_ext.rs:190` 一次 + `mod.rs:1764` 一次。
- **B3**（7× 类型链）：✅ 证实。`conversions.rs:110-213` 确有 None→Bool→BigInt→Float→Bytes→ByteArray→String→List→Dict→dataclass 的长链。
- **B4-B8**：✅ 合理识别。

**未列入但存在的新瓶颈来源**：
- **U1（已计入但量化不准）**：leaf `inner.parse()` 对 `Value::Bytes` / `Value::String` 的堆分配（~20-50ns，非 ~5ns）。不影响达标但影响估算精度。

##### 2.2 🔴 关键问题 P-CRIT-1：extract_batch_int_py "单次 C 调用" 声明事实错误

**文档位置**：§4.5 + §6.3 P4

**设计声明**：
> "items extract_batch_int_py | **1** extract(Vec\<i64\>) | 1 | ~500ns"
> "pyo3 对 Vec\<i64\> 的批量 extract 是单次 C 调用遍历 CPython list 内部数组，~5ns/元素"

**实际行为**（pyo3 0.22.6 源码 `types/sequence.rs:494-529` 已验证）：
```rust
fn extract_sequence<'py, T>(obj: &Bound<'py, PyAny>) -> PyResult<Vec<T>> {
    let seq = ...; // PySequence_Check
    let mut v = Vec::with_capacity(seq.len().unwrap_or(0));
    for item in seq.iter()? {       // per-element: PySequence_GetItem × N
        v.push(item?.extract::<T>()?);  // per-element: PyLong_AsLongLong × N
    }
    Ok(v)
}
```

**实际 FFI 计数（100 元素）**：~202 次 C 调用（1 PySequence_Check + 1 PySequence_Length + 100 PySequence_GetItem + 100 PyLong_AsLongLong），而非 1 次。实际耗时 ~3-5µs 而非 ~500ns。

**对 P4 预测的影响**：
| | 设计声明 | 修正估算 |
|---|---------|---------|
| batch extract FFI | 1 | ~202 |
| batch extract 耗时 | ~500ns | ~3-5µs |
| array build 总计 | ~2.6µs | ~5-6µs |
| vs Python ~30µs | ~11.5x | ~5-6x |

**判定**：P4 ≥4.0x 目标仍可能达标（5-6x > 4x），但余量大幅缩窄。设计声称的 batch extract 相对 per-index 的 "~100x FFI 优势" 实际为 "~0-1.2x"（两者都是 per-element）。batch 的真实优势仅在于避免 per-element trait object dispatch 和更好的缓存局部性。

**要求**：ARCH 必须修正 §4.5 和 §6.3 P4 的 batch extract 分析。选项：
(a) 更正为 per-element 估算，承认 batch 优势主要来自避免 `dyn PyInput` dispatch；或
(b) 将 batch extract 改为裸 CPython API 手动循环（`PyList_GET_ITEM` 宏 + `PyLong_AsLongLong`），明确这是绕过 pyo3 抽象的方案；或
(c) 将 array build 优化降级为 Phase 17 候选（如 numpy frombuffer）。

##### 2.3 🟡 重要问题 P-IMP-1：B7 (Box\<dyn PySink\>) 对大数组的影响被低估

**文档位置**：§6.2 B7 + §6.6

**设计声明**："B7 影响 ~20ns/字段，非主导"

**问题**：100 元素 array 的 B7 = 100 × 20ns = 2µs。P3 array parse batch 路径预测 ~6µs，B7 占 33%。若 B7 实际为 30-40ns（vtable + 堆分配 + 释放），则占 50-67%。

**判定**：非致命（P3 仍 >1.0x），但设计应诚实标注"对大数组 B7 占比可达 30-67%，Phase 17 栈式 sink 可获额外 1.3-2x 提升"。

##### 2.4 可证伪预测评估

| 预测 | 评估 | 备注 |
|------|------|------|
| P1 simple parse ~10x | ✅ 合理 | 7 FFI × ~40ns = ~300ns vs 3.0µs。量化推导充分 |
| P2 nested ~3.5-6x | ✅ 合理 | 按字段数线性推导，合理 |
| P3 array parse ~3-5x | ✅ 方向正确 | append 路径 FFI 计数合理；batch 路径的"1 FFI"对 parse 方向不适用（parse 无 batch extract） |
| P4 array build ~11.5x | ❌ **量化错误** | batch extract 不是 1 FFI（见 P-CRIT-1） |
| P5 geomean ≥2.0x | ⚠️ 依赖 P4 | 若 P4 实际为 ~5x（非 11.5x），geomean(10, 7.7, 6, 3.5, 3.2, 5) ≈ 5.3x，仍 ≥1.0x |

##### 2.5 反例声明评估

6 条反例声明方向正确。但 §6.4 中 "array build < 1.0x → 改用裸 CPython API" 的应对方案应提升为 **P4 的首选实现方案**，而非 fallback——因为 pyo3 Vec\<i64\> extract 本身就是 per-element 的。

##### 2.6 烟雾测试检查点

✅ 16.3 后 simple parse ≥1.0x 检查点位置正确。simple parse 不依赖 batch extract，可独立验证 PySink 设计有效性。

##### 2.7 常见陷阱逐项检查

| 陷阱 | 检查结果 |
|------|---------|
| leaf inner.parse() 瞬态 Value ~5ns？ | ⚠️ 对 Int/Float 合理（~5ns）；对 Bytes/String 低估（~20-50ns 堆分配）。不影响达标 |
| value_to_py_scalar 单 match 无类型链？ | ✅ 正确。§3.4 的实现确实是单 match arm per type |
| extract_batch_int_py 一次 C 调用？ | ❌ **错误**。pyo3 Vec\<i64\> 是 per-element（见 P-CRIT-1） |
| PySink Box 开销被低估？ | ⚠️ 对大数组（100 元素）占 33%（见 P-IMP-1） |
| ctx Value 树构建开销？ | ✅ 已计入。Struct 用 IndexMap insert(~30ns)，Array 用 Vec push(~5ns) |

---

#### 三、整体性：✅ 通过

1. **pyo3 版本统一**：construct-rs `pyo3 = { version = "0.22", optional = true, default-features = false }` + construct-py `pyo3 = "0.22"`。Cargo 合并 features 时统一版本。✅
2. **dispatch 完整性保证**：71-arm match on CompiledNode enum — Rust exhaustiveness checker 强制覆盖所有变体。新增变体漏实现 → 编译失败。✅
3. **CompiledExtension/External Py 路径**：§3.7 复用 `ext_parse()` → ProducedOutput → 适配 PySink。合理。✅
4. **feature 交互**：`Compressed` 变体 `#[cfg(feature = "compression")]` — dispatch 宏需同步门控。DEV 实现注意项。✅

---

#### 四、可行性：✅ 通过

1. **`#[cfg(feature = "python")]` 门控**：标准 Rust feature gating，无已知 pyo3 0.22 兼容性问题。✅
2. **PyCompiledExec trait + trait object**：`&mut dyn PySink` / `Box<dyn PySink>` 是标准 trait object 用法。无生命周期问题（Py\<PyDict\> 是 `'static`，`.unbind()` 后独立于 GIL token）。✅
3. **71-arm 宏 match 编译时间**：71 arm 不构成编译时间问题（Rust 支持数千 arm）。arm body 相同，LLVM 可优化。✅
4. **py: Python\<\'_\> threading**：从入口 `parse_bytes_raw` 获取 GIL token 后传递。标准 pyo3 模式，零 with_gil 开销。✅

---

#### 五、完备性：⚠️ 有条件通过（3 项必须补充）

##### 5.1 🔴 关键问题 C-CRIT-1：Container/ListContainer 包装机制未指定（BC-5）

**文档位置**：§3.3 PyDictSink2 + BC-5

**问题**：Python 原版 construct 的 parse 结果必须包装在 `Container`（dict 子类，支持 `result.field` 属性访问 + `search()` 方法）和 `ListContainer`（list 子类）中。当前 `conversions.rs:58-95` 的 `value_to_py` 对 List/Container 分支显式 import 并构造这些子类。

新设计的 `PyDictSink2::new_root_struct` 创建的是 **plain PyDict**，`into_root_py` 返回 **raw PyDict/PyList**，无 Container/ListContainer 包装。这将导致：
- `result.field_name` 属性访问失效（61 个 dataclass 测试可能失败）
- `result.search()` / `result.search_all()` 不可用
- Container 特有的 repr 格式丢失

设计在 P3 估算中提到了"ListContainer 包装 | call1 (root 时一次) | 2 | 200ns"，说明 ARCH 意识到了这个需求，但**未在接口设计中给出实现机制**。

**要求**：ARCH 必须在设计文档中补充 Container/ListContainer 包装的具体方案（建议：root sink 完成后一次性包装；或在 `new_root_struct` 时创建 Container 实例而非 plain PyDict）。

##### 5.2 🔴 关键问题 C-CRIT-2：build 方向 context._ 设置未指定（BC-3）

**文档位置**：§4 CompiledStruct::exec_build_py + BC-3

**问题**：当前 `compiled_ext.rs:190` 的 `build_from_raw` 执行：
```rust
if let Ok(embed) = input.as_value() {
    ctx.insert("_", embed);  // 供 IfThenElse(this._ == ...) 等条件构造使用
}
```

新设计的 `PyInput` trait **没有 `as_value()` 方法**（它有 `extract_scalar_py` / `sub_field_py` / `extract_batch_int_py`）。因此新 `build_from_raw` 如何设置 `ctx["_"]` 未定义。

选项：
(a) 在入口处对 root input 做一次缩减版 `py_to_value`（仅用于 `ctx["_"]`）——但这部分恢复了 B2；
(b) 跳过 `ctx["_"]` 设置——破坏 IfThenElse(this._ == ...) 等条件构造的正确性；
(c) 在 PyInput trait 增加 `as_value_for_ctx()` 方法——但这需要对 root dict 做全量转换。

**要求**：ARCH 必须明确指定新 `build_from_raw` 中 `ctx["_"]` 的处理方案。建议方案 (a)：仅在 root 入口做一次 `py_to_value` 用于 `ctx["_"]`，不传入 exec_build_py 链路（即 B2 的第一次 as_value 保留但只用于 ctx 初始化，不传入 Struct 的 per-field 读取）。

##### 5.3 🟡 重要问题 C-IMP-1：PyInput sub_field_py 需 getattr 回退（BC-7）

**文档位置**：§4.2 PyInput trait

**问题**：当前旧 `PyInput::get_field_bound`（py_input.rs:62-75）先试 `__getitem__`（dict），失败后试 `getattr`（dataclass/attribute）。新设计的 `PyInput::sub_field_py` 未指定是否保留此回退。

dataclass 实例的 build 输入依赖 getattr 访问。若 `sub_field_py` 仅用 `PyDict_GetItem`，dataclass build 会 FieldMissing。

**要求**：DEV 实现时 `sub_field_py` 必须保留 `get_item → getattr` 回退链（与旧 PyInput 一致）。

##### 5.4 🟡 重要问题 C-IMP-2：Adapter/Validator Py 路径模式未细化（BC-11）

**文档位置**：§3.7

**问题**：Adapter 的 decode 闭包对 Value → Value 变换。Py 路径中，wrapper 需要：(1) 通过 sub-PySink 获取 inner 的 (Value, PyObject)，(2) 对 Value 应用 decode，(3) 对 decode 后的 Value 重新 `value_to_py_scalar` 生成新 PyObject，(4) deposit 新 pair。步骤 (3) 的开销未在任何估算中计入。

**要求**：DEV 实现时注意 Adapter/Validator wrapper 的 exec_parse_py 需要"sub-sink → 取 Value → transform → 重新转 PyObject → deposit"模式。对非标量 decode 结果（如 Container），需走 `value_to_py_composite_fallback`。

##### 5.5 边界条件清单评估

| BC | 评估 | 备注 |
|----|------|------|
| BC-1 空 Struct | ✅ | PyDictSink2 root 空 dict |
| BC-2 嵌套 ctx 引用 | ✅ | ctx Value 正确插入 |
| BC-3 context._ 设置 | ❌ **见 C-CRIT-2** | 方案未指定 |
| BC-4 build None 输入 | ✅ | extract_scalar_py → Value::None |
| BC-5 Container 包装 | ❌ **见 C-CRIT-1** | 机制未指定 |
| BC-6 kw 注入 | ✅ | 入口逻辑保持 |
| BC-7 dataclass build | ⚠️ **见 C-IMP-1** | getattr 回退需保留 |
| BC-8 错误路径 PyObject 泄漏 | ✅ | sink drop → pyo3 decref |
| BC-9 CompiledExternal | ✅ | §3.7 覆盖 |
| BC-10 BigInt | ✅ | value_to_py_scalar BigInt 分支 |
| BC-11 Adapter/Validator | ⚠️ **见 C-IMP-2** | 模式需细化 |
| BC-12 batch extract 类型不匹配 | ✅ | 返回 None 回退 |
| BC-13 GreedyRange | ✅ | 循环 finish_item_py |

##### 5.6 Construct 类型覆盖评估

对照 `mod.rs` 的 71 个 CompiledNode 变体：
- **Leaf（16 种）**：§3.5 的 `impl_leaf_py_exec!` 宏覆盖全部 16 种。✅
- **Wrapper（Hex/HexDump/Renamed/Subconstruct/Const/Adapter/SymmetricAdapter/ExprAdapter/Validator/ExprValidator/Padded/Aligned/FixedSized/NamedTuple/TimestampAdapter/Default/Rebuild/Index/Computed/Enum/FlagsEnum/Mapping）**：需通过通用 wrapper 模式实现。设计 §3.7 提及但未逐一展开。⚠️ DEV 需注意每个 wrapper 的 Py 路径差异。
- **Composite（Struct/Sequence/Union/Select/FocusedSeq）**：§3.5 展示了 Struct 示例，其余类比。✅
- **Repetition（Array/ArrayExpr/GreedyRange/RepeatUntil）**：§4.5 展示了 Array 示例。✅
- **Lazy（Lazy/LazyStruct/LazyArray/Rebuffered）**：未显式提及。但编译树中 Lazy 是 eager delegation（mod.rs:608 注释），Py 路径应直接转发 inner。✅
- **Stream ops（Bitwise/Bytewise/Pointer/PointerExpr/Peek/RawCopy/Prefixed/Transformed/Restreamed/Compressed/Checksum/ByteSwapped/BitsSwapped）**：未显式提及。需"创建 sub-stream → 递归 exec_parse_py"模式。⚠️ DEV 需注意 sub-stream 场景下 `py: Python<'_>` 的传递。
- **Control flow（IfThenElse/Switch/Check/StopIf）**：未显式提及。需"求值条件 → 选择分支 → 递归 exec_parse_py"模式。✅
- **Escape-hatch（Dynamic/External）**：§3.7 显式覆盖。✅

**无遗漏的 Construct 类型**（所有 71 变体均有对应的实现路径，虽然部分需 DEV 根据通用模式推导）。

---

#### 六、检视结论

**总体结论：有条件通过（CONDITIONAL PASS）**

设计的核心架构（PySink 双产物、PyCompiledExec 独立 trait、per-field 类型已知 extract）是**正确的**，从架构层面能消除 B1-B6 的主要瓶颈。但存在 **3 项必须修正的问题**，需 ARCH 在 DEV 开始前以**设计澄清（design amendment）**形式补充（不需重新走完整 DESIGN_REVIEW，因架构不变）：

| 编号 | 级别 | 问题 | 要求 |
|------|------|------|------|
| P-CRIT-1 | 🔴 必修 | batch extract "单次 C 调用" 事实错误 | 修正 §4.5 + §6.3 P4 的 FFI 计数和耗时估算；明确 batch extract 实际是 per-element (~202 C 调用) |
| C-CRIT-1 | 🔴 必修 | Container/ListContainer 包装机制未指定 | 补充 root sink 完成后的包装方案 |
| C-CRIT-2 | 🔴 必修 | build 方向 context._ 设置未指定 | 补充新 build_from_raw 中 ctx["_"] 的处理方案 |

**2 项重要建议**（DEV 实现时必须注意，不需 ARCH 修改设计）：

| 编号 | 级别 | 问题 | DEV 注意事项 |
|------|------|------|-------------|
| P-IMP-1 | 🟡 注意 | B7 对大数组占 33% | 跟踪大数组性能；若不达标则优先实现栈式 sink |
| C-IMP-1 | 🟡 注意 | PyInput sub_field_py 需 getattr 回退 | 实现 sub_field_py 时保留 get_item → getattr 回退链 |
| C-IMP-2 | 🟡 注意 | Adapter/Validator Py 路径 | 实现 wrapper exec_parse_py 时注意"sub-sink → transform → 重新 deposit"模式 |

**进入 CODING 条件**：ARCH 提交上述 3 项必修设计澄清后，16.2 可开始 CODING。16.3 的烟雾测试检查点（simple parse ≥1.0x）是关键验证门——simple parse 不依赖 batch extract，可独立验证 PySink 核心设计。

---

### 备注
旧设计（方案 A）的瓶颈分析（B1-B4, FFI 调用计数）仍然有效，作为 ARCH 新设计的输入参考。
关键区别：旧设计试图优化 Value→PyObject 转换；新设计要消除这个转换本身。
新设计在 build 方向的预测（~7.7x-11.5x）远超旧设计（0.23x-0.33x 不达标），根本原因是将 PyInput/exec_build_py 引入 construct-rs 使类型已知 extract 成为可能。

**REV 检视补充备注**：
- pyo3 0.22.6 `Vec<T>` FromPyObject 源码（`types/sequence.rs:494-529`）已实证确认是 per-element 提取，非单次 C 调用。这是 P-CRIT-1 的依据。
- B1/B2/B3 瓶颈已对照源码逐行验证（py_sink.rs:294-306 / compiled_ext.rs:190 / mod.rs:1764 / conversions.rs:110-213）。
- 1515 纯 Rust 测试不受影响的判断基于编译矩阵分析（`#[cfg(feature = "python")]` 门控）。
