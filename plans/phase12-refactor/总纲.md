# Phase 12 总纲：编译执行树（桩）

> **阶段目标**：设计 CompiledNode 执行树 + SchemaCompiler 编译层，实现 build-once 模型。
>
> **状态**：待 Phase 11 验收后细化。

## 预估子任务方向

- 12.x CompiledNode enum 设计 + BuildConstruct trait
- 12.x SchemaCompiler：声明树 → 编译树
- 12.x 表达式编译系统（this.field → Rust 闭包）
- 12.x 字段路径预计算 + Enum 映射表固化
- 12.x 纯 Rust 路径通过编译执行树 parse/build

## REV 重要问题（本阶段必须解决）

- #1 CombinedConstruct vs CompiledNode 关系
- #2 嵌套 sink 机制
- #4 SchemaCompiler 内省机制（最高优先）
- #5 sink 实际机制（复合构造器双输出）

## 出口标准

（待 Phase 11 完成后细化）
