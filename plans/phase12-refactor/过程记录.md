# Phase 12 — 编译执行树：过程记录

> **阶段范围**：CompiledNode / CompiledSchema / SchemaCompiler / BuildConstruct trait / 表达式编译 / OutputSink 机制 / 纯 Rust 路径验证

---

## 子任务 12.0：设计检视（DESIGN_REVIEW）

| 项目 | 内容 |
|------|------|
| 状态 | 已驳回至 DESIGNING |
| 负责人 | REV |
| 开始时间 | 2026-06-16 |
| 完成时间 | 2026-06-16 |

### 检视对象
- `docs/refactor/模块设计-编译执行树.md`（1631 行）

### 对照文档
- `docs/refactor/顶层架构设计.md`（D2/D3/D4/D6/D7、§8 验收标准）
- `docs/refactor/模块设计-enum_dispatch迁移.md`（Phase 11 产物）

### 对照源码（已逐项核实）
- `construct-rs/src/combined.rs`（CombinedConstruct 定义，71 variant 含 Dynamic）
- `construct-rs/src/core/mod.rs`（Construct trait + dyn Construct 便利方法）
- `construct-rs/src/constructs/struct_.rs`（Struct.fields 私有性确认）
- `construct-rs/src/constructs/adapters.rs`（DecodeFunc/EncodeFunc = `Box<dyn Fn>`，无 Send+Sync）
- `construct-rs/src/expr.rs`（CombinedExpr enum 无 Clone；BinExpr/UniExpr/FuncPath/CondExpr/ListPath 均**无 Clone**）
- `construct-rs/Cargo.toml`（**不含 pyo3 依赖**）
- `construct-py/src/constructs_composite.rs:164-168`（PyStruct.inner 类型为 `construct::constructs::Struct`，非 CombinedConstruct）

---

## 设计检视报告

### 总体评价

**结论：驳回至 DESIGNING。**

设计在架构方向（build-once 编译执行树、选项 B 全新 enum、SchemaCompiler 直接 match CombinedConstruct、OutputSink trait）上是**正确且有说服力**的，对 pydantic-core 范式的借鉴合理，REV 问题（#1/#2/#4/#5）的回应方向正确。

但存在 **4 个阻塞级可行性问题**（B1-B4），其中 3 个会导致 Phase 12 代码**无法编译**，1 个是核心模块（表达式编译）的**方案与源码事实直接矛盾**。这些问题必须在进入 CODING 前由 ARCH 修正。

另有 4 个重要问题（I1-I4）和 3 个建议级问题（S1-S3）。

---

### 各维度问题汇总

#### 延续性：✅ 基本通过（1 个建议）

- CompiledNode 与 CombinedConstruct 的关系清晰（选项 B），职责分离正确。选项 B 优于 A/C 的论证成立。
- BuildConstruct trait 的 enum_dispatch 扩展方案（§5.2 `#[enum_dispatch(BuildConstruct)]`）正确，与 Phase 11 的 `#[enum_dispatch(Construct)]` 风格一致。
- Adapter 闭包 `Box<dyn Fn>` → `Arc<dyn Fn>` 的迁移方向正确，但存在类型系统可行性问题（见 B4）。

> **[S1 建议]** §2.3 各小节的 variant 分类计数互相矛盾：
> - §2.3.1 称叶子"11 个"，但 §2.3.1 表格只列了 10 行（漏 BytesExpr，它在 §2.3.2 单列）。
> - §2.3.5 称"重复/枚举/计算/流操作/惰性/控制流（43 个）"，但按 §2.1.6-§2.1.10 统计：枚举/计算 11 + 重复 4 + 惰性 4 + 流操作 14 + 控制流 4 = 37，不是 43。
> - 建议统一 variant 计数口径，避免 DEV 实现时混淆。

#### 可行性：❌ 不通过（4 个阻塞）

##### [B1 阻塞] CompiledNode 直接引用 pyo3 类型，construct-rs 无 pyo3 依赖

**文档位置**：§2.2 enum 定义 `PyCallback(PyCallbackNode)`；§2.5 `PyCallbackNode { callable: pyo3::Py<pyo3::types::PyAny>, ... }`；§6.2 `CompiledExpr::PyCallback(pyo3::Py<pyo3::PyAny>)`。

**问题**：经核实 `construct-rs/Cargo.toml`，Rust 内核 crate **不依赖 pyo3**（这是顶层设计 §1.1/§1.2 的架构边界：Rust 内核层与 Python 用户层隔离）。文档中所有 `pyo3::Py<pyo3::PyAny>` 引用在 construct-rs 中**无法解析，编译会失败**。

文档 §2.2 注释称"Phase 12 仅定义结构并允许编译产出"——但"定义结构"就已经引入了 pyo3 类型，无法编译通过。

**违反**：
- 顶层设计 §1.1 架构分层（Rust 内核层不依赖 pyo3）
- 顶层设计 §1.2 模块清单（PyCallback 是"construct-rs 新增"，但运行时 FFI 是 Phase 13）

**建议修改方向（三选一）**：
- **方案 A（推荐）**：PyCallback variant 和 PyCallbackNode 整体推迟到 Phase 13 定义。Phase 12 的 CompiledNode 不含 PyCallback variant；遇到 Python callable 来源时编译为 CompiledDynamic（文档 §4.3 已有此降级方案）。Phase 13 再在 construct-rs（增加 pyo3 可选依赖，feature gate `python`）或 construct-py 中扩展 CompiledNode。
- **方案 B**：PyCallback variant 用 `#[cfg(feature = "python")]` 门控，callable 字段用 `Box<dyn std::any::Any + Send + Sync>` 间接持有（Phase 13 downcast）。需在 Cargo.toml 增加 `[features] python = ["pyo3"]`。
- **方案 C**：CompiledExpr::PyCallback 同理处理（或直接删除该 variant，Phase 12 不产生它）。

##### [B2 阻塞] CombinedExpr 不实现 Clone，表达式编译方案与源码事实矛盾

**文档位置**：§6.3 `compile_expr` 代码 `Ok(CompiledExpr::Native(expr.clone_ref()))`；§6.4 "CombinedExpr 的所有 variant 都实现了 `Clone`（`#[derive(Clone, Debug)]`，见 `expr.rs:105`）"。

**问题**：经逐行核实源码，上述声称**全部错误**：
- `CombinedExpr` enum（`expr.rs:1082`）：`#[derive(Debug)]`——**无 Clone**。
- `BinExpr`（`expr.rs:316`）、`UniExpr`（`679`）、`FuncPath`（`761`）、`CondExpr`（`914`）、`ListPath`（`983`）：均只有 `#[derive(Debug)]`——**无 Clone**。
- `Path`（`105`）和 `ConstExpr`（`193`）有 Clone，但不能代表全部 variant。
- `clone_ref()` 方法在 CombinedExpr 上**不存在**（grep 确认无此方法）。

因此 §6.3 的 `compile_expr` 实现无法编译，§6.4 "运行时零拷贝（编译时 clone 一次）"的前提不成立。

**影响范围**：所有持有 `CompiledExpr` 的 CompiledNode variant（§2.4 列出 BytesExpr/ArrayExpr/SeekExpr/PointerExpr/IfThenElse/Switch/Computed 等）的表达式编译都会受影响。

**建议修改方向（二选一）**：
- **方案 A（推荐）**：`CompiledExpr::Native(Arc<CombinedExpr>)`。编译时 `Arc::new(expr_clone_or_move)`，clone 廉价（一次原子计数）。但前提是 `compile_expr` 接收 `&CombinedExpr` 时仍需 clone 整棵树——除非能消费（move）表达式。由于 `BuildConstruct::compile(&self, ...)` 只有 `&self`，表达式字段也是 `&`，无法 move。因此仍需 CombinedExpr 可 Clone。
- **方案 B**：为 CombinedExpr 及 BinExpr/UniExpr/FuncPath/CondExpr/ListPath 添加 `#[derive(Clone)]`。Value 已是 Clone（`value.rs:39`），Box<CombinedExpr> 在 CombinedExpr derive Clone 后可递归 clone。此方案需 ARCH 在设计文档中明确"Phase 12 需为 CombinedExpr 补充 Clone derive"作为前置改动，并纳入子任务 12.3。

> 无论哪个方案，§6.3 的 `clone_ref()` 必须改为 `expr.clone()`（方案 B）或 `Arc::new(expr.clone())`（方案 A+B）。

##### [B3 阻塞] CompiledNode variant 计数错误（71 vs 实际 72）

**文档位置**：§2.2 注释"variant 数量：71 个（70 个镜像 CombinedConstruct + 1 个 PyCallback）"；§10.4 出口标准"CompiledNode 定义完整（71 variant）"。

**问题**：实际 CombinedConstruct 有 **71 个 variant**（70 内置 + `Dynamic(Box<dyn Construct>)` escape-hatch，见 `combined.rs:182`）。CompiledNode enum 定义（§2.2 列表）包含：70 个内置镜像 + `Dynamic(CompiledDynamic)` + `PyCallback(PyCallbackNode)` = **72 个 variant**。文档反复说 71，自相矛盾。

若采纳 B1 方案 A（删除 PyCallback），则为 71；否则为 72。计数必须修正一致，否则出口标准无法核验。

##### [B4 阻塞] Adapter 闭包迁移的 Send+Sync 类型不匹配

**文档位置**：§5.4 方案 1："DecodeFunc/EncodeFunc 从 Box 改为 Arc（Send + Sync）；编译时 Arc::clone" + "Adapter::new 保持接受 Box，内部转 Arc: `decode: Arc::from(decode)`"。

**问题**：当前闭包类型是 `Box<dyn Fn(&Value, &Context) -> Result<Value>>`（`adapters.rs:45`，**无 Send+Sync bound**）。目标类型 `Arc<dyn Fn(...) + Send + Sync>`。文档称"Adapter::new 保持接受 Box（无 Send+Sync），内部转 Arc（有 Send+Sync）"——这在 Rust 类型系统中**不可行**：无法将 `Box<dyn Fn>`（无 Send+Sync 保证）转为 `Arc<dyn Fn + Send + Sync>`，编译器会拒绝。

`Arc::from(Box<dyn Fn>)` 最多得到 `Arc<dyn Fn>`（无 Send+Sync），而非文档要求的 `Arc<dyn Fn + Send + Sync>`。

**建议修改方向**：
- 明确 `DecodeFunc`/`EncodeFunc`/`SymmetricFunc`/`CheckFunc` 类型别名**全部**改为含 `Send + Sync` bound：
  ```rust
  pub type DecodeFunc = Arc<dyn Fn(&Value, &Context) -> Result<Value> + Send + Sync>;
  ```
- `Adapter::new` 的参数类型也相应改为 `Arc<...>`（或 `Box<dyn Fn + Send + Sync>` 再转 Arc）。
- 在子任务 12.6 中明确：需全局搜索所有闭包构造点，验证其捕获值满足 `Send + Sync`（非捕获闭包天然满足；捕获 i64/&str 等满足；捕获 Rc/RefCell 等不满足——需确认无此类用法）。
- 文档 §5.4 的代码示例 `Arc::from(decode)` 必须修正为类型匹配的转换。

---

#### 性能：⚠️ 有条件通过（2 个重要）

##### [I1 重要] 叶子节点 exec_parse 的 sink 写入路径低效且协议不清

**文档位置**：§2.3.1"叶子构造器的 exec_parse 直接委托给内嵌的 CombinedConstruct 具体类型"；§7.5 CompiledStruct::exec_parse 示例。

**问题**：CompiledExec trait 的 `exec_parse` 签名为 `-> Result<()>`（写入 sink，不返回 Value）。但：
1. 叶子节点（如 CompiledFormatField）调用 `inner.parse(stream, ctx)` 得到 `Value` 后，需要写入 sink。OutputSink trait 的方法是面向复合节点的（`set_field` 需要字段名，`push_item` 面向数组）——**叶子节点不知道自己的字段名/位置**，无法直接选择 sink 方法。
2. §7.5 的解决方案是对每个字段创建 `sub_sink = sink.sub_sink_for_field(name)` → 叶子写入 sub_sink → `sub_sink.into_value()` → `sink.set_field(name, value)`。这为**每个叶子字段**引入了一次 ValueSink 分配 + IndexMap 构建 + into_value 序列化 + 再写入父 sink 的 IndexMap。对于 100 字段 Struct，这是 100 次额外分配 + clone。

**评估**：文档 §7.7 承认"Phase 12 的 sink 价值主要是架构准备"，接受性能不优于旧路径。但**协议本身**（叶子如何写入）的设计需要更明确，否则 DEV 实现时会产生歧义。

**建议**：ARCH 应在文档中明确选择以下之一，并补充到 §7.3/§7.5：
- **方案 A**：OutputSink 增加 `set_leaf_value(Value)` 方法。复合节点在调用叶子 `exec_parse` 前设置"当前字段上下文"，叶子直接 `sink.set_leaf_value(value)`。ValueSink 用一个 `pending: Option<Value>` 字段暂存，复合节点取出后写入正确位置。避免每叶子创建 sub_sink。
- **方案 B**：CompiledExec 区分标量/容器——叶子节点的 exec_parse 返回 `Result<Option<Value>>`（Some 表示标量值），复合节点收集后写入 sink。但这改变 trait 签名。
- **方案 C（最小改动）**：保持当前设计，但在文档中明确标注"Phase 12 每叶子字段有一次额外 ValueSink 分配（已知开销，Phase 14 优化）"，并在出口标准中移除"编译执行树 ≥ 旧路径速度"的隐含要求（§8.4 已部分提及，但应更明确）。

##### [I2 重要] CompiledStruct::exec_parse 的 sink+Context 双写 clone

**文档位置**：§7.5 `sink.set_field(name, value.clone())` + `child_ctx.insert(name, value)`；§7.5 末尾"Phase 12 先用双写（正确性优先），Phase 14 优化为共享"。

**问题**：每个字段值 clone 一次。对大 Bytes/Container 有开销。

**评估**：文档已识别此问题并标注为阶段性权衡。**可接受**，但建议在出口标准中明确"Phase 12 不要求性能 ≥ 旧路径"（与 I1 方案 C 一致）。

##### 性能问题其余项：✅ 通过

- `&mut dyn OutputSink` 每字段一次虚分发：文档论证"字段写入远少于字节操作"，合理。100 字段 Struct = 100 次虚分发（~ns 级），相对字节解析可忽略。
- CompiledNode enum 大小（§10.3 风险点 1）：文档要求 DEV 实测 `size_of::<CompiledNode>()`，>128 字节则对大 variant 加 Box。参考 Phase 11 §6.1（CombinedConstruct ~80 字节）的评估方法正确。
- enum_dispatch 分发本身零开销，与 Phase 11 一致。

---

#### 完备性：⚠️ 有条件通过（2 个重要）

##### [I3 重要] CompiledDynamic 与 CombinedConstruct::Dynamic 的 BuildConstruct 分发问题

**文档位置**：§2.5 `CompiledDynamic { inner: Box<dyn crate::core::Construct> }`；§4.3"遇到上述来源时，编译为 CompiledDynamic"。

**问题**：CombinedConstruct 已有 `Dynamic(Box<dyn Construct>)` variant（`combined.rs:182`）。当 SchemaCompiler 通过 `BuildConstruct::compile` 分发到 Dynamic variant 时，enum_dispatch 会要求 `Box<dyn Construct>` 实现 `BuildConstruct`。但 `Box<dyn Construct>` 是 trait object，enum_dispatch 无法对它生成 match 分发（它不是 CombinedConstruct 的"内部具体类型"）。

文档 §5.2 说"enum_dispatch 宏会为 CombinedConstruct 生成 compile 方法的 match 分发"——但对 Dynamic variant，enum_dispatch 会尝试调用 `inner.compile()`，而 `Box<dyn Construct>` 不实现 BuildConstruct。

**建议**：ARCH 应明确 Dynamic variant 的特殊处理：
- 方案：在 CombinedConstruct 上为 BuildConstruct **手写** impl（不依赖 enum_dispatch 自动生成），对 Dynamic 分支直接返回 `CompiledNode::Dynamic(CompiledDynamic { inner: self.0.clone() })`。其余 variant 仍可走 enum_dispatch。
- 或：BuildConstruct trait 不用 enum_dispatch，改为 SchemaCompiler 内部手写 match（文档 §4.1 提到"直接 match CombinedConstruct variant"，与 §5.2 的 enum_dispatch 方案矛盾——应统一）。

##### [I4 重要] LazyBound 延迟编译机制未定义

**文档位置**：§8.5"LazyBound 的 compile 不能立即递归...需产出 CompiledLazyBound（持有延迟绑定引用），运行时首次访问时才编译子节点"；§10.3 风险点 4。

**问题**："运行时首次访问时才编译子节点"——这需要 CompiledLazyBound 在运行时能触发编译，即持有 SchemaCompiler 引用或等价能力。但 CompiledSchema 是编译产物，持有编译器会导致生命周期/所有权问题（CompiledSchema 应是 `'static` 的，编译器不应长存）。

文档未定义 CompiledLazyBound 的具体结构。

**建议**：ARCH 应明确选择：
- **方案 A（推荐，简单）**：CompiledLazyBound 持有原始的 `LazyBound` 声明树节点（含 `Box<CombinedConstruct>` 子构造器引用），运行时降级为旧路径执行（`inner.parse(stream, ctx)`），不走编译执行树。这与 §4.3 的"降级为 CompiledDynamic"思路一致。
- **方案 B**：CompiledLazyBound 持有 `OnceLock<CompiledNode>` + 编译所需的 `Box<dyn Fn() -> Result<CompiledNode>>`（闭包捕获绑定信息）。复杂度高，不推荐 Phase 12。

##### 完备性其余项：✅ 基本通过

- 11 个子任务覆盖了所有构造器类别（叶子/包装器/复合/枚举计算/流操作惰性/控制流/escape-hatch/测试）。**无遗漏类别**。
- Sink 场景覆盖：叶子/复合/嵌套/数组/条件分支均有提及（§7.5-7.6），虽协议需细化（见 I1），但场景识别完整。
- 错误路径：parse 中途失败时，`exec_parse` 返回 `Err`，`CompiledSchema::parse_bytes` 通过 `map_err(e.with_path_prefix)` 传播。sink 状态在 Err 时被丢弃（ValueSink 未 into_value）。这在纯 Rust 路径下是正确的（Err 即整体失败）。
- 边界条件：循环引用（max_depth=256）、空 Struct、空输入在 §8.2 测试矩阵中覆盖。

---

#### 整体性：✅ 通过（1 个建议）

- 与 Phase 13（FFI 执行层）的衔接方向清晰：OutputSink trait 让 Phase 13 只需新增 PyDictSink 实现而无需修改 CompiledNode parse 逻辑（§7.7）。这是设计的渐进性体现（顶层设计 G6）。
- CompiledSchema 缓存策略（OnceLock，§3.3）完整：首次编译触发，后续 `OnceLock::get`（约一次原子读）。满足 §8.2.1 缓存契约 < 1µs。
- 与顶层架构 D3（build-once）/D2（Value 双轨）/D6（per-tree FFI）/D7（表达式 enum_dispatch）的映射一致，无偏离。
- SchemaCompiler 的 `max_depth` 编译期栈溢出保护设计合理（§4.2）。

> **[S2 建议]** §5.2 与 §4.1 对 BuildConstruct 分发机制的描述存在轻微矛盾：
> - §4.1 称"通过 match 遍历它的 variant"（手写 match 语气）。
> - §5.2 称"无需手写 match，enum_dispatch 宏自动生成"。
> - 两者在"普通 variant"上是一致的（enum_dispatch 生成 match），但在 Dynamic variant 上需要手写（见 I3）。建议统一表述为"普通 variant 走 enum_dispatch 自动分发；Dynamic variant 手写降级分支"。

> **[S3 建议]** 子任务 12.1（CompiledNode enum 定义）与 12.11（escape-hatch + PyCallback 定义）有重叠——PyCallback 和 Dynamic 都是 CompiledNode 的 variant，应合并到 12.1 统一定义，避免 enum 定义与 escape-hatch 定义割裂导致 DEV 在两个子任务间反复修改同一个 enum。

---

### 对子任务拆分的建议（§10.1）

11 个子任务的**类别覆盖完整**，依赖关系图（§10.2）合理。具体建议：

| 子任务 | REV 评估 | 建议 |
|--------|---------|------|
| 12.1 CompiledNode enum | 方向正确 | **合并 12.11**；variant 计数修正（见 B3）；PyCallback 处理见 B1 |
| 12.2 OutputSink + ValueSink | 合理 | 叶子写入协议需先定（见 I1），否则 12.2 无法独立完成 |
| 12.3 CompiledExpr + 表达式编译 | **必须修改** | 阻塞（见 B2）：需先解决 CombinedExpr Clone 问题 |
| 12.4 BuildConstruct + SchemaCompiler | **必须修改** | 阻塞（见 B4 闭包迁移 + I3 Dynamic 分发）；§5.2/§4.1 表述统一 |
| 12.5 叶子构造器 compile | 合理 | 机械量大，风险低；可作为编译流程的 smoke test |
| 12.6 包装器 compile | **关键风险点** | Adapter 闭包 Arc 迁移（B4）是本阶段唯一语义性变更；需全局闭包 Send+Sync 审计 |
| 12.7 复合构造器 compile | **核心难点** | sink 递归协调；I1 协议必须先定 |
| 12.8 枚举/计算/控制流 | 合理 | Enum 映射固化（HashMap）是明确收益点 |
| 12.9 流操作/惰性 | **需补充** | LazyBound 方案必须先定（I4） |
| 12.10 等价性测试套件 | 合理且必要 | ~120 差分测试是 Phase 12 的核心验收手段 |
| 12.11 escape-hatch | 建议合并入 12.1 | 见 S3 |

**关键依赖链修正**：
```
B1 解决（PyCallback 处理）─┐
B2 解决（CombinedExpr Clone）─┤
B4 解决（闭包 Arc 迁移）─────┼─► 12.1 修正 ─► 12.3 ─► 12.4 ─► 12.5-12.9 ─► 12.10
I1 解决（叶子 sink 协议）─────┘                ▲
I3 解决（Dynamic 分发）───────────────────────┘
I4 解决（LazyBound 方案）────► 12.9 可开始
```

**建议 ARCH 修正后再提交检视的优先级**：
1. **B1 + B3**（同一问题：PyCallback 与 variant 计数）——决定 CompiledNode 的最终形态
2. **B2**（表达式编译可行性）——影响 12.3 及所有含表达式的 variant
3. **B4**（闭包迁移类型匹配）——影响 12.6 及所有包装器
4. **I1**（叶子 sink 协议）——影响 12.2 + 12.7
5. **I3**（Dynamic 分发）——影响 12.4
6. **I4**（LazyBound）——影响 12.9

---

### 是否可进入 CODING 阶段

**否。驳回至 DESIGNING。**

**驳回依据**：4 个阻塞级可行性问题（B1-B4）中，B1/B2/B4 会导致 Phase 12 代码**无法编译**，B3 是出口标准无法核验的计数错误。这些问题不能"先通过后续迭代"，因为它们是架构级可行性缺陷。

**ARCH 修正后需重新提交 DESIGN_REVIEW**，重点验证：
- [ ] B1：PyCallback 的 pyo3 依赖如何解决（feature gate / 推迟 / 间接持有）
- [ ] B2：CombinedExpr 的 Clone/共享方案
- [ ] B3：variant 计数修正（71 或 72，全文档一致）
- [ ] B4：闭包类型别名 + Adapter::new 签名的 Send+Sync 一致性
- [ ] I1：叶子节点 sink 写入协议的明确定义
- [ ] I3：Dynamic variant 的 BuildConstruct 手写分发
- [ ] I4：LazyBound 延迟编译的具体方案

**建议级问题（S1-S3）不阻塞，可在修正 B1-B4 时一并润色。**

---

### REV 操作日志
- [2026-06-16 10:00] [REV] 接收 PM 检视任务，阅读设计文档（1631 行）
- [2026-06-16 10:30] [REV] 对照顶层架构设计 + Phase 11 设计文档
- [2026-06-16 11:00] [REV] 核实源码：combined.rs（71 variant 含 Dynamic）、struct_.rs（fields 私有确认）、adapters.rs（Box<dyn Fn> 无 Send+Sync 确认）
- [2026-06-16 11:30] [REV] 核实源码：expr.rs（CombinedExpr **无 Clone**，BinExpr/UniExpr/FuncPath/CondExpr/ListPath **均无 Clone**，clone_ref 方法不存在）—— 发现 B2
- [2026-06-16 11:45] [REV] 核实 Cargo.toml（construct-rs **无 pyo3 依赖**）—— 发现 B1
- [2026-06-16 12:00] [REV] 核实 constructs_composite.rs（PyStruct.inner 类型为 Struct 非 CombinedConstruct）—— 确认 SchemaCompiler 输入方案方向正确但转换路径需 Phase 13 明确
- [2026-06-16 12:30] [REV] 完成 7 项问题定性（4 阻塞 + 3 重要 + 3 建议），写入过程记录
- [2026-06-16 12:45] [REV] 结论：**驳回至 DESIGNING**，返回检视报告给 PM

### 备注
- 本次检视未修改任何设计文档或代码，仅写入过程记录。
- 设计的**架构方向（build-once + 选项 B + SchemaCompiler match + OutputSink trait）是正确的**，驳回仅因可行性细节需修正。ARCH 修正 B1-B4 后预计可快速通过二次检视。
- B2（CombinedExpr 无 Clone）是本次检视的最重要发现——设计文档对源码事实的引用存在多处实质性错误，建议 ARCH 在后续设计中加强对源码 derive 属性的核实。

---

## 子任务 12.0：设计检视（DESIGN_REVIEW，v2 二次检视）

| 项目 | 内容 |
|------|------|
| 状态 | 待检视（v2 二次检视完成，有条件通过） |
| 负责人 | REV |
| 开始时间 | 2026-06-16 |
| 完成时间 | 2026-06-16 |

### 检视对象
- `docs/refactor/模块设计-编译执行树.md`（v2 修正版，1977 行，ARCH 声称 1980 行）

### 对照源码（二次核实）
- `construct-rs/src/expr.rs` — CombinedExpr + 6 variant 的 Clone 缺失确认（B2）
- `construct-rs/src/combined.rs` — CombinedConstruct 71 variant + Dynamic(Box) 确认（B3/I3）
- `construct-rs/src/constructs/adapters.rs` — 4 个闭包别名 Box<dyn Fn> 无 Send+Sync 确认（B4）
- `construct-rs/src/constructs/stream_ops.rs:1214-1241` — LazyBound 定义确认（I4/N1）

---

## 二次检视报告（v2）

### 总体评价

**结论：有条件通过（CONDITIONAL PASS）。可进入 CODING 阶段，前提是 ARCH 在子任务 12.9 开始前补充 N1 的前置改动说明。**

7 个原问题中 **6 个完全通过**（B1/B2/B3/B4/I1/I3），**1 个发现残留可行性缺口**（I4 → N1）。ARCH 的 v2 修正质量高——源码核实准确（B2 的 6 类型 Clone 缺失表逐行正确），方案选择合理，文档自洽性显著提升（variant 计数全篇一致）。

残留问题 N1 是 I4 降级路径的一个前置改动遗漏，解决方案清晰（与 B4 同类的 Box→Arc 迁移），只影响 LazyBound 这一边缘场景，不影响 12.1-12.8 的开发。

---

### 阻塞级问题验证结果（逐项）

#### [B1] pyo3 类型引用已移除 — ✅ 通过

**验证方法**：全文档搜索 `pyo3::` + 逐行检查所有代码定义块。

**结果**：
- `pyo3::` 仅出现 1 处（Line 1161），位于修正记录的描述性文字中（"原设计的 `CompiledExpr::PyCallback(pyo3::Py<...>)` variant 已移除"），**非代码定义**。
- §2.2 CompiledNode enum 定义（Line 179-264）：**无** `PyCallback` variant，确认已移除。
- §2.5 关键签名：**无** PyCallbackNode struct，仅保留 CompiledDynamic（Line 440）和 CompiledLazyBound（Line 448）。
- §6.2 CompiledExpr 定义（Line 1135-1143）：**无** PyCallback variant，仅 Native + Constant。
- PyCallback 在文档中的其余出现（~30 处）均为：修正记录描述、Phase 13 前瞻表格、注释说明——均非 Phase 12 代码定义。

**结论**：construct-rs 在 Phase 12 不引入 pyo3 依赖，代码可编译。Python callable 统一编译为 CompiledDynamic。✓

#### [B2] CombinedExpr Clone 方案 — ✅ 通过

**验证方法**：逐行核实 `expr.rs` 的 derive 属性 + 分析互递归 Clone 可行性。

**源码事实确认**（ARCH §6.4 的核实表完全正确）：
| 类型 | 行号 | 当前 derive | Clone? |
|------|------|------------|--------|
| CombinedExpr (enum) | 1082 | `#[derive(Debug)]` | ✗ |
| BinExpr | 316 | `#[derive(Debug)]` | ✗ |
| UniExpr | 678 | `#[derive(Debug)]` | ✗ |
| FuncPath | 760 | `#[derive(Debug)]` | ✗ |
| CondExpr | 913 | `#[derive(Debug)]` | ✗ |
| ListPath | 982 | `#[derive(Debug)]` | ✗ |

（Path/ConstExpr/BinOp/UniOp/PathRoot 已有 Clone，ARCH 核实正确。）

**互递归 Clone 可行性确认**：BinExpr/UniExpr/FuncPath/CondExpr/ListPath 均通过 `Box<CombinedExpr>` 递归引用 CombinedExpr。Rust 的 `#[derive(Clone)]` 生成 `match self { ... x.clone() ... }`，当所有参与类型同时 derive Clone 时，互递归成立。ARCH 的分析（§6.4 Line 1221-1232）逐类型验证字段 Clone 性，**全部正确**。

**compile_expr 修正确认**：§6.3 Line 1187 使用 `expr.clone()`（非不存在的 `clone_ref()`）。grep 确认 `clone_ref` 仅出现在修正记录描述中（Line 15/1185/1217），均为"已修正"的说明文字。

**结论**：方案 B（6 类型补 Clone）可行且正确。✓

#### [B3] variant 计数一致 — ✅ 通过

**验证方法**：全文档搜索 "71"/"70"/"72" + variant 计数 + 对照 combined.rs 实际 variant 数。

**combined.rs 实际 variant 计数**（逐行统计 Line 25-183）：71 个（70 内置含 cfg-gated Compressed + 1 Dynamic）。

**文档计数一致性确认**：
- §2.2 enum 注释（Line 170）："71 variants"
- §2.2 说明（Line 267-269）："71 个...开启时 71 / 关闭时 70"
- §2.3 分类（Line 278-280）：10+1+10+5+44=70 内置 + Dynamic = 71 ✓
- §2.3.5 分布（Line 367-369）：6+12+4+4+14+4=44 ✓
- §10.1 子任务（Line 1850）："71 variant"
- §10.4 出口标准（Line 1912）："71 variant"
- 附录 A（Line 1931）："71 variant"

**全篇计数一致为 71**（开启 compression）/ 70（关闭 compression），与 CombinedConstruct 1:1 对应。无矛盾。✓

#### [B4] 闭包类型迁移 — ✅ 通过

**验证方法**：核实 adapters.rs 当前闭包别名 + 验证 Arc::from 类型转换可行性。

**源码事实确认**：4 个闭包别名（adapters.rs Line 45/53/59/68）均为 `Box<dyn Fn(...)>`，**无 Send + Sync bound**。ARCH 修正记录（§5.4 Line 992-994）引用准确。

**类型转换可行性确认**：
- 目标类型：`Arc<dyn Fn(...) + Send + Sync>`（§5.4 Line 1008-1011）
- Adapter::new 参数：`Box<dyn Fn(...) + Send + Sync>`（Line 1019-1020）
- 转换：`Arc::from(Box<dyn Fn + Send + Sync>)` → `Arc<dyn Fn + Send + Sync>`
- Rust 的 `impl<T: ?Sized> From<Box<T>> for Arc<T>` 支持此转换（T = `dyn Fn + Send + Sync`）。**类型匹配**。✓

**审计点明确**：§5.4 Line 1044-1054 列出 CODING 阶段需审计的闭包捕获值 Send+Sync 情况（非捕获 ✓、基本类型捕获 ✓、Rc/RefCell 需排查）。从 adapters.rs 测试代码看，所有闭包均为非捕获或捕获基本类型，预期迁移无 break。

**结论**：方案正确，类型系统可行，审计点清晰。✓

---

### 重要问题验证结果（逐项）

#### [I1] 叶子 sink 协议 — ✅ 通过

**验证方法**：检查 OutputSink trait 定义 + ValueSink 实现 + 叶子 exec_parse 流程。

**确认项**：
- `set_scalar(Value)` 方法**已加入** OutputSink trait（§7.3 Line 1398）✓
- ValueSink 新增 `pending: Option<Value>` 字段（§7.4 Line 1476）✓
- 叶子 exec_parse 流程清晰（§7.5 Line 1601-1609）：`inner.parse` → `sink.set_scalar(value)` ✓
- `into_value` 优先级正确（§7.4 Line 1538-1547）：pending 标量 > fields(Container) > items(List) ✓
- 统一 sub_sink 协议完整（§7.3 Line 1342-1350）：父节点创建 sub_sink → 叶子 set_scalar / 复合 set_field → finish_subsink 整合 ✓

**小冗余（非阻塞）**：ValueSink 的 `pending_field_name` 字段（Line 1482）在 Struct 命名字段路径中未被使用（父节点直接调用 set_field）。可在 CODING 阶段优化，不影响设计可行性。

**结论**：协议清晰自洽，叶子/复合统一处理。✓

#### [I3] Dynamic BuildConstruct 分发 — ✅ 通过

**验证方法**：检查 Dynamic Box→Arc 迁移方案 + Arc<dyn Construct> 的 BuildConstruct impl。

**确认项**：
- CompiledDynamic.inner 类型为 `Arc<dyn Construct>`（§2.5 Line 440-442）✓
- `Arc<dyn Construct>` 实现 BuildConstruct 返回 CompiledDynamic（§5.2 Line 897-902）✓
- Dynamic Box→Arc 迁移方案清晰（§5.5 Line 1059-1099）：combined.rs Dynamic variant 类型 + dynamic() helper 更新 ✓
- 迁移影响范围分析完整（§5.5 Line 1086-1095）：combined.rs + 调用点 + enum_dispatch ✓
- S2 统一表述正确（§5.2 Line 908-911）：普通 variant 走 enum_dispatch，Dynamic 通过 Arc<dyn Construct> 的 BuildConstruct impl 降级 ✓

**enum_dispatch 可行性确认**：enum_dispatch 生成的 match arm 对 Dynamic variant 调用 `inner.compile(...)`，其中 inner 类型为 `&Arc<dyn Construct>`。由于 ARCH 为 `Arc<dyn Construct>` 实现了 BuildConstruct，分发自然落地。✓

**结论**：方案清晰可行。✓

#### [I4] LazyBound 延迟编译 — ⚠️ 部分通过（发现残留问题 N1）

**验证方法**：核实 LazyBound 源码定义 + 检查降级路径可行性。

**方案 A（降级旧路径）方向正确**：CompiledLazyBound 持有 `Arc<dyn Construct>`（§2.5 Line 448-450），运行时调用 inner.parse/build，由 LazyBound 自身处理延迟绑定。不递归编译（避免无限循环）。✓

**但发现残留可行性缺口（N1）**：

源码事实（`stream_ops.rs:1214-1217`）：
```rust
pub struct LazyBound {
    pub subcon_func: Box<dyn Fn() -> Box<CombinedConstruct>>,
}
```
- LazyBound **无 `#[derive(Clone)]`**
- 持有 `Box<dyn Fn() -> Box<CombinedConstruct>>`，此闭包类型**不可 Clone**
- 无手写 `impl Clone for LazyBound`

**问题**：§8.5 Line 1787-1789 的代码示例 `Arc::new(self.clone())` **无法编译**（LazyBound 不可 Clone）。文档声称的降级路径（Line 1804-1808："若 LazyBound 不可 Clone，则降级为 CompiledDynamic"）**同样不可行**——CompiledDynamic 的 inner 是 `Arc<dyn Construct>`，从 `&LazyBound` 创建 `Arc<dyn Construct>` 仍需 Clone。

**根因**：`BuildConstruct::compile(&self, ...)` 只有 `&self`（不可移动），无法从不可 Clone 的 `&LazyBound` 获得 owned 值来创建 Arc。

**解决方案（清晰，与 B4 同类）**：前置改动——LazyBound 的 `subcon_func` 从 `Box<dyn Fn>` 迁移到 `Arc<dyn Fn + Send + Sync>`，使 LazyBound 可 derive Clone。或：CombinedConstruct::LazyBound 内部存储改为 `Arc<LazyBound>`（类似 Dynamic 的迁移）。

**结论**：方案 A 方向正确，但缺少 LazyBound Clone 前置改动的明确说明。详见 N1。

---

### 新发现问题

#### [N1 重要] I4 残留：LazyBound 不可 Clone，降级路径前置改动未明确

**文档位置**：§8.5 Line 1782-1808（I4 修正方案 A + 简化说明）。

**问题描述**：LazyBound 持有 `Box<dyn Fn() -> Box<CombinedConstruct>>`（stream_ops.rs:1216），不可 Clone。设计文档 §8.5 的两条路径都依赖从 `&LazyBound` 创建 `Arc<dyn Construct>`：
1. `CompiledLazyBound { inner: Arc::new(self.clone()) }` — `self.clone()` 不可编译
2. 降级为 `CompiledDynamic { inner: Arc::clone(?) }` — 仍需从 `&LazyBound` 获得 `Arc<dyn Construct>`

**严重程度**：重要（代码无法编译），但不阻塞 12.1-12.8（LazyBound 归入 12.9）。

**建议**：ARCH 在文档 §8.5 补充前置改动说明（一行即可），明确归入子任务 12.9：
> **前置改动（N1）**：LazyBound 的 `subcon_func` 从 `Box<dyn Fn() -> Box<CombinedConstruct>>` 迁移到 `Arc<dyn Fn() -> Box<CombinedConstruct> + Send + Sync>`，并补充 `#[derive(Clone)]`。迁移后 `Arc::new(self.clone())` 可行。此改动与 B4（闭包 Box→Arc）同类，风险低。

**处理方式**：**不驳回**。要求 ARCH 在 12.9 开始前补充此说明。12.1-12.8 可立即开始。

---

### 各维度结论

| 维度 | 结论 | 说明 |
|------|------|------|
| **延续性** | ✅ 通过 | 与 Phase 11 衔接清晰，BuildConstruct enum_dispatch 扩展与 Construct 风格一致 |
| **性能** | ✅ 通过 | I1/I2 的阶段性开销（每叶子 ValueSink 分配 + 双写 clone）已明确标注为可接受，出口标准已修正为"不要求 ≥ 旧路径" |
| **整体性** | ✅ 通过 | 与 Phase 13 衔接方向清晰（OutputSink trait + PyDictSink），CompiledSchema 缓存（OnceLock）合理 |
| **可行性** | ⚠️ 有条件通过 | B1-B4 全部通过；I3 通过；I4 有残留（N1），解决方案清晰 |
| **完备性** | ⚠️ 有条件通过 | 边界条件覆盖完整（循环引用 max_depth、空 Struct、空输入）；I4 降级路径前置改动遗漏（N1） |

---

### 是否可进入 CODING 阶段

**是，有条件通过。**

**条件**：ARCH 在子任务 12.9 开始前，于设计文档 §8.5 补充 N1 的前置改动说明（LazyBound 的 subcon_func Box→Arc 迁移 + Clone 补充）。此补充不影响 12.1-12.8 的开发，可在并行期间完成。

**不驳回的理由**：
1. 7 个原问题中 6 个完全通过，修正质量高
2. N1 解决方案清晰（与 B4 同类的 Box→Arc 迁移，一行改动 + derive Clone）
3. N1 只影响 LazyBound（边缘场景，自引用结构），不影响核心编译路径
4. 完全驳回会延误 12.1-12.8 的开发，而 N1 可在 12.9 前快速解决

**建议 PM 的处理**：
- 标记子任务 12.0 状态为 **ACCEPTED（有条件）**
- 分派 12.1-12.8 给 DEV（可立即开始）
- 并行要求 ARCH 补充 N1 说明（在 12.9 开始前完成）
- 12.9 开始前由 REV 快速确认 N1 补充已到位

---

### 二次检视操作日志
- [2026-06-16 14:00] [REV] 接收 PM 二次检视任务，阅读 v2 修正版设计文档（1977 行）
- [2026-06-16 14:15] [REV] B1 验证：grep `pyo3::` + 检查全部代码定义块 → 通过
- [2026-06-16 14:20] [REV] B2 验证：逐行核实 expr.rs 6 类型 Clone 缺失 + 互递归可行性分析 → 通过
- [2026-06-16 14:30] [REV] B3 验证：全文档 variant 计数搜索 + 对照 combined.rs 71 variant → 通过
- [2026-06-16 14:35] [REV] B4 验证：核实 adapters.rs 4 闭包别名 + Arc::from 类型转换可行性 → 通过
- [2026-06-16 14:40] [REV] I1 验证：检查 OutputSink set_scalar + ValueSink pending + 叶子 exec_parse → 通过
- [2026-06-16 14:45] [REV] I3 验证：检查 Dynamic Box→Arc 迁移 + Arc<dyn Construct> BuildConstruct impl → 通过
- [2026-06-16 14:50] [REV] I4 验证：核实 stream_ops.rs LazyBound 定义（`Box<dyn Fn>`，不可 Clone）→ **发现 N1 残留**
- [2026-06-16 15:00] [REV] N1 定性：I4 降级路径前置改动遗漏，解决方案清晰（Box→Arc 迁移），不阻塞 12.1-12.8
- [2026-06-16 15:15] [REV] 结论：**有条件通过**，可进入 CODING（ARCH 需在 12.9 前补充 N1）
- [2026-06-16 15:30] [REV] 写入二次检视报告到过程记录

### 备注
- 本次检视未修改任何设计文档或代码，仅写入过程记录。
- ARCH 的 v2 修正质量显著提升：源码核实准确（B2 的 6 类型 Clone 表逐行正确），方案选择合理，文档自洽性高。
- N1 是唯一的残留问题，其根因与 B4 同类（闭包 Box 不可 Clone/Arc 转换），ARCH 在修正 B4 时遗漏了 LazyBound 的同类迁移。
- **建议 ARCH 在后续设计中，对所有持有 `Box<dyn Fn>` 或 `Box<dyn Construct>` 的类型统一排查 Clone/Arc 迁移需求**（LazyBound、Dynamic、Adapter 闭包均已涉及，但 LazyBound 的迁移说明遗漏）。

---

## 子任务 12.1：CompiledNode + CompiledSchema + OutputSink + BuildConstruct 基础设施

| 项目 | 内容 |
|------|------|
| 状态 | 开发完成（待 CODE_REVIEW） |
| 负责人 | DEV |
| 开始时间 | 2026-06-16 |
| 完成时间 | 2026-06-16 |

### 任务描述

定义 Phase 12 编译执行树的基础设施层：
- CompiledNode enum（71 variant，与 CombinedConstruct 1:1 对应，无 PyCallback）
- CompiledExec trait（exec_parse / exec_build / exec_sizeof）
- CompiledSchema（持有 `Arc<CompiledNode>`，提供 parse_bytes / build_bytes / sizeof 公开 API）
- OutputSink trait（7 方法：set_scalar / set_field / push_item / sub_sink_for_field / sub_sink_for_item / finish_subsink / into_value）
- ValueSink（OutputSink 的首个实现，collect 到 Value::Container / Value::List / pending scalar）
- BuildConstruct trait（`fn compile(&self) -> Result<CompiledNode>`）
- 71 个 compiled node unit struct（单元结构体，真实字段在 12.2-12.8 中添加）
- BuildConstruct stub impls（所有 71 个 construct 类型 + `Box<dyn Construct>` for Dynamic）
- CombinedConstruct 手写 match 分发（enum_dispatch 对 Dynamic variant 的 trait object 会 panic）

### 新建文件

| 文件 | 行数 | 内容 |
|------|------|------|
| `construct-rs/src/compiled/mod.rs` | ~955 | CompiledExec trait + 71 unit structs + CompiledNode enum + CompiledSchema + try_fold_static_size + 17 tests |
| `construct-rs/src/compiled/sink.rs` | ~560 | OutputSink trait + ValueSink impl + 18 tests |
| `construct-rs/src/compiled/build.rs` | ~390 | BuildConstruct trait + 71 stub impls + Box<dyn Construct> impl + CombinedConstruct 手写 match + 8 tests |

### 修改文件

| 文件 | 改动 |
|------|------|
| `construct-rs/src/lib.rs` | 新增 `pub mod compiled;` |

### 设计决策（实现层面）

1. **enum_dispatch → 手写 match**：设计文档 §5.2 使用 `#[enum_dispatch(BuildConstruct)]` 为 CombinedConstruct 自动生成 match 分发。实测发现 enum_dispatch 宏对 Dynamic variant（`Box<dyn Construct>`）生成代码时 panic（"This macro does not support unsized types"）。改为手写 match，功能等价。设计文档 §5.2/§4.1 已预留此方案（S2 统一表述："普通 variant 走 enum_dispatch 自动分发；Dynamic variant 手写降级分支"）。Phase 12.3 中 Dynamic Box→Arc 迁移后可恢复 enum_dispatch。

2. **CompiledNode 71 variant 均为单元结构体**：Phase 12.1 只定义 enum 结构和 trait，所有 compiled node struct（如 CompiledFormatField、CompiledStruct 等）暂为单元结构体 `pub struct CompiledFormatField;`。真实字段（inner construct、pre-compiled expression、mapping table 等）在 12.2-12.8 子任务中逐步添加。enum 定义和 variant 列表在后续阶段不变。

3. **CompiledExec stub 返回 ConstructError::Generic**：所有 71 个 compiled node 的 exec_parse/exec_build/exec_sizeof 均返回 `Err(ConstructError::Generic { path, message })`，message 含 "not yet implemented"。这确保编译可过且测试可断言，同时不使用 `unreachable!()` 或 `todo!()`。

4. **try_fold_static_size stub 返回 None**：所有 compiled node 的 try_fold_static_size 返回 None（大小未知），sizeof 走 exec_sizeof 路径（也是 stub Err）。真实实现从 12.4 开始填充。

5. **ValueSink.into_value 优先级**：pending scalar > fields (Container) > items (List)。与设计文档 §7.4 Line 1538-1547 一致。

6. **OutputSink 7 方法签名**：完全遵循设计文档 §7.3（v2 版本），包括 `set_scalar(Value)` 方法（I1 修正）。所有方法返回 `Result<()>`，into_value 返回 `Result<Value>`。

### 自检结果

| 检查项 | 结果 | 备注 |
|--------|------|------|
| `cargo build` | ✅ | 零 error 零 warning |
| `cargo clippy -- -D warnings` | ✅ | 退出码 0 |
| `cargo fmt --check` | ✅ | 格式正确 |
| `cargo test` | ✅ | 1357 passed, 0 failed（含 43 新增 compiled 模块测试） |
| `cargo test --features compression` | ✅ | 全部 PASS |
| 接口与设计文档一致 | ✅ | OutputSink 7 方法、CompiledExec 3 方法、CompiledNode 71 variant、BuildConstruct 签名 |
| 无 unwrap/panic（非测试代码） | ✅ | 所有 unwrap/unreachable! 仅在 `#[cfg(test)]` 模块内 |
| 文档注释完整 | ✅ | 所有 pub 项有 `///` 文档注释 |
| parse/build 对称性 | ✅ | CompiledExec 定义了 exec_parse 和 exec_build（均为 stub） |

### 测试覆盖

**compiled/mod.rs（17 tests）**：
- CompiledNode 构造（Pass / FormatField / Struct）
- stub exec_parse 返回 Generic error
- stub exec_build 返回 Generic error
- stub exec_sizeof 返回 Generic error
- CompiledSchema::parse_bytes 空 slice 返回 stub error
- CompiledSchema::build_bytes 传入 Container 返回 stub error
- CompiledSchema::sizeof 返回 stub error
- CompiledSchema::new + Arc 一致性
- try_fold_static_size 返回 None
- CombinedConstruct dispatch to compile（多种 variant）

**compiled/sink.rs（18 tests）**：
- ValueSink set_scalar + into_value
- 多次 set_scalar 覆盖
- set_field + into_value（Container）
- 多字段 set_field 保序
- set_field 同名覆盖
- push_item + into_value（List）
- 多次 push_item
- 混合写入 set_scalar + set_field 优先级
- sub_sink_for_field 嵌套
- sub_sink_for_item
- finish_subsink
- 多次 finish_subsink（List 收集）
- 空空如也 into_value
- set_scalar(None)
- 嵌套 Struct 模拟（3 层）
- 混合类型 Container
- sub_sink_for_field 未 finish 的错误
- OutputSink trait object 动态分发

**compiled/build.rs（8 tests）**：
- Pass::compile 返回 Err
- Pass::compile message 含 "not yet implemented"
- Struct::compile 返回 Err
- Renamed::compile 返回 Err
- CombinedConstruct dispatches compile
- Dynamic variant compile 返回 Err（含 "Dynamic" message）
- 所有构造器类别 dispatch（10+ category）
- INT8UB compile 返回 Err

### 偏离设计文档的地方

1. **[设计偏离] enum_dispatch → 手写 match for BuildConstruct on CombinedConstruct**：详见设计决策 #1。功能等价，不改变任何公开 API。Phase 12.3 Box→Arc 迁移后可恢复。

### 操作日志

- [2026-06-16 16:00] [DEV] 接收 PM 分派 12.1 任务，阅读设计文档 + Python 源码 + 已有 Rust 源码
- [2026-06-16 16:30] [DEV] 创建 `compiled/mod.rs`：CompiledExec trait + 71 unit structs + CompiledNode enum
- [2026-06-16 17:00] [DEV] 创建 `compiled/sink.rs`：OutputSink trait + ValueSink 实现
- [2026-06-16 17:30] [DEV] 创建 `compiled/build.rs`：BuildConstruct trait + 71 stub impls
- [2026-06-16 17:45] [DEV] 更新 `lib.rs` 添加 `pub mod compiled;`
- [2026-06-16 18:00] [DEV] 修复编译错误（enum_dispatch panic → 手写 match；Debug derive 补充；clippy needless question mark）
- [2026-06-16 18:15] [DEV] 修复测试编译错误（unsized type deref）
- [2026-06-16 18:30] [DEV] 运行 cargo build + clippy + fmt --check → 全部通过
- [2026-06-16 18:45] [DEV] 运行 cargo test → 1357 passed, 0 failed
- [2026-06-16 19:00] [DEV] 运行 cargo test --features compression → 全部通过
- [2026-06-16 19:10] [DEV] 代码质量检查：无 unwrap/panic/TODO 在非测试代码中
- [2026-06-16 19:15] [DEV] 写入过程记录，准备返回报告

### 备注

- Phase 12.1 是纯基础设施搭建，所有 exec/compile 方法均为 stub，在后续 12.2-12.8 中逐步填充真实实现。
- enum_dispatch panic 问题是本任务的唯一意外发现，已通过手写 match 解决，不影响设计文档的其他部分。
- `CompiledNode` enum 的 `#[derive(Debug)]` 是必需的（CompiledSchema derive Debug 依赖它）。
- 后续 12.2-12.8 添加真实字段时，unit struct → named-field struct 的转换不影响 enum 定义，只需更新 impl 块。

---

## 子任务 12.1：代码审查报告（CODE_REVIEW）

| 项目 | 内容 |
|------|------|
| 状态 | 待审查 → **通过（PASS）** |
| 负责人 | VET |
| 审查时间 | 2026-06-16 |
| 结论 | **通过，可进入子任务 12.2** |

### 流程审计：✅ 通过

| 审计项 | 结果 | 说明 |
|--------|------|------|
| ARCH 设计文档 | ✅ 存在 | `docs/refactor/模块设计-编译执行树.md`（v2 修正版，1977 行） |
| REV 设计检视报告 | ✅ 存在 | v1 驳回（4 阻塞 + 4 重要）→ v2 有条件通过（6/7 通过，N1 残留延后至 12.9） |
| DEV 开发阶段 | ✅ 执行 | 操作日志完整，自检结果齐全（build/clippy/fmt/test 全绿） |
| 工作流管道顺序 | ✅ 正确 | DESIGNING → DESIGN_REVIEW(v1 驳回) → DESIGNING(修正) → DESIGN_REVIEW(v2 通过) → CODING → CODE_REVIEW |

**无跳步**。流程审计通过。

### 审查文件

- `construct-rs/src/compiled/mod.rs`（955 行）
- `construct-rs/src/compiled/sink.rs`（547 行）
- `construct-rs/src/compiled/build.rs`（390 行）
- 对照：`construct-rs/src/combined.rs`（202 行，variant 清单核对基准）

### 复审验证（VET 独立执行）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| `cargo build` | ✅ | 零 error 零 warning（VET 复跑确认） |
| `cargo clippy -- -D warnings` | ✅ | 退出码 0（VET 复跑确认） |
| `cargo fmt --check` | ✅ | 格式正确（VET 复跑确认） |
| `cargo test --lib compiled` | ✅ | **49 passed**, 0 failed（VET 复跑；DEV 报告 43，实际更多，覆盖更充分） |

### 逻辑正确性：✅ 通过

**[核对 1] CompiledNode 71 variant 完整性（1:1 对应 CombinedConstruct）**

逐行核对 `compiled/mod.rs:369-526`（CompiledNode enum）与 `combined.rs:25-183`（CombinedConstruct enum）：

| variant | CombinedConstruct | CompiledNode | 对应 |
|---------|-------------------|--------------|------|
| Subconstruct | ✅ L28 | ✅ L372 | ✓ |
| Renamed | ✅ L30 | ✅ L374 | ✓ |
| FormatField ~ PaddedString (11 原子) | ✅ L33-53 | ✅ L377-397 | ✓ 全部对应 |
| Const / Mapping | ✅ L55-57 | ✅ L400-402 | ✓ |
| Adapter 系列 (5) | ✅ L60-68 | ✅ L405-413 | ✓ |
| meta 系列 (6) | ✅ L71-81 | ✅ L416-426 | ✓ |
| composite 系列 (5) | ✅ L84-92 | ✅ L429-437 | ✓ |
| enum/computed 系列 (12) | ✅ L95-115 | ✅ L440-460 | ✓ |
| repetition 系列 (4) | ✅ L118-124 | ✅ L463-469 | ✓ |
| lazy 系列 (4) | ✅ L127-133 | ✅ L472-478 | ✓ |
| stream ops 系列 (14，含 Compressed cfg) | ✅ L136-163 | ✅ L481-508 | ✓ |
| control flow 系列 (4) | ✅ L166-172 | ✅ L511-517 | ✓ |
| Hex / HexDump | ✅ L175-177 | ✅ L520-522 | ✓ |
| Dynamic | ✅ L182 | ✅ L525 | ✓ |

**总计 71 variant（含 Compressed），完全 1:1 对应**。无遗漏，无多余。

- `Compressed` cfg 门控一致性：combined.rs:154 `#[cfg(feature="compression")]` ↔ mod.rs:309(struct) + mod.rs:499(enum variant) — **三处门控一致** ✓

**[核对 2] CompiledExec trait 签名**

mod.rs:69-97 的三个方法签名与设计文档 §2.2 Line 147-164 **完全一致**：
- `exec_parse(&self, stream: &mut CombinedStream, ctx: &mut Context, sink: &mut dyn OutputSink) -> Result<()>` ✓
- `exec_build(&self, input: &Value, stream: &mut CombinedStream, ctx: &mut Context) -> Result<()>` ✓
- `exec_sizeof(&self, ctx: &Context) -> Result<usize>` ✓

**[核对 3] enum_dispatch 正确性**

mod.rs:53 `#[enum_dispatch::enum_dispatch]` trait + mod.rs:368 `#[enum_dispatch::enum_dispatch(CompiledExec)]` enum — 与设计文档一致。macro 生成分发经测试验证正确（`exec_dispatches_to_correct_variant` 测试覆盖）。

**[核对 4] CompiledSchema 方法实现**

mod.rs:663-757 的 parse_bytes / build_bytes / sizeof 实现与设计文档 §3.2 Line 546-590 逐行对比一致。路径前缀（PARSE_PATH/BUILD_PATH/SIZEOF_PATH）正确设置。

**[核对 5] try_fold_static_size stub**

mod.rs:773-775 返回 `None`（保守默认），符合 12.1 范围。真实折叠逻辑留待 12.4+。✓

### 行为一致性：✅ 通过

**[OutputSink trait] 7 方法与设计 §7.3 一致**

sink.rs:53-134 的 trait 定义与设计文档 §7.3 Line 1385-1460 逐方法对比：
1. `set_scalar(&mut self, value: Value) -> Result<()>` ✓（I1 修正的叶子写入方法）
2. `set_field(&mut self, name: &str, value: Value) -> Result<()>` ✓
3. `push_item(&mut self, value: Value) -> Result<()>` ✓
4. `sub_sink_for_field(&mut self, name: &str) -> Result<Box<dyn OutputSink>>` ✓
5. `sub_sink_for_item(&mut self) -> Result<Box<dyn OutputSink>>` ✓
6. `finish_subsink(&mut self, sub: Box<dyn OutputSink>) -> Result<()>` ✓
7. `into_value(self: Box<Self>) -> Result<Value>` ✓

**[ValueSink 实现] 与设计 §7.4 一致**

sink.rs:196-247 的 ValueSink 实现与设计文档 §7.4 Line 1498-1548 对比：
- `set_scalar`：`self.pending = Some(value)` ✓
- `set_field`：`self.fields.insert(...)` ✓
- `push_item`：`self.items.push(...)` ✓
- `sub_sink_for_field`：创建 sub，设置 `pending_field_name` ✓
- `sub_sink_for_item`：创建 sub ✓
- `finish_subsink`：`sub.into_value()?` → `self.items.push(value)` ✓（与设计 Line 1534 一致）
- `into_value` 优先级：**pending > fields > items** ✓（与设计 Line 1538-1547 一致，测试覆盖）

**[BuildConstruct 手写 match] 功能等价于 enum_dispatch**

build.rs:186-276 的手写 match 有 **71 arm**（含 cfg-gated Compressed），与 CombinedConstruct 1:1 对应。每个 arm 形式为 `CombinedConstruct::Variant(inner) => inner.compile()`，与 enum_dispatch 自动生成的分发语义等价。Rust 编译器对 match 穷尽性的静态检查提供了额外保证。

### 错误处理：✅ 通过

- 所有 stub 统一返回 `Err(ConstructError::Generic { path, message })`，message 含 "not yet implemented"
- `Box<dyn Construct>::compile` 返回带 "Dynamic" 标识的 Err（build.rs:163-169），便于诊断
- 路径前缀正确传播（PARSE_PATH / BUILD_PATH / SIZEOF_PATH）
- **无 panic / unreachable / todo 在非测试代码**（grep 确认，详见资源安全段）

### 资源安全：✅ 通过

**编译红线核查（VET 独立 grep 验证）**：

| 红线 | 非测试代码 | 测试代码 | 结论 |
|------|-----------|---------|------|
| `unwrap()` | 0 处 | sink.rs 多处 | ✅ 仅在 `#[cfg(test)]` |
| `expect()` | 0 处 | 0 处 | ✅ |
| `panic!` | 0 处 | 0 处 | ✅ |
| `unreachable!` | 0 处 | mod.rs:819, build.rs:321/363 | ✅ 仅在 `#[cfg(test)]` 的 match `_ =>` 兜底 |
| `todo!` / `unimplemented!` | 0 处 | 0 处 | ✅ |
| `unsafe` | 0 处 | 0 处 | ✅ |
| 不必要 `clone()` | 0 处 | — | ✅（stub 不涉及 clone） |

> **关于 "not yet implemented" 错误消息**：这不是 `todo!()` 宏，而是 `Err(ConstructError::Generic { message: "...not yet implemented..." })`。PM 已明确这是 Phase 12.1 的预期行为（stub 框架），不算 TODO 红线违反。✓

### API 一致性：✅ 通过（含 1 个预期偏离）

**文档注释完整性**：

| pub 项 | 文档注释 | 结论 |
|--------|---------|------|
| `CompiledExec` trait | ✅ mod.rs:39-52 | ✓ |
| 71 个 `pub struct Compiled*` | ✅ 各自上方 `///` | ✓ |
| `CompiledNode` enum | ✅ mod.rs:354-365 | ✓ |
| `CompiledSchema` + 方法 | ✅ mod.rs:635-757 | ✓ |
| `OutputSink` trait + 方法 | ✅ sink.rs:34-133 | ✓ |
| `ValueSink` struct + 方法 | ✅ sink.rs:140-193 | ✓ |
| `BuildConstruct` trait + 方法 | ✅ build.rs:55-82 | ✓ |
| `pub mod build;` / `pub mod sink;` | ⚠️ 无 `///`（有 `//!` 内部文档）| 建议级 S1 |

**设计偏离（1 个，预期且合理）**：

> **BuildConstruct::compile 签名简化**
> - 设计文档 §5.1：`fn compile(&self, compiler: &SchemaCompiler) -> Result<CompiledNode>`
> - 代码实现：`fn compile(&self) -> Result<CompiledNode>`
> - DEV 在 build.rs:62-72 文档注释中明确标注：Phase 12.4 将补充 `compiler` 参数
> - **评估**：合理简化。12.1 所有 impl 是 stub（不递归编译子节点），不需要 compiler 参数。不影响 12.1 功能。
> - **行动项**：12.4 开发时必须修正此签名（详见提醒 T1）

### 边界条件：✅ 通过

| 边界场景 | 测试覆盖 | 结论 |
|---------|---------|------|
| 空输入 `parse_bytes(b"")` | `compiled_schema_parse_bytes_returns_stub_error` | ✓ 返回 stub Err |
| 空输入 `build_bytes(&Value::None)` | `compiled_schema_build_bytes_returns_stub_error` | ✓ 返回 stub Err |
| 空 ValueSink `into_value` | `valuesink_empty_returns_empty_list` | ✓ 返回 `Value::List(vec![])` |
| 多次 `set_scalar` 覆盖 | `valuesink_set_scalar_overwrites_previous` | ✓ 后值覆盖 |
| 同名 `set_field` 覆盖 | `valuesink_set_field_overwrites_same_name` | ✓ 后值覆盖，计数不增 |
| `into_value` 优先级 | `valuesink_scalar_takes_priority_over_fields_and_items` + `valuesink_fields_take_priority_over_items` | ✓ pending > fields > items |
| sub_sink 独立性 | `valuesink_sub_sink_for_*_creates_independent_sink` | ✓ 父子隔离 |
| 嵌套 Struct 模式 | `valuesink_struct_like_integration_pattern` + `valuesink_nested_container_via_manual_integration` | ✓ 3 层嵌套正确 |
| 混合类型 Container | `valuesink_holds_various_value_types` | ✓ Int/UInt/String/Bytes/List 混合 |
| Dynamic variant 分发 | `dynamic_variant_compile_returns_error` | ✓ 分发到 Box<dyn Construct> stub |
| 所有类别 dispatch | `all_construct_categories_dispatch_compile` | ✓ Pass/Struct/INT8UB/Dynamic 全 Err |

### 设计偏离评估

**偏离 1：enum_dispatch → 手写 match（build.rs:186-276）**

- **DEV 报告原因**：enum_dispatch 宏对 Dynamic variant（`Box<dyn Construct>`）的 unsized type 生成代码时 panic（"This macro does not support unsized types"）
- **VET 评估**：**合理**。enum_dispatch 确实不支持 DST（dynamically sized types）。`Box<dyn Construct>` 和 `Arc<dyn Construct>` 都是 DST，无法作为 enum_dispatch 的 variant 内部类型
- **设计文档预留**：§5.2 Line 908-911（S2 统一表述）已预留"Dynamic variant 手写降级分支"方案
- **功能等价性**：手写 match 的语义与 enum_dispatch 自动生成的分发完全一致（`Variant(inner) => inner.compile()`）。Rust 编译器静态保证 match 穷尽性
- **未来路径**：Phase 12.4 Box→Arc 迁移后，Dynamic 内部仍是 trait object（Arc<dyn Construct>），enum_dispatch 仍不可用。手写 match 是**长期方案**，非临时绕行
- **结论**：偏离合理，无需修改设计文档

**偏离 2：BuildConstruct::compile 签名简化**（见 API 一致性段）

### 测试覆盖评估

**实际测试数（VET 统计）**：
| 文件 | DEV 报告 | VET 实际统计 | 差异 |
|------|---------|-------------|------|
| mod.rs | 17 | 16 | -1（DEV 略高估）|
| sink.rs | 18 | 23 | +5（DEV 低估，实际更多）|
| build.rs | 8 | 10 | +2（DEV 低估）|
| **合计** | **43** | **49** | **+6** |

`cargo test --lib compiled` 结果：**49 passed, 0 failed**。测试覆盖充分。

**测试质量**：
- stub 行为断言完整（返回 Err + message 含 "not yet implemented"）
- enum_dispatch 分发验证（多 variant）
- ValueSink 全协议路径覆盖（scalar/field/item/sub_sink/finish/优先级/嵌套/混合）
- CompiledSchema 全 API 覆盖（new/with_name/tree/parse_bytes/build_bytes/sizeof/debug）
- Dynamic variant 专门测试
- trait object 动态分发测试（`dyn OutputSink` / `dyn BuildConstruct`）

### 问题清单

#### 阻塞级（0 个）

无。

#### 重要级（0 个）

无。

#### 建议级（2 个）

> **[S1 建议] `pub mod build;` / `pub mod sink;` 缺少 `///` 文档注释**
>
> - **位置**：`compiled/mod.rs:23-24`
> - **问题**：严格按 AGENTS.md §8 红线"所有 pub 项必须有 /// 文档注释"，这两个模块声明缺少 `///`。模块本身有 `//!` 内部文档（build.rs:1-12, sink.rs:1-25），但模块声明行无 `///`。
> - **影响**：不影响功能。`cargo doc` 会用模块的 `//!` 作为文档。
> - **建议修复**：可选，在 mod 声明上方加 `/// Compiled node build (compile) infrastructure.` / `/// Output sink abstraction.`
> - **优先级**：低（不阻塞）

> **[S2 建议] ValueSink.pending_field_name 是死字段**
>
> - **位置**：`compiled/sink.rs:164`（字段定义）+ `sink.rs:215`（设置）+ `sink.rs:223-233`（finish_subsink 不读取）
> - **问题**：`pending_field_name` 在 `sub_sink_for_field` 中被设置到 sub_sink 上（sink.rs:215），但 `finish_subsink`（sink.rs:223）总是 `self.items.push(value)`，不读取这个字段。且 `into_value` 消费 sub 后字段丢失。
> - **根因**：设计层面的已知简化。Struct 路径不走 `finish_subsink`（手动 `into_value` + `set_field`），只有 Array 路径走 `finish_subsink`（总是 push_item）。
> - **设计文档对照**：REV 二次检视 Line 402 已标注"小冗余（非阻塞）：ValueSink 的 pending_field_name 字段在 Struct 命名字段路径中未被使用"。设计文档 §7.4 Line 1482 本身定义了这个字段。
> - **影响**：不影响功能正确性。未来如果有人误用 `sub_sink_for_field` + `finish_subsink` 组合，结果会被静默 push 为 item 而非 field——但当前无此调用路径。
> - **建议**：非 12.1 范围。可在后续迭代中统一 finish_subsink 协议（读取 name 字段），或移除死字段。不阻塞。
> - **优先级**：低（不阻塞）

#### 提醒（非问题，跟踪项）

> **[T1 提醒] BuildConstruct::compile 签名需在 12.4 补充 compiler 参数**
>
> - **当前**：`fn compile(&self) -> Result<CompiledNode>`（build.rs:81）
> - **目标**：`fn compile(&self, compiler: &SchemaCompiler) -> Result<CompiledNode>`（设计 §5.1 Line 872）
> - **原因**：12.4 起需递归编译子节点（`compiler.compile_node(&child)`），必须有 compiler 参数
> - **DEV 标注**：build.rs:62-72 文档注释已明确标注此变更
> - **VET 提醒**：12.4 开发时确保修正此签名 + 所有 71 个 impl + 手写 match。这是 12.1 的已知预期偏离，不算缺陷。

> **[T2 提醒] OutputSink trait finish_subsink 文档与 ValueSink 实现的语义差异**
>
> - **trait 文档**（sink.rs:109-120）说："integrates the result (as a field using the name remembered from sub_sink_for_field, or as a pushed item)"
> - **ValueSink 实现**（sink.rs:223-233）总是 `self.items.push(value)`，不区分 field/item
> - **影响**：当前无调用路径触发此差异（Struct 手动 set_field，Array 用 push_item）
> - **建议**：这是设计文档 §7.4 的已知简化（Line 1530-1534）。未来统一 sink 协议时需对齐 trait 文档与实现。非 12.1 范围。

### 是否可进入下一子任务（12.2）

**是。**

**理由**：
1. 71 variant 完整性经逐行核对确认 1:1 对应 CombinedConstruct
2. trait 签名（CompiledExec / OutputSink）与设计文档一致
3. BuildConstruct 手写 match 功能等价于 enum_dispatch，71 arm 完整（编译器保证穷尽性）
4. 编译红线全部通过（无 unwrap/panic/unsafe 在非测试代码）
5. 测试覆盖充分（49 个测试全 PASS，覆盖核心协议路径）
6. 设计偏离（enum_dispatch→手写 match / compile 签名简化）均合理且 DEV 已标注
7. 无阻塞级或重要级问题

**对 12.2（OutputSink + ValueSink）的提示**：
- 12.2 实际上已在 12.1 中一并完成（sink.rs 的 OutputSink trait + ValueSink 实现 + 23 个测试）。PM 可考虑标记 12.2 为"随 12.1 完成"或仅做增量验证。
- 若 PM 决定 12.2 独立审查，VET 建议快速确认（本审查已覆盖 sink.rs 全部内容）。

### VET 操作日志

- [2026-06-16 20:00] [VET] 接收 PM 审查任务，阅读 AGENTS.md §8 红线 + 设计文档 §2/§5/§7
- [2026-06-16 20:15] [VET] 流程审计：确认 ARCH 文档 + REV 检视 + DEV 日志齐全，管道顺序正确 → 通过
- [2026-06-16 20:30] [VET] 复跑 `cargo build` + `cargo clippy -- -D warnings` + `cargo fmt --check` → 全绿
- [2026-06-16 20:35] [VET] 复跑 `cargo test --lib compiled` → 49 passed, 0 failed
- [2026-06-16 20:45] [VET] 逐行核对 CompiledNode 71 variant vs CombinedConstruct → 1:1 完全对应，Compressed cfg 门控一致
- [2026-06-16 21:00] [VET] 核对 CompiledExec trait 签名 → 与设计 §2.2 一致
- [2026-06-16 21:10] [VET] 核对 OutputSink 7 方法 + ValueSink 实现 → 与设计 §7.3/§7.4 一致，into_value 优先级正确
- [2026-06-16 21:20] [VET] 核对 BuildConstruct 手写 match → 71 arm 完整，与 enum_dispatch 功能等价
- [2026-06-16 21:30] [VET] grep 非测试代码 unwrap/panic/unreachable/unsafe → 全部仅在 #[cfg(test)]，红线通过
- [2026-06-16 21:40] [VET] 评估设计偏离：enum_dispatch→手写 match（DST 限制，合理）；compile 签名简化（12.1 预期，12.4 修正）
- [2026-06-16 21:50] [VET] 识别 2 个建议级问题（S1 pub mod 文档注释 / S2 pending_field_name 死字段）+ 2 个提醒项（T1/T2）
- [2026-06-16 22:00] [VET] 结论：**通过**，写入审查报告

### 备注

- 本次审查未修改任何代码文件，仅写入过程记录。
- Phase 12.1 的代码质量高：variant 完整性、trait 签名、stub 统一性、测试覆盖均达标。
- 设计偏离（enum_dispatch→手写 match）是 enum_dispatch 宏对 DST 的已知限制，非 DEV 实现错误。手写 match 是长期方案（Phase 12.4 Box→Arc 迁移后 enum_dispatch 仍不可用）。
- **关键确认**：PM 说明"stub 返回 not yet implemented 是预期行为，不算 TODO 红线违反"——VET 确认代码使用 `Err(Generic)` 而非 `todo!()` 宏，符合此豁免。
- **对 12.2 的建议**：sink.rs 已在 12.1 完成，12.2 可标记为"随 12.1 完成"或仅做增量验证，避免重复工作。
