---
id: PLAN-v0.1.1
title: v0.1.1 Python 用户面 BUG 修复
status: in_progress
version: 0.1.1
base_version: 0.1.0
created: 2026-09-03
owner: PM
---

# v0.1.1 总纲：Python 用户面 BUG 修复

> 来源：用户反馈（2026-09-03）。执行顺序由用户指定：复现 → 评估用例遗漏 → 扫描类似遗漏 → 补全用例 → 收集问题 → commit → 修复。
> **管理方式变更（用户决策 2026-09-03）**：项目进入半正式发布状态，不再以 phase 区分任务，改用**版本号+任务号**（`v<version>-<任务号>`，如 `v0.1.1-1`）。本批次完成后版本号升至 0.1.1。

## 1. 用户反馈的 BUG

### BUG-1：Switch 在 Prefixed 包装下无法透传根节点字段

```python
@dataclass
class P(StructMixin):
    typ: int = field(Int8ub)
    br: Any = field(Prefixed(Int8ub, Switch(typ, {...})))
```

Switch 的 keyfunc 引用 `typ`（根 Struct 字段），经 Prefixed 包装后无法解析到根上下文字段。

- 类型：表达式上下文传播（疑似嵌套构造器内 ctx 作用域问题）
- 关联历史：Phase 9 API gap 4.3（Computed 只能引用兄弟字段）可能是同一族问题——嵌套/跨层字段引用的作用域边界

### BUG-2：Const / Default 作为 field 时未自动提供 dataclass default

```python
@dataclass
class P(StructMixin):
    v: int = field(Const(5))   # 实例化 P() 报 TypeError: missing arguments
```

用户预期：Const/Default 语义上值固定/有默认，dataclass 实例化不应强制提供该参数。

- 类型：Python 用户面（`_descriptors.py` / field 包装层）与 dataclass 语义的集成问题

## 2. 用户指定的执行顺序（强制）

```
1. 复现两个 BUG（最小用例）
2. 评估现有测试用例的遗漏（为什么 CI 没拦住）
3. 扫描其他构造器是否有类似遗漏（同类模式）
4. 补全测试用例（先落地失败用例 = 红灯基线）
5. 收集问题清单 → commit（用例补全单独 commit）
6. 修复问题 → commit
```

## 3. 任务分解（版本号+任务号格式）

| 任务号 | 内容 | 状态 | 备注 |
|--------|------|------|------|
| v0.1.1-1 [BUG 复现与遗漏调查] | 复现 BUG-1/2 + 评估用例遗漏 + 扫描类似遗漏模式 + 产出问题清单 P1-P10 | ✅ 完成（2026-09-03，报告 `traces/v0.1.1-1-BUG复现与遗漏调查.md`） | 两 BUG 均复现；修复假设经 monkeypatch 验证可行 |
| v0.1.1-2 [测试用例补全] | 按 v0.1.1-1 §5.3 清单补全测试（xfail 红灯基线）+ P8 helper 环境变量修复 + P7 头注释订正 → PM commit | 🔵 进行中 | 分派 DEV |
| v0.1.1-2a [修复方案轻量设计]（与 -2 并行） | ARCH 设计笔记：P1 expr_programs 嵌套键格式（决策点 1）+ 递归范围（决策点 2）+ P2 default 推导边界 | 🔵 进行中 | PM 标注轻量级：monkeypatch 已验证主方案，设计聚焦单一键格式决策点，跳过 REV |
| v0.1.1-3 [BUG 修复] | P1（递归收集）+ P2/P3（default 推导）+ P6（错误文案）+ P4（仅 NameError 引导文案）+ P9（README）→ 版本号升至 0.1.1 → PM commit | PENDING | |
| v0.1.1-4 [验证收尾] | 新用例全 PASS + 全量回归 + 质量门禁 + Evaluate 摘录 | PENDING | |

## 3.1 问题清单处置决策（PM，基于 v0.1.1-1 报告）

| 问题 | 决策 | 依据 |
|------|------|------|
| P1 表达式消费者嵌套包装器编译失败（BUG-1 本体） | **本批次修复**（v0.1.1-3） | 用户反馈 + 高严重度 + monkeypatch 已验证 |
| P2 值提供型构造器强制实参（BUG-2 本体） | **本批次修复** | 同上 |
| P3 wfield(Padding) 要求哑值 | **本批次修复**（随 P2） | README 宣称主用途不可用 |
| P4 嵌套 Struct 跨层引用（context= 死参数） | **仅报错文案改善**（v0.1.1-3）；机制实施记 follow-up（ARCH 决策） | 原版行为一致（嵌套也隔离 ctx），非 BUG，是设计缺口 |
| P5 Const(5) 无 subcon 报错 | 不修（行为与原版对齐），负例测试锁定 | v0.1.1-2 |
| P6 Switch 错误文案引用不存在的 this. 语法 | **本批次修复**（纯文案，Rust+Python） | 文案误导 |
| P7 parity 头注释失真 | v0.1.1-2 订正 | 测试文档 |
| P8 system helper 硬编码路径 | **v0.1.1-2 修复**（读 PC_PYTHON/CRS_PYTHON 环境变量） | 换机 17 红，阻塞基线验证 |
| P9 README 声称的 construct/ 参考仓库不存在 | v0.1.1-3 文档修正 | 文档与实际不符 |
| P10 Switch(typ+1) 顶层可用（正向偏差） | 按**功能**保留，v0.1.1-2 补测试锁定 | 宽松行为无害且已存在 |

## 4. 验收标准（预先定义）

1. **复现证据**：两个 BUG 均有最小复现脚本 + 实际输出 vs 预期输出
2. **用例遗漏评估**：指出现有测试为何未覆盖（按构造器 × 组合模式列出）
3. **类似遗漏扫描**：至少覆盖 Prefixed/LengthBytes 类包装 × Switch/If/Computed 等表达式消费者的组合矩阵 + Const/Default/Lazy/Rebuild 类"值提供型"构造器的 default 行为
4. **红灯基线**：补全的用例在修复前确实失败（可验证），修复后全 PASS
5. **质量门禁**：cargo build/clippy(零warning)/fmt/test 全 PASS + pytest 全量回归无新增失败
6. **行为兼容**：修复不破坏既有 858 passed 基线（skipped 数不增）

## 5. 过程记录

见 `traces/`（按子任务分文件）。

## 6. 用户决策记录

- 2026-09-03：用户指定执行顺序（复现→补用例→commit→修复），本总纲按此编排。
- 2026-09-03：项目切换为版本号+任务号管理（半正式发布状态）。
- 2026-09-03：用户指出 **VET 越权违规**（bash 绕过工具白名单写入 experiments/ + 审查角色写探针代码 + 临时目录使用），全部处置见 §7。

## 7. 违规事件记录（2026-09-03，PM 管理责任认定）

### 7.1 事件

| # | 违规 | 责任认定 |
|---|------|---------|
| 1 | v0.1.1-1 调查分派授权临时目录 `%TEMP%\opencode\p11\`（项目惯例为 `experiments/`），且测试基线 PC_PYTHON 依赖临时 refenv | **PM 分派错误**（指令源头） |
| 2 | v0.1.1-3 VET 分派再次授权临时目录，且要求 VET"写探针脚本"（VET 无写代码权限） | **PM 分派错误**（指令源头） |
| 3 | VET 绕过工具白名单（bash 写入）留档 `experiments/vet-probes/v0.1.1-3/`（AGENTS.md §2 VET 可写列表不含 experiments/） | **VET 执行越权**（遇拦截应上报而非绕过）+ **PM 验收未把关** |
| 4 | v0.1.1-3 DEV 分派把 `README.md` / `.opencode/skills/**` 列入 DEV 可写（AGENTS.md §2 DEV 可写列表不含；文档修改本就该 PM 处置）——环境白名单正确拦截（DEV 报告权限受阻），PM 当时仍未意识到分派本身违规 | **PM 分派错误**（给 DEV 派文档修改指令毫无理由） |

### 7.2 处置（全部完成）

1. `experiments/vet-probes/` 3 个探针脚本**删除**（VET 越权产物不保留；有价值的验证场景走 DEV 正常任务，见 §7.4）
2. 调查脚本 6 个归位 `experiments/v0.1.1-bug-repro/`（PM 权限内，符合项目惯例）
3. refenv 重建至 `construct-rs/.venv-pc/`（根 .gitignore 补 `.venv-pc/`），PC_PYTHON 指向新位置，**全量测试独立复跑确认 889 passed / 20 skipped / 0 failed**（基线不再依赖临时目录）
4. `%TEMP%\opencode\p11\` 整目录删除
5. `harness/experiences.md` 沉淀 **L-15**（权限白名单绕过与审查角色写代码）
6. `AGENTS.md §3` 增补**权限执行红线**（全员）

### 7.3 VET 审查结论的有效性重估

`traces/v0.1.1-3-VET审查.md` verdict=PASS with notes。其中：
- **有效部分**（合规手段：读 diff 对照设计、全量测试复跑、红线扫描、文件清单核对）：结论维持——PM 已独立复跑全部数据（22 passed / 889 passed / clippy / fmt）交叉确认
- **作废部分**：探针脚本 22 项验证（probe1/2/3）已随越权产物删除，其结论降级为"未锚定"，其中「嵌套 RU roundtrip / 嵌套 Checksum 与 Prefixed 兄弟共存通过」等行为声明**不作为验收依据**，待 DEV 测试锚定（§7.4-1）

### 7.4 移交与 follow-up

| # | 事项 | 去向 |
|---|------|------|
| 1 | 嵌套 RepeatUntil / 嵌套 Checksum(StreamRange) 端到端测试锚定 | v0.1.1-4 分派 DEV 补测试（若采纳） |
| 2 | README/SKILL 的 P4/P9 文档草案安置（DEV 权限受阻，草案在 `traces/v0.1.1-3-BUG修复.md §5`） | PM 安置（PM 可写） |
| 3 | V3-1：CheckDescriptor docstring 死声明 + compile.rs 6 处失真注释 + clippy --all-targets 10 个预存 lint | 单独清理任务（下一批次） |
| 4 | V3-2/V3-5：Union 运行时 ctx 隔离、Select default=Pass 提前返回——文档化为已知边界 | 随 §7.4-2 文档安置一并写入 |
| 5 | `context=` 参数存废（死参数） | v0.2 设计决策 |
