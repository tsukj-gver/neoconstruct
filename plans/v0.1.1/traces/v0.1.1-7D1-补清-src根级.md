# v0.1.1-7 D1 [补清] — src 根级文件

- 任务：v0.1.1-7 批次 D1（补清，批次号 D1）
- 范围：`construct-rs/src/` 根级 + `descriptors/` 共 9 文件（不含 compile.rs、不含 nodes/）
- 依据：总纲 §9 判断标准（全量精读每条注释 // /// //!，禁止仅关键词匹配）
- 日期：2026-09-03
- 方法：9 文件逐行精读人工判断；关键词仅作初筛定位与复扫（7 视角逐类扫描）

## 1. 逐文件处置统计（按 7 视角）

| 文件 | V1 Phase/阶段 | V2 问题编号 | V3 内部代号 | V4 日期 | V5 文档引用 | V6 角色流程 | V7 叙事 | 合计处置 |
|------|----|----|----|----|----|----|----|----|
| lib.rs | 0 | 0 | 0 | 0 | 0 | 0 | 0 | **0**（精读确认已净） |
| path.rs | 0 | 0 | 0 | 0 | 0 | 0 | 0 | **0** |
| schema.rs | 0 | 0 | 0 | 0 | 0 | 0 | 0 | **0** |
| instance.rs | 0 | 0 | 0 | 0 | 0 | 0 | 0 | **0** |
| descriptors/mod.rs | 0 | 0 | 0 | 0 | 0 | 0 | 0 | **0** |
| context.rs | 0 | 0 | 0 | 0 | 0 | 0 | 1（「新 Vec 化」轻微叙事保留判定见 §4） | **0** |
| expr.rs | 0 | 0 | 0 | 0 | 0 | 0 | 1 | **1** |
| stream.rs | 0 | 0 | 0 | 0 | 0 | 0 | 0 | **0** |
| error.rs | ~40 | ~8 | ~35 | 0 | ~20 | 3 | 1 | **~107** |

- **重要说明**：lib/path/schema/instance/descriptors/context/stream 7 文件在本会话开始前，工作树已含**同性质的未提交清理改动**（先前并行的 D 批次工作；git status 全库 M 态佐证）。本会话对其全量精读 = 验证性质，确认已达 §9 标准，零新增处置。
- error.rs 为本会话主处置对象（分派单所写「已知 97 处」与实际精读量级吻合，约 107 处注释级命中）。

## 2. error.rs 处置明细（~107 处，全部仅动注释/docstring）

1. **变体 docstring（V1/V5 混合，15 处）**：删除「Phase 8.x 引入（设计 §x.x）」整行 5 处（Checksum/Terminated/Union/CancelParsing/NamedTuple）；去前缀保触发场景 3 处（Mapping/Validation/Rotation）；`（C-6：…）`/`（C-4：…）`去编号 2 处；`（§7.3）` 1 处；`# 触发场景限制（PM 决策 D-7）`→`# 触发场景限制` + `不接 lambda（ADR-006）`→`不接 lambda` 2 处；分组注释「Phase 8 P1+P2 批次」→内容化 1 处；`PyResult<PyDict>`→`PyResult<Py<PyAny>>` 模块图失真修正 1 处（附带，见 §5）。
2. **match 臂注释（8 处）**：message()/path()/kind_str()/set_path() 四处 `// Phase 8 P0/P1+P2 新增（…）` → 内容化「校验与取消类（Const/…/CancelParsing）」「Mapping/Validation/Union/Rotation/NamedTuple」。
3. **push_path_* docstring（2 处）**：`（P0-3 优化：…）`→`（lazy path 模式：错误路径延迟重建）`；`（4.7：Array 系列 lazy path 迁移）`→`（Array 系列 lazy path 模式）`。
4. **ExceptionClasses 结构体（18 处）**：区头「（子任务 1.8）」删除；construct_error_base 字段 Phase 2 叙事改现状（含「后续子任务可增加」→「若需更精确的错误类型映射，可增加」）；15 个异常字段 `（Phase N.N）` 后缀全删；struct 内 2 个分组注释内容化。
5. **is_builtin_class（5 处）**：`O3 fast-path 前置条件检查（设计 §1.3.1 U1 第 ③ 条）`→`fast-path 前置条件检查`；`（破坏 BC3 兼容性）`删除（原因已在前文）；`（按 4.x-REV OBS-2 修订）`删除；数组扩容历史 4 行（Phase 6.2→7→8 P0→P1+P2 扩容叙事）压缩为 1 行现状（「数组覆盖全部 26 个缓存异常类；新增缓存字段时需同步扩容」）；数组内 2 个分组注释内容化。
6. **select_exception_class（11 处）**：`# O2 优化（4.x 错误路径 D 类）`→`# 借用返回（避免 incref 开销）`；match 内 Phase 前缀 10 处去编号保技术（含「设计 §7.3 规定复用」→「复用」）。
7. **try_fast_path_alloc（21 处）**：docstring `O3 fast-path…（4.x 错误路径 D 类优化）`→`异常构造 fast-path`；`16 个内置异常类`→`26 个`（失真修正）；`（对应设计 §1.3.1 U1-U4）`删除；`（O4：…）`去编号；ADR-019 决策句改技术表述（「repr/pickle 行为与慢路径存在已知差异（args 不含 path 前缀，属有意设计）」）；`（BC6 回退）`→`（回退到慢路径）`；`# 安全性（按设计 §1.3.1 …列表）`→`# 安全性`；U1-U4 叙述改「各自标注前置条件」；函数体 U1/U2/U3/U4→步骤 1-4、`U1 返回的非空对象`→`步骤 1 返回的`、`同 U2`→`同步骤 2`、BC6×2/BC3 注释去编号、`O4:` 去编号。
8. **From<ConstructError>（10 处）**：`## 4.x 错误路径 D 类优化（O1-O4）`→`## 错误路径性能优化`；优化表格删除 O1-O4/F1-F8 编号列（保留 4 行技术实现）；`优化路径（设计 §1.3）`去引用；BC3/BC6 去编号；函数体 O1×2/O2/O3+O4/BC1 去编号。
9. **From<PyErr>（3 处）**：`（P0-4 优化；Phase 8.10 CancelParsing 识别）`→`（含 CancelParsing 识别）`；`# Phase 8.10 CancelParsing 识别（设计 §6.2.2）`→`# CancelParsing 识别`；`// Phase 8.10：`去前缀。
10. **测试区（~27 处）**：4 个区块标题去编号内容化（P0-3/4.7/P0-4/4.x O1-O4 测试）；SF-2 前缀删除；build_test_classes 与 aligned 版的 concat!/初始化器 Phase 注释 8 处删除（相邻行类名自明）；测试 docstring `O3（设计 §1.3.1 U1 前置条件 ③）`/`O3 fast-path（按设计 §1.3 步骤 1-5）`/`T6（设计 §5.1 + 4.x-REV OBS-3）`/`O3 行为对齐`/`O3 行为差异（设计 §3.1.3 决策 4 + Consequences）`等去编号保技术；「13 个全部」「所有 13 个」两处失真计数删除（数组实际 14/缓存 26）。

## 3. expr.rs 处置（1 处）

- 测试辅助 `make_context` docstring：「使用新 Vec 化 API」→「使用 `init_expr_values` + `set_field_at`（按索引缓冲的 expr_values API）」——去除进度性「新」字（V7），其余为现状技术描述。

## 4. 字符串改动 + 断言依赖清单

| # | 文件:位置 | 性质 | 前 | 后 |
|---|----------|------|----|----|
| 1 | error.rs `try_fast_path_alloc_user_subclass_returns_none` assert 消息 | 测试断言消息（非用户面） | `…否则 __init__ 不被调用（BC3 违反）` | `…否则 __init__ 不被调用（破坏行为兼容）` |
| 2 | error.rs `fast_path_args_differs_from_slow_path_by_design` assert 消息 | 测试断言消息（非用户面） | `fast-path args 应与慢路径不同（设计显式批准）` | `fast-path args 应与慢路径不同（有意设计）` |

依赖查证：
- 两条均为 assert! 失败消息本体，全库（tests/**/*.py、tests/rust/*.rs、src/**）grep 无任何断言引用其文案；**生产错误消息字符串（#[error]/format!）零改动**——error.rs 全部 #[error] 属性与 stream.rs 全部错误文案精读确认不含管理信息模式。
- 附带失真修正 2 处（注释级，非字符串）：模块头错误流转图 `PyResult<PyDict>`→`PyResult<Py<PyAny>>`（parse 现返回实例，§0 语义）；`16 个内置异常类`→`26 个`（实际缓存数量）。

## 5. 复扫归类确认（处置后）

7 视角正则组（Phase/阶段、P\d/V3-、O/U/BC/T6/R2/OBS/方案/C-x/F-1/TM1/DF1/D3/E01/8.ENV/v0.1.1-N/v4/v5、日期全形态含英文月份、设计 §/ADR/L-xx/docs/plans/SKILL/总纲/traces、VET/REV/PM/ARCH/AUDITOR/设计质疑/验收/子任务/批次/遗留/follow-up、叙事词）复扫 9 文件：**管理信息命中 = 0**。

剩余命中逐条归类（保留，均有 §9 依据）：
| 剩余 | 位置 | 归类 |
|------|------|------|
| 「移交」×2 | context.rs:561「dict 移交给实例」、instance.rs:94「所有权移交给 Bound」 | **保留**：所有权交接的普通技术词义，非流程移交 |
| 「的方案」×1 | instance.rs:9「在 Python 侧执行 cls(**dict) 的方案」 | **保留**：技术方案对比用词，非内部方案代号 |
| 「v1/v2」×16 | context.rs/stream.rs 测试局部变量名（`let v1 = …`） | **保留**：代码标识符，非版本叙事 |
| 「Dec」×10 | error.rs「decoderunit/decode/_decode/Py_DecRef」 | **保留**：技术术语（V4 日期扫描误报） |
| core.py/lib/binary.py/_errors.py 行号锚点 | error.rs/stream.rs/path.rs 多处 | **保留**（同 B1 批次口径）：Python 原版行为对照锚点，用户可感知的兼容性依据 |

## 6. 自检结果

| 项 | 结果 | 说明 |
|----|------|------|
| rustfmt --check 逐文件 | ✅ | 9/9 文件通过（PYO3_PYTHON 已设；lib.rs 直查时连带报出的 nodes/aligned.rs 3 处为预存问题，见 §7） |
| 复扫（7 视角逐类） | ✅ | 管理信息 0 命中；剩余命中逐条归类（§5），保留项均可说出 §9 类别 |
| 只动注释/docstring | ✅ | git diff 新增行中非注释行仅 §4 清单 2 处断言消息字符串 |
| 未引入 this | ✅ | diff 新增行 `\bthis\b` 计数 = 0 |
| 中文保持中文 | ✅ | 全部改写后仍为中文 |
| 单次编辑 ≤300 行 | ✅ | 单次 Edit 最大约 50 行 |
| cargo build/clippy/test/maturin/pytest | 未执行 | 按分派红线禁跑（并行批次锁冲突，PM 统一验证） |
| git 操作 | 未执行 | 遵守禁令 |

## 7. 移交 PM 注意事项

1. **工作树预存改动**：本批 9 文件中的 7 个（lib/path/schema/instance/context/stream/descriptors）在会话开始前已含未提交的同性质清理改动（先前 D 批次工作残留于工作树）；error.rs/expr.rs 亦有预存修改。本会话在其现状基础上精读与处置，未回退任何先前工作。git diff 统计（error.rs 328 行等）含先前批次改动，非全部本会话产出。
2. **nodes/aligned.rs 预存 rustfmt 违规 3 处**（L441/455/469，测试 assert_eq 折行）：非本批范围（nodes/ 禁触），rustfmt --check 全 crate 时会失败——需归属批次处理或 PM 统一验证时知悉。
3. **断言消息字符串 2 处改动**（§4）：已预查证无依赖，PM 统一验证时以 cargo test 复跑为准。
4. **附带失真修正 2 处**（§4 末）：PyResult<PyDict> 图示与「16 个」计数，均为注释级事实订正，不改行为。
5. 分派单行数偏差：error.rs 实际 2327-2348 行区间（读数 2348，含预存改动后的现状），stream.rs 实际 2109 行（分派单写 1953），均按实际全量处理。
