---
id: TRACE-v0.1.1-3-VET
task: v0.1.1-3 [BUG 修复]
phase: CODE_REVIEW
reviewer: VET
date: 2026-09-03
design: docs/design/基础设施/v0.1.1-修复设计.md（v0.1.1-2a）
verdict: PASS with notes
---

# v0.1.1-3 VET 代码审查报告

审查对象：未提交工作区改动（7 文件，+491/-208，与 DEV 清单逐文件核对一致，无遗漏/多余）。

## 1. 分项结论表

| 检查项 | 结论 | 要点 |
|---|---|---|
| 设计符合性 | ✅ | 方案 C 合并语义/文案、9 槽位协议、值守卫、按字段防环、P2 分支顺序与伪代码逐条对应；4 处偏离均论证成立（见 §2） |
| 逻辑正确性 | ✅ | 防环（自引用+互相引用）、单例跨字段共享不误剪枝、槽位遍历对 None/非描述符无 AttributeError、RU 多实例产物键覆盖无害（同 ops ⇒ 同 getint 索引） |
| 行为一致性 | ✅ | 10 旧用例仅解包适配、断言期望未变；22 回归转绿；基线核算 861+19+9=889 吻合 |
| 错误处理 | ✅ | 冲突 CompilationError 含字段名+参数名（探针实测）；terminator None 沿用原缺失报错；表达式编译错误传播不变 |
| 资源安全 | ✅ | 纯 Python 面改动 + Rust 文案；递归有 seen 终止；无 unsafe |
| API 一致性 | ✅ | 返回契约变更为元组且 3 调用点+测试同步；新函数全有 docstring；snake_case |
| 边界条件 | ✅ | 空输入/None/叶子/类对象/环，单测 9 例 + 探针覆盖 |
| 测试质量 | ✅ | 9 新单测非 happy-path（防环/守卫/冲突/dedup）；xfail 移除干净（CompilationError 导入仍被 P5 负例使用）；期望值零篡改（diff 仅删标记行 + docstring 2 行备注） |
| 编码红线 | ✅ | 无 TODO/FIXME/unwrap/expect/panic 新增；compile.rs 非字符串改动行为零（diff 过滤验证） |

## 2. DEV 偏离点裁定

| # | 偏离 | 裁定 | 依据 |
|---|---|---|---|
| 1 | README/SKILL 无写权限，P4/P9 草案移交 PM | 接受 | 与 AGENTS.md §2 DEV 可写范围一致，不绕护栏正确 |
| 2 | RU terminator 读 `.terminator` 原始属性而非 `_expr_params` | **接受（优于设计建议）** | `set_compiled_expr_params`（_descriptors.py L1058-1062）只替换 `_expr_params`，`.terminator` 永不被触碰 ⇒ 幂等。旧协议风险已证实：延迟桩 `_retry_compile`（_mixin.py L1173）二次调 `_compile_expressions` 时，注入产物 `index_field_indices`（list of int）被当预编译 ops 透传，`_check_forward_reference` 的 `op[0]` 对 int 解包 → TypeError。探针 t1（probe2）实测新路径重编译幂等、运行时行为完好 |
| 3 | 节点守卫扩展（`_FieldDescriptor`/`_ExprRef`/类对象） | 接受（细化） | 设计 §2.2 本就要求类对象不遍历；防御性守卫与 getattr 协议兼容 |
| 4 | PrefixedArray docstring 连带订正 | 接受 | 失真声明被转绿测试直接反驳；未订正的 Array/RU 同类声明遵循"未实测不宣称"，处置一致 |
| 5 | Union ctx 隔离（运行期报错） | 接受 + 转 PM | 修复前编译期即失败，无既有行为破坏；失败点后移，建议 PM 记 follow-up |
| 6 | compile.rs 失真注释 + clippy --all-targets 预存 lint | 接受 | 超批准范围，单独立任务合理 |

## 3. 发现问题列表

| 编号 | 严重度 | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| V3-1 | 低 | `_descriptors.py` L2365-2366（CheckDescriptor docstring） | 仍声称 `_field_kind` "由 _mixin._apply_dataclass_field_config 识别"——与 Rebuild 同类的死声明（property 保留但全库无消费者，RO 识别实际走 `desc.mode`）。不在设计 §3.4 清单内故未订正 | 后续批次连同 compile.rs 失真注释（DEV §6.3）一并清理 |
| V3-2 | 低 | 运行时行为（非本次 diff 引入） | 深层嵌套 Checksum(StreamRange)（如 `field(Prefixed(len, Checksum(...)))`）编译/收集通过，但运行时校验值不匹配——子流坐标系下 start/end 主流位置语义歧义（探针 probe3 t1 现象）。修复前该组合编译期即失败，**非回归** | PM 记 follow-up：文档标注或运行时报明确不支持（设计 §2.4 已预留降级路径）；与 DEV §2.5 Union 隔离同批评估 |
| V3-3 | info | `_mixin.py` L651 | "同 ops"判据为 Python `==`：`[("const",1)]` 与 `[("const",True)]`/`[("const",1.0)]` 判等去重。语义等价、无可达误伤路径（float 常量不进 ops，NaN 不可能出现在 tuple 中） | 无需改动，记录即可 |
| V3-4 | info | 测试覆盖 | 设计 §2.4 两个"无实测"风险项本次由探针实测：嵌套 RU（Prefixed/Hex 内）roundtrip 通过；Checksum 与 Prefixed 兄弟共存 StreamRange 校验通过。正式测试锚定仍缺（DEV §6.4 已列） | PM 可决定将 `experiments/vet-probes/v0.1.1-3/` 探针转正为测试 |
| V3-5 | info | 用户文档角度 | `Select(Switch(...))` 且 Switch default 未指定（=Pass）时，Pass 恒成功使 Select 提前返回 None——与原版 construct 语义一致，非缺陷，但易被误解 | 可在 P9 文档批次加一句说明 |

## 4. 验证执行记录（VET 独立复跑）

| # | 项 | 结果 |
|---|---|---|
| 1 | `git status`/`git diff --stat` 文件清单核对 | 7 文件与 DEV 清单一致，无遗漏/多余 |
| 2 | 全量 `pytest tests/ -q --tb=no`（PC_PYTHON/CRS_PYTHON 已设） | **889 passed / 20 skipped / 0 failed**（34.75s）✓ |
| 3 | `cargo fmt --check` / `cargo clippy --release` | exit 0 / exit 0 ✓ |
| 4 | 红线扫描（diff 中 TODO/FIXME/unwrap/expect/panic） | 零命中 ✓ |
| 5 | compile.rs 非字符串改动过滤 | 零数据面改动 ✓（仅 2 处 format! 文案 + 1 行注释） |
| 6 | 探针（`experiments/vet-probes/v0.1.1-3/`，3 脚本 22 项） | 见 §5 |

注：探针原计划放 `%TEMP%`，因 Edit 工具权限白名单仅开放 plans/testing/harness/experiments，改放 `experiments/`（vetter-extension 授权范围，兼作留档证据）。

## 5. 探针脚本结果

**probe1（槽位/合并/防环/P2 边界，15 项全过）**：
- t1 Hex(Switch) 槽位 ✓；t2 Prefixed(Prefixed(Switch)) 双层 ✓；t3 Select 多槽位 ✓；t4 Switch default 槽位 ✓
- t5 同字段双 Switch 不同 key → CompilationError，文案含 `'v'`（字段定位）+ `'key'` ✓
- t6 同字段双 Switch 同 key → 去重、类可编译、命中分支正确 ✓
- t7 互相引用环（A↔B，mock）不无限递归 ✓；t8 单例跨字段共享不误剪枝（seen 按字段）✓
- t9 显式 default 优先（default=7 实例值生效；mismatched 值 build → ConstError，即设计 §3.3-e 声明行为）✓
- t10 隐式 default 字段在前、必填在后（kw_only 生效）✓；t11 Const 错值 → ConstError ✓
- t12 Hex(Const) 包装不识别（保守必填，无隐式 default 误扩散）✓
- t13 Rebuild RW 隐式 default + build 传值被忽略 ✓；t14 wfield(Padding) 与必填 RW 共存 ✓；t15 IfThenElse 双槽位 ✓

初跑 5 项失败经查**全部为探针自身错误**（测试数据构造、Select+Pass 语义期望、If 签名误用），修正后全过；其中 t9/t3 现象反向证实了设计声明行为（Const 非 None 值校验、Select 提前返回 None 与原版一致）。

**probe2（偏离 2 + 设计 §2.4 风险项，4 项全过）**：
- t1 RU 重编译幂等：对已 finalize 的 RU（`_expr_params` 已被注入产物替换）二次调 `_compile_expressions`，不 TypeError、产物一致、parse/build 完好——偏离 2 论证实测成立 ✓
- t2 嵌套 RU（Prefixed 内）parse/build roundtrip ✓（设计 §2.4 风险项实测通过）
- t3 Checksum(StreamRange) 与 Prefixed 兄弟字段共存，主流范围校验正确 ✓
- t4 Hex 包装 RU ✓

**probe3（补充边界，2/3 过，1 项记录）**：
- t1 深层嵌套 Checksum（Checksum 在 Prefixed 内部）：编译/收集通过，运行时校验值不匹配（V3-2，非回归）
- t2 field(Computed) 隐式 default + 零尺寸 build ✓；t3 field(Default) 隐式 default + 省略时节点层补默认值 ✓

## 6. 最终 verdict

**PASS with notes**（可提交 + 记录事项）

- 无中/高严重度问题；核心修复（P1 方案 C / P2+P3 / P6 / xfail 清理）逻辑正确、与设计一致、测试锚定真实
- DEV 6 项偏离/发现全部裁定接受，其中偏离 2 经源码+探针双重验证属"优于设计建议"的正确选择
- 移交 PM 事项：(a) V3-1/V3-2/V3-5 与 DEV §6 遗留合并处置；(b) P4/P9 文档草案安置（DEV §5）；(c) V3-4 探针是否转正
