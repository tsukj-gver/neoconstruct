# v0.1.1-7A [源码内部管理信息清除] 批次 A：Python 用户面

- 任务：v0.1.1-7 [源码内部管理信息清除]，批次 A
- 范围：`construct-rs/python/construct/**` 全部 11 个 .py 文件全量精读（每条注释/docstring 人工判断，非仅关键词匹配）
- 判断标准：`plans/v0.1.1/总纲.md §9`
- 结果：11/11 文件处置完毕；`compileall` 零错误；复扫剩余 4 处均属 §9 保留类别；diff 新增行 `\bthis\b` = 0（红线遵守）

## 1. 逐文件处置统计

| 文件 | 删 | 改写/压缩 | 保留（类别） |
|------|----|----|------|
| `__init__.py` | 3（设计依据 1 + "新增异常"分组注释 2） | 34（import/__all__ 的 Phase 分组注释去标记） | 纯分类注释、宽松导入说明、示例 |
| `_adapters.py` | 3（设计依据 2 + 文档引用句 1） | 11（PM 决策×3、ADR-022×2、ADR-014×2、Phase 标记×4） | FFI 对照表、示例、core.py 引用 |
| `_conditional.py` | 1（设计依据） | 7（PM 决策 1/2、§2.5、§11.4、v5、错误文案 Phase 2） | v0.1.1 起嵌套×5、core.py 行号、参数/示例 |
| `_descriptors.py` | 14（设计依据 13 + RepeatUntil v1 失真历史区块 1 整块） | ~100（Phase 分组注释 40+、设计文档编号 CN/PA/IX/SI/GR/AL/CS/PX/D-P0/C-1/C-2 ×15、PM 决策/方案 A ×8、ADR-014 ×5、v5 ×12、"Phase 2 表达式"×10、路径 B1/A2 ×4、设计 §N ×12） | core.py 行号、v0.1.1 起×7、_expr_params 协议、限制说明 |
| `_errors.py` | 4（设计依据 1 + PM 决策行 1 + __all__ 2） | 36（"Phase N 新增"前缀 18、映射表 Phase 标注 9、编号 D1/CP/C-4/C-6/ADR-006 ×6、Phase 1 历史叙事→当前状态 1、错误文案 1、StringEncoded 区块 1） | core.py 行号、触发场景、迁移指引 |
| `_hashalgo.py` | 2（设计依据 1 + L-14 教训引用 1） | 4（模块头 Phase 8.5、路径 B/B1/A1） | hashlib/zlib 对照、示例、CRC32 注意事项 |
| `_internals.py` | 1（设计依据） | 0 | core.py L1899/L1904/L1914、编译期机制说明 |
| `_macros.py` | 1（设计依据两行） | 8（F-1 REV×2、C-5 REV×2、D-P0-4、ADR-014、PM 决策 D-3、Phase 8 模块头） | core.py L3478、延迟导入技术原因 |
| `_mixin.py` | 1（设计依据块 6 行） | ~60（§ 引用 25、Phase 标记 8、v0.1.1 P1/P2/P3 ×8、SF-1/SF-5 ×5、DF1 1、[设计质疑]历史叙事压缩 1、方案 B'×3、v5×5、P4 域×2、RU-15 1、错误文案 1） | v0.1.1 起×4、编译流程、线程安全、中文错误文案 |
| `lib/__init__.py` | 1（设计依据） | 1（Phase 8.4 port） | Rust 端加载机制 |
| `lib/hex.py` | 1（设计依据） | 0 | 原版复刻全部 docstring |

净变化：+362/-474 行（清理净减 112 行）。

## 2. 典型改写示例（8 组）

1. **任务/进度标记 → 去标记改写**（保留分类语义）
   - `# Phase 3.1: bit 域描述符` → `# bit 域描述符`
   - `# Phase 6.1: Primitives 收尾` → `# Primitives 补充与别名`
   - `# Phase 8 P1+P2 新增异常。`（__init__）→ 删（子分组无信息量）

2. **内部文档链接 → 删**
   - `设计依据：``docs/design/模块设计/模块设计-BitStream.md`` §8.2。` → 整行删除（13 个区块全同此模式）

3. **内部流程引用（角色/决策/ADR/教训编号）→ 去引用保技术**
   - `PM 决策 2（双层分离强化）：通用 Adapter...基本在 Python 层实现` → `通用 Adapter...基本在 Python 层实现`
   - `用户主动选择继承 Validator = 接受性能折衷（ADR-022）` → `...接受性能折衷`
   - `全程零拷贝，详见 L-14 教训` → `全程零拷贝`
   - `**F-1（REV 驳回修正）**：import arrow 必须放在函数体内` → `**注意**：import arrow 必须放在函数体内`

4. **设计文档章节/问题编号 → 删编号保内容**
   - `限制（设计 §1 CK-5）：func 必须是 Phase 2 表达式（...）（与 Rebuild 同脉络，ADR-014 硬约束）` → `限制：func 必须是字段表达式（...）（与 Rebuild 同约束）`
   - `- **modulus 必须 >= 2**（AL-6/AL-7/AL-8）` → `- **modulus 必须 >= 2**`
   - `**已知 parity 差异（C-1）**` → `**已知 parity 差异**`

5. **内部术语 "Phase 2 表达式" → "字段表达式"**（含 3 处用户可见错误文案：Switch keyfunc / RepeatUntil terminator ×2——已核查测试断言 `test_repeat_until_descriptor.py:34` 为 or 条件且消息含 "callable"，不破坏）
   - `"Switch keyfunc must be int/bool constant or Phase 2 expression "` → `...or a field expression `
   - `"inherit from Adapter (Phase 6.3)."` → `"inherit from Adapter."`（已核查无测试断言）

6. **设计版本号 v5/v3 决策 → 删**（注意区分：产品版本 v0.1.1 保留）
   - `"""``Element()`` 描述符（v5 新增）。` → `"""``Element()`` 描述符。`
   - `IndexNode 是用户访问数组下标的唯一机制（v3 决策 §3.3）` → `...唯一机制`

7. **历史叙事 → 压缩为当前状态+原因**
   - `[设计质疑] 设计文档 §2.2.1 原文为"RW/WO 无 default → 保持不变（Phase 1 行为）"。但 Phase 2 为 _FieldDescriptor 添加了 __eq__...` → `注意：_FieldDescriptor 带有 __eq__ 运算符重载（表达式系统），导致 __hash__ = None。...`（保留完整技术因果，删去与设计文档原文的对照）
   - `Phase 1 的 Rust From<...> 实现将所有变体映射为 PyValueError` → `当前 Rust From<...> 实现将所有变体映射为 PyValueError`
   - RepeatUntil v1 区块（描述已删除的 Expr/PyCallable 双路径，与现状矛盾）→ 整块删除，保留当前约束表述
   - `使用方式：必须在 Bitwise/BitStruct 域内使用（Phase 3.1 仅实现节点与编译，BitwiseNode 由后续子任务实现）` → `...域内使用。`

8. **内部路径代号 → 纯技术表述**
   - `- **路径 B1**（推荐零拷贝）` / `- **路径 A2**（Python 原版兼容）` → `- **内置哈希路径**（推荐零拷贝）` / `- **Python callable 路径**（原版兼容）`
   - `（方案 B'：Rust 内部通过 create_class + force_setattr 构造实例...）` → `（Rust 内部通过 create_class + force_setattr 构造实例...）`

**保留示例**（版本变更/纯技术）：`v0.1.1 起 Switch/IfThenElse 可嵌套于包装器内`、`v0.1.1 起 field(Rebuild(...))（RW）可用`、`对应 Python construct core.py L1899`（外部参考行号）、`maturin develop 安装提示`（使用说明）。

## 3. 复扫剩余归类确认

复扫 pattern：`Phase|任务|决策|ADR-|L-1\d|总纲|traces|VET|PM |ARCH|DEV |AUDITOR|验收|遗留|follow-up|移交|暂缓|待办|设计质疑|过程记录|docs/design|plans/|SKILL\.md|批次|子任务|设计依据|§|SF-\d|CN-\d|RU-\d|硬约束|驳回|（设计|（PM|（ADR|（v\d` + 编号形态 `[A-Z]{1,3}-\d`。

| 剩余命中 | 位置 | 归类 | 依据 |
|------|------|------|------|
| `避免隐藏决策路径` ×2 | `_descriptors.py:939,969` | **保留**（设计意图-技术原因） | "决策路径"指 lambda 回调隐藏运行时判断逻辑的普通技术词义，非内部流程引用 |
| `（v0.1.1 起含任意深度的嵌套 RU）` ×2 | `_mixin.py:786,808` | **保留**（版本变更） | v0.1.1 起支持的行为变化，用户可感知 |
| `请确保已通过 maturin develop 安装` | `_mixin.py:1076` | **保留**（使用方法） | `develop` 是 maturin 子命令，DEV 关键词误报 |
| `this is the field that gets updated` | `_mixin.py:889` | **保留**（原有代码） | 错误消息英文代词，非本次引入（diff 新增行 `\bthis\b`=0），不动可执行字符串 |

其余关键词（LEB128/SHA256/UTF-8/parity 等触发的误报）均为技术术语。

## 4. 自检结果

- [x] `python -m compileall construct-rs\python\construct -q` EXIT=0 零错误（AST parse 全量复核 OK）
- [x] 复扫关键词初筛 → 剩余 4 处逐条归类确认（均属 §9 保留类别，见 §3）
- [x] 红线①：改写未引入 this（git diff 新增行 `\bthis\b` 计数 = 0）
- [x] 红线②：未改可执行代码逻辑（仅 3 处错误消息字符串字面量内容替换，已核查测试断言不破坏；无语句/结构改动）
- [x] 红线③：中文注释保持中文；docstring 结构合法（compileall 通过）
- [x] 未跑 cargo/maturin/pytest（按分派要求，PM 统一验证）
- [x] 未执行 git 写操作；git stash 查询被权限规则拦截一次（改用只读 git diff，行为正确）

## 5. 备注（供 PM 注意）

1. **测试断言核查**：修改 3 处用户可见错误文案（"Phase 2 expression" → "field expression"×2、"Adapter (Phase 6.3)" → "Adapter"）前已 grep tests/ 确认无精确断言依赖（唯一相关断言为 or 条件且另一分支恒真）。建议 PM 统一验证时确认 pytest 全量。
2. **失真修正（附带）**：`_descriptors.py` RepeatUntil 区块上方存在描述"已删除的 lambda 双路径"的 v1 历史区块（与 v5 后现状矛盾，属失真历史叙事），按 §9"历史叙事压缩"整块删除。
3. **发现编辑工具偶发回滚**：批量编辑中偶发 `FileSystem.writeFile` 错误后部分已报成功的编辑被回滚（Primitives/Checksum/C-1/C-2 等 6 处），已通过全量复扫逐一捕获并补齐——复扫环节不可省。
4. `__init__.py` 中 `__version__ = "0.1.0"` 为可执行代码未动（版本号升级属发布流程管辖）。
