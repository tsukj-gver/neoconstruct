# v0.1.1-8 [库更名 neoconstruct] — DEV 批 1 代码层过程记录

- 日期：2026-09-03
- 执行者：DEV
- 状态：**部分完成（步骤 1-5、8 完成；步骤 6、7、9 受阻于工具白名单）**
- 受阻原因：步骤 5 目录改名（construct-rs/ → neoconstruct/）后，环境白名单
  仍为 `edit construct-rs/**`，对 `neoconstruct/**` 与 `.github/workflows/**`
  的写操作全部被 deny。按 AGENTS.md §3 权限执行红线（拦截即停止上报），
  未使用 bash 绕过，就地固化为部分交付。

## 1. 步骤完成清单（§11.1 顺序）

| # | 步骤 | 状态 | 要点 |
|---|------|------|------|
| 1 | python 包目录改名 | ✅ | `python/construct/` → `python/neoconstruct/`（Move-Item）；随删旧编译产物 `_construct_rust.cp313-win_amd64.pyd`（gitignored 构建产物，防旧名遮蔽） |
| 2 | python 包内自引用 | ✅ | 相对导入 `._construct_rust`→`._neoconstruct_rust`（__init__.py×2、_descriptors.py×4、_mixin.py×1）；Rust 加载路径同步：`construct._internals`/`construct.lib.hex`/`construct._descriptors`/`construct._hashalgo`（docstring）→ neoconstruct.*；库名自称 construct-rs→neoconstruct；docstring 示例 `from construct import`→`from neoconstruct import`（含 doctest） |
| 3 | Rust 层 | ✅ | Cargo.toml：`[package] name="neoconstruct"`、`[lib] name="neoconstruct_rust"`；lib.rs `#[pymodule] fn _neoconstruct_rust`；error.rs `import_bound("neoconstruct._errors")`；compile.rs/enum_node.rs `neoconstruct._internals`；hex.rs/hex_dump.rs `neoconstruct.lib.hex`；34 个 .rs 文件自称清理（bytes 级替换保真 UTF-8/CRLF）。crate 内无 `use construct_rs::` 自引用（内部均 `crate::`），无需改 |
| 4 | pyproject.toml | ✅ | `name="neoconstruct"`、`module-name="neoconstruct._neoconstruct_rust"`、authors/maintainers→neoconstruct contributors；`python-source="python"` 不变；readme 指向不变 |
| 5 | 仓库目录改名 | ✅ | `construct-rs/` → `neoconstruct/`（Move-Item，同分区瞬时）。**此后写操作全部被白名单拦截** |
| 6 | tests 全量导入切换 | ❌ 受阻 | 未执行（写 neoconstruct/tests/** 被拦）。甄别预备工作已完成（见 §2） |
| 7 | workflows | ❌ 受阻 | 未执行（`.github/workflows/**` 不在白名单）。ci.yml 需改 12 处 working-directory/env 路径 + release.yml 同查（已读文件待改） |
| 8 | .venv 旧装清理 | ✅ | `pip uninstall construct-rs` 成功（原 editable 装指向旧路径）；site-packages 无 `construct/` 残留目录、无 .pth 残留；`.venv-pc` 未动。`.pytest_cache` 清理失败（ACL 损坏，连枚举都被拒，见 §5） |
| 9 | 其余代码面引用 | ⚠️ 部分 | testing/ci ✅ 完成（见下）；neoconstruct/bench/** 与 tests/_helpers/** ❌ 受阻；docs/** 禁改未动（断链清单移交 PM，见 §4） |

### 步骤 9 已完成部分（testing/ci，全部语法验证通过）

- `lib_smoke.ps1`：`$crsDir = Join-Path $root "neoconstruct"`（L1/L2 门禁目录断链点）
- `run_l1_quality.ps1`：注释 2 处 + 目录缺失错误消息
- `run_l2_functional.ps1`：venv 选择规则注释
- `run_l3_perf.py`：注释 3 处 + JSON 字段 `construct_rs_commit`→`neoconstruct_commit`（4 处；baseline 是 CSV，无旧报告读取耦合）
- `run_l4_consistency.py`：NODES_MOD_RS/DESCRIPTORS_PY/PYTHON_PKG_DIR 路径 + `_resolve_impl_module_path` 候选根 + 注释/错误消息（实跑验证路径解析正常）
- `install_hook.ps1` + `pre-commit.template`：hook 标记串同步为 "neoconstruct CI smoke gate"（注意：已装旧 hook 的开发机需 `-Force` 重装，否则标记不匹配）
- `ab_test/ab_bench.py`、`ab_test/ab_harness.ps1`：打印文案/示例路径
- `testing/ci/README.md`：叙述 3 处 + 字段名示例
- 验证：5 个 .py 全部 py_compile OK；7 个 .ps1 全部 Parser::ParseFile 零错误；`run_l4_consistency.py` 实跑（脚本可运行，FAIL 项见 §4）

## 2. 甄别决策记录（import 保留为原版的标准与清单）

**标准**（任务给定）：运行于 PC_PYTHON 解释器的代码中的 construct 导入是原版，保留。

| 位置 | 判定 | 依据 |
|------|------|------|
| parity 各文件 `_make_case_py()` 函数体内 import（Struct/this/len_/"name"/ 语法） | 保留原版 | PC 侧子进程脚本；Struct、this、len_ 均为原版 API |
| parity/system 各文件 `import construct as pc` | 保留原版 | pc.* fixture 惯例，PC 侧 |
| `_descriptors.py` 1998 行附近 Pointer docstring 示例 `from construct import Struct` + `Struct("ptr"/...)` | 保留原版（已补标注「原版 construct 写法」） | CRS 无 Struct 导出（__init__.__all__ 无、无 def/class Struct），示例描述原版场景；已加标注防误用 |
| `_descriptors.py` 3558 行附近 NamedTuple docstring 示例 `Struct(x=Int8ub...)` | 同上（已补标注） | 同上 |
| `lib/hex.py:10` `construct.lib.py3compat` | 保留 | 原版模块名引用（差异说明） |
| `lib/__init__.py:3`、`hex.py:7` `construct/lib/` 斜杠路径 | 保留 | 原版仓库路径基线引用 |
| 全部「Python construct 2.10.70」「core.py LNNNN」对照 | 保留 | 基线引用（终扫模式不含裸 construct） |
| `_construct_compiled`/`_construct_bitwise` 类属性、`ConstructError` 等类名 | 保留 | 内部标识符，非包名引用 |
| `tests` 内 CRS 变量名 `_CRS_PYTHON_DIR`/`CRS_PYTHON`/`_RS_PYTHON`/`crs_python_dir`/venv 目录名 `crs_venv_new` | 保留 | 内部代称，不在终扫模式内；改动徒增 diff |
| `python/tests/streams_smoke.py` | **应改**（CRS 侧，StructMixin API） | 已列入步骤 6 待改清单，被拦 |
| bench/results/*.txt/*.log/*.json | 保留（建议） | 历史测量记录，改名即失真；性质同档案 |

## 3. 残留归类表（模式 `construct-rs|construct_rs|_construct_rust`，扫描时点 2026-09-03）

| 类别 | 位置 | 处置 |
|------|------|------|
| A. 已完成区（零残留） | neoconstruct/src/**、neoconstruct/python/neoconstruct/**、neoconstruct/Cargo.toml、neoconstruct/pyproject.toml | 终扫通过（复扫验证 0 命中） |
| B. testing/ci 已清 | testing/ci/** 脚本+README | 复扫 0 命中；`reports/*.json` 历史报告含 `construct_rs_commit`（档案，保留） |
| C. 步骤 6 待改（受阻） | neoconstruct/tests/**（conftest 12 处自称 + 33 文件 import 切换 + `test_errors.py:216,242` 的 `from construct._construct_rust import` 直引用）+ neoconstruct/tests/_helpers/** + neoconstruct/python/tests/streams_smoke.py | **待白名单修复后执行** |
| D. 步骤 7 待改（受阻） | .github/workflows/ci.yml（12 处 working-directory/env/workspaces/安装步骤）+ release.yml（未逐行核，同模式） | 同上 |
| E. 步骤 9 bench 部分（受阻） | neoconstruct/bench/*.py 约 60 处自称/路径注释 + bench/README.md 4 处 + bench/_helpers/runner.py 1 处 | 同上 |
| F. PM 批 2 文档面 | neoconstruct/README.md（标题/安装说明等 5 处）、neoconstruct/LICENSE（contributors 1 处）、根 .gitignore（`construct-rs/target/`、`construct-rs/Cargo.lock` 2 行——**不改会致 target/ 误入库**——加注释 1 处）、根 README.md（10 处）、根 LICENSE（1 处）、AGENTS.md（5 处）、`.opencode/skills/construct-rs-usage/`（目录改名，§11.2） | PM 权限 |
| G. docs/** 数据面断链 | docs/perf-scenarios.csv 401 处（含 data_source 指针→旧 `construct-rs/bench/results/`，L4 C6 因此 FAIL）、constructors-inventory.csv 52 处、docs/design 等约 626 处 | PM/ARCH 分派（禁改） |
| H. python 包 docstring 漏网 2 处 | neoconstruct/python/neoconstruct/lib/__init__.py:1、lib/hex.py:1（裸 `construct.lib` 无 .hex 后缀，批量模式未覆盖，拦截前未及二轮清扫） | 单行修正，待白名单 |
| I. 合法保留 | bench/results 历史记录、testing/ci/reports、§2 甄别清单 | 不动 |

注：neoconstruct/Cargo.lock 的 `name = "construct-rs"` 随 PM 侧 `cargo build` 自动更新（gitignored）。

## 4. 自检结果

1. `python -m compileall neoconstruct/python neoconstruct/tests -q`：**exit 0 零错误** ✅
2. grep 残留：已扫描归类（§3；A 类区=0，B 类=0，C-H 类如列）✅
3. `import neoconstruct` 冒烟：**部分验证** ⚠️
   - 包结构/自引用正确：import 链走通 `__init__.py` → `_errors` → `_mixin`，最终在 `_mixin.py:1074` 抛"neoconstruct Rust 扩展不可用"（预期分支，文案已正确）——因 `.venv` 无 wheel（重装归 PM）
   - 子模块单独加载：_errors/_hashalgo/_internals/lib.hex 全部 OK
   - BitStructMixin 类定义触发编译失败 → 无扩展时包不可 import：**既有行为**（非更名引入），与 __init__.py「宽松导入」注释表述不符，建议 PM 另行评估
4. `pytest --collect-only`：**执行确认失败**（预期）：`ModuleNotFoundError: No module named 'construct'`（步骤 6 未完成 + .venv 已卸旧装未装新）。另见 §5 pyc 噪音说明

## 5. 受阻项与移交（PM 注意）

1. **白名单失效**（本任务主受阻）：`edit construct-rs/**` 需更新为 `neoconstruct/**`（建议保留旧模式一并存在或直接替换），并补 `.github/workflows/**`（分派承诺可写但白名单未配）。修复后 DEV 续作步骤 6/7/9-E + §3-H 两处单行修正
2. `.pytest_cache`（neoconstruct/ 下）ACL 损坏：`icacls`/枚举/删除全部 Access denied。不影响功能（缓存），建议重启或 takeown 后清理
3. 根 `.gitignore` 两行旧路径（`construct-rs/target/`、`construct-rs/Cargo.lock`）：不改则 PM 提交时 target/ 有误入库风险（76 文件 C 级）
4. 陈旧 `__pycache__`：.pyc 内嵌旧绝对路径（pytest 报错栈显示旧路径为噪音源）。PM 重建时可清各处 `__pycache__`
5. docs/perf-scenarios.csv 指针断链（L4 C6 FAIL）+ constructors-inventory.csv：需 docs/** 写权限方处理；L4 另有 2 个预存数据 FAIL（C7 Aligned 缺失、C8 meets_10x 不一致）与更名无关
6. workflows 的 release.yml 未逐行核（只读了 ci.yml）；PM 续派时提醒一并处理
7. git 侧：目录改名未 add（183 条 D 记录 + 新目录 untracked），git 操作归 PM
