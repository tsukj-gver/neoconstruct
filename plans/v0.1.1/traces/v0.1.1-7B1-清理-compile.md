# v0.1.1-7 B1 [源码内部管理信息清除] — compile.rs

- 任务：v0.1.1-7 批次 B1
- 对象：`construct-rs/src/compile.rs`（实际 7846 行，非分派单所写 7437）
- 依据：总纲 §9 判断标准（全量精读每条注释，禁止仅关键词匹配）
- 日期：2026-09-03
- 方法：分段精读（8 段 × ~950 行），每条注释人工判断；关键词仅作初筛与复扫定位

## 1. 分段处置统计

| 段 | 行区间（原文件） | 主要内容 | 处置 |
|----|----------------|---------|------|
| 1 | 1-950 | 模块头 / compile_schema / build_node_from_descriptor match 前半 | 模块头设计依据删除；Phase 前缀清理约 30 处；2 处失真历史句删除（expr_programs "2.3 阶段仅算标志"；bitwise "当前为前向兼容保留"——Padding 分支实际已使用该参数） |
| 2 | 951-1900 | match 后半 / Strings / BitsInteger / Padding / Array | Phase 8 节标记改内容描述（6 处）；"设计 §x.y.z / §N"尾缀清理约 18 处；内部编号（P2b/P6/P5/BI-3/方案 B/C-4）清理 |
| 3 | 1901-2845 | StopIf / Conditional / Switch / FocusedSeq / RepeatUntil / Rebuild / Adapter / Streams 节头 | RepeatUntil docstring 历史叙事压缩（v5/v4/阶段 6 占位 → 当前状态）；FS-8/FS-2/4、ADR-014、PM 决策清理；§0 合规段改写为"FFI 开销"技术表述 |
| 4 | 2846-3805 | Pointer / Prefixed / parse_expr_ops / Const / Default / Check / Probe / Aligned / Hex / Checksum | AL-6/11/12、A1/A2、B1/B2、"设计质疑"标记清理（Probe into 保留技术内容）；"方案 B"（Hex fmtstr）清理 |
| 5 | 3806-4765 | 单元测试区前半 | "方案 B'"（parse 返回实例）4 处清理；"设计修订 §x"2 处测试节标记清理 |
| 6 | 4760-5719 | 单元测试区中段 | Phase 2 子任务 2.3/2.5/2.6 测试节标记清理（4 处）；"Phase 1 兼容"3 处改写；make_expr_program 失真句（"2.3 阶段 Rust 仅检查是否 Some"）订正 |
| 7 | 5720-6679 | 单元测试区后段 | BI-1/2/3、P4、P1 修复、GR-3 编号清理；BI-1 四句重复叙事压缩为两句；Phase 3.1/3.2/3.3 子任务节标记清理（3 处） |
| 8 | 6680-7814 | GreedyRange 测试尾 / build_*_node 辅助函数 | Phase 4 子任务 4.2、Phase 8 P1+P2 节头清理（含"与 P0 同模式"内部批次引用）；设计 §尾缀 8 处清理；C-1 保留技术语义去编号 |

总量：git diff 192 insertions / 226 deletions（净 -34 行，主因删除设计依据整行与压缩历史叙事）。

## 2. 典型改写示例（8 组）

1. **设计依据整行删除**：
   - 前：`//! 设计依据：\`docs/架构设计.md\` §B.2（编译入口）、§E.3-E.4（编译管线）。`
   - 后：（删除；后续技术段落原样保留）
2. **去 Phase 前缀（分支注释）**：
   - 前：`// PaddingDescriptor（Phase 3.3）→ 根据 bitwise 上下文选择 BitPaddingNode（bit 域）或 PaddingNode（字节域）。设计 §4.2 / §4.6 / §7.2。`
   - 后：`// PaddingDescriptor → 根据 bitwise 上下文选择 BitPaddingNode（bit 域）或 PaddingNode（字节域）。`
3. **内部决策编号清理（保留技术内容）**：
   - 前：`// PM 决策 6.3-D1 接受。subcon 在 Rust 内执行，_decode/_encode 通过 Py<PyAny> 引用回调。`
   - 后：`// subcon 在 Rust 内执行，_decode/_encode 通过 Py<PyAny> 引用回调。`
4. **历史叙事压缩（RepeatUntil docstring）**：
   - 前：`（v5 阶段 6 完全重写）…**阶段 1 临时占位**…完整实现将在阶段 6 引入…# v5 关键变更（vs v4）：不再从 self.predicate 读取 callable（v4 路径已删）…`
   - 后：`（当前状态描述）… 不接收 callable predicate；终止表达式编译在 Python 侧 _compile_expr_tree 完成（与其他表达式字段同模式）。`
5. **内部问题编号 + 选项清理（"Phase 2 表达式"术语展开）**：
   - 前：`// v5：terminator 必须是 Phase 2 表达式（引用 Element 字段）…不接收 Python lambda/callable（用户硬约束 #1）。设计依据：\`docs/模块设计-Array.md\` §4.3 / §6.3.1。`
   - 后：`// terminator 必须是表达式（FieldRef/ExprRef/int，引用 Element 字段）…不接收 Python lambda/callable。`
6. **节标记内容化**：
   - 前：`// === Phase 8 P0 批次 ===` / `// === Phase 7.1 Conditional（4 个新分支）===`
   - 后：`// === Const / Default / Check ===` / `// === Conditional ===`
7. **版本变更保留 + 去内部方案名**：
   - 前：`// v0.1.1 起嵌套描述符的表达式参数递归收集（方案 C 扁平键 + 同名冲突检测）…报 Compilation（诚实限制）。`
   - 后：`// v0.1.1 起嵌套描述符的表达式参数递归收集（扁平键 + 同名冲突检测）…报 Compilation。`
8. **设计质疑标记 → 技术表述**：
   - 前：`/// [设计质疑] into 用 FieldName 代替 ExprProgram（详见 probe.rs 模块级注释）。`
   - 后：`/// into 用 \`FieldName\`（而非 ExprProgram）承载，详见 probe.rs 模块级注释。`

## 3. 复扫归类（处置后剩余 = 0）

关键词组（Phase|任务|决策|ADR-|L-1x|总纲|traces|VET|验收|遗留|follow-up|移交|暂缓|v0.1.1-N|docs/|plans/|SKILL.md|PM/ARCH/DEV/AUDITOR|本任务|本批次|设计修订|设计依据|设计 §|设计质疑|方案 B/C|路线 A|子任务|批次）复扫 **0 命中**。

保留项归类（人工确认，均符合 §9）：
- **v0.1.1 版本变更**（9 处）：Array/PrefixedArray/PascalString/GreedyRange/StopIf/Rebuild 的"# 嵌套表达式（v0.1.1 起）"docstring；Switch 错误消息 "supported since v0.1.1"；Rebuild "v0.1.1 起 field(Rebuild(...)) RW 也允许"。
- **纯技术注释**：pyo3 0.22 clippy 抑制说明、__slots__/slots=True 检测时序差异、Intern PyString、编码解析/引导文本、常量包装 Const ExprProgram（~5ns）、字节对齐/字节交换语义、"未使用，保留兼容"等——原样保留。
- **源码内交叉引用**（非管理档案）：probe.rs 模块级注释引用、`core.py L2637/L2934/L4079/L4727/L813`（Python 原版行为对照锚点）——保留（用户可感知的兼容性依据）。
- **"this"检查**：仅 2 处英文错误消息代词（"collected for this field"/"report this as a bug"），非 construct this 语法表述、非本次引入——未触碰。

## 4. 错误消息字符串改动清单（6 处，均已查证无测试断言依赖）

| # | 位置（函数） | 前 | 后 |
|---|------------|----|----|
| 1 | compile_schema（slots 检测） | `类 {} 使用了 __slots__，Phase 1 不支持 slots dataclass…` | `类 {} 使用了 __slots__，不支持 slots dataclass…` |
| 2 | build_bits_integer_node | `BitsInteger expression length not supported in Phase 3.1 \` | `BitsInteger expression length not supported \` |
| 3 | build_padding_node | `Padding expression length not supported in Phase 3.1 \` | `Padding expression length not supported \` |
| 4 | build_rebuild_node | `Rebuild func must be a Phase 2 expression (FieldRef/ExprRef/int)…(same constraint as RepeatUntil v5).` | `Rebuild func must be an expression (FieldRef/ExprRef/int)…(same constraint as RepeatUntil).` |
| 5 | build_default_node | `value must be a Phase 2 expression (FieldRef/ExprRef/int)…` | `value must be an expression (FieldRef/ExprRef/int)…` |
| 6 | build_check_node | `func must be a Phase 2 expression (FieldRef/ExprRef/int)…` | `func must be an expression (FieldRef/ExprRef/int)…` |

断言安全查证（处置前完成）：
- tests/**/*.py + tests/rust/*.rs 全量扫描：无上述文案断言。
- 唯一相关断言 `test_repeat_until_descriptor.py:34`（`"callable" in str(e).lower() or "Phase 2 expression" in str(e)`）针对 **Python 侧** `_descriptors.py` 的 RepeatUntil 拒绝文案（含 "not a Python callable"，or 条件命中 callable 分支），不经 compile.rs 路径——不受影响。
- 同步改动：build_bits_integer_node / build_padding_node docstring 中引用上述消息原文的两处括注同步更新。

## 5. 自检结果

| 项 | 结果 | 说明 |
|----|------|------|
| cargo fmt --check | ✅ | exit=0（construct-rs workdir，PYO3_PYTHON 已设；未走单文件回退路径） |
| 关键词复扫 | ✅ | 初筛组 0 命中；v0.1.1 版本表述按 §9 保留 9 处（归类见 §3） |
| 只动注释/文案 | ✅ | git diff 过滤验证：非注释行改动仅 §4 清单 6 处字符串 |
| 未引入 this | ✅ | 复扫 2 处英文代词为原有文案，未新增 |
| 中文注释保持中文 | ✅ | 全部中文注释改写后仍为中文 |
| cargo build/clippy/test/maturin/pytest | 未执行 | 按分派红线禁跑（并行批次锁冲突，PM 统一验证） |

## 6. 移交 PM 注意事项

1. **错误消息字符串 6 处改动**（§4）——文案变更请 PM 在统一验证时关注 pytest 断言（已预查证无依赖，但以 PM 复跑为准）。
2. **分派单行数偏差**：compile.rs 实际 7846 行（分派单写 7437），已按实际全量处理。
3. **工具环境**：本次会话 Grep 工具故障（Expand-Archive 模块加载错误），回退 Select-String（仅用于注释定位，未做批量替换，无 L-12 类批量替换风险）。
4. **其他文件**：src 其余文件（context.rs 17 / error.rs 82 / stream.rs 30 处 Phase 等）与 tests/bench 中管理信息属其他批次（B2+），未触碰。
