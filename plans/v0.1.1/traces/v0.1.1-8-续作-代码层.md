# v0.1.1-8 [库更名 neoconstruct] — DEV 续作批次（代码层收尾）过程记录

- 日期：2026-09-03
- 执行者：DEV
- 前序：批 1 完成步骤 1-5、8（目录/包/Rust/pyproject/venv 清理），受阻 6/7/9 + core 命名更正
- 本批：完成用户补充决策 1（core 命名）+ 步骤 6（tests）/ 7（workflows 核对）/ 9（bench/_helpers）收尾

## 1. 任务清单完成状态

| # | 任务 | 状态 | 要点 |
|---|------|------|------|
| 1 | core 命名更正 | ✅ | 见 §2 |
| 2 | tests 导入切换 | ✅ | 见 §3 |
| 3 | H 类残留（lib docstring 2 行） | ✅ | `lib/__init__.py:1`、`lib/hex.py:1` 裸 `construct.lib` 自称 → `neoconstruct.lib`；原版路径基线引用（`construct/lib/hex.py` 复刻说明、`construct.lib.py3compat` 差异说明）按批 1 甄别保留 |
| 4 | bench/_helpers 补清 | ✅ | bench 9 个脚本 rs 侧 import 切换 + 12 文件自称清理（92 处），见 §4 |
| 5 | workflows 核对 | ✅ 无需改 | PM 的 24 处替换完整且逻辑正确，见 §5 |

## 2. 任务 1：core 命名更正（用户决策：pydantic-core 惯例）

| 文件 | 变更 |
|------|------|
| `Cargo.toml` | `[package] name` "neoconstruct"→**"neoconstruct-core"**；`[lib] name` "neoconstruct_rust"→**"neoconstruct_core"**（+注释） |
| `src/lib.rs` | `#[pymodule] fn _neoconstruct_rust` → **`_neoconstruct_core`**（行为性变更，maturin 模块名随之）+ 4 处 doc |
| `src/error.rs` | 3 处 doc 的 `[crate::_neoconstruct_rust]` intra-doc 链接 → `_neoconstruct_core` |
| `pyproject.toml` | `module-name = "neoconstruct._neoconstruct_core"` |
| `python/neoconstruct/__init__.py` | 2 处（`from . import _neoconstruct_core as _native`、`CompiledSchema` 导入） |
| `python/neoconstruct/_descriptors.py` | 5 处（docstring 1 + import 4） |
| `python/neoconstruct/_mixin.py` | 1 处（`compile_schema` 延迟导入） |
| `Cargo.lock` | 随构建自动更新为 `neoconstruct-core`（gitignored） |

## 3. 任务 2：tests 导入切换（步骤 6）

### 3.1 CRS 侧已改（import 行 = 被改对象，期望值/断言逻辑零改动）

- **顶层/测试内 `from construct import` → `from neoconstruct import`**：test_errors、test_checksum_bench、test_v0_1_1_regressions、test_tell_computed、test_roundtrip（含函数内 FieldLengthError）、test_integration、test_bytes_expr、test_compile_errors、test_example、test_conditional、test_field_types、test_field_types_integration、test_edge_cases、test_expr_compile（含 828-875 函数内 4 处）、test_repeat_until_descriptor、system 4 文件顶层 + rs_code 模板（test_can_frame L300/373、test_iec104 L437、test_ipv4 L441/563、test_modbus_rtu L391/483）+ test_modbus_rtu L266 ChecksumError
- **`from construct._mixin import` → `from neoconstruct._mixin import`**：test_conditional、test_field_types、test_expr_compile
- **`from construct._descriptors import` → `from neoconstruct._descriptors import`**：test_repeat_until_descriptor（2 处）
- **`from construct._construct_rust import` → `from neoconstruct._neoconstruct_core import`**：test_errors L216/242（已知 2 处直引用）
- **`python/tests/streams_smoke.py`**：顶层 import 切换 + docstring 自称 2 处
- **conftest.py**：venv 布局探测（`_venv_python` 相对 `_PROJECT_ROOT`）与 `crs_python_dir` 逻辑目录改名后自动正确，核对无需补齐；自称文案 12 处清理；L4-7「与 construct-rs 同名」表述因更名失真，改写为「优先于 site-packages 安装副本」（docstring 层，非逻辑）
- **_system_helpers.py**：路径常量同为相对推导（核对无需补齐）+ 自称 10 处清理

### 3.2 保留（沿用批 1 甄别标准：运行于 PC_PYTHON 解释器的原版导入）

| 位置 | 判定 |
|------|------|
| parity 6 文件 `_make_case_py()` 体内 `from construct import`（Struct/this/len_/原版语法） | 保留（test_adapter L203、test_conditional L165） |
| parity/system `import construct as pc`（17 处） | 保留 |
| `_descriptors.py` L1998/3558 docstring 原版示例 | 保留（批 1 已补标注） |
| parity.py / bench 子进程模板内 `sys.modules` 清 `construct` 的逻辑 | 保留（行为逻辑，对 py 侧子进程无害） |
| `_CRS_PYTHON_DIR`/`CRS_PYTHON`/`crs_python_dir` 等内部代称 | 保留（批 1 决策） |
| 「Python construct 2.10.70」基线引用 | 保留 |

### 3.3 附带发现与修复：批 1 漏网的旧 lib 名 `construct_rust`

`tests/rust/bench_context_ops.rs` L24/25 `use construct_rust::...`（批 1 前的 lib 名，不在批 1 与本任务给定终扫模式内，cargo test 编译失败暴露）→ `use neoconstruct_core::...`。**教训**：crate 改名的终扫模式须覆盖 lib 名的 Rust 标识符形态（无连字符无前缀变体）。

## 4. 任务 4：bench/_helpers 补清（步骤 9-E）

- **rs 侧 import 切换**（`if IMPL/impl == 'rs'` 分支内 → neoconstruct）：bench_breakdown L87、bench_stages L85、bench_struct L103、bench_expr L81、bench_bitstream L112、bench_conditional_streams L87、bench_adapters_struct_streams L86/253/288、bench_primitives_strings_adapter L133/301/427、hex_parity_check L8（顶层，StructMixin API）
- **保留**：各文件 `import construct as pc`（py 侧 12 处）+ bench_breakdown/stages L165/198 pc 侧
- **自称清理**（`construct-rs` 字样，92 处 / 23 文件）：bench 12 文件 + bench/README.md（6 处，含「两个 construct 实现」→「两个实现」3 处失真修正）+ tests 侧（conftest/_helpers/parity/system/test_errors/test_checksum_bench 等）
- **失真修正**：4 个 parity 文件头「两个同名包」→「两个实现包」（更名后 neoconstruct 与 construct 不再同名）；bench/README.md 同族 3 处
- **results/ 历史测量档案**：不动（批 1 甄别决策：改名即失真，性质同档案）

## 5. 任务 5：workflows 核对（步骤 7）

- **ci.yml**：workspaces×2 / working-directory×10 / CRS_PYTHON+PC_PYTHON env×2 全指向 `neoconstruct`；注释与步骤名已同步；pytest 依赖 pyproject `pythonpath=["python"]` 在新目录下成立。**逻辑正确，零残留，无需改**
- **release.yml**：workspaces / working-directory（sdist+wheels）/ artifact path（`neoconstruct/dist/*`）全正确；wheel 名由 pyproject `name=neoconstruct` 驱动；Trusted Publishing 段不涉旧名。**逻辑正确，零残留，无需改**

## 6. 自检输出（PM 统一验证前静态自检）

1. **compileall**：`neoconstruct/python + tests + bench` → **exit 0 零错误** ✅
2. **maturin 构建 + 冒烟**：`PYO3_PYTHON=.venv` → `Compiling neoconstruct-core v0.1.1` → wheel `neoconstruct-0.1.1-cp313-cp313-win_amd64.whl` editable 安装 ✅；冒烟 `import neoconstruct` + `_neoconstruct_core.version()`→0.1.0 + StructMixin build/parse roundtrip → **SMOKE OK** ✅
   - 注：分派给的冒烟脚本 `b='XY'` 传 str，Bytes 字段要求 bytes（更名前同样要求，非回归）→ 按 API 实义以 `b"XY"` 复跑通过
3. **pytest --collect-only**（PC_PYTHON=.venv-pc / CRS_PYTHON=.venv）：**909 collected，0 import error** ✅（= 889 passed + 20 skipped 基线精确对齐）
4. **cargo test**：**1391 passed / 0 failed** ✅（另 4 ignored 为 bench 型 `#[ignore]` 测试；与 v0.1.1-7 记录 1392 的 1 差异为预存口径问题，本批未删改任何测试）
5. **cargo clippy --release**：零 warning ✅（lib test 配置 3 个预存 unused lint，对应总纲 §7.4-8 已知清理项，非本批引入）
6. **cargo fmt --check**：✅（首次运行暴露批 1 字符串替换导致的 hex.rs 1 处行长偏移，`cargo fmt` 修复后通过）

## 7. 残留归类终扫（模式 `construct-rs|construct_rs|_construct_rust|_neoconstruct_rust` + 补漏 `construct_rust`）

扫描范围：neoconstruct/（排除 .venv*/target/__pycache__/.pytest_cache）+ .github/workflows + testing/

| 类别 | 位置 | 数量 | 处置 |
|------|------|------|------|
| 合法保留 | `tests/parity/test_adapter_parity.py:203`、`test_conditional_parity.py:165`（`_make_case_py` 原版 import） | 2 | 不动（PC 侧） |
| 合法保留 | `python/neoconstruct/_descriptors.py:1998,3558`（docstring 原版示例，批 1 已加标注） | 2 | 不动（基线引用） |
| **PM 移交（F 类）** | `neoconstruct/README.md:1,7,12,18`（标题/简介/pip install/cd 路径） | 4 | **PM 批 2 文档面权限**（总纲 §11.2），本批未动避免并行冲突 |
| 其余全部 | src / python 包 / tests / bench / bench README / workflows / testing | 0 | 归零 ✅ |

## 8. 移交与备注（PM 注意）

1. `neoconstruct/README.md` 4 处待 PM 批 2（含 `pip install construct-rs` 发布面关键行）
2. 根 `.gitignore` 两行旧路径（`construct-rs/target/` 等）批 1 已移交，仍未改（PM 权限），target/ 误入库风险仍在
3. `.pytest_cache`（neoconstruct/ 下）ACL 损坏延续（批 1 已报），不影响功能
4. git 侧：目录改名+本批修改均未 add（归 PM）；Cargo.lock 为 gitignored 自动产物
5. 冒烟脚本 `b='XY'` 笔误建议 PM 统一验证时直接用 `b"XY"` 口径
