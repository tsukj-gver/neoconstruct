---
id: TRACE-v0.1.1-7E
task: v0.1.1-7E [补清 tests 与 bench]
role: DEV
created: 2026-09-03
updated: 2026-09-03（第二轮补清，见 §8）
status: done
---

# v0.1.1-7E 补清：tests + bench 漏网清除

## 1. 范围与方法

- 范围：`construct-rs/tests/**`（33 文件）、`construct-rs/bench/**`（12 脚本 + README + _helpers + results）、`construct-rs/python/tests/**`
- 方法：逐类正则扫描（Phase/阶段、P\d/V3-x、内部代号、任务号 v0.1.1-N、日期全形态、
  内部文档引用、角色流程词、进度叙事、this）→ 命中行全部人工精读上下文甄别 → 分『清除 / 保留』
- 边界：不动用例逻辑与期望值；场景标识符主键（与 docs/perf-scenarios.csv 关联）键名键值不动；
  json "phase" 类键名是数据 schema 不动、只清值中叙事；文件名不改（总纲 §9 红线④）

## 2. 代码/注释类修改（7 文件）

| # | 文件 | 修改 |
|---|------|------|
| 1 | tests/integration/test_v0_1_1_regressions.py | 类名 `TestP10TopLevelComplexSwitchLock`→`TestTopLevelComplexSwitchLock`、`TestP5ConstWithoutSubconNegative`→`TestConstWithoutSubconNegative`（pytest ID 去问题编号）；docstring 删「按功能保留」决策叙事 ×2，压缩为实测行为+边界描述 |
| 2 | tests/integration/test_field_types_integration.py | 函数名 `test_backward_compat_phase1_pattern`→`test_backward_compat_early_pattern` |
| 3 | tests/unit/test_repeat_until_descriptor.py | docstring「由 cargo test + experiments 覆盖」→「由 Rust 侧单元测试覆盖」（内部档案引用） |
| 4 | bench/bench_conditional_streams.py | docstring 删失效编号引用「switch.rs 单元测试 SW-5/SW-6/SW-12」（Rust 侧实为描述性命名，无此编号体系）→「Rust 侧单元测试覆盖」；`_PHASE7_MAKE_CASE`/`_make_phase7_case`→`_COND_STREAMS_MAKE_CASE`/`_make_cond_streams_case`（定义+2 引用点共 4 行） |
| 5 | bench/bench_bitstream.py | docstring 删「experiments/ 下的补测脚本」档案引用 →「直接调用 Rust 库的基准」口径对比说明 |
| 6 | bench/bench_opt_shared_retest.py | 内部代号群清除：`OPT-SHARED`×5 → shared-node 优化中性描述；迭代代号 `O1-A + O1-B + O2-A`×2（print 输出）→ 删除；`# Acceptance criterion`→`# Pass/fail criterion`；输出 json `phase` 值 `"OPT-SHARED"`→`"shared-node-retest"` |
| 7 | bench/README.md | 「OPT-SHARED 复测」→「共享节点优化后复测」 |

## 3. 断言连带修复（本批唯一授权改断言点）

| 位置 | 核查结论 | 处置 |
|------|---------|------|
| tests/unit/test_repeat_until_descriptor.py:34 | 现断言 `"field expression" in str(e)` 与 `python/construct/_descriptors.py:989` 现文案 `"RepeatUntil terminator must be a field expression"` **已匹配**（7A 批次已完成双向同步） | 无需改动 |
| 全库 grep 旧文案（"Phase 2 expression" 等） | tests+bench+python/tests 0 命中 | 无需改动 |

## 4. bench/results 数据文件（9 文件）

| 文件 | 处置 | 量 |
|------|------|-----|
| bench_opt_shared_retest.json | `phase` 键名保留，值 `"8.OPT-SHARED"`→`"shared-node-retest"`（与生成器脚本同步） | 1 值 |
| bench_opt_shared_vet_retest.json | 同上 | 1 值 |
| bench_conditional_streams_run1/2/3.log | 堆栈回显 `_make_phase7_case`×6 → `_make_cond_streams_case`；输出路径回显 `bench_phase7_csv.json`/`bench_phase7.json`×6 → 现行名 | 12 行 |
| bench_primitives_strings_adapter_csv_append.csv | 后三列 `data_source`（plans/phase6 档案路径）/`phase`（"6.6" 阶段号）/`notes`（"6.6 全量 bench" 任务叙事）值全部清空；前 14 列不动 | 223 行 ×3 列 |

**保留（数据主键/科学记录）**：scenario_id 主键（`6.6-V1-P` 等含 "6.6" 前缀者为主键，与 perf-scenarios.csv
关联，不动）；timestamp 测量时间戳（基准科学记录，同 v0.1.1-6 平台元数据先例）；`retest_*`/`category`
分类 schema 键名键值；`<opencode-temp>`/`<legacy-repo>` 脱敏占位符。改后 15 个 json 全部解析合法。

## 5. 复扫归类（各视角剩余 = 保留清单）

| 视角 | 剩余 | 归类 |
|------|------|------|
| Phase/阶段标记 | 0（`_PHASE7` 已改名；tests 无 Phase N 叙事） | — |
| P\d 编号 | test_bitstream_parity P1-P13；test_array_parity P1-P4/A1-A6/G1-G4；test_primitives_parity BI-1/2/3；test_strings_parity P2b | BitStream/Array/PrefixedArray/BytesInteger/Strings **场景 ID 主键**（与 PM 样本所述"问题编号 P1-P10"同名异义，文件内 _CASE_DEFINITIONS/ALL_CASES 自洽关联） |
| 问题编号 | test_v0_1_1_regressions BUG-1/BUG-2 ×30 行 | 用户反馈 bug 标识（用户可感知），非内部 P 编号 |
| 内部代号 | TM1/DF1/SK2/SW1-5/FS1-5/E01/E1-E4/B1-B7/BW/BS、bench P0/P1P2 分组名 | bench/parity 场景 ID 与 CLI 分组名（跨 results json 关联） |
| 「Checksum B1 零拷贝」 | bench 3 行 + test_checksum_bench | checksum.rs 双轨**路径技术代号**（Rust 源码 B1=内置 hashfunc+StreamRange，技术语义） |
| 版本叙事 | 「v0.1.1 回归」「v0.1.1 前该写法编译期失败」「v0.1.1 起」 | 版本变更（§9 保留类，用户可感知） |
| 内部文档引用 | bench/README.md:57 `docs/perf-scenarios.csv` 数据血缘说明 | 数据主键关联说明（保留） |
| 角色流程词 / 进度叙事 / 日期 / 任务号 / 迭代号 v5 / 8.ENV | 0 | — |
| this | 非 pc.this 原版 fixture 命中 0（py 分支 `this=pc.this` fixture 与原版语法对照均保留） | 红线 ✅ |
| 其他保留 | test_ipv4.py「## API gap」段（无编号技术边界：Checksum start/end 须先于 checksum 字段） | 纯技术 |
| python/tests | streams_smoke.py 干净，0 修改 | — |

## 6. 自检

1. `pytest tests/ --collect-only -q`（CRS_PYTHON/PC_PYTHON 指向 .venv/.venv-pc）：**909 collected，0 error**
   （初扫 42 行 'error' 命中经核查全部为 test_errors.py/test_compile_errors.py 测试名误匹配）
2. `python -m compileall tests bench python\tests -q`：退出码 **0**
3. bench/results json 解析：**15/15 合法**（ConvertFrom-Json 逐个验证）
4. 复扫：本轮已清标记（PHASE7/phase\d/OPT-SHARED/O1-A/O2-A/experiments//SW-\d/TestP\d+/8.OPT/8.ENV）**0 残留**；
   剩余命中 = §5 保留清单精确吻合
5. 未运行 cargo/maturin/完整 pytest（PM 统一）✅

## 7. 需要 PM 注意

- `test_backward_compat_phase1_pattern` 函数名已改（pytest ID 变化）；两个 regression 测试类名已改
  （`-k TestP10/TestP5` 类过滤词失效）。基线数不变（909 = 修改前收集数）。
- bench_conditional_streams_run*.log 为历史运行输出，其中回显的旧函数名/旧产物文件名已按语义替换为现行名
  （历史输出与现行代码一致性优先于原始性保留，如需审计原始运行请用 git 历史）。
- csv_append.csv 后三列清空后，若未来重新导入 perf-scenarios.csv 需确认导入器对空 data_source/phase/notes 的容忍度
  （列数与 schema 未变，仅值空）。

## 8. 第二轮补清（PM 以样本漏网重分派，全集系统性复扫）

方法：按用户指定全集类别（Phase/阶段标记、P\d/V3-x、内部代号 FS-x/SF-x/BI-x/AL-x/GR-x/IX-x/RB-x/IF-x/SW-x/
SK2/A1/B1/B2/O1/O2/方案 B'/C-4/F-1/TM1/DF1/E01、8.ENV、任务号 v0.1.1-N、迭代号 v5、日期全形态、内部文档引用、
角色流程词、进度叙事、场景标签叙事）逐类正则扫描 + results 数据文件逐个全文精读，不依赖第一轮结论。
第一轮结论经复核基本成立，但存在 4 处漏网（见 §8.1 前四行）。

### 8.1 本轮清除（10 文件）

| 文件 | 修改 |
|------|------|
| bench/_helpers/runner.py | 内部迭代代号 **O1** 漏网：ABResult 与 controlled_ab_test docstring「如 O1 off vs O1 on」×2 + 注释「O1 off/on × 3 次」×2 →「优化关闭 vs 开启」中性表述（4 行） |
| bench/bench_breakdown.py | docstring 历史叙事压缩（§9-7）：删「曾因 … 加速比仅 1.07x-1.62x」陈旧基线数字，保留技术因果（kwargs O(N²) 瓶颈 → Rust 内构造实例消除） |
| bench/bench_expr.py | 进度叙事「（暂停后续开发）」×2 删除（失败消息 f-string 与判据输出行各 1） |
| bench/bench_hex_opt_ab_retest.py | 内部优化方案代号 **Plan A+B / call1** ×4（docstring、横幅 print ×2、json 值）→「setattr + intern fmtstr」纯技术表述 |
| bench/results/bench_breakdown_results.txt | 「方案 B'」×5 删代号保技术（保留「raw == full，实例在 Rust 内构造」；对齐列宽修复） |
| bench/results/benchmark_results.txt | 「（§G.4.5）」「（性能门禁 §2）」内部设计文档节号删除，判据文本保留 |
| bench/results/benchmark_expr_results.txt | 「（§8.7）」「（CONFIRM-1 决定）」「，暂停后续开发」删除（陈旧生成器产物，现行生成器已无此叙事） |
| bench/results/benchmark_bitstream_results.txt | 「（S-PERF）」内部门禁标识删除，判据文本保留 |
| bench/results/bench_hex_opt_ab_retest.json | "optimization" **值** "Plan A+B (call1+setattr + intern fmtstr)" → "setattr + intern fmtstr"（键名不动；改后解析合法） |
| tests/system/test_ipv4.py | 「## API gap」缺口跟踪标签 →「## 已知边界」（技术内容原样保留） |

### 8.2 断言连带修复核查（本批唯一授权改断言点）

- `python/construct/_descriptors.py:989` 现文案 = "RepeatUntil terminator must be a field expression …"；
  `test_repeat_until_descriptor.py:34` 断言 = `"callable" in str(e).lower() or "field expression" in str(e)`
  → **已匹配**（7A 批次已完成库侧改文案 + 测试侧同步），无需改动。
- 全库（tests + bench + python/tests，含断言/正则）grep "Phase 2 expression" / "Phase 2" 旧文案：**0 命中**
  → 断言修复清单为空。唯一命中是编译产物 `.pyd` 二进制（陈旧构建工件，见 §9）。

### 8.3 复扫归类（第二轮终态，各视角剩余 = 保留清单）

| 视角 | 剩余 | 归类（保留） |
|------|------|------|
| L-1/F32L-1/F64L-1/F16L-1 | test_primitives_parity 12 行 | Long/Float **小端（L=Little）场景 ID 主键**，非 L-XX 教训编号（同名异义） |
| IHL-1 | test_ipv4 ids | IPv4 头字段参数化标签（技术） |
| 6.6-V5-P/B、V5 | csv_append.csv + bench 脚本 4 行 | 场景 ID 主键（"6.6-" 前缀为主键组成部分，perf-scenarios.csv 关联；分派明令键名键值不动；非迭代号 v5） |
| OO1/NO1/SK2 | results json/md + 生成脚本 | OneOf/NoneOf/Seek **构造器场景 ID 主键**（非内部代号 O1/SK2 叙事） |
| "drift confirmed" | bench_adapters_retest 判定值 | A/B 复测技术判定值（DRIFT/STRUCTURAL 为输出 schema 值） |
| IEC 60870-5-104 §4 | test_iec104 | **外部协议标准**章节引用（技术，非内部管理档案） |
| perf-scenarios.csv ×8 | bench 脚本/_helpers/README/logs | 数据血缘说明（CSV 为性能数据单一事实源，分派自身以其为场景主键关联目标） |
| 测量 timestamp ×20 | results json/md/txt | 测量时间戳（基准科学记录，沿 v0.1.1-6 平台元数据保留先例；非日期叙事） |
| "根因验证 / 复测 / 退化根因 / 已实现的构造器" | bench 脚本/README/tests\rust | 技术用途与功能现状描述（无任务号/角色/档案引用；"复测"为脚本功能语义） |
| this | 全部为 pc.this 原版 fixture / py 侧原版语法对照（bench 16 + parity 2 + system 9） | 红线明令例外 |
| BUG-1/BUG-2 ×30 行 | test_v0_1_1_regressions.py | 用户反馈 bug 标识（PM 分派示例目标形态本身含「BUG-1 回归：…」，保留） |
| bench_bitstream.py `phase` 变量 | 分组标签变量，值为 BitsInteger/BitStruct/ByteSwap 等技术分组名 | 改名涉及可执行代码（红线②），值无 Phase N 语义，保留并报告 |

### 8.4 本轮自检

1. `pytest tests/ --collect-only -q`（CRS_PYTHON/PC_PYTHON 指向 .venv/.venv-pc）：**909 collected，0 error**（基线不变）
2. `python -m compileall tests bench python\tests -q`：**exit 0**（全部修改后执行）
3. bench/results 15 个 json：**全部解析合法**（含本轮修改的 bench_hex_opt_ab_retest.json）
4. 全集类别终扫：剩余 = §8.3 保留清单精确吻合，0 漏网
5. 未运行 cargo/maturin/完整 pytest（PM 统一）；未做 git 写操作

## 9. 第二轮移交 PM 事项（范围外残留）

| # | 事项 | 说明 |
|---|------|------|
| 1 | **`.pyd` 编译产物含旧文案**：`python/construct/_construct_rust.cp313-win_amd64.pyd` 内嵌 "must be a Phase 2 expression"、"RepeatUntil v5" 等字符串 | 源码已清（7A/7B1），二进制为清理前构建产物；需 PM 统一 `maturin build` 重建后自然消散（DEV 禁 cargo/maturin） |
| 2 | **`src/` 残留 Phase/v5 标记（9 文件）**：error.rs（Phase 2 ×3）、nodes/{check,default,format_field,greedy_bytes,rebuild,repeat_until,stop_if,struct_node}.rs（Phase 2/v5/RU-v5-x/4.5 v5.1/Phase 2.6/「Phase 2 子任务 2.4」/REV 约束字样/docs/模块设计 引用） | 不在批次 E 可写范围（src 为 7A/7B1 范围）；其中 repeat_until.rs 密度最高（v5 重写叙事 ~25 处）。建议补一个 src 批次 |
| 3 | **文件名含角色词**：`bench/results/bench_opt_shared_vet_retest.json` 文件名含 "vet" | §9 红线④ 文件名不改，列报告由 PM 决策（改名需同步生成器脚本 bench_opt_shared_retest.py 的 out_path——注意该 json 与 retest.json 内容仅多 tm1_verdict 字段，或可评估合并） |
| 4 | json "phase" 键名（bench_opt_shared_retest.json 等 2 文件） | 键名为输出 schema（与消费脚本/CSV 导入关联），仅值已中性化（"shared-node-retest"）；如需去键名需同步生成器 + 消费方 |
