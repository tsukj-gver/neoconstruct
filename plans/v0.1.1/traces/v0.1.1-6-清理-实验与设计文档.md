# v0.1.1-6 [PyPI 发布准备·敏感信息清理] — experiments 与设计文档部分 过程记录

- 执行角色：ARCH｜日期：2026-09-03
- 范围：experiments/（34 文本文件）+ docs/design/基础设施/（2 文件）
- 规范：`<opencode-temp>`（3 种转义形态）→ `<opencode-temp>`；裸 <dev-user> → `<dev-user>`（0 适用）
- 事件：过程记录写 plans/ 被白名单拦截（ARCH 无 plans/ 写权），由 PM 安置

## 执行结果
36 文本文件、69 处替换：experiments 34 文件 65 处 + docs/design 2 文件 4 处（CI冒烟门禁设计 L286/448/449、测试框架设计 L234）。

## 验证
- git grep <dev-user> -- experiments docs → 文本残留 0（仅剩 4 dll 二进制，PM 已裁决删除）
- 28 个 .py py_compile 全过（phase4_47_perf_verify.py:18 SyntaxWarning 为预存，未触碰）
- 2 个 JSON 解析合法；numstat 每文件 +/- 对等（零结构改动）

## 遗留决策（PM 裁决 2026-09-03）
1. 4 个 dll（嵌入 <dev-user> 编译路径）→ **删除**（hash 佐证文件保留；可再生构建产物）
2. `<legacy-repo>` 历史仓库路径 → **脱敏 `<legacy-repo>`**（PM 执行，22 文件）
3. JSON platform/processor 环境元数据 → **保留**（基准科学记录，弱指纹）
4. 10.0.0.1 等为协议测试样例数据 → 排除

## 遗留异常原始记录（ARCH 移交原文）
1. dll 二进制各嵌入 7-8 处 <dev-user> 编译路径，文本工具不可编辑
2. legacy 历史仓库绝对路径 18 处/18 文件（2 ps1 + 16 py 的 workdir/test_parity .venv 变体）——PM 复扫实际 22 文件已全清
3. phase5_5_6a/baseline_ab_result.json、phase7_3b_parse_ab/parse_ab_result.json 的 platform/processor/machine 字段
